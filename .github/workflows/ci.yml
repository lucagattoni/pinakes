name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Three legs (I1): the light-only core, [pdf] added, then [claude] added on top. Each is a
        # supported install state (docs/DESIGN.md §4.5) and each must pass this job on its own —
        # a matrix that stopped at [light] would prove nothing about the other two for six
        # increments, the exact gap I1's own exit criterion closes.
        extras: ["light", "light pdf", "light pdf claude"]
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Python and dependencies
        # The [light] extra, never [st]: a 2GB torch download per job is untenable, and fastembed
        # exercises the same protocols (docs/DESIGN.md §4.5). [pdf]/[claude] install one pure-Python
        # SDK apiece and never call them — this job proves the extra installs and imports cleanly,
        # not that the paid path works.
        run: |
          set -e
          args=""
          for extra in ${{ matrix.extras }}; do
            args="$args --extra $extra"
          done
          uv sync --frozen $args

      # Model weights are a download, not an install. Keyed on the models the demo KB names, so a
      # model change invalidates the cache and nothing else does. Without this the reranker alone
      # would pull ~1GB per job.
      - name: Cache model weights
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: hf-${{ hashFiles('tests/demo-kb/pinakes.toml') }}
          restore-keys: hf-

      - name: Format
        run: uv run --frozen ruff format --check .

      - name: Lint
        run: uv run --frozen ruff check .

      - name: Types (ty, fast pre-check)
        run: uv run --frozen ty check --extra-search-path stubs .

      - name: Types (pyright, strict — the gate)
        run: uv run --frozen pyright

      - name: Tests
        # Warnings are errors here (pyproject), which is how a deprecation reaches a human.
        run: uv run --frozen pytest -q -rs

      - name: Model-backed tests
        # Pinned to the [light] leg alone (I1): `_runnable()` skips only when fastembed is absent or
        # the HF cache is empty, both true on every leg alike, so this dedicated step would triple
        # for no benefit on the other two. (The Tests step above already runs these once per leg too
        # when the cache is warm — this step's own redundancy predates the matrix and is unrelated.)
        if: matrix.extras == 'light'
        run: uv run --frozen pytest -q -m model

  eval:
    # The scoreboard §7 requires: a retrieval change has to move these numbers, and a regression
    # beyond tolerance fails the build rather than being noticed later.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - run: uv sync --frozen --extra light

      - name: Cache model weights
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: hf-${{ hashFiles('tests/demo-kb/pinakes.toml') }}
          restore-keys: hf-

      - name: Index the demo KB
        run: uv run --frozen pnk sync --kb tests/demo-kb

      - name: Health check
        run: uv run --frozen pnk doctor --kb tests/demo-kb

      - name: Golden-set evaluation against the baseline
        run: uv run --frozen python -m pinakes.eval tests/demo-kb

  pdf-eval:
    # I3b: joins CI in the same commit that adds the gate, not deferred until I9 the way the draft
    # plan would have left it for six intervening increments to land ungated.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - run: uv sync --frozen --extra light --extra pdf

      - name: Extraction-quality baseline + floor-drift check
        run: |
          uv run --frozen python -m pinakes.extract.quality tests/pdf-corpus \
            --check-floors src/pinakes/extract/floors.toml

  traversal-caps:
    # Its own job for the same reason link-density has one: `ci.yml` never invokes `check.sh`.
    # Needs no models, no index and no extras — the core has no dependencies at all, which is what
    # being pure buys.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - run: uv sync --frozen

      - name: A traversal cannot exceed its caps
        run: uv run --frozen python tools/traversal_cap_gate.py

      - name: The gate can still fail
        # The sibling link-density job has had this since it shipped, and this one was written
        # without it. Drives the same artifact against a cap the code does not have, and requires
        # both a non-zero exit and the stated reason — a crash satisfies the first alone.
        run: |
          if uv run --frozen python tools/traversal_cap_gate.py --expect-depth 99; then
            echo "traversal-cap passed against a cap the code does not have" >&2
            exit 1
          fi
          uv run --frozen python tools/traversal_cap_gate.py --expect-depth 99 2>&1 \
            | grep -q "the caps moved"

  link-density:
    # Its own job because `ci.yml` never invokes `check.sh` — a gate that lives only there runs
    # only on the machine of whoever remembers to run it. Needs no models and no index (it reads
    # the committed sidecars), so it is the cheapest job here and runs on core alone.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - run: uv sync --frozen

      - name: Authored links in both corpora stay sparse
        # Through ruamel.yaml since L5b — the library the product itself reads sidecars with, so
        # the gate's population and the product's cannot drift apart.
        run: uv run --frozen python tools/link_density_gate.py

      - name: The gate can still fail
        # A gate nobody has watched fail is a gate nobody knows works. Drives the same artifact at
        # an impossible threshold and asserts a non-zero exit AND the stated reason: a crash, a
        # missing corpus, or `uv` itself falling over all satisfy a bare non-zero exit too.
        run: |
          if uv run --frozen python tools/link_density_gate.py --max-degree 0; then
            echo "link-density passed at --max-degree 0 — the gate is not gating" >&2
            exit 1
          fi
          uv run --frozen python tools/link_density_gate.py --max-degree 0 2>&1 \
            | grep -q "above the degree cap of 0"

  eval-reproducibility:
    # G1. Its own job for the same reason link-density and traversal-caps have one: `ci.yml` never
    # invokes `check.sh`, so a gate living only there runs only on the machine of whoever remembers
    # to run it. Needs no models and no extras — the tie-heavy fake lives inside the gate, which is
    # what keeps it a second long and able to run on core alone.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - run: uv sync --frozen

      - name: The golden set answers the same way however the index was built
        run: uv run --frozen python tools/eval_reproducibility_gate.py

      - name: The gate can still fail
        # Corrupts one question's outcome and requires the gate to notice, exit non-zero and name
        # the question. A bare non-zero exit is also what a crash, a missing corpus or `uv` falling
        # over produce, so the printed reason is asserted too.
        run: |
          if uv run --frozen python tools/eval_reproducibility_gate.py --inject-difference; then
            echo "eval-reproducibility passed with a difference injected — it is not gating" >&2
            exit 1
          fi
          uv run --frozen python tools/eval_reproducibility_gate.py --inject-difference 2>&1 \
            | grep -q "questions changed outcome"

  eval-cross-machine:
    # The half one machine cannot answer (G1). The gate above drives a deliberately tie-heavy fake,
    # which is what makes ordering observable at all but says nothing about the real model — where a
    # different CPU, ONNX build or BLAS could reorder equally-scored passages. So the real [light]
    # models run on two operating systems and the per-question outcomes are diffed.
    #
    # Outcomes, not metrics: two machines can agree on `recall@k` to four decimal places while
    # disagreeing about which documents came back, and the graph release's gate reads the latter.
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - run: uv sync --frozen --extra light

      # Keyed per runner OS as well as per model: one cache shared between two architectures would
      # be a download saved and a measurement lost.
      - name: Cache model weights
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: hf-${{ runner.os }}-${{ hashFiles('tests/demo-kb/pinakes.toml') }}
          restore-keys: hf-${{ runner.os }}-

      - name: Index the demo KB
        run: uv run --frozen pnk sync --kb tests/demo-kb

      - name: Record per-question outcomes
        run: |
          uv run --frozen python tools/eval_reproducibility_gate.py \
            --record-outcomes outcomes.json

      - uses: actions/upload-artifact@v4
        with:
          name: outcomes-${{ matrix.os }}
          path: outcomes.json

  status-header:
    # docs/STATUS.md's header — line 3, `**Latest release: x.y.z**` — must name
    # `pinakes.__version__`. It drifted for four consecutive releases (0.5.0 → 0.7.1) while the
    # same release sweeps updated every table below it, in a public repo, in the file whose own
    # preamble says it is the only place that says what is built (plans/20260731_1202-open-corrections.md,
    # 20260803). Its own job for the same reason every sibling gate has one: `ci.yml` never
    # invokes `check.sh`. Core-only — the gate needs `pinakes.__version__` and nothing else.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - run: uv sync --frozen

      - name: docs/STATUS.md's header names the released version
        run: uv run --frozen python tools/status_header_gate.py

      - name: The gate can still fail
        # A gate nobody has watched fail is a gate nobody knows works. Drives the same artifact
        # against a version the tree does not have and asserts a non-zero exit AND the stated
        # reason — a crash, a missing file or `uv` falling over all satisfy a bare non-zero exit
        # too. `--expect-version` mutates the expectation, never docs/STATUS.md itself.
        run: |
          if uv run --frozen python tools/status_header_gate.py --expect-version 99.99.99; then
            echo "status-header passed against a version the tree does not have — it is not gating" >&2
            exit 1
          fi
          uv run --frozen python tools/status_header_gate.py --expect-version 99.99.99 2>&1 \
            | grep -q "the header drifted from the release"

  template-drift:
    # A template version number must mean the bytes it denotes. `pnk doctor` compared the recorded
    # reference against the installed one for eleven releases and could never fire: `notes` said
    # `1.0` in every commit while the files it denotes changed in ten later ones. Its own job for
    # the same reason every sibling gate has one: `ci.yml` never invokes `check.sh`. Core-only —
    # the gate needs jinja2 and `pinakes.init`'s constants, no models and no index.
    runs-on: ubuntu-latest
    steps:
      # **`fetch-depth: 0`, and it is the only checkout in this file that sets it.** Leg (vii) —
      # the one leg that catches an author editing a published archive and its ledger row in a
      # single commit — reads `git log` per archived directory. GitHub's default is depth 1, where
      # `git log -- <path>` returns nothing for every path; the gate detects that and *skips* the
      # leg rather than reporting a pass it did not earn, so without this line the job would go
      # green having silently dropped its strongest check.
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - run: uv sync --frozen

      - name: Every template matches the version it declares
        run: uv run --frozen python tools/template_drift_gate.py

      - name: The history leg actually ran
        # A skip is not a pass, and the difference is invisible in an exit code. If `fetch-depth`
        # is ever dropped from the checkout above, this is what says so instead of the job quietly
        # losing leg (vii).
        run: |
          uv run --frozen python tools/template_drift_gate.py | grep -q 'history leg (vii) ran'

      - name: The gate can still fail
        # A gate nobody has watched fail is a gate nobody knows works. Edits a consumed file
        # without bumping the version — the exact drift this gate exists for — in the disposable
        # CI checkout, and asserts a non-zero exit AND the stated reason: a crash or `uv` falling
        # over satisfies a bare non-zero exit too.
        run: |
          printf '\n<!-- ci mutant -->\n' >> src/pinakes/templates/notes/README.md
          if uv run --frozen python tools/template_drift_gate.py; then
            echo "template-drift passed an unversioned edit to a consumed file — it is not gating" >&2
            exit 1
          fi
          uv run --frozen python tools/template_drift_gate.py 2>&1 \
            | grep -q 'README.md differs'
          git checkout -- src/pinakes/templates/notes/README.md

  eval-cross-machine-compare:
    needs: eval-cross-machine
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: outcomes-*
          path: outcomes

      - name: Both machines answered every question the same way
        # `diff` rather than a hash: when this fails the useful output is *which* questions moved,
        # and a mismatched digest says only that something did.
        run: |
          set -e
          test -f outcomes/outcomes-ubuntu-latest/outcomes.json
          test -f outcomes/outcomes-macos-latest/outcomes.json
          if ! diff -u outcomes/outcomes-ubuntu-latest/outcomes.json \
                      outcomes/outcomes-macos-latest/outcomes.json; then
            echo "::error::the two machines disagree about per-question outcomes. The golden set is"\
                 "not reproducible across architectures, and the graph release's sign test reads"\
                 "exactly this movement as evidence about retrieval."
            exit 1
          fi
          echo "both machines produced identical per-question outcomes"

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Build the wheel and sdist
        run: uv build

      # Proves the templates travel inside the wheel. `pnk init` reads them through
      # importlib.resources, so a packaging mistake is invisible until someone installs it.
      - name: Smoke-test the built wheel
        run: |
          set -e
          uv run --isolated --no-project --with dist/*.whl pnk --version
          mkdir -p /tmp/smoke
          uv run --isolated --no-project --with dist/*.whl pnk init /tmp/smoke/kb
          test -f /tmp/smoke/kb/pinakes.toml
          test -f /tmp/smoke/kb/eval/questions.yaml
          grep -q '.pinakes/' /tmp/smoke/kb/.gitignore

      # The dependency swap, asserted on an *installed* copy rather than in the source tree. L5b's
      # AST scan and free-path check both run against the repo; neither can see what the wheel
      # actually declares, so a `pyyaml` that crept back into `[project.dependencies]` would be
      # invisible to them and present on every user's machine.
      #
      # Scoped to the **bare** wheel deliberately. `pinakes[light]` does get PyYAML transitively,
      # from `huggingface_hub` — measured — and that is not Pinakes' doing and not something this
      # can or should police. What it does police is that Pinakes never asks for it. The corollary
      # matters more than the assertion: because `import yaml` *will* succeed on a real user's
      # machine, a stray import in `src/` would work rather than fail loudly, which is why the AST
      # scan is load-bearing rather than belt-and-braces.
      - name: The installed wheel has ruamel.yaml and no PyYAML
        run: |
          uv run --isolated --no-project --with dist/*.whl python -c "
          import importlib.util, sys
          assert importlib.util.find_spec('ruamel.yaml') is not None, 'ruamel.yaml is not installed'
          assert importlib.util.find_spec('yaml') is None, 'PyYAML is back in the runtime'
          "

      # `floors.toml` and `prices.toml` are the two package-data files whose absence silently
      # disables a spending guard: a source-tree test finds them by path and passes on every
      # installed copy that does not have them. Asserted here through `importlib.resources`,
      # against the built wheel, because that is the only place the question is real.
      - name: The wheel carries the files the spending guards read
        run: |
          set -e
          uv run --isolated --no-project --with dist/*.whl python -c "
          from pinakes.budget.prices import load_prices
          from pinakes.extract.floors import load_floors
          prices, floors = load_prices(), load_floors()
          assert prices.models, 'prices.toml reached the wheel with no models in it'
          assert floors.text_yield_floor > 0, 'floors.toml reached the wheel with no fitted floor'
          print(f'prices as_of {prices.as_of}; text-yield floor {floors.text_yield_floor}')
          "

      # A core-only install can index nothing that needs embeddings and no PDF at all. Both are
      # expected, and both must fail by *naming the extra to install* rather than with a traceback.
      #
      # **`pnk sync` cannot make the extractor half of that claim, and the obvious version of this
      # step silently makes the wrong one.** The embedding backend loads before any extractor runs,
      # so a PDF-only KB still fails on sentence-transformers and names `pinakes[st]` — a grep for
      # `pinakes[` passes while proving nothing about PDFs. Verified by running it (I9). `pnk
      # doctor` is the surface that reaches the extractor question, because it reports a failing
      # backend as a check and carries on, so both assertions below are real ones.
      - name: A core-only install names the extra it needs, and never traces back
        run: |
          set -e
          uv run --isolated --no-project --with dist/*.whl pnk init /tmp/smoke/corekb --name core
          python3 - <<'PY'
          from pathlib import Path
          manifest = Path("/tmp/smoke/corekb/pinakes.toml")
          body = manifest.read_text(encoding="utf-8")
          include = 'include = ["**/*.md", "**/*.txt"]'
          assert include in body, "the template's include line has changed shape"
          manifest.write_text(body.replace(include, 'include = ["**/*.pdf"]'), encoding="utf-8")
          PY
          cp tests/pdf-corpus/baseline-1p.pdf /tmp/smoke/corekb/docs/a.pdf

          # (a) the embedding half: a clean, named refusal, not a stack trace.
          set +e
          out=$(uv run --isolated --no-project --with dist/*.whl pnk sync --kb /tmp/smoke/corekb 2>&1)
          code=$?
          set -e
          echo "$out"
          if [ "$code" -eq 0 ]; then echo "::error::a core-only install must not report success"; exit 1; fi
          echo "$out" | grep -q 'pinakes\[st\]' || { echo "::error::it must name the embedding extra"; exit 1; }
          if echo "$out" | grep -qi 'traceback'; then echo "::error::it must not be a traceback"; exit 1; fi

          # (b) the extractor half, which only `doctor` reaches on a core-only install.
          set +e
          health=$(uv run --isolated --no-project --with dist/*.whl pnk doctor --kb /tmp/smoke/corekb 2>&1)
          set -e
          echo "$health"
          echo "$health" | grep -q 'pinakes\[pdf\]' || { echo "::error::doctor must name the PDF extra"; exit 1; }
          if echo "$health" | grep -qi 'traceback'; then echo "::error::doctor must not trace back"; exit 1; fi

      # plans/20260725_1317-v0.1.md promised this gate under "Verification of the whole" and it never shipped:
      # the item carried no increment number, so no increment owned it (see that plan's close-out,
      # rule 8). I7a replaces the unconditional grep that stood here with the allowlist gate: an
      # unconditional grep admits no exceptions, so it would have turned main red on every commit
      # from I7b — which adds `import anthropic` to the one module permitted to have it — until
      # someone noticed. The permitted paths live in .paid-path-allowlist, which this script,
      # check.sh and tests/test_paid_path.py all read, so three copies cannot drift.
      #
      # Stdlib-only and importing nothing from this project, so it runs in the build job without
      # installing the package.
      - name: The free path stays free — no paid-API client imported in src/
        run: python3 tools/paid_path_gate.py

      # Same reasoning and the same job: stdlib-only, imports nothing from this project, and needs
      # no install. A release sweep adds one row to each of six ordered sequences, and a row added
      # in the wrong position is invisible to every other gate here — the table is complete, the
      # links resolve, mkdocs is green. Four consecutive sweeps did exactly that (docs/RELEASING.md).
      - name: The release history reads in release order
        run: python3 tools/release_order_gate.py

      - name: The gate can still fail
        # A gate nobody has watched fail is a gate nobody knows works. Scrambles a *copy* — the two
        # newest sections in ROADMAP swapped — and asserts a non-zero exit AND the stated reason;
        # a crash or a missing file satisfies a bare non-zero exit too.
        run: |
          mkdir -p "$RUNNER_TEMP/scrambled/docs"
          cp CHANGELOG.md "$RUNNER_TEMP/scrambled/"
          cp docs/ROADMAP.md docs/STATUS.md "$RUNNER_TEMP/scrambled/docs/"
          python3 - "$RUNNER_TEMP/scrambled/docs/ROADMAP.md" <<'PY'
          import re, sys
          path = sys.argv[1]
          text = open(path, encoding="utf-8").read()
          heads = re.findall(r"^## (\d+\.\d+\.\d+) — .*$", text, re.MULTILINE)
          a, b = heads[-2], heads[-1]
          text = text.replace(f"## {a} — ", "## 99.99.99 — ").replace(f"## {b} — ", f"## {a} — ")
          open(path, "w", encoding="utf-8").write(text.replace("## 99.99.99 — ", f"## {b} — "))
          PY
          if python3 tools/release_order_gate.py "$RUNNER_TEMP/scrambled"; then
            echo "release-order passed over a scrambled tree — it is not gating" >&2
            exit 1
          fi
          python3 tools/release_order_gate.py "$RUNNER_TEMP/scrambled" 2>&1 \
            | grep -q "reads ascending, but"

      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
