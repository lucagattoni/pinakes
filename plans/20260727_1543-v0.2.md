# Pinakes v0.2 — implementation plan

> ℹ️ **Version numbers below reflect the convention in use when this was written.** Unbuilt
> work is now **named, not numbered** ([STATUS.md](../docs/STATUS.md)). This record is left as it was.

**Status:** draft — revised after adversarial passes 1 (7 HIGH, 19 MEDIUM, 8 LOW), 2 (5 HIGH, 18
MEDIUM, 8 LOW), 3 (12 HIGH, 31 MEDIUM, 17 LOW) and 4 (19 HIGH, 39 MEDIUM, 23 LOW, in three narrow
passes)

**Date:** written 20260727 15:21, revised 20260727 16:12, 17:00, 20:23 and 20260727 20:57

**Source of truth:** [`docs/DESIGN.md`](../docs/DESIGN.md) (pass 7). This plan sequences §8's v0.2
row — and amends §8, because the decisions below take v0.2 past what that row says. Every amendment
is in one of two tables; where DESIGN.md and this plan disagree on anything *not* in them, DESIGN.md
wins and this plan has a bug.

**Written against** [`20260725_1317-v0.1.md`](20260725_1317-v0.1.md)'s close-out ("What the build taught", twelve rules) and the
53 findings in [`../docs/RETROSPECTIVES.md`](../docs/RETROSPECTIVES.md) — 7 HIGH, 29 MEDIUM, 17 LOW,
plus 27 untagged design notes. Every rule is obeyed somewhere below; where a rule shapes a section,
it is cited by number.

**Baseline:** v0.1.3, `main`, clean. `schema_version` 1, extras `st` and `light`.

## Goal

A PDF dropped into `docs/` is indexed, chunked, retrievable and citable by page — and the quality of
the text that made it in is **measured, not assumed**. DESIGN §9 calls PDF extraction *"the most
likely source of silent quality loss"*; the release exists to make that loss visible before it
reaches a user's answers.

v0.2 ships **two** extractors: a free, local, offline default (`pypdfium2`) and an **opt-in paid
Claude-vision extractor** that puts scanned PDFs in scope for the first time. Because a paid path
exists, v0.2 also ships **the whole budget machinery**: §5 says the budget system ships in the same
release as the first thing that can spend, "which is the honest ordering", and that first thing is
now the extractor rather than `pnk ask --deep`. Not scope creep — the design's own rule applied to
the user's decision. Every increment ends **green** (`./check.sh`, equivalently `make check`) and
**committed on its own**, so any regression bisects to one increment.

## Decisions taken · **CLOSED 20260729 — shipped across 0.2.0–0.4.0, except decision 12, whose re-extraction loop was deferred to v0.3 and has never shipped**

These were the plan's open questions and are now settled by the user. Each is load-bearing below.
| # | Decision | Consequence |
|---|---|---|
| 1 | **`pypdfium2`, not PyMuPDF.** Verified 20260727 16:01 against PyPI: PyMuPDF 1.28.0's licence field reads *"Dual Licensed - GNU AFFERO GPL 3.0 or Artifex Commercial License"*. Pinakes is Apache-2.0 and `pnk serve` is a network service, so AGPL §13 would reach anyone embedding it | DESIGN §8 names pymupdf and is amended. I3b targets pdfium's lower-level text API; I1's registry keeps a second extractor a one-increment change |
| 2 | **An opt-in PAID Claude-vision extractor ships in v0.2**, beside the free default. Results are cached, so a document is paid for once | I7a/I7b/I7c. Drags the budget machinery (I6a/I6b) into this release, turns the CI gate into an allowlist, amends CLAUDE.md's paid-path and `.pinakes/`-is-disposable invariants |
| 3 | **Scanned PDFs are in scope**, via the paid path only | DESIGN §9's "scanned-PDF OCR is explicitly out of scope in v1" is amended; the corpus gains a scanned stratum with hand-authored ground truth (I2) |
| 4 | **The span invariant is restated over the extracted/indexed text**, pinned by an extraction fingerprint, with `page_start`/`page_end` so citations read `path:page` | I5. DESIGN §4.6 amended. The alternative — Claude's citations API — is a **checked dead end**, below |
| 5 | **`schema_version` bumps to 2**; every KB rebuilds; no migration | I5. §3 already designs this refusal; the CHANGELOG and the error's remedy carry it |
| 6 | **The `notes` template's `include` gains `**/*.pdf`, commented out** | I9, mirroring `[retrieval.confidence]` — `init` cannot assert something about an environment it cannot see |
| 7 | **The PDF corpus stays out of the demo KB** (`tests/pdf-corpus/`) | I2. §7's golden-set numbers must not move in a release that changes extraction |
| 8 | **The paid extractor sends a K-page slice per request, never the whole document.** Pass 2 (below) showed the whole-document-per-page shape is quadratic in input and stops fitting `claude-opus-5`'s 1M context window at ~166 pages | I6a's estimate is linear; I7b slices with pypdfium2 and drops the prompt-caching basis; `[claude]` now requires `[pdf]` |
| 9 | **The extraction backend joins the re-extraction decision, in the paid direction only.** A free run never overwrites a paid extraction; a paid run re-extracts what the free path indexed | I5. DESIGN §6.4 amended. Fixes the hook-forced permanent downgrade and `--rebuild`'s silent one |
| 10 | **The paid backend bypasses `layout.py`'s geometry stages.** It returns per-page text with no coordinates, so `reading_order` and `strip_running_heads` have nothing to work on. It **does** apply the pure string policy, which moves to its own versioned module (decision 15) | I7b. The free-vs-paid difference is **measured** in I7b's human-gated run, not assumed away |
| 11 | **Extraction provenance lives in the sidecar**, not only in the index. Pass 3 verified against `sync.py:231-241` that `--rebuild` creates an empty database and reads its "before" snapshot from *that* — so a backend recorded only in `index.db` is invisible to the one run that most needs it | I5. DESIGN §2.2 amended. Without this, decision 9 cannot work at all |
| 12 | **The completeness audit is report-only in v0.2.** It computes and reports `word_coverage`; it triggers no paid re-extraction. Its floor cannot be fitted before paid output exists, and a threshold that spends money may not be guessed | I3b, I7c. The re-extraction loop moves to v0.3, fitted on a real distribution |
| 13 | **A paid-fingerprint mismatch WARNs and marks the result; it does not refuse the query.** The hard refusal stays for the free backend, where the rebuild is free | I5. DESIGN §4.4 amended. A `uv lock --upgrade` must not brick a KB behind a paywall |
| 14 | **A changed content hash on a paid-extracted document under a free run is a `failures` row** — neither skipped nor overwritten | I5. DESIGN §6.4 amended; the two axes now have a stated precedence |
| 15 | **The pure string policy is versioned separately from layout.** `normalise()` moves out of `layout.py` into `extract/textpolicy.py` with its own `TEXT_POLICY_VERSION`, hashed into **both** backends' fingerprints | I3a. `LAYOUT_VERSION` becomes genuinely free-path-only, which decision 10 assumed and did not deliver |

**Checked dead end — do not re-litigate.** Claude's **citations API** was investigated as a source
of exact character offsets and ruled out. Verified in the Anthropic API reference: a PDF `document`
block returns **`page_location`** (`start_page_number` / `end_page_number`, 1-indexed) —
**`char_location` is returned only for plain-text documents**. Citations are also a paid, query-time
call and are **incompatible with `output_config.format`** (400), the mechanism I7b uses to validate
extraction. So they cannot supply the offsets, cannot be free, and cannot coexist with the
extractor. Decision 4 stands.

**Checked and declined — server-side refusal fallbacks.** The Anthropic guidance is to ship
`claude-opus-5` code with the `fallbacks` parameter on by default (`fallbacks: "default"` under the
`server-side-fallback-2026-07-01` beta), because Opus 5's safety classifiers can decline a request
and a fallback silently rescues it. **v0.2 declines it, deliberately**: a rescued request is served
by a *different model*, and the extraction fingerprint (I7b) hashes the model id. A fallback would
therefore write text produced by one model into a cache entry claiming another — precisely the
silent-provenance failure the fingerprint exists to prevent, and undetectable afterwards. A refusal
here is a page failure with a recorded reason, on a synthetic corpus, which is a survivable outcome;
mislabelled cached text is not. Revisit only if the fingerprint learns to record the model that
actually answered, per page.

## Ground rules (apply to every increment) · **its single-release assumption superseded 20260728 — assign the next version from `main`, never from this plan**

- **The gate is an artifact, not a list of commands** (rule 1): `./check.sh` / `make check` passes
  immediately before `git commit`, and every new gate is **added to that script**, never described
  in prose. Two v0.1 commits landed red because the gate was a shell pipeline. New gates and owners:
  extras-not-core (I1), corpus-regenerates (I2), `pdf-eval` baseline (I3b), price-table
  well-formedness (I6a), paid-path allowlist (I7a).
- **A gate that cannot run must say so and still be a gate.** `pdf-eval` and the corpus's scanned
  stratum need `pinakes[pdf]`; gate 4 of the allowlist needs `pinakes[claude]`. Each **skips with a
  printed reason** when its half of the environment is absent (`pdf-eval: skipped — pinakes[pdf] not
  installed`), never silently, and a test asserts the printed reason — a skip nobody can see is the
  vacuous-metric failure (rule 3) wearing a different hat. CI runs the legs that make each gate
  meaningful.
- **Worktree + branch per increment**, `YYYYMMDD_HHMM-i<N>-<slug>` per CLAUDE.md.
- **No inline type suppressions at all** (rule 7). Both that existed in v0.1 hid real defects; the
  Anthropic SDK's response unions are the temptation here, so narrow with an explicit `block.type ==
  "text"` check, never a cast.
- **Test-machinery rules, beside the algorithm rules** (rule 5): a fake must not derive its answer
  from the input under test, so **the fixture generator is not the extractor** and each fixture's
  ground truth is authored from its *spec*; a stand-in must be adversarial in the dimension it
  stands in for, so the registered `fake` extractor — **created by I1 with the registry, and named
  in its tests**, since manifest validation checks registry membership and its presence is therefore
  user-visible — returns deliberately ugly text (double spaces, a stray running head, a hyphen join)
  and the **recorded** paid-API fixtures include a refusal, a schema-invalid page, a truncated
  (`stop_reason == "max_tokens"`) page and a page that drops content; no `or` in an equality
  assertion; a skip condition must check **every half** of its environment claim.
- **Two markers, stated scope.** `pdf` marks tests needing `pinakes[pdf]`, and its predicate checks
  the extra imports **and** the fixture exists. `paid` marks **only** tests that make a real API
  call, and its predicate checks `anthropic` imports **and** a key is present **and**
  `PINAKES_ALLOW_SPEND=1` is set. The recorded-fixture suite is **unmarked**: it must run with
  `anthropic` absent, which is what proves the registry seam. `PINAKES_ALLOW_SPEND` is a **pytest
  condition only, never a product guard** — the product's opt-in is already explicit (`[extraction]
  backend`, `--extract=`, the accountant), and a second env-var gate would be a footgun a user sets
  once in a shell profile and forgets.
- **Test-environment configuration lands in I1** (rule 6): both markers and their multi-part
  predicates, `SOURCE_DATE_EPOCH` pinning for fixture generation, and a probe that **both**
  `pypdfium2` and `anthropic` import silently under `filterwarnings = ["error"]` — a warning at
  import turns every test in that leg red, and the `anthropic` leg is new in this release.
- **Retrospectives are an input, not only an output** (rule 10): re-read `docs/RETROSPECTIVES.md` at
  the *start* of each increment, and land findings and fixes as their **own commit** — v0.1 lost
  that in 5 of 15 increments, and bisectability depends on it. - Docs rule (CLAUDE.md): a
  user-facing surface added in an increment updates its doc home (`docs/README.md`'s routing table)
  and `--help` **in the same commit**; every date carries `HH:MM`; the CHANGELOG `[Unreleased]` line
  lands with the code. **Superseded 20260728: this draft assumed one release, `[0.2.0]`, cut at I9
  — the versioning rule (global CLAUDE.md) instead cut it early, after I5, because I1–I5 already
  passed the SemVer table as complete, self-contained work; `[0.2.1]` followed for the docs
  restructure. Check `main` before assigning the next number (project CLAUDE.md) — do not assume
  I9 still owns a version cut.**
- **CI never spends money.** No CI job holds an API key; the paid path is exercised end to end from
  committed recorded responses. Real-API runs are human-gated (rule 9), with date, model and cost
  recorded.
- **v0.2 must not move the demo KB's eval numbers.** If `make eval` shifts, that is a defect to
  explain, not a tuning opportunity (CLAUDE.md's retrieval rule).

## Prior art: PDFScout

[PDFScout](https://github.com/lucagattoni/PDFScout) (the user's own project, MIT) is a LangGraph
multi-agent PDF extractor on Claude's native PDF vision. I7b is written against it deliberately, and
says so in its module docstring.

**Borrowed.** (a) **The completeness audit against the PDF's own embedded text layer** — PDFScout's
`coverage_node` compares significant words (alphabetic, ≥5 chars, NFKC-folded, order-free) from the
native layer against the model's per-page output and re-extracts pages that dropped or duplicated
content. Here it is *free*, because pypdfium2 already produced that layer in I3b, so it is the paid
path's quality gate at zero marginal cost. It brings PDFScout's own caveat too, which becomes a risk
row: a scanned page has **no usable native layer**, so the audit is blind on exactly the pages the
paid path exists for, and the corpus's hand-authored ground truth is what covers those. (b)
**Per-page schema validation with bounded retries** (≤3), so a malformed page is re-asked rather
than silently truncating the document. (c) **Local pre-checks before any API cost** — page count and
encryption detection, offline.

**Rejected after pass 2 — provider-side prompt caching as the cost strategy.** PDFScout re-sends the
whole document on every per-page request and reports pages 2–N at ~10% of input cost. That figure is
real but it is a *hit-path* figure over a cost the design creates: re-sending an N-page document N
times is `N × doc_tokens` of input where sending each page once is `N × page_tokens`. The quadratic
term is what caching then discounts. Two consequences make it unusable rather than merely expensive:
`doc_tokens` exceeds `claude-opus-5`'s **1M context window** at roughly **166 pages** at this plan's
own 6,000-tokens-per-page constant — below the 600-page API limit the pre-check tests, and below the
200-page case this plan works through — and the `ephemeral` marker is a **5-minute** TTL, so any run
longer than five minutes re-pays the 1.25× write repeatedly. v0.2 sends a **K-page slice** instead
(decision 8): each page is sent exactly once, input is linear, there is no page ceiling, and no cost
claim depends on a cache that expires mid-run. Caching may return in v0.3 as an optimisation *on top
of* a linear baseline, where a cache miss costs a discount rather than an order of magnitude.

**Rejected.** *LangGraph and the multi-agent graph* — Pinakes has one extraction task, and a graph
framework is exactly the "second, worse agent framework" §9 names as a risk. *The classifier and
hierarchy agents* — they produce a document tree; Pinakes needs linear text with page spans, and a
hierarchy would be a second structure with no consumer (v0.1's I6 is the record of what an unused
structured column costs). *SQLite checkpoint/resume* — resume uses per-page staging files inside the
extraction cache instead (I7c), rather than a second durability surface beside the cache and
per-document transactions that already exist. *Per-page parallel fan-out, in v0.2* — the hard cap is
a **running** reservation total (§5) and concurrent calls can breach it between reservation and
reconciliation; parallelism lands in v0.3 once the accountant has a ledger to prove itself against.

## What this plan deliberately does NOT decide

- **Table structure recovery.** The table stratum measures whether a table's text survives in
  readable order with label/value adjacency intact — not whether a grid is reconstructed as a grid.
- **PDF outlines/bookmarks as a `heading_path` substitute.** Pages are the unit v0.2 ships.
- **The Files API** (`files-api-2025-04-14`) instead of base64 document blocks. It uploads once and
  references a `file_id` across requests, but needs the beta header on **both** the upload and the
  message call plus an upload lifecycle to clean up. Under decision 8 each request carries a
  *different* K-page slice, so there is nothing an upload would be reused across within a document.
  **Default: base64 slices.** Revisit only if slices become large enough that upload bandwidth, not
  tokens, is the cost.
- **Figures, embedded images, captioning** beyond what the paid extractor's page output already
  says; Office, HTML, email, chat (§1 rules them out); any change to chunking parameters, fusion,
  reranking or the confidence signal; `pnk link` and cross-KB traversal (v0.3); `sqlite-vec` (v0.5).
- **`pnk ask --deep`** — still v0.4; v0.2 ships the budget machinery it will use, not the loop.
  **Default:** the accountant's "operation" for v0.2 is one `pnk sync` invocation — which bounds a
  single invocation and nothing else, so the day and month windows (I6a) are what bound a *sequence*
  of them.

## DESIGN.md amendments

Each row is edited **in the increment that makes it true**, never batched into I9 (I9 audits that
every row landed). Every edit carries `YYYYMMDD HH:MM`.

**One increment per row** (pass 2, M-15): a row listing four increments has no crisp pass condition
when I9 audits it row by row, so each clause is its own row with its own owner.
| § | Amendment | Increment |
|---|---|---|
| §8 | v0.2 row: `pypdfium2` **not** pymupdf | I1 |
| §8 | v0.2 row's scope gains the budget machinery; v0.4's row loses "budget ledger, reservations, `pnk budget`" and keeps `pnk ask --deep` | I6a |
| §8 | v0.2 row's scope gains the paid Claude-vision extractor | I7a |
| §8 | the status line's shipped version is corrected (it still reads v0.1.1 while the package is 0.1.3) | I9 |
| §9 | "scanned-PDF OCR is explicitly out of scope in v1" → **in scope via the paid path**. New risk rows: unbounded spend across invocations, price-table staleness, audit blind spot on scanned pages | I7a |
| §9 | the scanned stratum's **measured** numbers, with date, model and euros actually spent | I9 (from I7b's human-gated run) |
| §7 | Quality gains a **second scored regime**: extraction quality has its own corpus (`tests/pdf-corpus/`), its own six metrics, its own committed baseline and its own gate, separate from the golden set — which §7 currently presents as the only scoreboard | I3b |
| §5 | Opening sentence: "Nothing in v0.1–v0.3 costs money — the paid path doesn't exist yet" → **v0.2 is the first release that can spend**, via the opt-in extractor; and "ships in the same release as the first thing that can spend (`--deep`)" → the v0.2 extractor. The whole control table applies from v0.2, with `monthly_eur` and the new `daily_eur` enforced pre-call beside `per_operation_eur` | I6a |
| §5 | one sentence stating that **the shipped `[budget]` defaults refuse the paid extractor by design** — a reader of DESIGN alone otherwise sees a paid extractor and a working budget block with no relationship | I6a |
| §5 | control table, "Estimate before running" row: "count input tokens locally" → the v0.2 spender estimates from a per-page constant; an **exact** count is `--estimate-only`, which is a network call | I6a |
| §5 | "What 'operation' means" row: "a whole `pnk ask --deep`, not one API call" → for v0.2, one `pnk sync` invocation; and a second identifier, `call_id`, keys the reservation protocol | I6a |
| §5 | the ledger-contents sentence "timestamp, operation, model, token counts, cost, KB id, **nothing more**" → gains `call_id`, record kind, `cost_usd`, `usd_per_eur` and `as_of`. Still no query text and no document content | I6b |
| §5 | the pricing paragraph's "`--deep` refuses to estimate against prices older than a configurable age" → the v0.2 extractor refuses too, and the age has a name, `max_price_age_days` | I6a |
| §5 | `on_exceed = "partial"` is defined as **corpus-level only**: completed documents keep their entries and the run stops cleanly; it never means a half-extracted document | I7c |
| §1 | "Paid LLM **reasoning** is an explicit, budgeted opt-in, and the default agent surface never triggers it" → paid LLM **work (reasoning and PDF extraction)**, with "never triggers it" restated as an **enumerated allowlist of paid entry points**, matching the CLAUDE.md amendment | I7a |
| §4.6 | Span invariant restated: `text == indexed_text[char_start:char_end]`, where indexed text is the decoded file for text sources and the **pinned extraction** for PDFs, pinned by `documents.extraction_fingerprint`. The trailing clause "so a citation can be located exactly in the original file" is **false for PDFs** and is replaced: a PDF citation locates a *page*, and the exact offsets address the pinned extraction, not the file | I5 |
| §4.4 | Coherence gains a **per-document** extraction check, with two outcomes rather than one. A **free**-backend mismatch **refuses the query**, same remedy shape as a model mismatch, because its rebuild is free. A **paid**-backend mismatch **WARNs in `pnk doctor` and marks every affected result** as produced by an earlier extractor version, because the refusal's remedy would be an invoice and old paid text is still correct text — unlike embeddings across models, which are meaningless. Fingerprint **inputs** resolve from a client-free registry module, so the check never imports a paid client | I5 |
| §2.2 | The sidecar's `provenance:` block gains `extraction: {backend, fingerprint, extracted}`. §2.2's "**the sidecar carries no content hash**" reasoning is preserved and distinguished in the same edit: a content hash dirties the sidecar on **every document edit** and goes stale between syncs, whereas the extraction record changes only when the *extractor* does and is stale in no state the index can detect. It is here because `--rebuild` discards the index, and DESIGN §2's own rule is that anything that must outlive the index lives in a committed file | I5 |
| §6.4 | The change decision gains a second axis beside path and content hash, **with a stated precedence over the existing ordered table**: a document whose recorded `extraction_backend` differs from the run's effective backend is stale **in the paid direction only** — a paid run re-extracts what a free run indexed; a free run, `--rebuild` and a free `--extract` **never** overwrite a paid extraction. Where the two axes collide — a **changed content hash** on a paid-extracted document under a free effective backend — the document is neither skipped nor overwritten: it is a **`failures` row** naming the path, with the paid re-extraction as its remedy | I5 |
| §3 | `chunks` gains `page_start` / `page_end` (INTEGER, NULL for non-paged sources); `documents` gains `extraction_backend` and `extraction_fingerprint` (TEXT, NULL for non-extracted sources); `schema_version` → 2 | I5 |
| §2.1 | New `[extraction]` block (`backend`, `model`) | I1 |
| §2.1 | `[budget]` gains `max_price_age_days` and `daily_eur`, with their defaults and the fact that they refuse the paid extractor until raised | I6a |
| §2.1 | the shipped template finally matches §2.1's `include` example | I9 |
| §4.5 | "**CI runs `[light]`**" → CI's `check` job is a three-leg matrix over `[light]`, `[light,pdf]` and `[light,pdf,claude]`; the light-core rule itself is unchanged. The same sentence in §9's torch risk row is amended with it | I1 |
| §1 | the decisions table's "Claude for reasoning only" row, which row 15 above does not reach — it amends §1's prose sentence only, leaving the table showing a design where Claude never touches extraction | I7a |
| §5 | the Visibility row gains `pnk budget --resolve <call_id> --actual <eur>`, the documented way to close an `unknown outcome` an operator has checked against the vendor's dashboard | I6b |
| §6.3 | `--clear-cache=paid`, the explicit form required to destroy paid entries, which `--yes` deliberately does not authorise | I7c |
| §4.7 | `pinakes_search` and `pinakes_get` results carry the `stale_extraction` marker a paid-fingerprint mismatch sets (§4.4), so the marker reaches the agent surface and not only the CLI | I8 |
| §4.5 | Extras gain `[pdf]` (pypdfium2, BSD-3-Clause/Apache-2.0) and `[claude]` (the Anthropic SDK — named for the vendor, because an extra whose name hides the vendor hides which client lands in your environment). **`[claude]` requires `[pdf]`**: the paid path slices PDFs, pre-checks the free text yield and audits against the native layer, all through pypdfium2 | I1 |
| §4.7 | `pinakes_search` results carry `page_start` / `page_end`; `pinakes_get` accepts an optional page range and marks page boundaries in what it returns — one citation vocabulary across both surfaces, as v0.1 pass 2 established for filters | I8 |
| §4.1 | the pipeline's output line "cited passages + doc IDs + confidence signal" gains the **page citation format** `path:page` / `path:p1-p2`, and the stale-extraction marker a paid mismatch adds (§4.4) | I8 |
| §7 | the golden set's numbers are stated to be **unaffected by v0.2**, so a shift is a defect rather than a tuning result — the rule CLAUDE.md already carries, made visible in the section that defines quality | I9 |
| §6.3 | `pnk sync` gains `--extract=BACKEND` with its stated scope (one run, overrides `[extraction] backend`) | I1 |
| §6.3 | `--clear-cache` is real, clears staged partial extractions too, and confirms before destroying results; `--yes` is defined and scoped | I4 |
| §6.3 | the three hooks and `pnk init --ci` **force the free backend** | I6b |
| §6.3 | `--estimate-only` — builds the real request and calls `count_tokens` without generating; **it is a network call**, not an offline estimate | I7b |
| §6.3 | `--force` with its stated scope (exactly one thing: the healthy-PDF refusal) | I7c |

### CLAUDE.md amendments

Two invariants in CLAUDE.md change in v0.2. They had no table and therefore no auditor (pass 2,
M-15); I9 walks this one too.
| Invariant today | Amendment | Increment |
|---|---|---|
| "No code path may make a paid API call outside `pnk ask --deep`" | replaced by an **enumerated allowlist** of paid entry points, mirroring DESIGN §1 and `.paid-path-allowlist` | I7a |
| "`.pinakes/` is disposable except `ledger.jsonl`" | gains a second exception: **paid extraction cache entries** are derived state that costs real money to derive | I7c |
| "Core dependencies stay light … embedding backends are extras (`[st]`, `[light]`). **CI runs `[light]`**" | two more extras (`[pdf]`, `[claude]`), and CI's `check` job becomes a **three-leg matrix**; the light-core rule itself is unchanged and re-stated | I1 |
| "**`docs/` belongs to the user.** Never modify source documents" | unchanged for documents, and **explicitly extended** to say the sidecar's new `provenance.extraction` block is written by sync — sidecars were always machine-written, but a rule about `docs/` now has a machine writing more into it | I5 |

---

## Increments

**I1** is the foundation. **I2** and **I3a** fan out from it; **I3b** joins them. **I4** and **I5**
both need I3b and are independent of each other. **I6a → I6b** is a parallel track needing only I1 —
but **I7a lands before I6b**, because I6b adds `pnk budget` to gate 4's run list and that gate does
not exist until I7a creates it. The letter suffixes are not a total order on their own; this is the
one place the numbering and the dependency graph disagree, so it is stated rather than inferred.
**I7a** needs only I1 and lands **before any paid code exists**, because a gate that arrives with
the thing it guards has never once refused it. **I7b** needs I4, I5, I6b and I7a; **I7c** needs I7b.
**I8** joins everything. **I9** is last. I7's three parts use the same letter-suffix convention as
I3a/I3b and I6a/I6b, so nothing downstream is renumbered.

### I1 — Extras, the extractor seam, and an honest core-only failure

`pyproject.toml` gains `pdf = ["pypdfium2>=5.12"]` and `claude = ["anthropic>=0.116",
"pinakes[pdf]"]` under `[project.optional-dependencies]`. Both floors verified against PyPI
**20260727 16:00**: pypdfium2 5.12.1 is the latest release (uploaded 20260717) and 5.x is a major
jump from the 4.x series that touches the character-level text API I3b targets, so the floor is
deliberately at 5.12 rather than `>=5`; `anthropic` 0.116.0 exists (uploaded 20260702) and 0.120.0
is the latest (20260724), so the floor admits the current SDK without pinning to it. **`[claude]`
depends on `[pdf]`, not beside it**: the paid path slices PDFs into K-page sub-documents, pre-checks
the free text yield before spending, and audits its output against the native text layer — three
pypdfium2 uses, so `pip install "pinakes[claude]"` without `[pdf]` would install a backend that
cannot run its own pre-checks.

`src/pinakes/extract/` becomes a package. `__init__.py` holds:

- **The `Extractor` protocol**, `extract(path, ctx) -> ExtractedText` — and **nothing else**.
  Fingerprint inputs are deliberately *not* a protocol method: they live in the registry entry
  (below), because §4.4's per-document check runs on every query and must never import a backend to
  ask a question about a fingerprint. A method here would be a second source of truth for one dict,
  with nothing asserting the two agree. The `path`-only signature the draft carried could not carry
  what I7b needs — the accountant, the ledger writer, the staging directory, `--force`, and
  page-range resume — so the context is explicit from the start rather than retrofitted at the
  increment that discovers it. `ExtractionContext` is frozen and defined here; its paid fields are
  `None` on the free path.
- **`ExtractedText(text, page_spans, per_page_provenance)` as the one type crossing the seam.** Each
  backend runs its own assembly and returns finished text: the free backend calls I3a's `assemble()`
  over `RawPages`, the paid backend builds it from per-page JSON. `RawPages` / `Page` / `Block` /
  `CharSpan` remain, but they are the **free backend's internal geometry**, not the seam — the paid
  backend has no coordinates to put in them (decision 10), and a protocol that demanded geometry
  would have forced it to invent some. I4 caches `ExtractedText`, I5 chunks it: one type, three
  consumers, no cache-hit path where the consumer wants a field the cache never stored.
- **The registry** — open, module-level, keyed on backend name, with lazy imports: the single best
  structural call of v0.1 (I7), and what lets the real CLI be driven end to end with neither extra
  installed. Both names (`pypdfium2`, `claude-vision`) are **registered at I1** with loaders that
  raise `ExtractionError("the pypdfium2 adapter lands in I3b")` / `ExtractionError("the
  claude-vision extractor lands in I7b")`; manifest validation checks membership in the key set and
  **never imports**, so an unknown backend is rejected at I1 without either extra being installable.
- **Fingerprint inputs, resolvable without importing the backend.** Each backend's *inputs* are
  declared in the registry entry (a client-free dict of version strings and constants); the hash is
  computed by a shared pure function over that dict. §4.4's per-document coherence check (I5) runs
  on **every query**, so if it had to import a backend to ask for its fingerprint, the free query
  path would import `anthropic` on any KB holding one paid document — defeating I7a's gate 4, which
  passes today only because its run list contains no paid documents. Inputs that genuinely need the
  installed library (pypdfium2's version, pdfium's build) are read through `importlib.metadata`, not
  by importing it.

`errors.py` gains `ExtractorMissingError` (names the extra) and `ExtractionError`, both routed
through the existing `_rebuild` helper so they survive pickling (v0.1 I1/I4). `chunk.source_type`
maps `.pdf` → `"pdf"`. `sync._index_document` **routes on `source_type`** instead of unconditionally
calling `read_text(encoding="utf-8")`; a PDF today raises `UnicodeDecodeError` and is logged as an
opaque `index`-stage failure.

**The manifest gains the whole `[extraction]` block here, and `pnk sync` gains `--extract`.** Both
keys (`backend = "pypdfium2"`, `model = "claude-opus-5"`) and the flag land in the increment that
owns the registry, not in the increments that first *consume* them: I6a's estimator needs a model id
and I6b's hooks are written to contain `--extract=pypdfium2`, so if the flag arrived at I7b as the
draft implied, I6b would ship a `pre-commit` hook carrying an argument `pnk sync` rejects — blocking
every commit for anyone who ran `pnk install-hooks` between I6b and I7b. `--extract=BACKEND`
overrides `[extraction] backend` for one run and is validated against the registry's key set, so at
I1 it already rejects an unknown backend and already accepts `claude-vision` with the honest "lands
in I7b" error.

`pnk doctor` gains a `pdf extractor` check: OK when importable, WARN naming the extra when `include`
can match `*.pdf`. Test-environment configuration per rule 6, listed in the ground rules. **Tests:**
registry dispatch and a monkeypatched import failure raising `ExtractorMissingError` naming
`pinakes[pdf]` — **and a second, symmetric case naming `pinakes[claude]`**, since a one-backend test
proves only that one string is right (`tests/test_extract.py`); the I1 stub loaders raising
`ExtractionError` naming their increment, no uninstall needed; the extended `source_type` table
(`tests/test_chunk.py`); a KB with one PDF and no extractor recording a `failures` row at stage
`extract`, indexing the other N−1 documents, exiting non-zero and printing the remedy **once**
rather than per document (`tests/test_sync.py`); an unknown `backend` rejected without importing
anything, and the same for `--extract` (`tests/test_manifest.py`, `tests/test_cli.py`);
`tests/test_packaging.py::test_extractors_stay_extras` asserting neither library appears in
`[project.dependencies]`, so the `check.sh` gate has a test that owns it (rule 8 — v0.1's paid-API
gate had no owner and never shipped), and `::test_claude_extra_requires_pdf_extra`. **Exit criteria
(repo-verifiable):** `./check.sh` green under `--extra light`, under `--extra light --extra pdf`,
and under `--extra light --extra pdf --extra claude`, **and the CI `check` job is a matrix over
those same three legs in this commit** — the draft left the matrix until I7a, so I2 through I6b
would have landed with two of their three environments proved only on a developer's machine while
the ground rules promise "CI runs the legs that make each gate meaningful"; a new `check.sh` gate
fails if `pypdfium2` or `anthropic` appears in `[project.dependencies]`; `pnk doctor` prints the new
check in both install states. **Amendment rows landed here, and exactly these:** §8's pypdfium2 row,
§2.1's `[extraction]` row, both §4.5 rows, §9's torch row, §6.3's `--extract` row, and CLAUDE.md's
core-dependencies row. Also here: the extras gate extends to Pillow (dev-only, and nothing checked
it); the `pytest -m model` step is pinned to the `[light]` leg; and the `filterwarnings` probe and
both marker predicates get named tests, since the risk table already claims the probe as a
mitigation.

### I2 — The synthetic hard-case PDF corpus and its generator

`tests/pdf-corpus/`: a committed generator, the committed PDFs it produces, and one hand-authored
`<name>.expected.txt` per fixture. **No real-world PDF is ever committed** (CLAUDE.md: the repo is
the engine). Ground truth comes from the generator's *spec*, never from extractor output — otherwise
the test asserts only that the extractor agrees with itself. Sized **per stratum, not in aggregate**
(rule 4 — v0.1 sized its golden set at ≥40 in aggregate and still ended with an 8-question no-answer
stratum it had to label a floor):
| Stratum | n | Must contain |
|---|---|---|
| Stratum | n | Pages | Must contain |
|---|---|---|---|
| two-column | 3 | 6 | reading order across a gutter; a caption straddling both columns; a left column ending mid-sentence. **`two-column-a` is a named paid twin** |
| tables | 3 | 6 | a bordered grid; a borderless whitespace-aligned table; one with a spanning header cell. **`tables-bordered` is a named paid twin** |
| headers / footers | 3 | 16 | a repeating running head; page numbers; a footer present only on page 1. **`headers-repeating` is a named paid twin** |
| ligatures & hyphenation | 3 | 6 | U+FB01/U+FB02; a soft hyphen; a word split across a line break **and** one across a page break. **`ligatures-a` is a named paid twin** |
| **scanned / image-only** | 3 | 10 | one 6-page and two 2-page fixtures, rastered from known glyph coordinates with **no text layer** — clean (6pp), skewed 2° (2pp), low-contrast (2pp). The 6-page fixture is deliberately **larger than K = 5**, so a paid run crosses a slice boundary and exercises the short final slice; the draft's "2 pages each … exercised at a slice boundary" is impossible, since `ceil(2/5) = 1`. This is the stratum the paid path exists for |
| pathological | 2 | 2 | a text layer in invisible render mode; a truncated/corrupt-header file |
| baseline | 2 | 13 | plain single-column prose, one page and twelve pages. **`baseline-12p` is a named paid twin** |

Nineteen documents, **59 pages** — a stated total, derived from the column above rather than
asserted loosely, and checked by `::test_stratum_page_counts` so "≥45" cannot quietly become 20. The
headers/footers stratum carries 16 rather than 9 because its repeating-head fixture is 10 pages, the
resolution *T*'s fit needs (I3a).

**≤ 2 MB committed in total, of which the scanned stratum's ten pages are ≤ 1.5 MB.** The headroom
is thin and the arithmetic says so rather than the first regeneration run: six clean pages at ~120
KB leave `(1500 − 720)/4 = 195 KB` for each of the four skewed and low-contrast pages — only
**1.63×** a clean page, on exactly the pages that compress worst, since a 2° rotation destroys the
axis-aligned runs Flate exploits. If generation exceeds it, the fix is a lower DPI or fewer scanned
pages, decided here rather than under a red gate. The text-layer half is comfortable: `2 − 1.5 = 0.5
MB` over 49 pages ≈ 10 KB/page against 1–3 KB actual for raw base-14-font content streams.

**Five fixtures are named `paid twins`** — one per stratum where `layout.py` does real work, plus
the 12-page baseline as a control. They are the documents I7b's human-gated run re-extracts through
the paid backend to measure decision 10's free-vs-paid delta. Naming them here is what makes that
measurement runnable: it needs healthy text-layer PDFs (so `--force` on each), it must cover the
dimensions where bypassing `layout.py` can matter rather than only prose, and a set nobody named is
a measurement nobody can repeat.

The generator writes raw content streams for the sixteen text-layer fixtures, no layout engine and
**no dependencies**: those fixtures are not documents, they are **text layers with known
coordinates** using the PDF base-14 fonts, and a layout engine would hide the variable under test
behind its own decisions.

**The scanned stratum is generated differently, and gated differently.** It rasters the *same*
coordinates as its text-layer twin, so its ground truth is identical to the twin's — which makes
free-vs-paid directly comparable on the same content. Producing that raster needs a renderer and an
image writer: `pypdfium2` (the `pdf` extra, already present) renders the twin's page to a bitmap,
and **Pillow** (new, in the dev dependency group only — never core, never an extra) applies the skew
and contrast transforms and writes the image-only PDF at 150 dpi, 8-bit grayscale, Flate-compressed.
JPEG is never used: JPEG encoders differ across builds, and lossless Flate keeps the pixels the
generator produced. Rendering is not extraction, so rule 5's "the generator is not the extractor"
holds — the ground truth still comes from the spec.

**Byte-identical regeneration is not achievable for rasters** and pretending otherwise would make
the gate CI-flaky on any pdfium or Pillow bump. So the gate splits: the sixteen text-layer fixtures
must regenerate **byte-identically**; the three scanned fixtures are checked by structure (no text
layer, expected page count, image geometry and colour mode) plus a pixel-difference tolerance.

**The tolerance is a changed-pixel fraction, not a whole-page mean, and the arithmetic is why.** A
mean absolute difference of ≤ 2/255 sounds strict and is not: at 150 dpi an A4 page is 1240 × 1754 =
**2,174,960 px**, so a moved 10 pt word at ~20% ink changes ~682 px and moves the mean by ≈0.08.
Reaching 2.0 would take ~17,059 fully flipped pixels — **about two moved lines of text**, counting
both the vacated and the occupied ink at 20% coverage. A whole-page mean therefore accepts arbitrary
reflow while claiming that "a moved word must" fail it. So the gate is: **more than 300 pixels
differing by more than 32 levels is a failure** — an absolute count, ~0.014% of the page. The count
has to be derived, because the obvious fraction is wrong twice over: 0.1% of 2,174,960 px is 2,175
px, while a moved 10 pt word changes ~682 px on the plan's own realistic-ink model (mean 0.08 →
`0.08 × 2,174,960 / 255`) — so a 0.1% gate **passes** a moved word exactly as the whole-page mean
did. One ink model, stated once: a 6-character 10 pt word at 150 dpi occupies a ~62 × 15 px box at
~20% ink ≈ 200 ink px, and moving it changes both the vacated and the occupied ink ≈ **400–680 px**.
300 sits ~2× under that signal and ~7× over sub-pixel noise on a handful of glyphs. The computation
and this arithmetic go in the test's docstring, so the next reader checks the number rather than
trusting it. A renderer bump that exceeds the tolerance is handled by regenerating and committing
the new rasters in a commit that says so.

**Regeneration is deterministic outside pytest too.** `SOURCE_DATE_EPOCH` is pinned in pytest
configuration (rule 6), but `make corpus` and `check.sh` run the generator *outside* pytest — so the
generator sets it itself when unset, and the `check.sh` gate exports it explicitly. Without that,
the gate's own regeneration rewrites the committed fixtures with fresh `CreationDate`s and then
reports the diff it just created.

**Tests:** `tests/test_pdf_corpus.py` — byte-identical regeneration of the text-layer strata (fixed
producer string, `CreationDate`/`ModDate` from `SOURCE_DATE_EPOCH`); the scanned stratum's
structural + tolerance check, `pdf`-marked and skipping with a printed reason when the extra **or
Pillow** is absent — the marker's predicate checks all three parts of its environment claim, not two
(rule 5, in the increment that first needs the third); every fixture has a ground truth and every
ground truth a fixture; stratum counts **and page counts** match the table, so the corpus cannot
silently shrink; the committed byte budget holds; `::test_named_paid_twins_exist`, so I7b's
human-gated measurement cannot reference a fixture nobody committed. **Exit criteria
(repo-verifiable):** `make corpus` leaves no diff on the text-layer strata and no tolerance breach
on the scanned stratum, and that check is a gate in `check.sh` that skips the scanned half with a
printed reason when its dependencies are absent. **Not** an exit criterion: any claim about
extraction quality — nothing extracts yet.

### I3a — Extraction core, pure (rule 11: the pure half)

`extract/layout.py`, operating only on the I1 types — no PDF library, no filesystem:

- `blocks_from_chars(chars) -> list[Block]` — pdfium's text API is **character-level**, and turning
  a character stream into blocks is the single densest piece of logic in the extraction path. It
  lives here, pure and table-driven against hand-built character streams, rather than inside the I/O
  adapter where it would be reachable only through an end-to-end corpus score (rule 11). -
  `reading_order(page)` — column detection by x-gap clustering, then top-to-bottom within a column.
- `strip_running_heads(pages)` — a line recurring in the same y-band on ≥ *T* of pages, digits
  normalised, is a running head. ***T* = 0.6, and it is fitted, not guessed** — it changes the
  extracted text and therefore the free backend's fingerprint, so a bare constant would be exactly
  the "threshold with no provenance" I3b refuses elsewhere. I3b fits it over the headers/footers
  stratum (whose spec states which lines are running heads and which are genuine headings) and
  commits it with `fitted_on` beside the text-yield floor; 0.6 is where the fit begins, not the
  shipped answer. **The fit needs resolution the draft's stratum could not give it**: at 3 pages per
  document a per-document recurrence can only be 1/3, 2/3 or 3/3, so *every* T in (1/3, 2/3]
  reproduces the corpus identically and "fitted" would name a value the data never chose. The
  repeating-running-head fixture is therefore **10 pages**, giving resolution 1/10. **T reaches
  `layout.py` as a parameter, never a file read** — this module is pure and `test_layout_is_pure`
  asserts it — so the caller passes it in and I3a's table-driven tests are parameterised on it
  rather than hard-coding 0.6. **The threshold ships with its denominator**: the result reports
  `suppressed N of M pages`, so a wrong *T* is visible instead of silently eating a real heading.
- `join_hyphenation(lines)` — join a trailing hyphen or U+00AD when the continuation starts
  lowercase; never join across a heading or a suppressed running head.
- `normalise(text)` — ligature expansion (U+FB00–U+FB06), NFC, an explicit whitespace policy. **It
  lives in `extract/textpolicy.py`, not here** (decision 15): it is the one stage *both* backends
  run, it is pure string→string with no geometry, and leaving it inside `layout.py` made decision
  10's claim that the paid backend "bypasses `layout.py`" false — while `LAYOUT_VERSION`'s exclusion
  from the paid fingerprint then left a whitespace or ligature change invisible to every paid cache
  entry and to §4.4. It carries **`TEXT_POLICY_VERSION`**, hashed into **both** backends' inputs.
  **It is length-changing** — one ligature codepoint becomes two or three, NFC composes, whitespace
  collapses — so the increment that emits offsets states the order explicitly (below), and the paid
  backend states it too (I7b).
- `assemble(pages) -> ExtractedText(text, page_spans, per_page_provenance)` — three fields, the same
  three everywhere (I1); the free backend supplies an empty `per_page_provenance`. **Offsets are
  emitted with the text, never accumulated afterwards** (v0.1 I6: spans from `match.start()` rather
  than a running total are exact by construction rather than by accounting). **Offsets are computed
  *after* `normalise()`, never before** — stated because `normalise()` is length-changing, so
  computing spans on pre-normalised text puts every offset past the first ligature out by one or
  two, while the spans still tile perfectly and no property assertion can see it.
- `LAYOUT_VERSION` — a hand-bumped constant hashed into the fingerprint of **every backend that runs
  this module**, because a change to reading order or hyphenation changes the extracted text as
  surely as a library upgrade does, and would otherwise be a silent cache hit. With `normalise()`
  moved out (decision 15), that is genuinely the free backend only, and the split is now honest in
  both directions: a reading-order refactor cannot invoice a paid KB, and a whitespace-policy change
  cannot slip past a paid fingerprint. A free-path constant must not be able to send an invoice —
  and a shared constant must not be able to hide behind that rule.

**Tests:** `tests/test_extract_layout.py`, table-driven — one case per rule, plus the compound cases
a table cannot express: a two-column page continuing mid-sentence into the right column; a running
head that is a genuine heading on exactly one page; a hyphenated word split across a page break
where the next page opens with a running head. `blocks_from_chars` gets its own dense table:
characters out of reading order, a word split across two text runs, overlapping bounding boxes, a
zero-width character, and a page with no characters at all.

**Three property assertions over every case, and the third is the one that matters.** The draft
named two — `"".join(text[s:e] for s, e in page_spans) == text`, and `page_spans` contiguously
covering `[0, len(text))` — which are **one property and its own corollary**: contiguous
non-overlapping coverage *entails* the join identity, so the pair is the tautological-test shape
this repo already killed once (`docs/RETROSPECTIVES.md`, I2). Worse, neither says *which page* a
span belongs to: `page_spans = [(0, L), (L, L), (L, L), …]` satisfies both, and every `page_start`
in the index silently becomes 1. So the third assertion is **content-anchored**: for each page, a
sentinel string the fixture spec places on that page and no other must fall inside that page's span.
That is the assertion a wrong page number cannot survive. **Exit criteria (repo-verifiable):** one
named test per rule and per compound case; all three properties hold across all of them; an
import-graph test asserts `layout.py` imports no PDF library and `textpolicy.py` imports neither
that nor `layout.py`.

### I3b — The pypdfium2 adapter, the quality metrics, and the two fitted floors (rule 11: I/O)

`extract/pdfium.py` is a **thin reader**: open a file, guard its size against **256 MB** (a stated
number, tested at the boundary — large enough that no legitimate corpus document trips it, small
enough that a runaway file fails fast instead of exhausting memory), refuse an encrypted or
corrupt-header file with a named error, and emit `list[CharSpan]` per page for I3a's
`blocks_from_chars` to assemble. It also exposes **`slice_pages(path, first, last) -> bytes`**,
which writes a K-page sub-document — I7b's request unit (decision 8), put here because this is the
module that owns pdfium and I7b must not become a second PDF I/O boundary. **The API is verified
against pypdfium2 5.12.1 (20260727 17:00), not assumed:** `PdfDocument.new()`, `import_pages(pdf,
pages=None, index=None)` and `save(dest)` all exist, and a round trip over a 12-page document
produced a 5-page slice and a 2-page short final slice, ~1 KB each. Decision 8 rests on this call
existing in the BSD-licensed library the project already chose, so it is checked here rather than
discovered at I7b. Everything that decides *what the text is* lives in the pure half; what remains
here is I/O, and that is where review effort is budgeted (rule 11's corollary: v0.1's pure half
produced one finding and the I/O half four, all environmental).

Its option set is **pinned and hashed** into the fingerprint. Fingerprint **inputs** are declared in
I1's client-free registry entry and hashed by one shared pure function — per-backend *inputs*, not
one shared formula, because a shared formula would hash `pypdfium2`'s version for a backend that
never loads it while omitting the model that actually produced the text (pass 1, H4); and inputs
rather than a method on the backend, because §4.4's per-document check runs on every query and must
never import a paid client to ask a question about a fingerprint (pass 2, M-1). This backend's
inputs are backend name, pypdfium2 version, pdfium build, the option dict, `LAYOUT_VERSION` and the
fitted running-head threshold *T*; the paid backend's are stated in I7b. The value is written to
`documents.extraction_fingerprint` (with `documents.extraction_backend`) at sync, and into the I4
cache key.

`extract/quality.py` scores an extraction against a ground truth. **Every rate ships with its
denominator as a co-metric** (rule 3 — v0.1's `false_abstain: 0.0` was vacuous and would have passed
a CI gate forever):
| Metric | Numerator | Denominator |
|---|---|---|
| `char_recall` | expected non-space characters found, in order | expected non-space characters |
| `order_fidelity` | LCS length over word sequences | expected word count |
| `junk_rate` | extracted words absent from the ground truth | extracted word count |
| `pair_adjacency` | asserted (label, value) pairs within **80** characters of each other — a bare constant with a stated basis rather than a fit: correct reading order puts a label and its value 0–40 characters apart, a reading-order failure puts them hundreds apart, so any value in ~50–150 separates the two cases and 80 is the midpoint | asserted pairs |
| `word_coverage` | significant native-layer words present in the extraction | significant native-layer words |
| coverage | `documents_scored`, `pages_scored` | `documents_total`, `pages_total` |

`word_coverage` is the PDFScout completeness audit, defined here as a pure function of two strings
so it can be tested against hand-computed answers; I7c consumes it as the paid path's re-extraction
trigger, and a metric that can be wrong on a known input is not fit to gate anything.

**v0.2 fits exactly one threshold that gates spending, and says plainly why the other cannot be
fitted yet.**

- **There is no `word_coverage` floor in v0.2** (decision 12). Two review passes each tried to fit
  one and each picked a pair that is not the pair it is applied to: against the hand-authored ground
  truth (the draft), then against pypdfium2's own extraction (pass 2's fix). The applied pair is
  (native layer → **Claude's** output), and **no Claude output exists at I3b**, so the correct pair
  is unavailable by construction. Fitting against pypdfium2 is worse than arbitrary: pypdfium2's
  extraction *is* the native layer transformed by `layout.py`, so that distribution measures
  `strip_running_heads` and `join_hyphenation` rather than extraction fidelity — and the six
  non-scanned strata include the corrupt-header fixture this increment's own reader contract
  requires to **raise**, so a 5th percentile over ~16 values is the minimum in all but name and the
  floor lands near zero, leaving the audit inert on every installed copy. The metric ships, is
  scored and is reported; the threshold and the re-extraction loop land in **v0.3**, fitted on a
  real distribution. A threshold that spends money may not be guessed, and "fitted" against the
  wrong population is a guess wearing a measurement's clothes.
- **The `text yield` floor is fitted here**, because its pair *is* available: it compares a free
  extraction against nothing at all — it counts characters. Non-whitespace characters **per page**;
  the three scanned fixtures are the positive control at zero, the twelve-page baseline the negative
  control. It is consumed by I7b's free-path pre-check *and* I8's `pnk doctor` check, so it is
  defined here rather than in either, because neither may define it for the other. **Its blind spot
  is stated rather than discovered:** the pathological stratum's invisible-render-mode fixture
  yields characters while being useless text, so it clears the floor and still needs the paid path.
  The fitting population contains no bad-but-nonzero case, so this floor separates *empty* from
  *non-empty* and nothing finer. `--force` is the documented escape and `pnk doctor`'s remedy says
  so.

**The floor ships as package data and fails closed when absent.** `extract/floors.toml` sits beside
I6a's `prices.toml` in the wheel, written by the fitting run and carrying each value's `fitted_on`
(corpus revision + `YYYYMMDD HH:MM`). `tests/pdf-corpus/baseline.json` is a *test* artifact and is
not installed, so keeping the floor only there would give every installed copy of Pinakes no floor
at all while two runtime consumers on a user's own KB depend on it. **Absent the fitted floor, the
paid path refuses to run** rather than paying to re-extract a PDF it cannot prove is unhealthy — the
same fail-closed shape as v0.1's `confidence: unknown`. `floors.toml` is packaged and asserted here;
**`prices.toml` is I6a's to package and assert**, with its own
`::test_prices_are_installed_package_data`, because a file that ships from a source tree and not
from a wheel is broken on every installed copy from I6a to I9 with the whole suite green — the exact
failure this paragraph identifies for floors. I9's wheel smoke asserts both in the built artifact,
so neither rests on a source-tree test alone. The running-head threshold *T* (I3a) is fitted in the
same run and written to the same file; it costs no money, so its absence is a startup error rather
than a refusal to spend.

**Nothing here re-fits automatically, so `fitted_on` can go stale while still reading as evidence.**
A `layout.py` or `textpolicy.py` change silently invalidates *T* and any committed floor. `make
pdf-eval` therefore recomputes the floors and **fails if a committed value has drifted** beyond the
per-stratum tolerance — the fit is a gate, not a one-time ceremony.

`python -m pinakes.extract.quality tests/pdf-corpus` writes a JSON report; `--baseline` compares
**per stratum** with a tolerance, mirroring `eval.compare`; `make pdf-eval` wraps it and joins
`check.sh` **and CI, as its own job, in this commit** — the draft left the CI job until I9, so the
extraction-quality baseline would have gated nothing across six intervening increments. It skips
with a printed reason when `pinakes[pdf]` or the corpus is absent (I1's exit criterion is that the
gate is green under `--extra light`). The scanned stratum's expected free-path `char_recall` is
**0.0** — a declared zero with its fixtures named, not a passing score.

**Every rate ships its denominator, and a denominator that is legitimately zero is declared, not
asserted away.** "Every stratum's denominators are non-zero" cannot hold and should never have been
an exit criterion: the scanned stratum has no native layer, so `word_coverage`'s denominator is zero
*by design*, and it extracts no words on the free path, so `junk_rate` is `0/0`. The corrupt-header
fixture cannot be scored at all. So `baseline.json` carries an explicit **per-metric, per-stratum
exemption map** with a reason string per entry, and the gate asserts that every non-exempt
denominator is non-zero **and** that every exemption is one of the declared ones. Rule 3's point is
that a rate with no denominator is invisible — an exemption with a printed reason is the opposite of
invisible.

**Tests:** `tests/test_extract_quality.py` — each metric on hand-built strings with hand-computed
answers; `documents_scored == documents_total` for every **non-exempt** stratum; a planted
regression (one fixture's extraction truncated to its first page) **fails** the baseline comparison;
the text-yield floor and *T* are present in the **shipped** `floors.toml` with `fitted_on`
(`::test_floors_are_installed_package_data` reads them through `importlib.resources`, the way a user
would, not through a repo-relative path); `::test_a_drifted_floor_fails_the_gate`;
`::test_exemptions_are_declared_with_reasons`. `tests/test_extract_pdfium.py` (`pdf`-marked) — the
reader's own contract: the corrupt-header fixture raises a named error rather than crashing, the
size guard fires at 256 MB, an encrypted file is refused before any parse, the invisible-render-mode
fixture yields the characters it actually contains, a zero-page file is an error not an empty
success, and `slice_pages` returns a valid sub-document whose page count and text match the
requested range (including a range that runs past the last page). `tests/test_check_script.py` —
`pdf-eval` skips with its printed reason when the extra is absent, and `check.sh` still exits
0. **Not tested here:** the paid path's refusal when the floor is absent — that is I7b's test, in
  I7b's file, because an exit criterion that depends on a later increment's code is not an exit
  criterion. **Exit criteria (repo-verifiable):** `make pdf-eval` green against a committed
  `tests/pdf-corpus/baseline.json` **and green as a CI job**; the planted-regression test passes by
  failing the comparison; every non-exempt denominator is non-zero and every exemption carries a
  reason; the text-yield floor and *T* committed in `floors.toml` with `fitted_on`, present in the
  built wheel, and re-fitting produces no drift; DESIGN §7 amended in this commit.

### I4 — The extraction cache

`extract/cache.py`. One JSON per entry — `{schema, content_hash, backend, fingerprint, page_count,
page_spans, text, per_page_provenance, operation_id, call_ids}` — because a text/JSON pair
introduces a half-written-pair corruption class that need not exist. It stores exactly I1's
`ExtractedText` plus its provenance, so what a cache **hit** returns is what a cache **miss**
returns; a draft in which I5 consumed "the extraction's blocks" while the cache stored only text and
spans would have worked on every miss and failed on every hit, which is rule 2's failure shape in
the one increment pair the plan cites to justify the trace test.

`operation_id` and `call_ids` are the join key to the ledger. Without them I7c's promise that
`--clear-cache` prints what the cached entries cost is unimplementable: no cache entry recorded a
call and no ledger line records a document, so there was nothing to join on. `call_ids` is a list
because one paid document is several K-page requests (I7b); both are `null` for a free extraction,
which costs nothing and joins to nothing. The ledger's no-content rule is untouched — the identifier
travels from the cache to the ledger, never document text from the ledger to anywhere.

Written to a sibling temp and `os.replace`d. Invalidation is **by key, never by mutation**: an
edited PDF or a changed fingerprint simply misses.

```text
.pinakes/ ├── index.db          ← disposable, rebuilt ├── ledger.jsonl      ← NEVER touched (§6.3)
└── cache/extract/ ├── <content_hash>-<fingerprint>.json      ← a complete extraction └──
partial/<content_hash>-<fingerprint>/  ← per-page staging, written only by I7c
```

Eviction: after a **fully successful** sync, entries whose `content_hash` matches no active document
are removed — never after a run that recorded failures, never during `--sidecars-only`, never during
`--rebuild` before the swap succeeds, and **never for an entry written by a paid backend**. That
last exclusion is the same rule I7c writes into CLAUDE.md, applied to the path that would otherwise
break it silently: a soft-deleted document (§6.4 keeps `state = deleted`) is not an "active
document", so a routine delete or an un-sidecarred rename would sweep away an extraction the user
paid for — with no prompt and no printed euros, while the *manual* `--clear-cache` gains both. Paid
orphans are **reported** by the sweep and by `pnk doctor` — as `paid orphans: N` here, since neither
the ledger reader nor a paid cache entry exists yet, and **I7c adds the euros** in the same commit
as `--clear-cache`'s. That is the split this increment already makes for `--clear-cache` a few lines
below; making it for one line and not the other is how a forward reference ships. They are removed
only by an explicit `--clear-cache=paid`. `pnk sync --clear-cache` (named in §6.3, never
implemented) empties `cache/` only; `--rebuild` leaves it alone, which is the whole point of a
cache.

**`--clear-cache` confirms before it destroys.** It prints the entry count and bytes it is about to
remove and requires a `y`; `--yes` skips the prompt for cron use. I7c extends the printed summary
with the euros those entries cost, read from the ledger, and amends CLAUDE.md's "`.pinakes/` is
disposable" invariant to except the paid extraction cache — the two halves are split because the
paid entries and the ledger reader do not exist until then, and a plan that pretends otherwise is a
plan with a forward reference in its code.

`pnk doctor` reports entry count, bytes, `orphans / entries`, and paid orphans separately.
**Tests:** `tests/test_extract_cache.py` — a second sync of an unchanged PDF calls the adapter
**zero** times (spy-counted); a changed document, a changed fingerprint and a truncated cache file
each miss rather than crash; two KBs holding the same PDF get two cache files;
`::test_the_sweep_spares_paid_entries` — a soft-deleted document whose entry was written by a paid
backend survives a fully successful sync and is reported, while its free twin is swept. Plus
`tests/test_sync.py::test_clear_cache_preserves_the_ledger` and
`::test_clear_cache_without_yes_and_without_a_tty_aborts`. **Exit criteria (repo-verifiable):** the
zero-call assertion; the ledger-survives assertion; the paid-entry survives the sweep;
`--clear-cache` and `--yes` in `pnk sync --help` and in §6.3's prose in the same commit.

### I5 — PDF chunking, page provenance, and the span invariant (`schema_version` 2)

`chunk_document(kind="pdf")` consumes **I1's `ExtractedText`** — text plus `page_spans`, the same
object on a cache hit and a cache miss — and splits at page boundaries so a chunk never spans two
pages without recording both. It does not consume blocks: blocks are the free backend's internal
geometry, the cache never stored them, and the paid backend never had them. The `_fit` / `_atomise`
machinery is reused unchanged — and its guarantees are **re-asserted for the new path, not
assumed**: the token limit, never-trim, and the overlap-must-not-breach-`max_tokens` rule that
v0.1's I6 found broken. `store.py`: `chunks.page_start` / `page_end` (INTEGER, NULL for non-paged
sources), `ChunkRow` and `Chunk.as_row()` grown together (already coupled), `SCHEMA_VERSION = 2`.
`heading_path` stays **NULL** for PDFs — a PDF has pages, not headings, and stuffing `"p. 7"` into a
free-text filter/citation column is v0.1's I6 defect rewritten.

**The fingerprint is per document, not per index.** `documents` gains `extraction_backend` and
`extraction_fingerprint` (TEXT, NULL for non-extracted sources). A single `meta` key could not
describe a corpus where `pnk sync --extract=claude-vision` ran over some documents and the free path
over others — which the CLI explicitly allows — so it would either be wrong or refuse every query on
a perfectly coherent KB after any `--extract` run. §4.4's coherence gate therefore checks **each
document against its own recorded backend**: for every distinct `(backend, fingerprint)` pair among
active documents, re-derive that backend's current fingerprint **from I1's client-free registry
inputs** and compare. Any mismatch **refuses the whole query**, naming the affected documents and
the count, with the same remedy shape as a model mismatch — partial results from a half-stale index
are the silent-garbage failure §4.4 exists to prevent.

**A free mismatch refuses; a paid mismatch WARNs and marks** (decision 13). The draft refused both,
which meant a `uv lock --upgrade` bricked every paid KB: `claude-vision`'s inputs include
pypdfium2's version and pdfium's build (I7b), the fingerprint is in the cache key, and the remedy —
a rebuild — is therefore a cache miss and a fresh invoice. The model-mismatch precedent the draft
borrowed does not transfer: embeddings computed under a different model are *meaningless*, so
refusing is the only honest answer, whereas text extracted by an earlier extractor version is still
**correct text** — merely older. So a paid mismatch WARNs in `pnk doctor`, and every affected result
on both surfaces carries a `stale_extraction` marker naming the recorded fingerprint. The free
backend keeps the hard refusal, because there the remedy costs nothing.

Resolving inputs rather than importing the backend is what makes this affordable and safe. The draft
had the check re-derive the fingerprint from a method on the backend, which on a KB holding one paid
document would have imported `anthropic` **on every query** — violating the invariant I7a's gate 4
asserts, and passing CI only because that gate's run list uses a KB with no paid documents. It also
carried a false rationale for skipping: an absent `anthropic` was said to mean the extraction cannot
have changed, but claude-vision's fingerprint inputs are model id, prompt version, schema version
and request-shape version — **all package constants**. A `pinakes` upgrade that bumps the prompt
genuinely changes the extraction whether or not the SDK happens to be installed, so the skip
disabled the check on precisely the backend where staleness costs money. With inputs in the registry
the check **always runs**; the only genuine skip left is a backend name the registry does not know
at all (a KB written by a future version), which `pnk doctor` WARNs about and queries proceed past,
because a fingerprint that cannot be computed cannot be compared.

**The backend is part of the change decision, in the paid direction only** (decision 9). The
decision table lives in **`pairing.py`**, not `sync.py` (`sync.py:3`: *"The decisions all live in
`pairing.py` (§6.4); this module does the I/O around them"*), so the work lands there:
`pairing.IndexedDocument` gains the backend, `read_index_snapshot`'s `SELECT` (`sync.py:180`) gains
the column, and `pair()`'s ordered table gains the clauses below. Naming the wrong module is how an
increment's review effort lands on the wrong file. §6.4's "path and hash unchanged → skip" was the
only re-processing trigger, so two silent failures followed from I6b's hook rule and I4's cache
respectively: a scanned PDF committed while hooks force `--extract=pypdfium2` is indexed as empty
and then **skipped forever** by every later manual sync, on a KB whose manifest asks for the paid
backend; and `--rebuild` re-extracts a paid document with whatever the manifest says, missing the
paid cache entry and overwriting it with pypdfium2's empty output. §4.4 cannot see either, because
each document still matches *its own recorded backend* — which is now the free one. Both are the
silent quality loss §9 names as this release's reason to exist. The rule:

- A document whose recorded backend is **free** and whose effective backend for this run is **paid**
  is stale: it is re-extracted, subject to every cap and confirmation. - A document whose recorded
  backend is **paid** and whose effective backend is **free**, with an **unchanged** content hash,
  is **never** re-extracted. Not by a hook, not by `--rebuild`, not by `--extract=pypdfium2`. The
  existing extraction stands and the run says so once. Only `--force` **together with an explicit
  free `--extract`** overwrites it, and it prints what it discards first. - The same document with a
  **changed** content hash is neither skipped nor overwritten: it is a **`failures` row** naming the
  path, the run exits non-zero, and the remedy names the paid re-extraction (decision 14). This is
  the collision §6.4's ordered table would otherwise resolve silently in favour of "hash changed →
  re-chunk", and it is the *common* case, not an exotic one: hooks force the free backend on every
  commit, so re-scanning a paid-extracted PDF hits it. Letting the hash win overwrites paid text
  with pypdfium2's empty output on an image-only PDF; letting the backend win leaves the index
  describing a file that no longer exists, permanently, because the hash never changes again. A
  failure row is the only outcome where nothing is silently wrong, and it isolates per document
  exactly as v0.1 already does.
- `pnk doctor` lists all three gaps by path: documents awaiting a paid extraction, documents whose
  paid extraction the manifest no longer asks for, and paid documents whose file has changed.

**`pnk sync` gains `--force` here**, in the increment that first needs it — not at I7c, which only
*completes* its two-part scope. `cli.py:334` has `--force-unlock` today and nothing else, so I5's
`test_backend_drift` case 5 and I7b's healthy-PDF refusal would both have referenced a flag that did
not exist. That is pass 2's hooks-shipping-an-unbuilt-flag defect for the third time, and this is
where it stops.

**Provenance lives in the sidecar, because `--rebuild` discards the index** (decision 11). Verified
against `sync.py:231-241`: a rebuild opens `<index>.db.new`, calls `store.create(target)`, and then
reads `before = read_index_snapshot(connection)` from that **empty** database — so every document is
"new" and nothing above can consult a recorded backend that lives only in `index.db`. I5's own "a
paid-indexed PDF is not re-extracted by `--rebuild`" case is unimplementable without this. So the
sidecar's existing `provenance:` block gains `extraction: {backend, fingerprint, extracted}`, and
`documents.extraction_backend` / `extraction_fingerprint` become the index's *cache* of it, seeded
from the sidecars on a rebuild.

This is deliberately **not** the content hash DESIGN §2.2 refuses to put there, and the amendment
says so in the same breath: a content hash dirties the sidecar on every document edit and is stale
in exactly the window between an edit and the next sync. The extraction record changes only when the
*extractor* changes, is written by the same sync that does the extraction, and is stale in no state
the index can detect — which is why it must survive the index rather than live inside it. It is also
what DESIGN §2's own rule requires: anything that must outlive the index lives in a committed file.
**Writing it is new code, and the cost is real.** Verified: `write_sidecar` has exactly two call
sites — inside `_mint` (`sync.py:392`), which runs only when `doc_id is None`, and inside
`_write_missing_sidecars` (`sync.py:415`), the `--sidecars-only` path. A document that **already has
a sidecar** — precisely decision 9's case — is never rewritten today, so I5 adds an additive
read-merge-write for `provenance.extraction`. Two consequences, both accepted deliberately:

- **`sidecar.py`'s own docstring defers this**: *"In v0.1 sidecars are created and moved, never
  rewritten in place, so PyYAML dropping comments on dump costs nothing yet. `pnk link` (v0.3) is
  what will need a comment-preserving writer."* v0.2 pulls that forward without solving it, so a
  hand-annotated sidecar **loses its comments and has unknown keys re-sorted** the first time that
  document is paid-extracted. The write happens only on a deliberate paid extraction, never on a
  hook; the alternative was building v0.3's round-trip writer here, which is a whole increment of
  unasked-for scope. CLAUDE.md's "`docs/` belongs to the user" gains the sentence that sync now
  writes `provenance.extraction` into an existing sidecar.
- **`--index-only` cannot write into `docs/` at all** (`sync.py:388-390` returns before the write,
  and `test_index_only_never_writes_into_docs` asserts it). That contract is **kept**: a paid
  backend under `--index-only` is **refused** with a remedy naming a normal sync. It costs nothing,
  because `--index-only` is what the `post-commit`/`post-merge` hooks run and I6b already forbids
  hooks from spending.

A document with no sidecar yet has no protection and is treated as free-extracted, which is correct
— a paid extraction is a human invocation, and a human invocation writes the sidecar. **Tests:**
`tests/test_chunk_pdf.py` — **the span invariant over all 19 fixtures and every chunk** (`chunk.text
== extracted.text[char_start:char_end]`); every character of the extraction lands in ≥1 chunk
(v0.1's never-drop guarantee, re-proved here); page numbers monotonic and within `1..page_count`; a
two-page chunk recording both; `heading_path is None` for every PDF chunk.
`::test_a_rebuild_preserves_paid_provenance` — paid-extract, `pnk sync --rebuild` under a free
manifest, assert the document is untouched and the sidecar's `provenance.extraction` seeded the new
index. `::test_a_rebuild_after_clear_cache_still_preserves_it` — the sequence a cache-based answer
would have failed. `tests/test_store.py` — schema version 2, and a version-1 index refusing to open
naming `pnk sync --rebuild`. `tests/test_search.py` —
`::test_a_changed_free_fingerprint_refuses_the_query` (a mixed-backend index where every document
matches its own backend queries fine; mutating one **free** document's stored fingerprint refuses
with the remedy and names it); `::test_a_changed_paid_fingerprint_warns_and_marks` (mutating a
**paid** document's fingerprint leaves the query working, marks that result `stale_extraction`, and
WARNs in doctor — decision 13, with both halves asserted, because a rule with two outcomes tested on
one outcome is half a rule); an **unknown** backend name WARNs and does not refuse; and
`::test_coherence_never_imports_a_paid_client` — a query over a KB holding a paid document, run in a
fresh subprocess, asserting `"anthropic" not in sys.modules` (the case I7a's gate 4 does not cover).
`tests/test_sync.py::test_backend_drift`, **six** cases: a free-indexed PDF re-extracted by a paid
run; a paid-indexed PDF **not** re-extracted by a hook-style free run, by `--rebuild`, or by
`--extract=pypdfium2`; `--force` plus an explicit free `--extract` overwriting it only after
printing what it discards; and **a paid-indexed PDF whose content hash changed under a free run
landing in `failures` with the paid remedy** (decision 14) — the case the draft's four omitted and
the one that actually happens. **Exit criteria (repo-verifiable):** the invariant holds over the 15
extractable fixtures with the four exclusions declared; `SCHEMA_VERSION == 2`; a v0.1 index raises
`IndexSchemaError` with the rebuild remedy; both coherence outcomes pass; the coherence check
imports no paid client; all six backend-drift cases pass; both rebuild-provenance tests pass; §2.2,
§4.4 and §6.4 amended and CLAUDE.md's `docs/` rule extended, with the CHANGELOG entry stating the
forced rebuild, all in this commit. **Amendment rows landed here, and exactly these:** §4.6, §4.4,
§2.2, §6.4, §3, and CLAUDE.md's `docs/` row. `test_coherence_never_imports_a_paid_client` skips
loudly when `anthropic` is absent rather than passing vacuously — I7a's gate-4 lesson, applied to
the test that reaches this invariant two increments earlier.

### I6a — Budget core, pure (rule 11: the pure half of the money machinery)

`src/pinakes/budget/`, with **no I/O and no `anthropic` import**. `prices.toml` ships in the wheel
like the templates, carrying `as_of` as `YYYYMMDD HH:MM`, per-model `input_per_mtok_usd` /
`output_per_mtok_usd`, and — because the manifest prices in EUR and the vendor in USD — a
`usd_per_eur` rate carrying the **same** `as_of`. It does **not** carry cache-write or cache-read
multipliers: decision 8 removed the only marker that would have used them, and a shipped constant
with no consumer is the unused-column cost v0.1's I6 already paid. Seeded from the current table:
`claude-opus-5` at $5.00 / $25.00 per MTok, with `usd_per_eur` seeded at **1.08** carrying the same
`as_of`. The rate is stated because every euro figure in this plan depends on it — a slice is
$0.3515 ≈ €0.32 only at 1.08 — and a euro number whose rate is implicit is exactly the unprovenanced
figure I6b's ledger rule exists to prevent. Euro figures below hold at the seeded rate.

**`estimate.py` estimates over *requests*, and a request is a K-page slice** (decision 8). Getting
the unit wrong is the difference between a reservation that bounds a run and one that is wrong by an
order of magnitude — in either direction. Pass 1 corrected a per-*page* estimate that under-reserved
against whole-document requests; pass 2 then showed the whole-document request shape is itself the
defect, because it makes input quadratic and stops fitting the context window. Sending each page
exactly once restores the linear model pass 1 was right to reject only because the request shape
made it false.

So: **worst case per request = `(K × page_tokens + prompt_tokens) × input_price + max_tokens ×
output_price`**, and a document is `ceil(N / K)` requests. `prompt_tokens` — the instructions plus
the JSON schema, a measured module constant — is in the formula because a "worst case" that omits a
term is not a worst case, and `test_reservation_bounds_every_usage_table` asserts the bound. It is
~1% of a slice, but the only slack absorbing it would be the page constant, which is itself
unmeasured until I7b.

There is **no cache multiplier**, because there is no `cache_control` marker. The reason is not that
slices share no prefix — the system prompt and instructions *are* a shared prefix, rendered ahead of
the document block — but that the prefix is a few hundred tokens against `claude-opus-5`'s 512-token
cache minimum, so it very likely cannot be cached at all, and even cached it is ~1% of a request
dominated by 30,000 document tokens. Paying a 1.25× write premium to chase that is a loss.

The output bound is `max_tokens` — the request's hard output ceiling, which on `claude-opus-5` caps
thinking and response text *together*, so it is the correct and only safe bound. Reconciliation
folds the response's true `usage` (`input_tokens`, `output_tokens`) back in and returns the
difference to the running total.

**The whole document is checked against all three windows before the first call.** Per-call
reservation alone bounds each call and nothing else: with `per_operation_eur` and `daily_eur` raised
and the shipped `monthly_eur = 5.00`, a 200-page document reserving ~$14 proceeds to roughly call 15
and then refuses (out of the box `per_operation_eur = 0.05` refuses at call 1 and `daily_eur = 1.00`
at call 4, so this is the measurement KB's state, not a fresh install's), `on_exceed = "abort"`
stops the run, I7c's all-or-nothing rule writes no cache entry, and the user has spent the month's
budget with nothing in the index. The staging directory means the money is recoverable next month
rather than lost — but nothing told the user the run was doomed before it started, and
`--clear-cache` destroys the staging. So `reserve_document(estimate, caps, spent)` runs first and
refuses up front with the same three-window message, and `pnk sync` says how many documents in this
run it can afford. **It bounds the no-retry path, and says so**: I7b's ceiling is 6 semantic calls
per slice with the truncation retry at `max_tokens = 16000`, so a worst-case slice costs `5 ×
$0.3515 + $0.5515 = $2.31` — **6.6×** the single-attempt estimate, and $92 rather than $14 for 200
pages. Reserving at the worst case would refuse every real document under any shipped cap, so the
plan reserves at the estimate and states the multiplier in the same breath rather than letting a
reader infer a guarantee the number does not carry. The per-call checks are what actually stop the
spend; `test_an_unaffordable_document_is_refused_before_the_first_call` asserts the multiplier
alongside the refusal, so the gap is a tested constant rather than a surprise. The per-call checks
stay: they are what catches a run that drifts past its estimate mid-flight.

**`confirm_above_eur` prompts once per operation, against the whole-document estimate** — never per
call. DESIGN §5 places the prompt at estimate time, and a per-call reading with the shipped
`confirm_above_eur = 0.01` against a ~€0.32 slice would prompt **forty times** for one 200-page
document, which is how a confirmation becomes something a user learns to hold down `y` through.

**A context-window pre-check is part of the estimate, not an afterthought.** `K × page_tokens` plus
the prompt must fit the model's documented maximum input — 1M tokens on `claude-opus-5`. At K = 5
and the conservative 6,000-token page constant that is 30K, so it never fires in practice; it exists
because its absence is precisely what made the draft's design unimplementable at 166 pages while its
only size check tested a 600-page API limit that would never be reached. A check that cannot fire
under the shipped constants but names the exact limit that bounds them is cheap; discovering the
bound from a 400 at page 150 of a paid run is not.

**Worked example, the case the draft could not serve.** A 200-page scan at the conservative constant
is 40 requests: input `200 × 6,000 = 1.2M` tokens at $5/MTok = **$6.00**; output reserved `40 ×
8,000 = 320K` at $25/MTok = **$8.00**; plus `40 × 300` prompt tokens = **$0.06**, the term the
formula above calls non-optional — **$14.06 reserved**. Reconciliation returns about half the output
half (~$4.00, since five pages produce ~4,000 of the 8,000 reserved output tokens), so roughly 29%
of the reservation comes back. The same document under the draft's shape did not fit in the context
window at all, and at 100 pages reserved ~$375 of input plus ~$20 of output — a number no cap in
this plan can approve, so the feature would have failed closed with a refusal no user could satisfy.

**K = 5, and it is a semantic constant, not a tuning knob.** Output per request is then ~4,000
tokens against a `max_tokens` of 8,000 — 2× headroom, so truncation is rare and a truncation retry
re-asks five pages rather than twenty. Larger K needs a proportionally larger `max_tokens`, which
raises every reservation and makes each retry more expensive; K = 1 sends each page with no
neighbours, which loses a table or sentence continuing across a page break — the thing slicing at a
5-page boundary keeps in the common case. **K is hashed into the request-shape version** (I7b),
because the context a page is transcribed with is part of what produced its text: changing K is a
cache miss and a coherence refusal, not a silent re-tuning.

The per-page token constant is **measured**, not guessed, with `measured_on` recorded beside it.
Until that measurement (human-gated, I7b) it uses a documented conservative ceiling of **6,000 input
tokens per PDF page**, derived from the vendor's own documented high-resolution page budget (up to
~4,784 visual tokens for a rendered page, plus that page's text) — a per-page number applied to the
*actual* page count, never the API's 600-page maximum. The estimate ships its denominator as
`pages_estimated N of M`.

**Money is `Decimal` end to end, quantised once.** Every euro and dollar amount is `Decimal`;
`prices.toml`'s values parse as `Decimal`, not `float`. Quantisation to cents happens at exactly one
point — when a reservation or a reconciliation is **written to the ledger** — and comparisons
against caps are `<=` on the quantised value, so `spent + reserved == cap` proceeds and one cent
more does not. Stating this matters because the boundary tests assert exact equality: over floats
those tests are flaky, and a cap that is off by a representation error is not a hard cap. **It is
also work this increment must schedule**: `manifest.BudgetSection` types all three caps as `float`
today (`manifest.py:105-107`, parsed via `table.number`), so I6a converts them to `Decimal` and the
§2.1 `[budget]` amendment row covers the type change, not only the two new keys. A `Decimal`
reservation compared against a float-derived cap is the representation error wearing a different
hat.

`reserve.py` is the §5 accountant, and it is pure: `reserve(estimate, caps, spent) -> Decision`,
where `spent` is a `WindowTotals(operation, day, month)` the caller supplies. **Before every call it
checks all three ceilings** — `per_operation_eur`, the new `daily_eur`, and `monthly_eur` — and if
any of `spent.X + reserved` exceeds its cap the call is **never made**. The refusal names which
window blocked it and by how much. One operation being one `pnk sync` bounds a single invocation
only; the day and month windows are what stop a sequence of invocations, which is the failure mode a
hook-driven KB actually has.

`window.py` aggregates an in-memory list of ledger records by day/month in `[budget] timezone`;
reading the file is I6b's job, which is what keeps this half pure. `[budget]` gains
`max_price_age_days` (default 30) — an estimate against older prices is refused, not silently wrong
— and `daily_eur` (default 1.00, between the 0.05 per-operation cap and the 5.00 monthly one: a
burst limiter, where monthly is the real ceiling).

**How a reservation/reconciliation pair aggregates — the rule the draft never stated.** Every call
writes two ledger lines (I6b), and until this is pinned down the same ledger supports three
different totals: summing both lines double-counts every call and refuses at half the real cap;
counting only reconciliations makes an in-flight reservation invisible, so the cap is not a cap; and
counting reconciliations plus unreconciled reservations breaks at a window edge. The rule:

- **A pair is one record, attributed to the *reservation's* timestamp.** A call reserved at 23:59:58
  and reconciled at 00:00:03 belongs entirely to the first day. Attribution never moves.
- **The reconciliation supersedes the reservation's amount in place** — it does not add to it.
- **An unreconciled reservation counts at its reserved amount**, so an in-flight or crashed call
  consumes headroom rather than vanishing. It is reported as `unknown outcome`, never as zero.
- **A `void` record (I7b) closes a reservation at zero** — the escape hatch for a call that never
  billed, without which a handful of transient failures would permanently consume a month's budget.

Which reading applies is not academic, though the spread is narrower than it first looks and the
arithmetic is worth stating rather than gesturing at. At the shipped constants a slice reserves
`(30,000 + 300) × $5/MTok + 8,000 × $25/MTok = $0.3515` and reconciles at `$0.1515 + 4,000 ×
$25/MTok = $0.2515`. So counting reserved amounts buys **~70%** of what counting reconciled actuals
buys, and the widest spread across the three readings — double-counting versus reconciled-only — is
**~2.4×**. It cannot exceed 3.3× in principle, because the input half is fixed whatever the response
contains. A 2.4× error in a hard cap is still a hard cap that is wrong.

**The default caps refuse the paid extractor, and that is correct — but the refusal has to say so
completely.** One 5-page request at `max_tokens = 8000` reserves `30,000 × $5/MTok` input plus
`8,000 × $25/MTok` output = **$0.35**, which converts through `prices.toml`'s own `usd_per_eur` (a
bare euro figure here would be exactly the unprovenanced number I6b's ledger rule exists to
prevent). That is well past the €0.05 `per_operation_eur` default, which was sized for one `pnk ask
--deep` question. Raising it alone is **not sufficient** for any real document: `daily_eur` binds
next, then `monthly_eur`, and a refusal naming one cap at a time walks the user through three
sequential manifest edits to discover that. So the refusal prints **all three windows with their
current headroom**, the computed estimate, the complete manifest edit that would admit this run, and
one line stating the ongoing exposure that edit creates — because `--extract=claude-vision` is a
per-run override while a raised cap is permanent, and nothing in the draft acknowledged the
asymmetry. The plan does not lower `max_tokens` to sneak under a default that was never meant to
cover a 200-page document.

**Tests:** `tests/test_budget_core.py`, table-driven — the exact cap boundary for **each** of the
three windows (`spent + reserved == cap` proceeds; one cent more does not, asserted over `Decimal`),
and a case where the operation cap passes but the month's does not;
`test_reservation_bounds_every_usage_table` — hand-written `usage` records asserting the reservation
is never below the reconciled actual; `test_a_pair_straddling_midnight_is_attributed_to_the_start`
and its month-end and DST twins, in a non-UTC timezone — the aggregation that *produces*
`WindowTotals`, which the draft tested only downstream of a caller-supplied value;
`test_a_void_closes_a_reservation_at_zero`; `test_the_refusal_names_all_three_windows`;
`test_an_unaffordable_document_is_refused_before_the_first_call` (spy asserts zero calls, and the
message names all three windows); `test_confirmation_is_once_per_document_not_per_slice`; confirm
threshold and hard cap staying independent, so confirming at the cap value is reachable (DESIGN pass
3's finding); a stale `as_of` refusing to estimate and naming the remedy; a malformed `prices.toml`
as a startup error, not a silent zero; `test_the_context_window_precheck_names_its_limit`. **Exit
criteria (repo-verifiable):** the boundary tests for all three windows; the four window-attribution
tests; an import-graph test asserting `budget/` imports neither `anthropic` nor anything under
`extract/`; a `check.sh` gate parsing `prices.toml` and failing if `as_of` is missing or
unparseable; `prices.toml` ships in the wheel with `::test_prices_are_installed_package_data`, and
the README sentences this increment falsifies (`README.md:45`, `:48-51`) are corrected here.
**Amendment rows landed here, and exactly these:** §8's v0.2-scope and v0.4 rows, all five §5 rows
this increment owns, and §2.1's `[budget]` row — which covers the `float` → `Decimal` change, not
only the two new keys. **Deliberately not a CI gate:** price *staleness* — a wall-clock gate fails
the build on a quiet weekend with no code change, so staleness is a `pnk doctor` WARN and a runtime
refusal, where it protects a user instead.

### I6b — Budget I/O: the ledger, the prompt, `pnk budget`, and hooks that cannot spend

`budget/ledger.py` appends to `.pinakes/ledger.jsonl`; each line is a single sub-4KB `O_APPEND`
write, atomic on POSIX (§5). **The ledger stores no query text and no document content** —
timestamp, `operation_id`, `call_id`, record kind, operation kind, model, token counts, cost, KB id,
nothing more — and a test asserts it by running a paid extraction from a recorded fixture whose page
text contains a unique sentinel and grepping the whole ledger for it.

**Two identifiers, because the draft used one word for two things.** `operation_id` is one `pnk
sync` invocation — the unit `per_operation_eur` bounds. `call_id` is one API call — the unit a
reservation/reconciliation pair keys on, and what I4's cache entries join against. Collapsing them
made `per_operation_eur` ambiguous between "one sync" and "one call" in a section where the
difference is a factor of forty.

**Cost is recorded in USD with its conversion provenance, never as a bare number.** Each line
carries `cost_usd`, the `usd_per_eur` rate used, and the price table's `as_of`. EUR is computed at
read time. A ledger line that says only `cost: 0.043` is unreadable a month later — nobody can tell
which currency it is or which rate produced it — and the rate is exactly the number that drifts.
`pnk budget` shows, per window, which rate and which `as_of` its totals were computed with, and
flags a window that spans more than one rate.

**Three record kinds, keyed by `call_id`.** A **reservation** written *before* the call, then
exactly one of a **reconciliation** (the call returned; true `usage` supersedes the reserved amount
in place) or a **void** (the call never billed — cost zero, closing the reservation). That is the
durability answer (rule 12): a crash between the call and the ledger write would otherwise lose
spend history permanently, the one thing in `.pinakes/` that cannot be recomputed, and a reservation
with neither successor is reported as `unknown outcome`, not dropped and not counted as zero.

The `void` kind is what makes the protocol survivable. Without it, every call that *raises* — 429,
5xx, an oversize 400 — leaves a reservation nothing ever closes, and I6a's rule counts an
unreconciled reservation at its reserved amount. `ledger.jsonl` is append-only and CLAUDE.md forbids
editing it, so that phantom spend is permanent: a handful of transient failures would lock a user
out of their monthly budget with no way back short of a new month.

**A void is written only when no response was received, never from a bare `finally`.** A
`response_received` flag is set the instant `messages.create` returns, and only the not-received
path voids. A bare `finally` cannot tell "the call never happened" from "the call returned and then
schema branching or the staging write raised" — and in the second case it would record **€0 for
money that left the account**, permanently, in an append-only file. The plan guards the
phantom-charge direction thoroughly; this is the same defect pointing the other way, and it
under-counts rather than over-counts, which is the direction a budget system must never be wrong in.

**Timeouts are the residual, and they are bounded rather than left open.** A timeout may or may not
have billed, so it is neither reconciled nor voided; I6a counts it at its reserved amount forever,
which is the identical lockout the void exists to prevent — three timeouts consume the €1.00 day,
sixteen consume the €5.00 month. So `pnk budget` surfaces `unknown outcome` records prominently with
their total, `pnk doctor` WARNs once their total exceeds a quarter of any window, and **`pnk budget
--resolve <call_id> --actual <eur>`** appends a *reconciliation* for an operator who has checked the
vendor's usage dashboard. That is an append, not an edit: the ledger's invariant holds, and the
alternative is a KB that silently becomes unusable with no documented way back.

Reading the ledger into I6a's `WindowTotals`, under I6a's attribution rule, is this module's job;
the wiring is tested, not assumed.

`confirm_above_eur` prompts on a TTY. **When a confirmation is due and there is neither a TTY nor an
explicit `--yes`, the run aborts** — the scope matters: read broadly this would abort every
non-interactive sync, which is the shape of the pass-1 finding it was written to fix, so the abort
fires only when a confirmation is actually owed. `--yes` (required for cron) says in `--help` what
it authorises: it answers `confirm_above_eur`'s prompt, and **raises no cap**. It does **not**
answer `--clear-cache`'s confirmation when paid entries are present — `pnk sync --yes --clear-cache`
in a cron job would otherwise destroy paid extractions unattended, which is the behaviour the
verification row "`--clear-cache` cannot destroy paid results unattended" claims to forbid while
`--yes`'s stated scope permitted it. Clearing paid entries non-interactively requires the explicit
`--clear-cache=paid`, which no hook or CI workflow writes.

**Hooks and CI cannot spend.** DESIGN §6.3 makes `pnk sync` machine-driven — `pre-commit`,
`post-commit`, `post-merge`, and the workflow `pnk init --ci` writes — **except that `pnk init --ci`
does not exist**. DESIGN §6.3 designs it and v0.1 never shipped it (`init.py` takes `path`,
`--name`, `--template` only), so I6b **builds it here**, in the increment that needs it to force the
free backend: writing a modification to an unbuilt writer is the same defect as writing a hook
around an unbuilt flag. All four are non-interactive, so on a KB configured for `claude-vision` the
no-TTY abort above would kill hook-driven freshness on every single commit, and a `--yes` in a hook
would spend afresh on every commit with a fresh per-operation allowance each time. Resolution: `pnk
install-hooks` and `pnk init --ci` write `pnk sync --extract=pypdfium2` **explicitly** (the flag
exists from I1, so a hook written here is runnable here), forcing the free backend regardless of the
manifest, print one line saying so at write time, and carry the same line as a comment in the
generated hook. A machine-driven sync therefore indexes a scanned PDF's (empty) free extraction and
records it honestly — **and recoverably**, because I5's backend-drift rule makes that document stale
until a paid run picks it up, rather than skipped forever on a content hash that never changes
again. Paid extraction stays a deliberate human invocation. `pnk doctor` reports the combination
(paid backend configured, hooks installed and forcing the free one, N documents awaiting paid
extraction) so the split is visible rather than surprising. `make budget` wraps `pnk budget` and is
created **here**, by this increment — I9 only documents it.

`pnk budget` shows spend by day/month/operation with `reservations N, reconciled M, voided V,
unknown K` and the euro total held by unknowns; `pnk budget --resolve` is documented here. `pnk
doctor` gains a price-table check (OK, or WARN past `max_price_age_days` naming the file) and an
unknown-outcome check. **`monthly_eur` is per KB**, stated once here: the risk table's framing ("one
`pnk sync` is capped; nothing caps the tenth") holds one level up too, and a user with ten paid KBs
has ten monthly allowances. v0.2 does not add a global cap; it says so rather than leaving a reader
to assume one. **Tests:** `tests/test_ledger.py` — the sentinel test; a process killed between
reservation and reconciliation leaving a readable ledger and an `unknown outcome` row; a call that
raises leaving a `void` and **no** lasting headroom consumption; concurrent appends from two
processes interleaving no record; `--rebuild` and `--clear-cache` both leaving the ledger
byte-identical; every line carries `cost_usd`, `usd_per_eur` and `as_of`. `tests/test_cli_budget.py`
— `pnk budget` on an empty ledger printing zeros rather than a traceback; totals on a mixed ledger
matching a hand-computed fixture, including a window spanning two `usd_per_eur` rates; a
confirmation due with no TTY and no `--yes` exiting non-zero with a remedy, **and a non-interactive
run with nothing to confirm proceeding normally** — the two halves of L-6's ambiguity, one test
each; **a KB whose ledger already holds €4.99 of a €5.00 month refuses the next call even though the
operation cap is untouched** — the seam between I6a's arithmetic and I6b's ledger read, proved
rather than assumed. `tests/test_hooks.py` — `::test_hooks_force_the_free_backend` **executes** each
generated hook against a KB configured for `claude-vision` and asserts the sync ran with the free
backend and spent nothing (spy-counted). Asserting the string `--extract=pypdfium2` is *present* is
the string-assertion failure the ground rules exist to prevent: it passes on a hook that never runs.
A second, cheaper test keeps the grep as a readability check on all three hooks and the CI workflow.
**Exit criteria (repo-verifiable):** all of the above; §5 amended to say the machinery ships in v0.2
with all three windows enforced, and §8's v0.4 row updated in the same commit.

### I7a — The paid-path allowlist gate and the invariant amendments (lands first)

**No paid code exists yet, and that is the point.** v0.1's paid-API grep gate never shipped because
it was promised in a section with no owner (rule 8); a gate that arrives in the same increment as
the thing it guards has never once refused that thing. So the gate lands before `extract/claude.py`
exists, and its first job is to prove it fails on a planted violation.

Today `.github/workflows/ci.yml` fails if `anthropic` is imported anywhere under `src/` — an
**unconditional** grep, in the `build` job. The new gate: one file, `.paid-path-allowlist` at the
repo root, lists the permitted paths — exactly `src/pinakes/extract/claude.py` at v0.2 — and
**`check.sh`, CI and the test all read that one file**, so three copies cannot drift.

**The existing unconditional grep is replaced *here*, not at I9.** The draft had I9 "add the
allowlist gate to the `build` job" while I7b adds the `anthropic` import at I7b — which would leave
the `build` job **red on every commit from I7b to I9**, because today's grep admits no exceptions.
Replacing it in this increment is also the honest ordering: this is the increment whose entire
subject is that gate.

- **Gate 1:** every listed path must exist, so a stale entry cannot rot unnoticed. At I7a the listed
  path does not exist yet, so I7a ships the allowlist **empty** and I7b adds the single line in the
  same commit that creates the module — which is itself the gate working.
- **Gate 2:** the paid-client grep (`anthropic|openai|cohere|mistralai|google\.generativeai`) runs
  over `src/` **excluding** the listed paths; any hit is a hard failure with the existing message.
  **It gets a negative test too.** The part most likely to be wrong is the path exclusion itself: an
  entry of `src/pinakes/extract/claude.py` implemented as a prefix or substring match would silently
  exempt a whole directory, and gate 1 (the paths exist) cannot see that. So a test plants an import
  in a **non**-allowlisted module and asserts gate 2 fails. This increment's own principle — "a gate
  only ever observed passing is a gate nobody has tested" — applies to every gate in it, not only to
  the one the draft happened to test.
- **Gate 3:** `anthropic` must not appear in `[project.dependencies]` (I1's gate, reused).
- **Gate 4, the one that actually matters,** is a test rather than a grep: after a full free-path
  run — `pnk sync`, `pnk search`, `pnk doctor`, and an MCP handshake — `"anthropic" not in
  sys.modules`. The draft's run list also named `pnk budget`, which **I6b creates** while I7a
  depends only on I1; implemented in the stated order, gate 4 would fail on an unknown command — the
  same shape as pass 2's `--extract`-in-hooks finding, one increment along. I6b adds `pnk budget` to
  this list in its own commit, where the command exists. Three things make the gate real rather than
  decorative:
1. **It runs in a fresh subprocess** (`uv run python -c …`) which prints its sorted `sys.modules`
  for the test to assert against. In-process it would be defeated by any earlier test's import — the
  same shape as v0.1's `-wal` test, which was correct and then defeated by an environmental fact (a
  read-only connection recreating the files it asserted were gone).
2. **It skips with a printed reason when `anthropic` is not installed**, because with the package
  absent the assertion is true by construction and can no longer distinguish a clean free path from
  a compromised one. That is the `false_abstain: 0.0` failure (rule 3) reappearing in the flagship
  safety check, so the skip is loud and CI runs a leg where it is meaningful.
3. **A negative test proves it in the other direction**: the same subprocess is run with an injected
  `import anthropic` preamble, and the test asserts the checker **fails**. A gate only ever observed
  passing is a gate nobody has tested.

`pnk doctor` must report a paid backend's availability **without importing a paid client** —
`importlib.util.find_spec("anthropic")` answers "installed?" and, for a top-level module, adds
nothing to `sys.modules`. Doctor is in gate 4's run list precisely so that this stays true.

**The `[light,pdf,claude]` CI leg already exists** — I1 made `check` a matrix over all three install
states, so this increment adds gates to a leg rather than a leg to a gate. That leg is where gate 4
and the recorded-fixture paid suite are meaningful. No job holds an API key, so it installs the
client and never uses it against the network.

**CLAUDE.md is amended in this increment.** "No code path may make a paid API call outside `pnk ask
--deep`" becomes an enumerated allowlist:

> **Paid entry points are an enumerated allowlist.** Exactly these may spend: `pnk sync` with
> `[extraction] backend = "claude-vision"` or `--extract=claude-vision`; `pnk ask --deep` (v0.4).
> Each goes through the §5 accountant. Adding an entry point edits this list,
> `.paid-path-allowlist` and DESIGN §1 in the same commit.

**Tests:** `tests/test_paid_path.py` — `test_the_allowlist_matches_the_source_tree` (gate 1),
`test_no_paid_client_outside_the_allowlist` (gate 2),
`test_a_paid_import_outside_the_allowlist_fails_gate_2` (gate 2's negative),
`test_the_free_path_never_imports_the_paid_client` (gate 4, skipping loudly without the extra), and
`test_the_free_path_gate_fails_when_an_import_is_planted` (gate 4's negative). **Exit criteria
(repo-verifiable):** the four gates in `check.sh` **and** CI; **both** negative tests pass by making
their gate fail; the `[light,pdf,claude]` leg green; CLAUDE.md's paid-path invariant, DESIGN §1,
§8's v0.2 scope row and §9's out-of-scope row amended in this commit.

### I7b — The paid Claude-vision extractor: request shape, validation, retries

`src/pinakes/extract/claude.py` — **the only module in `src/` permitted to import `anthropic`**, and
the line I7a's allowlist gains in this commit. Registered as backend `claude-vision`, reached only
when the manifest says so or `pnk sync --extract=claude-vision` is passed. Every free step precedes
every paid one:

1. **Local pre-checks, zero cost** (PDFScout): page count, encryption detection, per-request size
  against the limit that can actually bind — **32 MB per request**, checked on the base64-encoded
  slice — **and against the model's context window** via I6a's pre-check. The documented 600-page
  and 100-page request limits are deliberately **not** checked: a request carries at most K = 5
  pages, so they sit 120× and 20× above anything a request can contain, and testing an unreachable
  limit is precisely the charge this plan levels at the draft it replaced. and the free extractor's
  own text yield against **I3b's fitted floor** — if the free path already clears it, the paid path
  **refuses to spend** unless `--force`, and says so. **The floor is per page and the decision is
  per document**, so the aggregation is stated rather than left to the reader: the paid path refuses
  when **fewer than 10% of pages** fall below the floor. A median would be the wrong statistic — a
  200-page report with eight scanned inserts has a healthy median and still needs the paid path —
  and 10% is what makes I8's worked example (8 of 200 = 4%, refused) come out the way I8 says it
  does. Paying to re-extract a healthy PDF is the most likely way a user loses money by accident.
  **With no fitted text-yield floor installed, this step refuses to spend at all** rather than
  proceeding without its guard (I3b). The 32 MB check is on the **base64-encoded** slice, not the
  raw bytes — base64 inflates by 4/3, so the effective raw ceiling is ~24 MB. A slice that exceeds
  it is **split in half and retried**, down to a single page; a single page that still exceeds it is
  a page failure naming the path and the size. Reducing K is not available, because K is hashed into
  the fingerprint.
2. **Estimate and reserve** through I6a, **before every single call**: the first attempt, every
  schema retry, the refusal retry and the truncation retry each take their own pre-call reservation
  and their own ledger pair keyed by `call_id`. §5 requires reserving before *each* call, and a
  single reservation covering a slice loop is not a cap. **Two ceilings, because there are two kinds
  of retry, and collapsing them into one constant is how the count went wrong twice.** A
  **semantic** budget of **6 calls** per slice — one first attempt, up to three schema retries, one
  refusal retry, one truncation retry (decision 12 removed the audit re-extraction that was the
  draft's seventh). And, *within* each of those, a **transport** budget of 2 backoff attempts for
  429/5xx. They are separate counters: one shared constant would let two early 429s silently consume
  the schema-retry budget and refuse a legitimate retry, an outcome no test could tell from a
  genuine exhaustion. Worst case is therefore **6 token-billed calls and up to 18 HTTP requests**
  per slice, and both numbers appear in the risk table rather than one standing in for the other.
  The client is constructed as **`Anthropic(max_retries=0)`**: the SDK's default of 2 silently turns
  one `messages.create` into up to three billed HTTP requests — and a request retried after a
  timeout can be billed twice for generation the server already completed — so the accountant, not
  the SDK, owns retry.
3. **Failure handling for calls that never return a response.** Step 5's branches cover HTTP 200s;
  this covers everything else, and without it the reservation written in step 2 is never closed. A
  **`void`** ledger record (I6b) releases the reservation at zero — written only when
  `response_received` is false, never from a bare `finally`, because a response that arrives and
  then raises during branching or staging *was* billed. Then: **429 and 5xx** retry with bounded
  exponential backoff, at most twice, each attempt taking a **fresh** reservation. **Timeouts** are
  billable-unknown: no automatic retry, the reservation stays `unknown outcome` rather than voided
  because the server may have generated, and I6b's `pnk budget --resolve` is the documented way out.
  **4xx other than 429** is a page failure with the API's message; it is deterministic, so retrying
  only spends again. **Connection-level errors** (`APIConnectionError`, a sibling of
  `APIStatusError` rather than a subclass) split by phase: a failure before any response byte is a
  void and retries; a failure mid-response is billable-unknown, like a timeout.
4. **Request shape — a K-page slice, not the whole document** (decision 8). `pdfium.slice_pages`
  writes a K = 5-page sub-document; it goes in a `document` content block placed **before** the text
  block — `{"type": "document", "source": {"type": "base64", "media_type": "application/pdf",
  "data": …}}`, newline-free base64, no beta header. **No `cache_control` marker**, for the reason
  I6a states: the shared prefix is real but almost certainly below the 512-token cache minimum and
  ~1% of a request in any case. Structured output via `output_config: {"format": {"type":
  "json_schema", "schema": …}, "effort": "low"}` — the current parameter, not the deprecated
  top-level `output_format`, and the reason citations are unavailable (they 400 together). The
  schema is an array of per-page objects and the response maps to pages **positionally**, so **an
  explicit `len(pages) == last - first + 1` assertion runs before that mapping**, treating a
  mismatch as a schema failure. The draft claimed the schema itself enforced this; array-length
  constraints are not something to rely on here, and the failure it prevents is the worst one in the
  increment — a 4-element response to a 5-page slice stores pages 2–5 under numbers 1–4, spans still
  tile, the cache entry is written, and the order-free completeness audit cannot see a page shift,
  so every citation for that document is silently off by one, for good. Thinking is **explicitly**
  `{"type": "disabled"}`: per-page transcription is not a reasoning task, thinking bills at the
  output rate, and `max_tokens` caps thinking and response together so an un-budgeted thinking phase
  would truncate the JSON body and burn paid retries. `claude-opus-5` accepts disabled thinking
  **only at effort `high` or below**, so the pair is pinned together as module constants. Of the two
  documented disabled-thinking failure modes, one is genuinely structural and one is not:
  tool-call-as-text cannot occur, because no tools are declared. But `output_config.format`
  constrains the response's **structure**, not its **string content** — a `<thinking>` fragment can
  land *inside* a page-text field and be cached, chunked and cited as extracted text. So three
  things, not one: the prompt carries the vendor's recommended **generic** instruction (*do not
  include internal or system XML tags in the response* — generic because naming thinking tags
  specifically is measurably less effective); the prompt carries **no** don't-think/don't-reason
  instruction, which increases leakage; and a page whose text matches a leaked-tag pattern is **a
  schema failure, retried, never silently stripped** — stripping would change the extracted text and
  would therefore have to enter the fingerprint. A tag-leaking recorded fixture proves the branch,
  because "checked rather than assumed" with no test behind it is assumed. The escape hatch, if the
  human-gated run shows otherwise, is adaptive thinking at `low` effort with `max_tokens` raised to
  budget for it — and the reservation follows automatically, because its output bound is
  `max_tokens`. `max_tokens` is **8000**, ~2× the ~4,000 tokens five pages of prose produce.
  `temperature`, `top_p` and `top_k` are never sent: they 400 on this model. `response.model` is
  recorded per slice in `per_page_provenance` — the documented idiom is
  `response.model.startswith(MODEL)`, and recording it is what would let a future fingerprint name
  the model that actually answered, which the "checked and declined" note on server-side fallbacks
  gives as the precondition for revisiting that feature.
5. **Per-slice validation with ≤3 schema retries** (PDFScout), branching in this order: `stop_reason
  == "refusal"` is checked **before reading `content`**, counts as a failure, is retried once and
  then recorded — the *cheap* failure, because a pre-output refusal is not billed at all.
  `stop_reason == "model_context_window_exceeded"` is a **hard** failure naming the slice and the
  computed token count: at K = 5 it should be unreachable, so reaching it means the page constant is
  wrong, and retrying an identical oversize request only spends again. `stop_reason == "max_tokens"`
  is checked **next, before schema validation**: a truncated response is invalid JSON, and without
  this branch it would be retried identically three times, all paid, all truncating at the same
  place. A truncated slice is re-asked **once** at `max_tokens = 16000` under a fresh reservation; a
  second truncation records a failure naming the pages. **A retry re-asks the whole slice, never a
  single page from it**: a page transcribed with different neighbours is a different extraction, and
  mixing the two inside one document is the provenance mess the fingerprint exists to prevent. The
  plan does **not** split a page: the sub-page boundary has no ground truth in the corpus, so a
  split would produce text nothing can score.
6. **Assembly runs the string policy and nothing else** (decision 10). The response is per-page text
  with no coordinates, so `blocks_from_chars`, `reading_order` and `strip_running_heads` have
  nothing to operate on; fabricating geometry to feed them would be invented data driving a stage
  that reasons about position. This backend builds `ExtractedText` from the per-page strings,
  applying `textpolicy.normalise()` — the one stage both backends share (decision 15).
  **`normalise()` is length-changing, so page offsets are computed *after* it**, exactly as I3a
  requires of the free path: ligature expansion turns one codepoint into two or three, and offsets
  taken before it would be out by one or two from the first ligature onward, while the spans still
  tile perfectly and no property assertion could see it. The pipeline difference that remains —
  running-head handling and reading order — is **measured in this increment's human-gated run**, not
  assumed away.
7. **The fingerprint.** `claude-vision`'s inputs, declared in I1's client-free registry entry, are
  `(backend name, model id, prompt version, schema version, request-shape version — which includes
  K, `TEXT_POLICY_VERSION`, pypdfium2 version, pdfium build)`. It includes the pdfium versions
  because this backend **does** load pypdfium2 — to slice, and to pre-check the free text yield. It
  includes `TEXT_POLICY_VERSION` because it runs `normalise()`, which changes the text. It
  **excludes `LAYOUT_VERSION`**, which it genuinely does not run (decision 15 is what makes that
  true rather than merely asserted). It **excludes `max_tokens`**, a budget knob rather than a
  semantic one — a slice re-asked at a higher cap yields *more complete* text of the same kind, and
  hashing it would change the fingerprint mid-run at exactly step 5's retry. Changing `[extraction]
  model` or K produces a cache **miss** and, on an already-built index, the §4.4 **WARN and
  stale-extraction marker** decision 13 defines — not a refusal, because the remedy would be an
  invoice.
8. **`--estimate-only`.** Builds the real request for the first slice and calls
  `client.messages.count_tokens` to measure exact input tokens without generating anything, then
  prints the extrapolated whole-document estimate and exits. It tightens the reservation constant at
  a fraction of a real run's cost, and it is the documented first step of the human-gated
  measurement below. **It is a network call, not an offline estimate** — stated in `--help`, in §6.3
  and here, because "estimate" reads as free and this one requires a key. It is never *required*:
  the estimate must exist on an install with no client at all.

**The recorded-fixture set is this increment's artifact**, at `tests/fixtures/claude/`, with
`::test_the_recorded_fixture_set_covers_every_branch` asserting it contains one case per branch the
plan cites it for: refusal, schema-invalid, truncated, content-dropping, tag-leaking, short-array,
429, 500 and timeout. It is the sole evidence for I7b and I7c and the risk table's mitigation, and
until now it had no owning increment, no path and nothing asserting its coverage — a fixture set
described in the ground rules, which is a section with no owner.

**Tests:** `tests/test_extract_claude.py`, driven entirely by those **committed recorded responses**
(of the synthetic corpus only — no real content ever enters this repo), and **unmarked** so the
whole suite runs with `anthropic` absent: a happy multi-slice extraction over a document whose page
count is not a multiple of K, so the short final slice is exercised; a schema-invalid slice retried
and recovered; a slice that exhausts retries; a `stop_reason == "refusal"` handled before `content`
is touched; a `stop_reason == "max_tokens"` re-asked once at the raised bound and succeeding, and
one that truncates twice and fails; a `stop_reason == "model_context_window_exceeded"` failing hard
without a retry; **a tag-leaking page** whose response is schema-valid but whose text contains a
`<thinking>` fragment (M-12); a `429` retried under a fresh reservation with the first one voided,
and a `500` exhausting its two backoffs; a timeout leaving `unknown outcome` rather than a void; a
reservation that would breach the cap so the call is never made (spy asserts zero calls); **every
retry taking its own reservation and writing its own ledger pair** (spy counts reservations ==
calls); the **6-call semantic ceiling** refusing a seventh, and the **2-attempt transport ceiling**
refusing a third backoff *without* consuming a schema retry — one test each, because a single
constant is exactly what conflated them; a slice whose base64 exceeds 32 MB being split and retried,
and a single page that still exceeds it failing by name; a `--force`-less refusal on a PDF whose
free text yield clears I3b's floor, and another when no floor is installed;
`test_changing_the_model_misses_the_cache_and_warns`; `test_changing_k_misses_the_cache`;
`test_a_short_page_array_is_a_schema_failure` — a recorded 4-element response to a 5-page slice,
asserting the retry rather than a silent positional shift, which is the failure no downstream check
in this plan can detect; `test_estimate_only_makes_no_generation_call` (spy: `messages.create` 0,
`count_tokens` 1).

**Both page-attribution properties are asserted here too, over every recorded fixture** — the
content-anchored one from I3a included. This backend is a **second, independent** implementation of
offset emission, and the draft asserted neither property against it: I3a's tests run against
`assemble()`, which this backend never calls, and I5's corpus-wide invariant runs over the free
path, since no paid extraction can sweep the corpus without paying. v0.1's I6 recorded exactly this
class of defect, and a wrong paid page span produces a citation pointing at the wrong page with
nothing revealing it. `::test_normalise_runs_before_offsets` pins the order that would otherwise
break silently.

`test_the_client_disables_sdk_retries` is **split in two**, because the draft's single test was
declared runnable with `anthropic` absent — where the only thing it could inspect is the stand-in
asserting a property of itself, rule 5's "a fake must not derive its answer from the input under
test", in the test guarding the triple-billing finding. So: an unmarked test asserts
`build_client_kwargs() == {"max_retries": 0, …}` — a pure function, real values, no client — and a
`paid`-marked test constructs the actual `Anthropic` client and asserts `max_retries` on it.

`test_reservation_bounds_every_recorded_usage` lives here, not in I6a: it needs this increment's
fixtures, and an exit criterion that depends on a later increment's artifacts is not an exit
criterion. **Exit criteria (repo-verifiable):** the whole unmarked paid suite green with `anthropic`
**not installed** (the recorded-fixture driver stands in for the client through the registry); the
same suite green on the `[light,pdf,claude]` leg; `.paid-path-allowlist` gains its single line in
this commit and gate 1 goes from vacuously-true to load-bearing; §6.3's `--estimate-only` row
amended here. **Exit criteria (human-gated — these gate nothing in the repo)** (rule 9): one
real-API run with a key, in a **measurement KB whose `pinakes.toml` raises all three `[budget]` caps
explicitly** — the shipped defaults refuse a single slice, which is correct behaviour and is the
documented first step of the measurement, not an obstacle to work around. (`PINAKES_ALLOW_SPEND` is
*not* part of this recipe: the ground rules define it as a pytest condition and never a product
guard, and putting it in a CLI recipe is what turns it into one.) The run has three parts, in this
order: (a) `pnk sync --extract=claude-vision --estimate-only` over a one-page PDF, fixing the input
half of the constant for a token-counting call rather than a generation; (b) one real single-slice
extraction over a 5-page PDF, which fixes the output half at the shipped K; (c) the scanned stratum,
to record quality in §9 with date, model, and the euros actually spent; **(d) the free-vs-paid
pipeline delta** — decision 10's justification, which the draft left with no owning increment at
all. It runs the paid backend over the **five named text-layer twins** (I2 names them: one per
stratum where `layout.py` does real work — two-column, tables, running heads, hyphenation — plus the
12-page baseline as a control, so the delta is measured where the bypass can matter rather than only
on prose where it cannot; the scanned and pathological strata supply no twin, the first because a
raster is not a text-layer twin and the second because its fixture must raise), each requiring
`--force` because they are healthy by design, and records the per-metric delta beside the free
numbers. It costs money and needs a key, so it can never be a repo gate; that is precisely why it is
listed here with its euros rather than described as "measured in I3b". The measured per-page
constant and `measured_on` are written to `prices.toml`; the thinking/effort choice in step 4 is
confirmed or replaced against what the run actually shows; and `response.model` is compared against
the requested alias with `startswith`.

### I7c — The completeness audit, staging, and all-or-nothing commit · **CLOSED 20260729, shipped whole in 0.3.0 — the audit is report-only by design, and the re-extraction loop it defers to v0.3 has never shipped**

1. **Completeness audit — report-only in v0.2** (decision 12). `word_coverage` is computed per page
  against pypdfium2's native layer and **reported**; nothing is re-extracted and nothing is spent.
  Pages with no usable native layer (the scanned stratum) are **exempt and reported as exempt**,
  with their denominator: `audited N of M, exempt K of M, below-median P`. The re-extraction loop
  needs a floor, and I3b explains why no honest floor exists before paid output does — the applied
  pair is (native layer → Claude) and no Claude output exists at fitting time. So v0.2 ships the
  measurement and **v0.3 ships the loop**, fitted on the distribution this release's first real runs
  produce. This is the release's own rule applied to itself: a threshold that spends money is fitted
  or it does not exist. It also removes an inconsistency the loop had inherited — the audit
  re-extracted a *page* while I7b forbids re-asking a single page from a slice, and the estimator
  has only a per-slice formula. Nothing that does not run needs that contradiction resolved. `pnk
  doctor` and the sync report surface the low-coverage pages by path so a human can decide.
2. **Staging and commit.** Each validated page is written to
  `cache/extract/partial/<hash>-<fingerprint>/<page>.json` (temp + `os.replace`) as it completes, so
  an interrupted 200-page run does not re-pay for pages already extracted. Under decision 8 the
  resume is **genuinely free of re-payment for staged pages**: each request carries only its own
  slice, so there is no prompt cache to re-prime and no 1.25× write to re-pay — the draft's honest
  caveat about a 5-minute TTL simply no longer applies. Resume granularity is the **slice**: a slice
  interrupted mid-flight is re-asked whole, because its pages were transcribed together. Only when
  every page passes is the single complete cache entry written and the staging directory removed. A
  partially extracted document writes **no** complete entry and goes to `failures`; `on_exceed =
  "partial"` is honoured at the corpus level (completed documents keep their entries, the run stops
  cleanly), never at the page level — a half-extracted document in the index is the silent
  truncation §4.6 exists to prevent. **DESIGN §5 gains that definition in this commit**: it is a
  rule about what `partial` means, and §5's existing amendment row covers only the opening sentence
  and the control table.
3. **`--clear-cache` learns what it is destroying.** Its printed summary (I4) gains the euros the
  cached entries cost, joined from the ledger on the entry's `call_ids` (I4) — the draft said "by
  operation id", which no cache entry recorded and no ledger line could be matched to, so the
  behaviour its verification row asserts was unimplementable as specified. CLAUDE.md's
  "**`.pinakes/` is disposable except `ledger.jsonl`**" invariant is amended in this commit to
  except the paid extraction cache too: those entries are derived state that costs real money to
  derive, which is a different thing from disposable. I4's automatic eviction already spares them,
  so the invariant and the sweep agree rather than contradicting each other.
4. **Flag scope, stated once and in `--help`.** The complete list, so none of them can arrive
  without a stated scope again: `--extract=BACKEND` (I1) overrides `[extraction] backend` for one
  run. `--estimate-only` (I7b) measures and exits, **and makes a network call**. `--yes` (I4)
  answers `confirm_above_eur`'s prompt and raises no cap; it does **not** authorise destroying paid
  cache entries. `--clear-cache` (I4) empties the free extraction cache after confirming;
  `--clear-cache=paid` is the explicit form required to destroy paid entries, and no hook or CI
  workflow writes it. `--force` bypasses **exactly two things**, both refusals to spend or discard
  that a user may legitimately overrule: step 1's refusal to pay for a PDF whose free text yield is
  already healthy, and — **only together with an explicit free `--extract`**, as I5 states — I5's
  refusal to let a free run overwrite a paid extraction. That qualifier is part of the scope, not a
  detail of I5: without it, a bare `pnk sync --force` on a KB whose manifest names the free backend
  destroys every paid extraction it has. `--force` never bypasses `per_operation_eur`, `daily_eur`,
  `monthly_eur`, the stale-price refusal, the missing-floor refusal, or the no-TTY abort — a flag
  that can widen a hard cap is not a hard cap.

**Tests:** `tests/test_extract_claude.py` (continued) — the audit reporting a low-coverage page and
spending **nothing** (spy asserts zero additional calls), which is decision 12's whole content; the
exempt scanned pages reported as exempt with their denominator; an interrupted run resumed from
staging with **zero** re-calls for staged pages (spy-counted); a slice interrupted mid-flight
re-asked whole; a partially extracted document writing no complete entry and landing in `failures`;
`--force` bypassing the healthy-PDF refusal and **not** bypassing a cap breach; `--force`
**without** an explicit free `--extract` refusing to overwrite a paid extraction.
`tests/test_sync.py::test_clear_cache_reports_spend_and_confirms` — with a ledger and cache entries
that actually join, so the euro figure is asserted against a hand-computed total rather than against
zero; `::test_yes_does_not_authorise_destroying_paid_entries`. **Exit criteria (repo-verifiable):**
all of the above; §6.3's `--force` and `--clear-cache` rows and §5's `on_exceed` row amended;
CLAUDE.md's `.pinakes/` invariant amended in this commit.

### I8 — `pnk doctor` text yield, `path:page` citations on both surfaces, and the trace passes

`pnk doctor` gains a `text yield` check over PDF documents. **It reports and thresholds per page,
not per document.** A document-level median against a per-page floor is a different statistic from
the one I7b spends against, and it hides the case that matters: a 200-page report with eight scanned
inserts has a healthy median, so the check stays silent *and* I7b's pre-check refuses to pay for it.
So: `median C chars/page over N of M PDF documents`, plus `pages below floor: P of Q`, listing by
path **and page**. The floor is I3b's, fitted there on the I2 corpus and shipped in `floors.toml`,
so the increment that spends against it (I7b) and the increment that reports it (I8) consume one
number neither invented; with the floor absent the check reports the distribution and says the floor
is missing rather than inventing one. Its remedy names the paid path and its cost rather than "out
of scope".

**Page citations ship on both surfaces, in the same increment.** DESIGN §1's first decision is
"agent-first", and v0.1 pass 2 established one filter vocabulary across two surfaces; shipping page
provenance on the CLI and not on MCP would recreate exactly the divergence v0.1 fixed, in the
release whose entire goal is page-citable PDFs, on the surface the design calls primary. So:

- `pnk search` renders `path:page` in citations — **and `path:p1-p2` when a chunk spans two or more
  pages**, which I5 explicitly allows and the draft's singular rendering had no answer for; `--json`
  carries `page_start`/`page_end` as separate fields either way. **The shape is already taken**:
  `search.py:64-66` renders `f"{path}:{char_start}-{char_end}"` today, so `report.pdf:12-480`
  currently means *character offsets* and `report.pdf:12-13` would mean pages — same syntax, two
  meanings, silently. PDFs therefore render `path:p12-13` with an explicit `p`, and non-paged
  sources keep today's bare offsets unchanged. A citation format that can be misread is worse than
  one that is ugly.
- `pinakes_search` results carry `page_start`/`page_end` (null for non-paged sources).
- `pinakes_get` accepts an optional page range and marks page boundaries in what it returns, so an
  agent can cite `path:page` from a `get` as well as from a `search`.

**Trace one datum end to end** (rule 2 — the two most expensive plannable misses in v0.1 were both
producer/consumer *name* agreement with *column* disagreement, and five consistency passes could not
see them). Three tests in `tests/test_pdf_trace.py`:

1. `test_a_table_cell_word_survives_every_hop` — one word from a value cell of the bordered-table
  fixture, six hops, each asserting against the value carried from the previous one: **extraction**
  (the word is in `ExtractedText.text` at offset `o`, page `p`) → **cache** (the cached JSON for
  that content hash yields the same `o` and `p`) → **chunk** (**at least one** chunk has `char_start
  <= o < char_end`, and **every** such chunk contains the word and reports `page_start == p`) →
  **FTS** (`SELECT rowid FROM chunks_fts WHERE chunks_fts MATCH …` returns that chunk's rowid) →
  **CLI** (`pnk search` returns that passage with `path:p` in the rendered citation) → **MCP**
  (`pinakes_search` returns the same passage with `page_start == p`). The last two hops assert the
  *same* page number, which is the only way a CLI/MCP divergence is visible. **"Exactly one" was
  wrong and would have made this test pass or fail by fixture accident.** Verified in
  `chunk.py:239-269`: `_fit` prepends the carried overlap tail and sets `start` from the *carried*
  piece's position, so chunk *n+1*'s `char_start` lies strictly inside chunk *n*'s range whenever a
  block splits — and I5's own text says "≥1 chunk". A table cell is the fixture most likely to be
  one oversize block that `_atomise` splits, so the contradiction sits exactly where the test aims.
  Asserting the property over *every* matching chunk is strictly stronger than asserting there is
  one.
2. `test_every_filter_dimension_resolves_for_pdfs` — every dimension a PDF can be selected by
  (`source_type`, `path_prefix`, `mtime`, `tags`) **returns the expected PDF rows when actually
  filtered on**. The draft asserted each resolves to a column "non-null for every PDF row", which is
  wrong for `tags` and vacuous besides: verified in `store.py:38-48` and `search.py:128-132`, tags
  are not a column at all but `json_each(d.metadata, '$.tags')` over a `metadata` field that is `NOT
  NULL DEFAULT '{}'` — so the non-null assertion is true by schema for every row, PDF or not, and
  would pass on a corpus where no PDF has a single tag. Exercising the filter is the only version of
  this test that can fail.
3. `test_a_paid_slice_traces_from_estimate_to_the_budget_report` — the money equivalent, and the
  reason the risk table aims at machinery: one **slice's** cost from **estimate** → **reservation
  line** → the recorded response's **`usage`** → **reconciliation line** → the total `pnk budget`
  prints. The unit is the slice, because after decision 8 no per-page number exists anywhere in the
  money machinery and a trace whose unit nothing produces cannot assert against the layer below it.
  Each hop asserts against the previous one's number, so a unit slip (tokens vs MTok, USD vs EUR)
  cannot hide in an intermediate layer.

**Tests (beyond the traces):** `tests/test_serve.py::test_mcp_search_carries_page_spans` and
`::test_mcp_get_is_page_aware`; `tests/test_doctor.py::test_text_yield`. **Exit criteria
(repo-verifiable):** all three trace tests pass; both MCP tests pass; the doctor check WARNs on the
scanned fixtures and is OK on the baseline; `--help`, the MCP tool descriptions and README show the
new citation format; §4.7 amended in this commit.

### I9 — Docs sweep, template, CI, 0.2.0 · **CLOSED 20260729, re-scoped and shipped in 0.4.0 — 0.2.0 cut early after I5**

**Superseded 20260728 — check `main` before trusting this section's version claims (project
CLAUDE.md).** This section assumed `[0.2.0]` was still uncut and that the four README sentences
named below were still standing. Neither holds: `0.2.0` shipped early, after I5, and a parallel
docs restructure (`0.2.1`, 20260728) already replaced the README/DESIGN.md content described here —
split across `docs/GUIDE.md`, `docs/CLI.md`, `docs/MANIFEST.md`, `docs/STATUS.md`, with README.md
and DESIGN.md now deliberately version-free — and already fixed the specific stale claims the
paragraph below names. Whatever remains of I9's scope (the CI write-up, the completion walk) should
re-read `docs/README.md`'s current routing table and "landing a new increment" checklist rather than
the paragraph below, and its exit criteria should target whatever version I6–I9 collectively resolve
to, not the already-released `0.2.0`: I6a kept its own work in `[Unreleased]`, heading toward `0.3.0`
once the paid extractor is genuinely usable (I7b), never bundled into a `0.2.x` patch.

DESIGN sweep — each increment updated its own surfaces; **this one audits that the amendment table
is fully applied**, row by row. The `notes` template's `[sources] include` gains `**/*.pdf`
**commented out**, with a one-line pointer to `pinakes[pdf]`, mirroring `[retrieval.confidence]`;
§2.1's example and the shipped template finally agree, and §8's status line is corrected (it still
reads v0.1.1 while the package is 0.1.3).

**README — four sentences v0.2 falsifies. Three are corrected by the increment that falsifies
them**, not here: `README.md:45` and `:48-51` by **I6a** (the increment that makes the budget system
guard something) and `README.md:119` by **I7b** (the increment that makes spend happen before v0.4).
The ground rule is "a user-facing surface added in an increment updates DESIGN, the README and
`--help` **in the same commit**", and CLAUDE.md calls a stale README a **severe** defect — leaving
three false sentences standing for three increments because a docs increment exists at the end is
exactly the drift that rule prevents. I9 corrects `README.md:11` (the status line, which is only
false once 0.2.0 is cut) and makes the additions below. Verified against the file at this revision:
`README.md:45` "`pnk ask --deep` … **is the only thing the budget system will guard**";
`README.md:48-50` "**Spending will be bounded** … **nothing consumes it yet**"; `README.md:119`
"**from v0.4**, your spend"; `README.md:11` "**Status: v0.1**". Every one is false the moment the
opt-in paid extractor ships, and none was in the draft's list — which described additions to the
README while saying nothing about the claims it already makes. Additions, too: install lines for
both extras (`pinakes[claude]` noted as pulling `[pdf]` with it) with the paid one's cost stated
plainly, **naming all three `[budget]` caps** that must be raised rather than only
`per_operation_eur`, since raising one and hitting the next is the discovery path I6a's refusal
exists to prevent; the KB diagram gets its `.pdf` back (0.1.2 removed it because v0.1 could not
ingest one); and the Development section documents `make corpus` / `make pdf-eval` / `make budget`
(each created by the increment that owns it: I2, I3b, I6b). **The check is CLAUDE.md's own: run the
commands the README shows, install line included** — an audit at 0.1.2 found four false claims this
way while `cli.py` and the CHANGELOG were correct in the same places.

**CI: documents rather than creates.** The three-leg `check` matrix (I1), the `pdf-eval` job (I3b)
and the allowlist gate replacing today's unconditional grep in the `build` job (I7a) all land in
their own increments; I9 writes them up. The wheel smoke test gains three assertions: a core-only
wheel still runs; a PDF in a core-only KB fails **naming the extra** rather than with a traceback —
and it uses a **core-only KB that does not need embeddings**, because today's smoke KB fails on
sentence-transformers long before it reaches an extractor, so the assertion would prove nothing; and
**`floors.toml` and `prices.toml` are present in the installed wheel**, the two package-data files
whose absence silently disables the spending guards while every source-tree test still passes.

**Completion walk over all four lists** (rule 8), written into the release commit message: DESIGN
§8's amended v0.2 row item by item; the **DESIGN.md** amendment table row by row; the **CLAUDE.md**
amendment table row by row; and this plan's "Verification of the whole" table row by row. **Exit
criteria (repo-verifiable):** `make check`, `make pdf-eval` and the wheel smoke pass in CI;
`pinakes.__version__ == "0.2.0"`; `make release-check` prints a matching `__version__` and tag; the
CHANGELOG `[0.2.0]` heading carries `YYYYMMDD HH:MM` and has a matching link definition (0.1.2 had
to fix one that did not); every amendment row in both tables is applied. **One row is deliberately
not repo-verifiable and is listed here so I9 cannot block on it** (rule 9): §9's *measured* scanned
numbers come from I7b's human-gated run, which by definition gates nothing — I9 asserts the numbers
are **present and dated**, not that it produced them. **Exit criteria (human-gated)**: merge to
`main` from the primary checkout, `git tag -a v0.2.0`, **then** assert `git merge-base --is-ancestor
v0.2.0 main` — in that order, since the assertion needs the tag to exist; the GitHub release; PyPI
publication, still behind `PUBLISH_TO_PYPI` because trusted publishing is unconfigured.

---

## Verification of the whole · **SUPERSEDED as a lookup 20260729 — `docs/VERIFICATION.md` is the resolved mapping**

Every row carries an increment number and a test path — a promise in a section with no owner is a
wish (rule 8; v0.1's paid-API grep gate never shipped for exactly that reason).

> **Superseded as a *lookup* 20260729 (I9) — [`docs/VERIFICATION.md`](../docs/VERIFICATION.md) is
> the resolved mapping.** These test paths were written *before* the tests existed, and
> implementation renamed most of them: at I9, **61 of the 98 references below did not resolve**. The
> properties are almost all tested, usually under a better name — but a table whose paths cannot be
> resolved verifies nothing, which is the failure the paragraph above warns about. This table stays
> as the record of what was *predicted*; the live mapping is `docs/VERIFICATION.md`, and
> `tests/test_verification.py` asserts every test it names exists, so it can only go stale in the
> commit that breaks it.
| What must be true | Increment | Where it is checked |
|---|---|---|
| no extractor library enters `[project.dependencies]` | I1 | `check.sh` gate + `tests/test_packaging.py::test_extractors_stay_extras` |
| a core-only install fails naming the extra | I1 | `tests/test_extract.py::test_missing_extractor_names_the_extra` |
| an unknown backend is rejected without importing anything, from the manifest **and** from `--extract` | I1 | `tests/test_manifest.py::test_unknown_extraction_backend_is_rejected`, `tests/test_cli.py::test_unknown_extract_flag_is_rejected` |
| `pinakes[claude]` cannot be installed without `[pdf]` | I1 | `tests/test_packaging.py::test_claude_extra_requires_pdf_extra` |
| every backend's missing-extra error names its own extra | I1 | `tests/test_extract.py::test_missing_extractor_names_the_extra` (both backends) |
| one unreadable PDF does not block the corpus | I1 | `tests/test_sync.py::test_extract_failure_is_isolated` |
| the text-layer corpus regenerates byte-identically | I2 | `tests/test_pdf_corpus.py::test_regeneration_is_reproducible` |
| the scanned corpus regenerates within tolerance | I2 | `tests/test_pdf_corpus.py::test_scanned_regeneration_within_tolerance` |
| the corpus cannot silently shrink or balloon | I2 | `tests/test_pdf_corpus.py::test_stratum_counts`, `::test_byte_budget` |
| character-to-block assembly is unit-tested, not only scored | I3a | `tests/test_extract_layout.py::test_blocks_from_chars_*` (seven cases) |
| page offsets tile the extracted text exactly **and** anchor to their page's content | I3a | `tests/test_extract_layout.py::assert_extraction_properties` — the shared helper (join-identity, contiguous coverage, content-anchoring) every `test_assemble_*` case asserts against |
| the string policy is versioned apart from layout | I3a | `tests/test_extract_layout.py::test_layout_is_pure` (import graph) |
| the pure core imports no PDF library | I3a | `tests/test_extract_layout.py::test_layout_is_pure` |
| the pdfium reader refuses corrupt, encrypted and oversize files | I3b | `tests/test_extract_pdfium.py::test_reader_refusals` |
| every quality rate has a non-zero denominator | I3b | `tests/test_extract_quality.py::test_every_stratum_is_scored` |
| a quality regression fails the build | I3b | `tests/test_extract_quality.py::test_planted_regression_fails_baseline` |
| the one spending threshold is fitted, dated and committed | I3b | `tests/test_extract_quality.py::test_floors_are_fitted_with_provenance` |
| a committed floor cannot silently drift from its corpus | I3b | `tests/test_extract_quality.py::test_a_drifted_floor_fails_the_gate` |
| the floor reaches an installed copy, not just the repo | I3b | `tests/test_extract_quality.py::test_floors_are_installed_package_data` |
| the floor's absence stops the paid path from spending | I7b | `tests/test_extract_claude.py::test_no_yield_floor_no_paid_run` |
| a zero denominator is declared with a reason, never asserted away | I3b | `tests/test_extract_quality.py::test_exemptions_are_declared_with_reasons` |
| the audit reports without spending | I7c | `tests/test_extract_claude.py::test_the_audit_spends_nothing` |
| a gate that cannot run says so and still exits 0 | I3b | `tests/test_check_script.py::test_pdf_eval_skips_with_a_reason` |
| a cached extraction is never re-parsed | I4 | `tests/test_extract_cache.py::test_second_sync_does_not_reparse` |
| a cache hit returns exactly what a cache miss returns | I4 | `tests/test_extract_cache.py::test_hit_and_miss_yield_the_same_extractedtext` |
| the automatic sweep never destroys a paid entry | I4 | `tests/test_extract_cache.py::test_the_sweep_spares_paid_entries` |
| `--clear-cache` never touches `ledger.jsonl` | I4 | `tests/test_sync.py::test_clear_cache_preserves_the_ledger` |
| `--clear-cache` aborts unattended without `--yes` | I4 | `tests/test_sync.py::test_clear_cache_without_yes_and_without_a_tty_aborts` |
| `--clear-cache`'s euro figure joins real ledger lines | I7c | `tests/test_sync.py::test_clear_cache_reports_spend_and_confirms` |
| `--yes` does not authorise destroying paid entries | I7c | `tests/test_sync.py::test_yes_does_not_authorise_destroying_paid_entries` |
| `chunk.text == indexed_text[start:end]` for every PDF chunk | I5 | `tests/test_chunk_pdf.py::test_spans_are_exact_over_the_whole_corpus` |
| no character of an extraction is dropped | I5 | `tests/test_chunk_pdf.py::test_every_character_lands_in_a_chunk` |
| a v0.1 index refuses to open, with a remedy | I5 | `tests/test_store.py::test_schema_version_1_refuses_with_a_rebuild_remedy` |
| a stale **free** extraction refuses the query | I5 | `tests/test_search.py::test_a_changed_free_fingerprint_refuses_the_query` |
| a stale **paid** extraction warns and marks, never refuses | I5 | `tests/test_search.py::test_a_changed_paid_fingerprint_warns_and_marks` |
| a rebuild cannot destroy paid provenance | I5 | `tests/test_chunk_pdf.py::test_a_rebuild_preserves_paid_provenance`, `::test_a_rebuild_after_clear_cache_still_preserves_it` |
| the coherence check never imports a paid client | I5 | `tests/test_search.py::test_coherence_never_imports_a_paid_client` |
| a free run never overwrites a paid extraction, and a paid run picks up what a free one indexed | I5 | `tests/test_sync.py::test_backend_drift` (six cases) |
| a changed file on a paid document is a failure, not a silent overwrite or skip | I5 | `tests/test_sync.py::test_backend_drift[changed_hash]` |
| a call that would breach any window is never made | I6a | `tests/test_budget_core.py::test_reservation_at_the_cap_boundary` |
| the reservation is never below the reconciled actual | I6a | `tests/test_budget_core.py::test_reservation_bounds_every_usage_table` |
| a pair straddling a window edge is attributed once, to its start | I6a | `tests/test_budget_core.py::test_a_pair_straddling_midnight_is_attributed_to_the_start` (+ month-end, DST) |
| a refusal names every window, not just the first to bind | I6a | `tests/test_budget_core.py::test_the_refusal_names_all_three_windows` |
| a request that cannot fit the context window is caught before the call | I6a | `tests/test_budget_core.py::test_the_context_window_precheck_names_its_limit` |
| an estimate against stale prices is refused | I6a | `tests/test_budget_core.py::test_stale_prices_refuse_to_estimate` |
| the budget core imports no paid client | I6a | `tests/test_budget_core.py::test_budget_is_pure` |
| the ledger records no query text or content | I6b | `tests/test_ledger.py::test_the_ledger_holds_no_document_text` |
| every ledger line carries its currency and FX provenance | I6b | `tests/test_ledger.py::test_lines_carry_cost_usd_rate_and_as_of` |
| an interrupted call leaves a visible unknown outcome | I6b | `tests/test_ledger.py::test_reservation_without_reconciliation_is_reported` |
| a call that never billed does not permanently consume budget | I6b | `tests/test_ledger.py::test_a_raised_call_writes_a_void` |
| a call that **did** bill is never voided to zero | I6b | `tests/test_ledger.py::test_a_response_then_raise_is_not_voided` |
| an unresolvable unknown outcome has a documented way out | I6b | `tests/test_cli_budget.py::test_resolve_appends_a_reconciliation` |
| an unaffordable document is refused before the first call | I6a | `tests/test_budget_core.py::test_an_unaffordable_document_is_refused_before_the_first_call` |
| confirmation is once per document, not once per slice | I6a | `tests/test_budget_core.py::test_confirmation_is_once_per_document_not_per_slice` |
| a non-interactive run never spends silently, and never aborts with nothing to confirm | I6b | `tests/test_cli_budget.py::test_no_tty_without_yes_aborts`, `::test_no_tty_with_nothing_to_confirm_proceeds` |
| the month's cap stops a run the operation's cap would allow | I6b | `tests/test_cli_budget.py::test_monthly_window_refuses_within_operation_cap` |
| hook-driven and CI syncs cannot reach the paid path — proved by running them | I6b | `tests/test_hooks.py::test_hooks_force_the_free_backend` |
| the allowlist cannot rot (a listed path must exist) | I7a | `check.sh` gate + `tests/test_paid_path.py::test_the_allowlist_matches_the_source_tree` |
| no paid client is imported outside the allowlist | I7a | `tests/test_paid_path.py::test_no_paid_client_outside_the_allowlist` |
| the allowlist cannot widen (an exclusion cannot exempt a directory) | I7a | `tests/test_paid_path.py::test_a_paid_import_outside_the_allowlist_fails_gate_2` |
| the free path never imports the paid client | I7a | `tests/test_paid_path.py::test_the_free_path_never_imports_the_paid_client` |
| that gate can actually fail | I7a | `tests/test_paid_path.py::test_the_free_path_gate_fails_when_an_import_is_planted` |
| a refusal is handled before `content` is read | I7b | `tests/test_extract_claude.py::test_refusal_stop_reason_is_a_page_failure` |
| a truncated response is not retried identically three times | I7b | `tests/test_extract_claude.py::test_max_tokens_stop_reason_raises_the_bound_once` |
| an oversize request fails hard instead of being re-paid | I7b | `tests/test_extract_claude.py::test_context_window_exceeded_is_not_retried` |
| a transient failure is retried under a fresh reservation, and the old one is voided | I7b | `tests/test_extract_claude.py::test_429_retries_under_a_fresh_reservation` |
| a leaked internal tag never reaches the indexed text | I7b | `tests/test_extract_claude.py::test_a_leaked_thinking_tag_is_caught` |
| every call, including every retry, is reserved and ledgered | I7b | `tests/test_extract_claude.py::test_every_call_takes_its_own_reservation` |
| the semantic and transport ceilings are separate counters | I7b | `::test_the_seventh_semantic_call_is_refused`, `::test_transport_retries_do_not_consume_schema_retries` |
| a short page array is caught before positional mapping | I7b | `tests/test_extract_claude.py::test_a_short_page_array_is_a_schema_failure` |
| the paid backend's page spans are content-anchored, not merely tiling | I7b | `tests/test_extract_claude.py::test_page_spans_are_content_anchored` |
| offsets are computed after the length-changing string policy | I7b | `tests/test_extract_claude.py::test_normalise_runs_before_offsets` |
| `--estimate-only` generates nothing | I7b | `tests/test_extract_claude.py::test_estimate_only_makes_no_generation_call` |
| the SDK's own retries are disabled — asserted without a stand-in | I7b | `::test_client_kwargs_disable_sdk_retries` (unmarked) + `::test_the_client_disables_sdk_retries` (`paid`) |
| the reservation is never below the actual | I7b | `tests/test_extract_claude.py::test_reservation_bounds_every_recorded_usage` |
| changing the model **or K** misses the cache and marks the results stale | I7b | `::test_changing_the_model_misses_the_cache_and_warns`, `::test_changing_k_misses_the_cache` |
| a short final slice is handled | I7b | `tests/test_extract_claude.py::test_page_count_not_a_multiple_of_k` |
| a healthy PDF is not paid for by accident | I7b | `tests/test_extract_claude.py::test_paid_path_refuses_a_healthy_pdf` |
| every new `pnk sync` flag has a stated scope in `--help` | I7c | `tests/test_cli.py::test_every_sync_flag_documents_its_scope` |
| `--force` alone cannot discard a paid extraction | I7c | `tests/test_extract_claude.py::test_force_without_explicit_free_extract_refuses` |
| every new `pnk doctor` check is exercised | I8 | `tests/test_doctor.py::test_every_v02_check_appears` |
| `--force` widens no cap | I7c | `tests/test_extract_claude.py::test_force_does_not_bypass_a_cap_breach` |
| an interrupted paid run re-pays for nothing staged | I7c | `tests/test_extract_claude.py::test_resume_skips_staged_pages` |
| a table-cell word survives extraction → chunk → FTS → CLI **and** MCP | I8 | `tests/test_pdf_trace.py::test_a_table_cell_word_survives_every_hop` |
| every filter dimension actually selects PDF rows when filtered on | I8 | `tests/test_pdf_trace.py::test_every_filter_dimension_resolves_for_pdfs` |
| one slice's cost survives estimate → ledger → report | I8 | `tests/test_pdf_trace.py::test_a_paid_slice_traces_from_estimate_to_the_budget_report` |
| page provenance reaches the MCP surface, not only the CLI | I8 | `tests/test_serve.py::test_mcp_search_carries_page_spans`, `::test_mcp_get_is_page_aware` |
| a chunk spanning two pages renders as `path:p1-p2` | I8 | `tests/test_cli.py::test_a_two_page_chunk_renders_a_range` |
| a low-yield **page** is flagged inside a healthy document | I8 | `tests/test_doctor.py::test_text_yield_is_per_page` |
| the demo KB's eval numbers do not move | I3b | `make eval` vs `tests/demo-kb/eval/baseline.json` (unchanged file) |
| the named paid twins exist and are five | I2 | `tests/test_pdf_corpus.py::test_named_paid_twins_exist` |
| the price table ships in the wheel and fails its gate when malformed | I6a | `tests/test_extract_quality.py::test_prices_are_installed_package_data`, `tests/test_check_script.py::test_a_malformed_price_table_fails_the_gate` |
| the recorded-fixture set covers every branch it is cited for | I7b | `tests/test_extract_claude.py::test_the_recorded_fixture_set_covers_every_branch` |
| the request shape is pinned, not just the responses | I7b | `tests/test_extract_claude.py::test_the_request_shape_is_pinned` |
| a populated `per_page_provenance` survives a cache round trip | I7b | `tests/test_extract_claude.py::test_paid_provenance_survives_a_cache_hit` |
| `on_exceed = "partial"` keeps completed documents and stops cleanly | I7c | `tests/test_extract_claude.py::test_partial_is_corpus_level` |
| `pnk budget --resolve` appends rather than edits | I6b | `tests/test_cli_budget.py::test_resolve_appends_a_reconciliation` |
| the free-vs-paid delta is present and dated in §9 | I9 | I9's amendment walk (human-gated input from I7b) |
| a v0.2 sidecar merge preserves every key it did not write | I5 | `tests/test_sidecar.py::test_provenance_merge_is_additive` |
| a core-only wheel still installs and runs | I9 | CI `build` job smoke step |
| the shipped wheel carries `prices.toml` and `floors.toml` | I9 | CI `build` job smoke step |

## Durability sweep (rule 12)

*Which writes lose unrecoverable information if interrupted?*

- **`ledger.jsonl` — the only genuinely unrecoverable write, and it is new.** A crash between the
  API returning and the ledger append loses spend history permanently: money left the account and
  nothing records it. Answered by I6b's reservation → reconciliation-or-void protocol — a torn write
  degrades to a *visible* `unknown outcome`, never to a silent zero, and a call that never billed is
  closed at zero rather than consuming a month's headroom for good.
- **Paid staging entries — recoverable, but only at cost.** Regenerable in principle, and that
  regeneration is exactly what the user is paying for; written per page, atomically, consumed on
  resume (I7c). Without them an interrupted 200-page run re-pays for everything. **With them and
  decision 8's slicing, a resume re-pays for nothing already staged** — each request carries only
  its own slice, so there is no vendor-side cache to re-prime; the only re-payment is the slice that
  was in flight.
- **Free cache entries — recoverable and free. Paid cache entries are not.** Both are atomic: a
  half-written JSON that happens to parse is worse than a miss. **The eviction sweep** is the only
  new delete — derived state only, only after a fully successful sync, guarded and tested (I4) — and
  it **skips paid entries entirely**, because "derived" and "free to re-derive" are different
  properties and only the second one licenses a silent delete.
- **Sidecars for PDFs — unchanged and already atomic** (v0.1 I5, rename-based). Re-asserted rather
  than assumed: the ULID a sidecar carries is still the one thing no later command can recompute.
- **The `-wal`/`-shm` lesson (v0.1 I8b) transfers:** cache and staging are *sets* of files under
  directories, so `--clear-cache` and the sweep are tested by listing the directory afterwards,
  never by trusting that one `unlink` was enough.

## Risks specific to this sequencing

Aimed at **machinery**, deliberately: v0.1's risk table enumerated algorithmic risk, and all seven
of its HIGH findings came from areas it never contemplated — durability, file naming, boundary
arithmetic, vacuous metrics, and the gate itself.
| Risk | Why it is machinery | Mitigation |
|---|---|---|
| **Unbounded spend across invocations.** One `pnk sync` is capped; nothing caps the tenth one. Hook-driven freshness makes `pnk sync` machine-driven, so a per-invocation cap is an allowance renewed on every commit | the cap is arithmetic across a *running* total, and per-invocation framing hides that the invocations are the loop | `reserve.py` checks `per_operation_eur`, `daily_eur` **and** `monthly_eur` before every call, computed from the ledger in `[budget] timezone` (I6a boundary tests, I6b's wiring test); the three hooks and `pnk init --ci` force the free backend and say so; no-TTY aborts without `--yes`; the free path is checked first and refuses to pay for a healthy PDF |
| **Retries spend outside the reservation** | one reservation before a slice loop is not a cap; the SDK's own `max_retries=2` turns one call into three billed requests invisibly; and a *single* retry constant lets transport failures silently eat the semantic budget | every call takes its own pre-call reservation and ledger pair, spy-asserted; **two separate counters** — 6 token-billed semantic calls per slice, and within each, 2 transport backoffs, so worst case is 6 billed calls across up to 18 HTTP requests, both stated; the client is pinned to `Anthropic(max_retries=0)` so the accountant owns retry |
| **A call that never returns leaves permanent phantom spend** | the ledger is append-only and an unreconciled reservation counts at its reserved amount, so timeouts and 5xxs would consume a month's budget for money that never left the account, irreversibly | a third record kind, `void`, written from a `finally` path closes the reservation at zero; 429/5xx retry under a *fresh* reservation; a timeout stays `unknown outcome` because the server may have generated; tested per branch |
| **The reservation is sized on the wrong unit** | per-page input looked wrong against whole-document requests (pass 1) and the whole-document request shape was itself wrong (pass 2) — quadratic in input, and past the context window at ~166 pages | the request unit is a K-page slice, so input is linear and each page is sent once; the estimate is `K × page_tokens × input + max_tokens × output` with no cache multiplier, because there is no cache marker; a context-window pre-check names the limit that bounds it |
| **The cache key is right and the cache *path* is wrong** | the I8b shape: correlated filenames, correctness argued about one of them | the cache lives under each KB's own `.pinakes/`; a test syncs two KBs holding the same PDF and asserts two files; staging is keyed by the same pair plus page |
| **The fingerprint hashes the wrong things — in either direction** | hashing too little serves old-model text from the cache and blinds the coherence gate; hashing too much makes a free-path refactor invalidate every paid entry and refuse every query until the user pays to re-extract — a constant that can send an invoice | per-backend **inputs**, declared in a client-free registry entry so the check never imports a paid client; `claude-vision` hashes model id, prompt, schema, request shape (including K) and the pdfium versions it genuinely uses, and **excludes** `LAYOUT_VERSION`, which it never runs; tests assert that changing `[extraction] model` or K both misses the cache and refuses the query |
| **The backend is not part of the change decision — and the record of it does not survive the command that most needs it** | §6.4 keys re-processing on path and content hash alone, so a document indexed by the wrong backend is skipped forever. Worse, `--rebuild` (verified, `sync.py:231-241`) creates an empty database and reads its "before" snapshot from *that*, so a backend recorded only in `index.db` is invisible exactly when it matters | provenance lives in the **sidecar**, a committed file that outlives the index (decision 11), with the index as its cache; the recorded backend joins the change decision in the paid direction only; a **changed content hash** on a paid document under a free run is a `failures` row rather than a silent overwrite or a permanent skip (decision 14); six named cases in `test_backend_drift`, plus two rebuild-provenance tests |
| **Partial or interrupted extraction leaves a document half in the index** | a truncated document is exactly the silent quality loss §4.6 exists to prevent, and nothing in the output reveals it | all-or-nothing commit: no complete cache entry until every page passes, and the document goes to `failures`; `on_exceed = "partial"` is corpus-level only, stated in `--help` and §5 |
| **The paid-path allowlist erodes, or its decisive gate is inert** | a one-line import in a new module quietly makes the free path paid; and a behavioural gate that asserts a package is absent is vacuously true wherever that package is not installed | one `.paid-path-allowlist` read by `check.sh`, CI and the test; the gate lands before the code it guards; it runs in a fresh subprocess, skips loudly where it cannot be meaningful, is exercised on a `[light,pdf,claude]` CI leg, and has a negative test that plants an import and asserts it fails |
| **Scanned-PDF quality cannot be measured by the audit that measures everything else** | the completeness audit's witness is the native text layer, and a scanned page has none — the gate is blind on precisely the stratum the feature exists for | the audit reports `exempt K of M` rather than scoring exempt pages as passing; the scanned stratum's ground truth is hand-authored from the generator's spec; its numbers land in §9 with date, model and cost, labelled as measured on synthetic rasters |
| **Price-table staleness, and the USD→EUR rate inside it** | the manifest prices in EUR and the vendor in USD; the rate is a second number that goes stale with nothing saying so, and a ledger that records only a EUR figure cannot be re-derived later | `usd_per_eur` carries the same `as_of` as the prices; every ledger line records `cost_usd`, the rate and the `as_of`, and EUR is computed at read time; `pnk budget` shows which rate each window used; `pnk doctor` WARNs past `max_price_age_days`; an estimate against stale data refuses rather than guessing. Not a CI gate — see I6a |
| **A threshold that spends money is chosen by intuition, fitted on the wrong pair, or never ships** | two passes each tried to fit `word_coverage`'s floor and each picked a pair that is not the applied pair, because the applied pair needs Claude output that does not exist at fitting time — and a floor living only in `tests/` reaches no installed user at all | the `word_coverage` floor **does not exist in v0.2**: the audit is report-only and the loop moves to v0.3, fitted on a real distribution (decision 12). The one threshold that does gate spending — text yield — compares an extraction against a character count, needs nothing that does not exist, ships in `floors.toml` as package data read through `importlib.resources`, re-fits as a gate so it cannot drift, and its absence refuses the paid path outright |
| **A response that is structurally valid and semantically shifted** | a slice returning 4 pages instead of 5 is schema-valid, maps positionally onto pages 1–4, tiles its spans perfectly, and passes an order-free completeness audit — so every citation in that document is off by one, permanently, with no downstream check able to see it | an explicit `len(pages) == last - first + 1` assertion runs **before** positional mapping and treats a mismatch as a schema failure; a recorded short-array fixture proves the branch |
| **The paid backend re-implements offset emission and nobody tests it** | `assemble()`'s span properties are asserted in `test_extract_layout.py`, which the paid backend never calls, and I5's corpus-wide invariant runs over the free path — so a second, independent implementation ships unproved, and `normalise()` is length-changing, so an offset computed one stage early is wrong while the spans still tile | both properties, including the content-anchored one, are asserted over every recorded paid fixture in I7b's own file; `test_normalise_runs_before_offsets` pins the order; v0.1's I6 is the record of what this class costs |
| **The raster tolerance cannot fail on what it says it catches** | "a moved word must" fail the gate, but at 150 dpi a moved 10 pt word moves ≈0.08–0.37 mean absolute levels against a 2/255 threshold — reaching 2.0 needs ~17,000 fully flipped pixels, about four lines of text, so the gate accepts arbitrary reflow | the tolerance is stated **per changed pixel fraction**, not as a whole-page mean: a change is a failure when more than 0.1% of pixels differ by more than 32 levels, which a moved word trips and a resampling change does not; the arithmetic is written into the test's docstring so the next reader checks it rather than trusting it |
| **The paid path leaks the model's internal text into the corpus** | `output_config.format` constrains structure, not string content, so a `<thinking>` fragment inside a page-text field is cached, chunked and cited as extracted text — and the draft asserted this "cannot" happen | the prompt carries the vendor's recommended generic no-internal-XML-tags instruction and **no** don't-reason instruction, which increases leakage; a tag-leaking recorded fixture proves the handling rather than asserting it |
| **The recorded-fixture harness diverges from the real API** | a fake that encodes only the happy path proves nothing about retries, refusals or truncation | fixtures include a refusal, a schema-invalid page, a truncated page and a page that drops content; the one human-gated real-API run at I7b is the reconciliation, and its date and model are recorded |
| pypdfium2 or anthropic warns at import, and `filterwarnings = ["error"]` turns a whole matrix leg red | a pyproject setting colliding with a dependency, invisible until the extra is installed | I1 probes **both** before anything depends on them; the fix, if needed, names that exact warning — never a blanket ignore |
| the committed PDFs drift from their generator | a byte diff nobody runs | `make corpus` is a gate in `check.sh`, not an instruction — byte-exact for the sixteen text-layer fixtures, within a stated pixel tolerance for the three rasters, because a raster gate that demands bit-exactness fails on the next renderer bump instead of on a real change |
| quality thresholds are chosen from the same corpus they score | v0.1 shipped precisely this for confidence and had to label the result a floor | the corpus is *ground-truth-labelled*, so the metrics are measurements; only the thresholds come from it, and the report header says so |
| a `schema_version` bump strands users who will never index a PDF | a forced rebuild is a machinery cost with no feature benefit to them | announced in the CHANGELOG and in the error's remedy; rebuilds are free and `ledger.jsonl` survives |
| pdfium builds, or the layout algorithm, change extraction silently | every stored span would be stale with nothing saying so | the free backend's fingerprint includes the pypdfium2 and pdfium versions, `LAYOUT_VERSION` **and** the fitted running-head threshold, so any of them changing is a cache miss **and** a per-document coherence refusal; the paid backend hashes the pdfium versions too, because it slices and audits with them |
| a PDF-shaped file that is not a PDF, or a 400 MB PDF, hangs sync | resource behaviour, not parsing behaviour | I3b's reader is the only free I/O boundary; a corrupt-header fixture, an encrypted fixture and a size guard with a stated limit are tested there by name; the paid path re-checks the 32 MB / 600-page API limits before encoding |
| CI time grows from a three-leg matrix | build machinery, felt every PR | 19 tiny synthetic files under a 2 MB budget, no model weights, no API key; the `[light,pdf,claude]` leg installs one pure-Python SDK and never calls it; the `model`-marked tests are the one cost this release genuinely adds, because they do **not** skip on the new legs — verified: CI installs `--extra light` (never `st`), and `_runnable()` skips only when `fastembed` is absent or the HF cache is empty, both of which hold on `[light,pdf]` and `[light,pdf,claude]` too, so a three-leg matrix would triple them. I1 therefore pins the `pytest -m model` step to the `[light]` leg alone, keeping today's coverage and adding none |

## Iteration log

**Pass 1** (20260727 16:12, adversarial review of the draft, by a reviewer reading the plan,
DESIGN.md, `20260725_1317-v0.1.md`'s twelve rules, CLAUDE.md and `ci.yml` fresh) — 7 HIGH, 19 MEDIUM, 8 LOW
resolved. The verdict was that structure, the twelve rules and every Anthropic API fact in the plan
were sound, and that **the money path was bounded per-invocation and unbounded everywhere else,
while the gate meant to catch a paid import could not fail in CI.**

*HIGH:* `monthly_eur` appeared nowhere and only `per_operation_eur` was checked, so a hook-driven KB
on a paid backend spent an allowance per commit — and the same KB would have aborted every commit
for want of a TTY, silently killing hook-driven freshness (`reserve.py` now checks three windows;
hooks and `pnk init --ci` force the free backend). One reservation covered a page loop that makes up
to four calls, and the SDK's default `max_retries=2` made each of those up to three billed requests
(every call now reserves and ledgers; `max_retries=0`). The reservation's input basis was per-page
while each request re-sends the whole document, an order-of-magnitude under-reservation at 200 pages
(estimates are now per *request*, reserved at the uncached 1.25× rate; the `ephemeral` 5-minute TTL
also corrected the resume story). The extraction fingerprint had one formula for both backends,
hashing pypdfium2's version for a backend that never loads it and omitting model, prompt and schema
— so changing `[extraction] model` was a cache *hit* (now a per-backend protocol method). Gate 4,
"the one that actually matters", asserted `"anthropic" not in sys.modules` in an environment where
no CI job installed `anthropic`, making it true by construction — the `false_abstain: 0.0` class
reappearing in the flagship safety check (now a subprocess, a loud skip, a `[light,pdf,claude]` CI
leg, and a negative test that plants an import). `extraction_fingerprint` sat in `meta`, one key per
index, while `--extract` allows per-run backend choice — so a mixed corpus was uncheckable and every
query after an `--extract` run would have been refused (moved to `documents`). Page citations
shipped on `pnk search` only, recreating the CLI/MCP divergence v0.1 fixed, in the release whose
goal is page-citable PDFs, on the surface §1 calls primary (both surfaces now, in I8, with named
tests).

*MEDIUM:* I7 was split into I7a (gate + amendments, landing first), I7b (client, request shape,
validation, retries) and I7c (audit, staging, all-or-nothing) — one increment carrying six
sub-steps, a four-gate CI replacement, three file-level amendments, nine tests and a human-gated API
run does not bisect. Two thresholds that trigger spend — `word_coverage`'s re-extraction floor and
the text yield floor — were undefined or defined in a later increment than the one consuming them;
both are now fitted in I3b with `fitted_on`, and an unfitted `word_coverage` floor triggers no spend
at all. `thinking`, `effort` and `max_tokens` went unmentioned though `claude-opus-5` runs adaptive
thinking by default and `max_tokens` caps thinking and response together; `stop_reason ==
"max_tokens"` was unhandled and would have been retried identically three times, all paid.
`extract/pdfium.py` — the module the plan says deserves most review — had no unit tests;
character-to-block assembly moved into the pure `layout.py` and the reader gained its own test file.
The scanned stratum named no rasteriser, promised byte-identical raster regeneration that no
environment can deliver, and left its "stated byte budget" unstated. `--clear-cache` silently
destroyed paid results; three new flags had no DESIGN home and no stated scope for `--force`; the
ledger recorded a bare `cost`; the per-page token constant's measurement was deadlocked by its own
cap. Plus: a `test_packaging.py` promise with no owning increment (rule 8, recurring), `pdf-eval`
contradicting I1's `--extra light` exit criterion, I1 validating a backend whose adapter lands two
increments later, and two DESIGN sentences contradicted with no amendments row.

*LOW:* a "Verified" claim without `HH:MM`; 123 prose lines over 100 characters, re-wrapped; `make
budget` promised by I9 and created by nobody (now I6b); the `[extraction]` amendments row omitting
the increment that consumes `model`; the `filterwarnings` probe covering pypdfium2 but not
anthropic; the `paid` marker's scope and `PINAKES_ALLOW_SPEND`'s status left unstated; server-side
refusal fallbacks never considered (now a recorded "checked and declined", because a fallback would
attribute one model's text to another's fingerprint); and DESIGN §4.6's trailing clause, false for
PDFs, left standing.

*Verified in this pass rather than recalled:* PyMuPDF 1.28.0's licence field (PyPI, 20260727 16:01);
`pypdfium2` 5.12.1 latest, 5.12.0 present (PyPI, 20260727 16:00); `anthropic` 0.116.0 present and
0.120.0 latest (PyPI, 20260727 16:00).

**Pass 2** (20260727 17:00, a second adversarial reviewer reading the revised plan, DESIGN.md,
`20260725_1317-v0.1.md`, `docs/RETROSPECTIVES.md`, CLAUDE.md and `ci.yml` fresh, and loading the `claude-api`
skill before judging any API claim) — 5 HIGH, 18 MEDIUM, 8 LOW resolved. The finding that matters
most is not any single item: **four of the five HIGHs were created by pass 1's own fixes, not
survivals of pass 1's findings.** A review pass is a change, and a change needs its own review; two
passes are the minimum for a document this size, not a formality.

*HIGH.* The per-page-request shape pass 1 introduced was itself the defect — re-sending the whole
document on every page makes input quadratic and stops fitting `claude-opus-5`'s 1M context window
at ~166 pages, below both the 600-page API limit the pre-check tested and the 200-page example the
plan worked through twice; a 100-page document reserved ~$375, which no cap here can approve, so the
feature failed closed with a refusal no user could satisfy (decision 8: K-page slices, linear input,
no cache dependence, $14 reserved for the same 200 pages). The three spend windows were computed
from a ledger whose reservation/reconciliation pairing rule was never stated, and the three readings
it admitted differ by about 2.4× on the shipped monthly default (pass 2 said five; pass 4's
arithmetic corrected it — the input half is fixed, so the ratio cannot exceed 3.3×) (now: one
record, attributed to the reservation's timestamp, superseded in place, with boundary tests for a
pair straddling midnight, a month end and a DST shift). Every call that *raised* left a reservation
nothing could close, permanently consuming budget for money that never left the account, in an
append-only file CLAUDE.md forbids editing (now a `void` record from a `finally` path, plus a stated
transient-error policy `max_retries=0` had left undefined). Both fitted floors lived in a test-only
file that no wheel installs, while three runtime consumers on a user's own KB depend on them — and
the fail-closed rule was stated for one floor only, so the guard against paying to re-extract a
healthy PDF was silently disabled on every installed copy (now `floors.toml` as package data, and
the rule stated and tested per floor). The extraction backend was not part of §6.4's change
decision, so a hook-forced free sync indexed a scanned PDF as empty and every later sync skipped it
on an unchanged content hash, while `--rebuild` silently downgraded paid extractions — both exactly
the silent quality loss §9 names as this release's reason to exist (decision 9).

*MEDIUM.* The per-document coherence check would have imported `anthropic` on **every query** of any
KB holding one paid document, and its stated reason for skipping was false — claude-vision's
fingerprint inputs are all package constants, so an absent SDK proves nothing about whether the
extraction changed (fingerprint *inputs* now live in a client-free registry entry). The
`word_coverage` floor was fitted on (ground truth → pypdfium2) and applied to (native layer →
Claude), different distributions, which would have left the audit inert. The `Extractor` protocol
was frozen at I1 in a shape I5 could not consume on a cache hit and I7b could not use at all. The
`--extract` flag had no owning increment while I6b's hooks were written to contain it — shipping a
`pre-commit` hook that blocks every commit. `--clear-cache`'s euro report had no join key in either
the cache or the ledger. The eviction sweep destroyed paid entries the manual path had just learned
to confirm. "Up to four calls per page" contradicted an enumeration two lines above it (seven).
`output_config.format` was said to make `<thinking>` leakage impossible; it constrains structure,
not string content. Plus: no negative test for the allowlist's exclusion logic; a `max_retries=0`
test that could only inspect its own stand-in; money with no stated numeric type under
exact-equality boundary tests; a refusal naming one of three caps; DESIGN §7 gaining nothing though
v0.2 adds a second quality regime; amendments made outside the amendments table and two CLAUDE.md
amendments with no auditor; the CI matrix and `pdf-eval` job landing six increments after the gates
they make meaningful; and `--estimate-only` appearing exactly once, in a human-gated criterion, with
no owner, no scope and no test — a verbatim recurrence of a pass-1 finding *inside the fix for it*.

*LOW:* a euro figure with no FX rate in the increment that makes FX provenance mandatory; three
unstated gating constants; `response.model` never recorded though the fallback note says in as many
words why it matters; `PINAKES_ALLOW_SPEND` declared pytest-only and then put in a CLI recipe; the
no-TTY abort's scope readable two ways; `path:page` undefined for the two-page chunk I5 explicitly
allows; and four sequencing nits, including DESIGN's status line still reading v0.1.1 against a
0.1.3 package.

*Verified in this pass rather than recalled (`claude-api` skill, 20260727 17:00):* `claude-opus-5`
is 1M context / 128K max output — the number that makes the quadratic shape unimplementable;
`model_context_window_exceeded` is a real stop reason on 4.5+ models and was absent from the branch
list; `thinking: {"type": "disabled"}` **is** legal at effort `low` (400 only at `xhigh`/`max`);
`$5.00 / $25.00` per MTok, the 512-token cache minimum and the 5-minute `ephemeral` TTL are current.
The reviewer confirmed every other Anthropic API claim in the plan was already correct.

**Pass 3** (20260727 20:23, three reviewers: one fresh, one anchored on the first and told to break
it, one aimed at what the others under-covered — all reading the plan, DESIGN.md, `20260725_1317-v0.1.md`,
`docs/RETROSPECTIVES.md`, CLAUDE.md, `check.sh` and `ci.yml`, loading the `claude-api` skill before
judging any API claim, and installing pypdfium2 to check decision 8's foundation) — 12 HIGH, 31
MEDIUM, 17 LOW resolved. **The pattern held for a third time: five of the twelve HIGHs were created
by pass 2's own fixes.** What broke it was method, not effort: **five of the twelve were found only
by checking the plan against `src/` and against arithmetic, not by reading the plan.**

*The five that no consistency pass could have found, each verified in this repository:* `--rebuild`
creates an empty database and reads its "before" snapshot from *that* (`sync.py:231-241`), so
decision 9's protection — recorded only in `index.db` — was invisible to the one command that most
needed it, and I5's own "not re-extracted by `--rebuild`" test case was unimplementable (decision 11
moves provenance to the sidecar). The trace test asserted "**exactly one** chunk" contains the
traced offset while I5's prose said "≥1" and `chunk.py:239-269` disproves it — `_fit` prepends the
carried overlap and takes `start` from the *carried* piece, so chunk *n+1* begins inside chunk *n*
whenever a block splits, which a table cell is the fixture most likely to do. The filter-column
trace named `tags`, which is not a column at all (`store.py:38-48`, `search.py:128-132`) but
`json_each` over a `NOT NULL DEFAULT '{}'` field — so the assertion was true by schema for every row
and would have passed on a corpus with no tags. The raster gate's "a moved word must fail" is false
by its own arithmetic: at 150 dpi a moved 10 pt word moves a whole-page mean by ≈0.08–0.37 against a
threshold of 2.0, so the gate accepted about four lines of reflow. And I9's README list enumerated
*additions* while four sentences already in `README.md` (lines 11, 45, 48–50, 119) are falsified the
moment a paid extractor ships — the same defect, and the same count, as the audit at 0.1.2.

*The five that were pass 2's fixes turning over:* the `word_coverage` floor was re-fitted onto a
second wrong pair, over a population including a fixture the reader contract requires to raise, so
the audit would have been inert on every installed copy (decision 12 makes it report-only). Decision
10 said the paid backend "bypasses `layout.py`" while step 6 had it calling `normalise()`, which
lives there — so a whitespace or ligature change would bump `LAYOUT_VERSION`, miss every free cache
entry, and **hit** every paid one, invisibly (decision 15 versions the string policy separately).
The 7-call ceiling omitted the transport retries the same pass introduced, and the audit
re-extracted a *page* while I7b forbids re-asking a page from a slice. The paid fingerprint hashed
pdfium's build, so `uv lock --upgrade` refused every query on every paid KB with a paid remedy
(decision 13 downgrades that to a WARN). And decision 9 had no rule for a *changed content hash* on
a paid document — the common case under hooks, since they force the free backend on every commit
(decision 14 makes it a `failures` row).

*Also HIGH:* structured outputs were relied on to enforce a slice's page count, which is not
something to rely on — a 4-element response to a 5-page slice is schema-valid, maps positionally
onto pages 1–4, tiles its spans, and passes an order-free audit, so every citation in that document
is silently off by one for good (an explicit length assertion now runs before the mapping). The two
"property assertions" over page spans were one property and its own corollary, neither said *which*
page a span belongs to, and neither was asserted against the paid backend's independent
implementation at all. Nothing checked the whole document against the caps before call 1, so an
unaffordable document could spend a month's budget and index nothing. And decision 10's
justification — "measured, not assumed" — had no owning increment, no named fixtures and no
verification row, the third recurrence across three passes of the rule-8 defect this plan cites by
name.

*MEDIUM and LOW* covered the corpus generator (a slice-boundary claim that `ceil(2/5) = 1` makes
impossible; `SOURCE_DATE_EPOCH` pinned only inside pytest while `make corpus` runs outside it), six
DESIGN/CLAUDE.md amendments made in the body with no row in either table, `--estimate-only` still
having no test after two passes fixed two-thirds of it, a `finally`-written void that could zero a
*billed* call, timeouts reproducing the exact lockout the void was introduced to prevent, `--yes
--clear-cache` destroying paid results in cron, a `pnk budget` invocation in I7a's gate 4 that I6b
creates, a `build` job left red from I7b to I9, and a doctor check that thresholds a per-document
median against a per-page floor.

*Verified in this pass rather than recalled:* every Anthropic API fact in the plan is correct
(`claude-opus-5` 1M/128K, $5/$25, disabled thinking legal at effort `low`, `output_config`'s current
shape, base64 document block before text, 32 MB / 600 pages, `page_location` for PDFs, citations ×
`output_config.format` → 400, SDK `max_retries` default 2, the 512-token cache minimum, ~4,784
visual tokens per high-res page, `model_context_window_exceeded`), with one claim the plan made
about `json_schema` array-length enforcement that could not be confirmed either way — so the plan
now asserts the application-level check and does not claim the schema does it.

**Pass 4** (20260727 20:57) — run as **three narrow passes instead of one wide one**, on pass 3's
recommendation and its evidence: a code-reality pass resolving every claim about `src/` against the
file, an arithmetic pass recomputing every stated number, and a promise-ledger pass walking every
enumerated bound, flag, floor, gate and amendment row asking only "which increment makes this true,
and which test proves it". Together: **19 HIGH, 39 MEDIUM, 23 LOW**, and the shape of the findings
vindicates the method — almost none of them are things a fourth general reading would have caught.

*Code-reality (5 HIGH).* Decision 11 rested on "sync writes the sidecar before it writes the index",
which is false: `write_sidecar` has two call sites, `_mint` (new documents only) and the
`--sidecars-only` path, so a document that **already has** a sidecar — decision 9's entire case — is
never rewritten. `sidecar.py`'s own docstring defers in-place rewriting to v0.3 for want of a
comment-preserving writer, so v0.2 pulls that cost forward; accepted deliberately, with the loss
stated. `--index-only`, which the hooks run, cannot write into `docs/` at all, so a paid backend
under it is now refused. `pnk init --ci` does not exist — I6b was written to modify a workflow
writer nobody had built. And CI installs `--extra light`, never `st`, with the `model` tests in the
`check` job, so a three-leg matrix would have **tripled** them rather than skipping them as the risk
table claimed.

*Arithmetic (5 HIGH).* Pass 3's raster-tolerance fix **re-created the defect it fixed**: 0.1% of an
A4 page at 150 dpi is 2,175 px, while a moved 10 pt word changes ~682 px on the plan's own ink model
— so the new gate passed a moved word exactly as the old one did. Now an absolute 300-px count with
one ink model stated once. "Counting reserved buys roughly a fifth of what actuals buy" is ~70%, and
the spread across the three readings is 2.4×, not 5× — it cannot exceed 3.3× in principle. "Six
named paid twins" while the table names five. The 600-page pre-check can never fire when a request
carries five pages — the plan's own charge against the draft, unfixed. And `reserve_document`
answered "can you afford this?" with a single-attempt number the retry ceiling exceeds by **6.6×**.

*Promise ledger (9 HIGH, of 256 promises enumerated; 204 fully specified).* `--force` had no
creating increment at all — pass 2's flag-without-an-owner shape, third recurrence. Two findings sat
**inside pass 3's own fixes**: the `pnk budget`-in-gate-4 fix was assigned to I6b, which lands
*before* the I7a that creates the gate; and the six-amendments-with-no-row fix added a CLAUDE.md row
for "CI runs `[light]`" while the same sentence in DESIGN §4.5 and §9 still had none. The
`Extractor` protocol still declared `fingerprint_inputs()` after pass 2 moved inputs to the
registry. Two verification rows tested a re-extraction loop decision 12 had deleted — vacuous by
construction, in the table that exists to prevent that. And six increments' exit criteria disagreed
with the amendment table about which rows they own.

*Also settled here:* the `word_coverage`-adjacent `T` was "fitted" over a stratum whose 3-page
documents give it a resolution of 1/3 — every `T` in (1/3, 2/3] reproduces the corpus, so the
repeating-head fixture grows to 10 pages and the fit becomes real; `path:p1-p2` collided with the
character-offset citation format `search.py:64-66` already prints; and the text-yield floor was
per-page in one increment and per-document in two others, with the aggregation never stated.

*Verified while applying the fixes, because decision 8 depends on it:* pypdfium2 5.12.1 exposes
`PdfDocument.new()`, `import_pages(pdf, pages=None, index=None)` and `save(dest, ...)`, and a live
round trip produced a 5-page slice and a 2-page short final slice from a 12-page document (20260727
17:00). A plan that re-architects the money path around a library call it never checked would be the
"asserted without evidence" defect the retrospective process exists to catch.
