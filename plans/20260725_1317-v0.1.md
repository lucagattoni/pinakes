# Pinakes v0.1 — implementation plan

> ℹ️ **Version numbers below reflect the convention in use when this was written.** Unbuilt
> work is now **named, not numbered** ([STATUS.md](../docs/STATUS.md)). This record is left as it was.

**Status:** **shipped** — I1–I15 all landed; 0.1.0 released 20260725 15:27, tagged 20260727 14:58
· **Planned:** 20260725 10:04 (5 passes, 28 findings — passes 1–4 are logged below; pass 5 was the
clean confirming pass and, unlike `DESIGN.md`'s, was never written down)
**Close-out:** ["What the build taught"](#what-the-build-taught) at the end of this file, written
20260727 15:05 against the 15 shipped increments and the 52 findings in
[`../docs/RETROSPECTIVES.md`](../docs/RETROSPECTIVES.md). **Read it before writing the next plan.**
The sections above are preserved as written and are stale where the close-out says so.
**Source of truth:** [`docs/DESIGN.md`](../docs/DESIGN.md) (pass 7). This plan sequences §8's v0.1
scope into ordered increments; where the two disagree, DESIGN.md wins and this plan has a bug.

## Goal

Ship the v0.1 vertical slice end to end: `pnk init` → `pnk sync` → `pnk search` → `pnk serve`,
with `pnk doctor`, `pnk install-hooks`, the golden-set eval harness, CI, and a 0.1.0 PyPI release.
Every increment below ends **green** (`uv run ruff check`, `uv run ruff format --check`,
`uv run pyright`, `uv run pytest`; `uv run ty check` as a fast pre-check) and
**committed on its own** — no batching, so any regression bisects to one increment.

## Ground rules (apply to every increment) · **historical — the `__version__` and free-path bullets are v0.1-only (v0.2 added the paid path, allowlisted); the live rules are `docs/BUILDING.md` § One increment at a time**

- **Worktree + branch per increment**, named `YYYYMMDD_HHMM-i<N>-<slug>` per the global convention.
- pyright **strict** from the first line; no `# type: ignore` without a stated reason.
- Tests accompany the increment that introduces the behaviour — never deferred to a "testing" phase.
- Docs rule (CLAUDE.md): any user-facing surface added in an increment updates `--help` text in the
  same commit. README's worked examples update when the command they show becomes real; README's
  status banner flips only in I15.
- The free-path invariant is enforced structurally: nothing in v0.1 imports an API client at all.
- `__version__` stays `0.0.0` until I15 cuts `0.1.0`.

## Decisions this plan makes (and why)

| Decision | Choice | Why |
|---|---|---|
| CLI framework | stdlib `argparse` subparsers | Zero dependency; the bootstrap stub already uses it; v0.1's flag surface is small. Revisit only if flags become unmanageable — that revisit is a MINOR change, not a rewrite |
| Manifest writing | Template rendering (jinja2), never TOML serialization | `tomllib` is read-only (verified: no `dumps`). §6.1 fixes `pinakes.toml.j2`, so jinja2 enters core deps — pure-Python, no weight. After `init`, **nothing ever rewrites the manifest**; it is user-owned like `docs/` |
| The one v0.1 template | `notes` | README's quickstart already names it; plainest possible blueprint |
| Template packaging | `src/pinakes/templates/notes/` via `importlib.resources` | Works from a wheel; no path assumptions |
| Embedding `revision` semantics | Enforced when set; `pnk init` leaves it unset with a comment; `pnk doctor` reports the resolved revision and suggests pinning | `init` cannot know the HF sha offline, and a manifest field silently written by machinery would violate "user-owned". Coherence (§4.4) always enforces provider+model+dim; revision additionally when present |
| Backend protocol | One `EmbeddingBackend` protocol: `embed(texts) -> ndarray`, `count_tokens(text) -> int`, `info() -> ModelInfo(provider, model, revision, dim, max_seq_length)`; one `Reranker` protocol: `score(query, passages) -> list[float]`, `info()` | Both `sentence-transformers` and fastembed satisfy it (fastembed exposes a HF `tokenizers.Tokenizer`; verified in-session). Chunking and retrieval test against fakes, so model downloads never enter unit tests |

## What this plan deliberately does NOT decide

PDF ingest (v0.2) · `pnk link` and cross-KB traversal (v0.3) · budget/ledger/`ask --deep` (v0.4) ·
`sqlite-vec` tier and template ecosystem (v0.5) · demo-KB prose content (written in I14, only its
*coverage contract* is specified here) · exact calibrated threshold values (produced by I14's
fitting, not chosen by a human).

---

## Increments

Dependency shape: I1→I2→I3 are the foundation; I4 (storage) and I5–I7 (sidecar/chunk/embed) fan out
from it; I8 (sync) joins I3–I7; I9 (retrieval) joins I4+I7; I10–I13 hang off I8/I9 independently;
I14 needs I8+I9+I10; I15 is last.

### I1 — Package skeleton, errors, CLI dispatch

`src/pinakes/`: `errors.py` (a `PinakesError` hierarchy — every user-facing failure derives from it
and carries a remedy string), `cli.py` rebuilt as argparse **subparsers** with one module-level
`run(args) -> int` per command, all still exiting 1 with the design-stage message. Exit-code
contract: 0 success, 1 operational failure, 2 usage error (argparse default).
**Tests:** help output lists every §8 command; each stub exits 1; unknown command exits 2.
**Exit criteria:** the current stub's tests are replaced, not deleted — behaviour parity proven.

### I2 — Identity: ULIDs and `pnk://` URIs

`ids.py`: mint/parse ULIDs (python-ulid), `KbId`/`DocId` NewTypes. `uri.py`: parse/format
`pnk://<kb-ulid>/<doc-ulid>`; `self` accepted on parse, **expansion requires the owning KB's id**
(API takes it explicitly — no ambient state); aliases rejected in URIs by construction.
**Tests:** round-trip, malformed rejection, `self` expansion, alias-in-URI rejection.

### I3 — Manifest model

`manifest.py`: frozen dataclasses mirroring §2.1 exactly (`[kb]`, `[sources]`, `[embedding]`,
`[chunking]`, `[retrieval]` incl. `[retrieval.confidence]` optional, `[rerank]`, `[budget]`,
`[[links.kb]]`), loaded with `tomllib`, validated with precise error messages naming the offending
key. Unknown keys are a **hard error** (a typo'd `finall_k` silently ignored is a wrong-behaviour
bug). `[budget]` is parsed and carried but consumed by nothing until v0.4 — validated so a KB
authored today doesn't break then. Also here: `find_kb_root(start=cwd)` — walk up to the nearest
`pinakes.toml` (git-style); every command that operates on a KB (`sync`, `search`, `doctor`,
`install-hooks`) uses it, accepts `--kb PATH` to override, and errors with a remedy when no
manifest is found.
**Tests:** golden valid manifest; one test per validation rule; unknown-key rejection; root
discovery from nested dirs, override, and not-a-KB error.

### I4 — Storage engine

`store.py`: schema DDL exactly per §3's table list, `schema_version = 1` in `meta`;
`open_rw(path)` (WAL, foreign keys on) and `open_ro(path)` (`file:…?mode=ro`, `busy_timeout`);
version mismatch raises with the `pnk sync --rebuild` instruction; FTS5 external-content table +
insert/update/delete triggers. Embedding vectors: float32 BLOB column; a loader returning one
contiguous `np.ndarray` plus row-aligned chunk ids (the §3.1 NumPy tier's substrate).
**Tests:** DDL applies on 3.13's sqlite; triggers keep FTS in sync through update/delete; ro handle
cannot write; version gate fires; BLOB round-trips exactly.

### I5 — Sidecars

`sidecar.py`: read/validate/write `<file>.pnk.yaml` (yaml.safe_load only), skeleton generation
(id, title from filename, created, empty tags/links), link entries with alias→ULID and self→ULID
expansion **at write time** (consumes I2 + I3's `[[links.kb]]`), duplicate-ID scan across a set of
sidecars. No `content_hash` — per §2.2 change detection is index-only.
**Policy:** v0.1 *creates* sidecars and *moves* them on rename (§6.4) but never rewrites an
existing sidecar's content — so pyyaml's comment-stripping on dump is moot until v0.3's `pnk link`
edits sidecars in place (which may adopt a comment-preserving writer then).
**Tests:** round-trip preserving unknown user keys (sidecars are user-editable; a rewrite that
drops their extra fields destroys data); expansion cases; duplicate detection.

### I6 — Structural chunking

`chunk.py`: Markdown chunker (split on heading boundaries, then paragraphs; heading-path recorded),
plain-text/code chunker (blank-line blocks; for code, no syntax parsing in v0.1 — stated
limitation), `max_tokens` enforcement via a `TokenCounter` protocol with overlap, oversize blocks
**split, never trimmed** (§4.6); validation of manifest `max_tokens` against
`max_seq_length - special_tokens` is a hard error.
**Tests:** fake counter (token = word) makes everything deterministic: heading paths, spans,
overlap arithmetic, the never-trim guarantee (every source char lands in ≥1 chunk).

### I7 — Embedding backends and reranker · **historical spec — falsified in detail by the file's own stale table; verify against `src/` before quoting**

`embed.py`: the two protocols from the decisions table; `load_backend(manifest)` dispatching on
`provider` through a **module-level registry dict** (open, not an enum: manifest validation accepts
any provider string; an unknown one fails at load time listing the registered providers — and tests
register a `fake` provider in that dict, which is what lets the quickstart acceptance test drive the
real CLI without model weights) with **lazy imports** — a core-only install raises `BackendMissingError` naming the
exact extra (§4.5); dim mismatch between manifest and model is a hard error. `st_backend.py`
(sentence-transformers + `CrossEncoder`) and `fe_backend.py` (fastembed `TextEmbedding` +
`TextCrossEncoder`). `max_seq_length` is not public fastembed API: `fe_backend` derives it from the
loaded HF tokenizer's truncation config; `st_backend` reads `model.max_seq_length`; both canary
tests assert the derived value is a sane positive int matching the model card (512 for the v0.1
defaults). Model resolution honours `HF_HOME` — for fastembed by passing an explicit cache dir under it,
since fastembed's own default is `$TMPDIR/fastembed_cache` (§4.5, verified upstream); `--offline` is a flag on every command that may load models
(`sync`, `search`, `serve`, `doctor`), mapped to the libraries' offline modes, failing fast with a
remedy when weights are absent.
**Tests:** unit tests use fakes only. One `@pytest.mark.model` integration test per backend
(embed + rerank a canary sentence, assert dim and score ordering) — skipped locally unless weights
are cached, always run in CI where I15's `HF_HOME` cache holds `[light]` weights.

### I8a — Sync pairing core (pure) · **historical spec — falsified in detail by the file's own stale table; verify against `src/` before quoting**

`pairing.py`: the §6.4 two-phase algorithm as a **pure function**:
`pair(before: IndexSnapshot, after: WalkSnapshot) -> list[Action]` — no I/O, no SQLite, no
filesystem. `IndexSnapshot` = the `documents` rows (id, path, content_hash, sidecar_hash, state);
`WalkSnapshot` = every source file's path+hash plus every sidecar's path, its `id` field, and
the hash of the sidecar file itself (which feeds `documents.sidecar_hash` — not to be confused
with the in-sidecar hash field §2.2 deliberately doesn't have).
Actions: `Skip`, `Reembed(id)`, `RefreshMetadata(id)`, `Rename(id, old, new)`,
`AdoptSidecar(id, path)`, `Mint(path)`, `SoftDelete(id)`, `AmbiguousDuplicate(...)`,
`DuplicateIdError(...)` — the §6.4 table row-for-row plus its three stated consequences:
soft delete removes chunks/embeddings, sidecar-only edits emit `RefreshMetadata`, and in
rename+edit the sidecar adoption wins over deletion (no `SoftDelete` for an adopted ID; the
sidecar-didn't-travel case is reported).
**Tests:** table-driven, one case per §6.4 row plus the compound cases (rename+edit in one sync;
duplicate content where one candidate has the old-ID sidecar; N-way ambiguity) — this is the
highest-risk logic in v0.1 and gets the densest test file.

### I8b — `pnk sync`

`sync.py` + `lock.py`. Walk sources per manifest include/exclude — `*.pnk.yaml` is
**categorically excluded** from the document set regardless of include patterns (an include of
`**/*.yaml` must never ingest sidecars as documents); hash; feed I8a; execute actions —
each document in its own transaction, parse/embed failures recorded in `failures`, run continues,
exit non-zero listing them (§6.4). Executing `SoftDelete` removes the document's **chunks and
embeddings** (and FTS rows via triggers) so a deleted document can never be retrieved; the
`documents` row stays (`state = deleted`) because it is the identity §6.4's pairing needs.
Sidecar-only edits refresh `documents.metadata` and `links` without re-embedding, tracked via
`documents.sidecar_hash` — semantics and schema are canonical in §6.4/§3 (design pass 7), the plan
just implements them. Lock per §6.5: pid/host/start-time record; live-holder → exit 0
quiet; dead-pid same host → reclaim + warn; other host → refuse; `--force-unlock`.
`--rebuild`: build `index.db.new`, `wal_checkpoint(TRUNCATE)`, close, rename; `ledger.jsonl`
untouched (create nothing — the ledger file itself is v0.4's; rebuild must simply not delete
files it doesn't own). `--quiet` suppresses progress output for hook/cron use (errors still print). `--sidecars-only
[--stage]`: mint+write sidecars for new docs; with
`--stage`, only for docs staged in git, then `git add` the sidecars (the §6.3 pre-commit mode);
outside a git repo `--stage` errors.
**Tests:** end-to-end temp-KB syncs with a fake backend; every §6.4 action observable in the index;
failure isolation (one poisoned file, N−1 indexed, exit ≠ 0); lock matrix (live/dead/other-host,
monkeypatched liveness); rebuild leaves a stranger file in `.pinakes/` alone and swaps atomically
(no `-wal` survivor).

### I9 — Retrieval · **historical spec — falsified in detail by the file's own stale table; verify against `src/` before quoting**

`search.py`: the §4.1 pipeline. Metadata filter → SQL WHERE (tags via sidecar-derived
`documents.metadata`, path prefix, source_type, created range — the full unified filter
vocabulary I10/I13 expose); FTS5 BM25 with **query escaping** (user text is
not FTS5 syntax — quote it); NumPy cosine over the I4 loader's array; RRF (k=60) →
`fusion_top_k`; reranker (when `rerank = "local"`) → `final_k`; result objects carry passage text,
doc id, path, heading path, span, and scores. Coherence gate (§4.4): provider/model/dim (+revision
when set) checked against `meta` before any query; mismatch refuses with the rebuild instruction.
Confidence: reranker scores → thresholds **only when** `[retrieval.confidence]` present *and*
`fitted_for` matches the active reranker model@revision; else `unknown` (§4.2). Term-coverage
tiebreak only.
**Tests:** deterministic fake vectors make ranking assertions exact; FTS injection strings; RRF
golden values; rerank=none path; confidence `unknown` on absent block, on `fitted_for` mismatch,
and thresholds on a fake scorer; coherence refusal.

### I10 — `pnk init` + the `notes` template + `pnk search` CLI · **historical spec — falsified in detail by the file's own stale table; verify against `src/` before quoting**

Template `src/pinakes/templates/notes/`: `template.toml` (`notes@1.0`), `pinakes.toml.j2`,
`README.md`, `eval/questions.yaml` (starter golden set for KBs stamped from it). `pnk init NAME
--template notes`: refuse non-empty target; mint KB ULID; render manifest; create `docs/`,
`.gitignore` (`.pinakes/`). `pnk search QUERY [--kb PATH] [--tag T]... [--path-prefix P] [--source-type T]
[--created-after D] [--created-before D] [-k N] [--json]` — the §4.1 filter set (tags, path, date,
source_type) in full. Human output: ranked cited passages + a confidence line; low confidence appends the §4.2
escalation note — worded as *"low confidence — retrieval-only result; `pnk ask --deep` (planned,
v0.4) will offer paid synthesis"* so the CLI never advertises a command that doesn't exist as if it
did.
**Tests:** init in temp dir → manifest parses via I3, ULID valid, re-init refuses; search
end-to-end against a fixture KB with fake backend; `--json` schema stable; help-text snapshot.

### I11 — `pnk doctor` · **historical spec — falsified in detail by the file's own stale table; verify against `src/` before quoting**

Checks, each `ok | warn | fail` with a remedy string: sqlite version/FTS5/`enable_load_extension`
probe (§3.1); backend importable (which extra if not); model + reranker weights present under `HF_HOME`
(true for both backends by construction — §4.5); index coherence incl. `fitted_for`, reporting
the resolved model revision and suggesting a manifest pin when `revision` is unset (the decisions
table's promise, kept here); orphaned sidecars; duplicate IDs; dangling
intra-KB links (cross-KB targets reported as `unchecked` — traversal is v0.3); hook status
(present/absent/foreign); held sync lock + age; chunk count vs 50k advisory (§3.1); git-repo
status. `--prune` deletes orphaned sidecars only after printing the list (§6.4). Exit: 0 all-ok,
1 any fail, 0-with-output on warns.
**Tests:** one fixture per check in both states; prune prints-then-deletes; never touches
non-orphaned sidecars.

### I12 — `pnk install-hooks`

Writes the §6.3 three hooks. The scripts invoke `pnk` from `PATH` and exit 0 with a one-line
warning if it is not found — a hook that hard-fails every commit because a venv wasn't activated
would train users to `--no-verify` permanently. Existing user hook files: **refuse and print the lines to add** —
silently appending to or overwriting a user's hook is a trust violation; idempotent when the hook
is already ours (marker comment). `pnk doctor`'s hook check recognises ours/foreign/absent.
**Tests:** in a temp git repo — install, re-install (idempotent), foreign-hook refusal; a real
commit fires pre-commit and the sidecar lands in the same commit; post-commit syncs the index.

### I13 — `pnk serve` (MCP) · **historical spec — `FastMCP` is gone: `mcp` 2.0.0 removed it and 0.28.0 moved `serve.py` to `MCPServer`; the filter is `--modified-*` not "created range"; and `pinakes_links` joined the three tools in 0.5.0. Verify against `src/` before quoting**

`serve.py` with `FastMCP` (verified import, mcp 1.28.1): tools `pinakes_search`, `pinakes_get`,
`pinakes_list_kbs`, each with a `kb` argument (the KB's `[kb].name` or its ULID, default = first
configured KB). `pinakes_get` returns the document's metadata (id, path, title, tags) plus its
source text from `docs/`, inside the same delimited evidence framing as search results;
`pinakes_list_kbs` returns name, ULID and document count per served KB. `pinakes_search` takes
the same filter set as the CLI (tags, path prefix, source_type, created range) — one filter
vocabulary, two surfaces.
Serves **only** KBs named on its command line (`pnk serve PATH...`); no tool argument accepts a
filesystem path; `pinakes_get` resolves doc ULIDs through the index only (§4.7). Read-only index
handles; per-request `stat()` staleness check → reopen (§6.5). Passages returned inside a
delimited evidence field (§4.7).
**Tests:** in-process MCP client session: tool listing, search/get round-trip, unknown-kb error,
path-shaped `kb` arg rejected, index swap mid-session picked up on next request.

### I14 — Demo KB, golden set, eval harness, calibration

`tests/demo-kb/`: synthetic corpus per §7 — ~30 Markdown docs written for the purpose, committed
with sidecars; its manifest **pins model revisions** (we control this KB, and the pin makes the CI
cache key stable). ≥40 questions in `eval/questions.yaml` spanning lexical-only, paraphrase-only,
filter-dependent, multi-hop, and **no-answer** cases. Multi-hop questions are scoreable without an
agent: each ships its scripted hop sequence (query per hop, expected doc to `get` between hops);
the harness executes the script and scores recall/MRR on the final hop — testing that the corpus
*supports* the §4.3 loop, not simulating an agent's judgement. `eval.py` (module + `python -m
pinakes.eval`): recall@k, MRR, rerank precision, false-abstain, false-confidence; JSON report;
`--baseline` compare with per-metric tolerance for CI. `calibrate.py` (`python -m
pinakes.calibrate`): fits
`low_below`/`high_above` on the golden set, emits the `[retrieval.confidence]` block +
`fitted_for`; the fitted values are committed into the **demo KB's manifest only**. The `notes`
template ships the block **commented out** with a pointer to calibration: thresholds fitted on the
demo corpus were not fitted on the user's corpus, and committing them to the template would assert
a calibration that never happened — `confidence: unknown` is the design's honest default (§4.2),
and the template's starter golden set exists so users can calibrate their own KB.
Uses `[light]` models (CI-cached); this is where CLAUDE.md's "golden-set eval justifies retrieval
changes" rule becomes enforceable.
**Exit criteria:** eval runs green in CI; calibrated thresholds committed; baseline JSON checked in.

### I15 — CI, packaging, 0.1.0

`.github/workflows/ci.yml`: uv-managed 3.13; `[light]` + dev deps; ruff, pyright, pytest (model
marks included), eval-vs-baseline gate; `HF_HOME` cached keyed on the demo KB's model ids+revisions
(§4.5). Release: `uv build`; wheel smoke test (`uvx --from dist/... pnk --version` and `pnk init`
in a temp dir — proves template packaging); README status banner flipped, quickstart verified
against the built wheel **by a scripted acceptance test, not by reading**; CHANGELOG 0.1.0 cut
(MINOR per the global release rule); tag `v0.1.0`; PyPI trusted publishing — **manual prerequisite:** the PyPI project and its
trusted-publisher binding to this repo must be configured by the user in the PyPI UI before the
release workflow can succeed; the plan flags this as a human gate, not automatable.
**Exit criteria:** a fresh `uv add "pinakes[light]"` from PyPI reproduces the README quickstart.

---

## Verification of the whole (not just the parts) · **the paid-import grep never shipped in v0.1 — a CI grep landed in 0.1.1 (20260727 14:52) and `tools/paid_path_gate.py` replaced it in 0.3.0; `tests/test_quickstart.py` never existed (the quickstart test is `tests/test_cli_search.py::test_the_readme_quickstart_works`)**

- The README quickstart is an executable acceptance test from I10 onward (`tests/test_quickstart.py`
  drives the real CLI in a temp dir with the fake backend; I15 runs it against the wheel with real
  `[light]` models).
- §8's v0.1 sentence is the checklist: at I15, every item in it maps to a shipped increment — walk
  the list explicitly in the release commit message.
- Invariants spot-checked in CI: no paid-API import anywhere (`grep` gate for `anthropic`/`openai`
  in `src/`), `.pinakes/` never committed (gitignore test), MCP tools all `pinakes_`-prefixed.

## Risks specific to this sequencing

| Risk | Mitigation |
|---|---|
| I8a's pairing table is subtly wrong and everything downstream builds on it | It's pure and lands early with the densest tests; §6.4's table is transcribed row-for-row into test names so a missing row is visible |
| Fake-backend tests pass, real models behave differently | The `@pytest.mark.model` canaries (I7) and the real-model eval (I14) both run in CI; only unit tests use fakes |
| `notes` template golden set too weak to calibrate on | I14 calibrates on the demo KB (≥40 questions); the template's starter set is seed material, and `confidence: unknown` is the honest fallback the design already specifies |
| jinja2/argparse decisions prove wrong mid-build | Both are contained: one renderer callsite, one CLI module; swapping either is a single-increment change |

## Iteration log

- **Pass 1** — 2 HIGH, 5 MEDIUM, 7 LOW. H: soft-delete left chunks retrievable (I8b now removes
  chunks/embeddings, keeps the identity row); sidecar-only edits froze under §6.4's Skip row (sync
  now hashes sidecars and refreshes metadata/links without re-embedding). M: `*.pnk.yaml` excluded
  from the document walk categorically; demo-fitted thresholds no longer transplanted into the
  `notes` template (block ships commented out); backend registry made open so the fake provider can
  drive the real CLI; `max_seq_length` derivation specified per backend; `find_kb_root` walk-up
  defined once in I3. L: `pinakes_get`/`pinakes_list_kbs` return shapes; `--offline` placement;
  `--quiet` added to I8b; hooks warn-and-exit-0 when `pnk` absent from PATH; v0.1
  never-rewrites-sidecar-content policy; PyPI trusted-publisher named a human gate; serve alias =
  `[kb].name`.
- **Pass 2** — 1 HIGH, 4 MEDIUM, 3 LOW; the HIGH and three of the MEDIUMs were design-level and
  fixed in DESIGN.md (logged there as pass 7) with the plan aligned to cite it. H: fastembed
  ignores `HF_HOME` by default (`$TMPDIR/fastembed_cache`, verified upstream) — CI cache and doctor
  would probe the wrong place; backend now forces a cache dir under `HF_HOME`. M: `sidecar_hash`
  had no schema home (added to §3 `documents`; I4's "exactly per §3" is again literally true);
  rename+edit compound had no stated winner (sidecar adoption wins, §6.4); multi-hop golden
  questions were unscoreable by single-shot retrieval (I14 now scripts hop sequences); the filter
  vocabulary was inconsistent across §4.1/CLI/MCP (unified: tags, path prefix, source_type,
  created range, on both surfaces). L: snapshot field contents specified in I8a; demo KB pins
  model revisions; doctor weights-check wording tied to §4.5.
- **Pass 3** — 0 HIGH, 1 MEDIUM, 4 LOW; two self-inflicted by pass 2. M: the unified filter
  vocabulary skipped I9, the layer that implements it (created range added to the SQL WHERE
  list). L: header cited design pass 6 (now 7); calibrate invocation named; doctor's
  resolved-revision/pin-suggestion promise from the decisions table carried into I11's check
  list; I8a's "sidecar content hash" disambiguated from the field §2.2 doesn't have.
- **Pass 4** — 0 HIGH, 0 MEDIUM, 1 LOW. Completeness walk of §8 against the increments came back
  clean (every v0.1 item incl. the ships-in-v0.1 schema list maps to an increment); the one
  finding was a pass-2 run-on in I10 muddling the flag list and the output spec — split.

---

## What the build taught

Written 20260727 15:05, after all fifteen increments shipped. Sources: this plan as written, the 52
severity-tagged findings in [`../docs/RETROSPECTIVES.md`](../docs/RETROSPECTIVES.md) (7 HIGH, 28
MEDIUM, 17 LOW), and the shipped code. Everything above this line is preserved as it was reviewed;
nothing there was retconned.

### The headline

**The plan was substantially correct.** Across 15 increments and 52 findings, *not one finding
invalidated a decision from the decisions table, the increment ordering, or the scope boundary.*
The only divergence from a plan-level decision is a signature refinement: `pair()` was specified to
*return* `DuplicateIdError` among its actions and ships *raising* `DuplicateIdsError`, because the
condition is fatal for the whole run and an action every caller must remember to inspect is one a
caller eventually forgets.

What the plan got wrong is one consistent, diagnosable class: **it specified algorithms in detail
and machinery almost not at all** — test fidelity, gate mechanism, warning policy, metric
denominators, write durability. Every expensive miss below is machinery.

### Where the plan earned its keep

- **Splitting I8a (pure pairing) from I8b (I/O) was the best structural call in the plan.** I8a
  produced zero pairing-logic defects, and none surfaced downstream; all four of I8b's findings are
  in the I/O half the split isolated. It also paid a dividend the plan didn't predict: writing one
  test per §6.4 row forced two decisions the design had left implicit (a sidecar disagreeing with
  the index wins; a whole-picture rule must be order-independent).
- **The open backend registry with a `fake` provider** is what lets 3,722 lines of tests run with no
  model download while the quickstart acceptance test drives the *real* CLI.
- **Real-model canaries at I7, seven increments before calibration needed them,** caught that
  reranker scores are raw logits, not probabilities — recorded then, and decisive at I14.
- **Strict-by-default validation.** Twice the first thing a new guard caught was its own author: a
  test fixture violating a manifest invariant, and a 64-dim fake refused by the stored-vector width
  check.
- **`confidence: unknown` as the honest default.** Building it made the reason concrete — `pnk init`
  cannot know anything about a corpus that does not exist yet, so any threshold it wrote would be a
  number with no provenance.

### Where the plan was wrong, and whether planning could have caught it

| # | The gap | Catchable at plan time? |
|---|---|---|
| 1 | **The gate was a list of commands, not an artifact.** `uv run pyright \| tail -1 && git commit` reports `tail`'s exit status, so two commits landed red looking green. Not fixable by resolving to be careful — the shell was reporting success. Fixed by `check.sh` (`set -e`). | **Yes**, cheaply. Highest-leverage miss in the plan: it silently invalidated the "every increment is green and bisectable" discipline for an unknown number of commits. |
| 2 | **The filter vocabulary named a column that does not exist.** "created range" became `mtime` / `--modified-*`: a sidecar's `created` is optional, and filtering on an optional field silently excludes every document lacking it — worse than no filter. | **Yes.** Three of five review passes touched this area and checked it for *consistency across surfaces*; none checked it for *existence in the schema*. |
| 3 | **Heading text was unsearchable.** Headings were consumed as pure structure, so a word appearing only in a heading could not be found. `heading_path` looked like it covered this; it is a separate column v0.1 never searches. | **Yes**, but only by a kind of pass never run — producer (I6) and consumer (I9) are three increments apart and each reads correctly alone. Same shape as #2: **cross-layer agreement on a name, disagreement on a column.** |
| 4 | **Eval rates shipped without denominators.** `false_abstain: 0.0` and `false_confidence: 0.0` were vacuous — with no fitted thresholds, confidence was always `unknown`, so neither error could ever be counted. A CI gate on false-confidence would have passed forever, and passed loudest exactly when calibration was missing. | **Yes**, and it generalises: *every rate that gates CI ships with its denominator as a co-metric.* The plan listed five ratios and zero denominators. |
| 5 | **Overlap breached `max_tokens`.** The carried tail was prepended unconditionally, so `overlap=9, max_tokens=10` produced 12-token chunks — the silent truncation §4.6 exists to prevent, reintroduced by the feature meant to improve context. The earlier test used a comfortable ratio and passed. | **Yes**, as a test-design rule: two interacting parameters need a swept matrix, not one comfortable ratio. The plan named the invariant and the interacting feature in one sentence and never asked whether they interact. |
| 6 | **Sidecar writes were not atomic.** `write_text` truncates before writing, and the one thing a sidecar carries that cannot be recomputed is the document's permanent ULID — no later command can repair it, because nothing else knows what the id was. | **Yes**, one paragraph. The plan already used rename-based atomicity for the rebuild swap; it simply never asked the question of `docs/`. |
| 7 | **A WAL database is three files, not one.** SQLite names companions after the *path*, not the inode, so after `os.replace` the old `-wal`/`-shm` sat beside the new index claiming to be its log. | **Split, and instructive.** The plan specified exactly the right test ("no `-wal` survivor") and the test was written — then defeated by an environmental fact deducible from no document: *a read-only connection creates the companion files itself*, and the test read the index before asserting they were gone. |
| 8 | **`ty` is different from pyright, not merely weaker.** Both `pyright: ignore`s in `src/` hid real defects, and both were found by the other checker. | **Build-only** for the overlap structure; **plannable** for the rule, which this plan got backwards: it permitted suppressions "with a stated reason". The escape hatch was used exactly twice and was wrong both times. |
| 9 | **No warning policy.** `filterwarnings = ["error"]` arrived at I10 and immediately bought a Python-3.14 break (`importlib.abc.Traversable`) and three SQLite handle leaks, for one afternoon. | **Yes** — a `pyproject.toml` setting decidable with zero code. Arriving nine increments late meant leaks accumulated and were mis-attributed on discovery. |
| 10 | **No rule about what a fake may derive from.** A word-counter that called a 400-character run one token never exercised the character-cut path; a `FakeBackend(section.dim)` reported whatever the manifest claimed, making the dim-mismatch guard untestable. | **Yes**, as two ground rules. Roughly a quarter of all findings were about tests, fakes and fixtures rather than product code. |

### The named risks: what actually happened

Of the four risks this plan named, **one materialised as framed** (fake-vs-real model divergence —
and the mitigation fired all three times, though its own skip condition had a hole: `model`-marked
tests skipped when weights were absent but not when the *backend* was, so a worktree without the
extra failed instead of skipping). **One materialised one level up from where the mitigation was
aimed**: the worry was a weak template golden set, and the mitigation named the demo KB as the safe
harbour — but the demo KB's own no-answer stratum is 8 questions, which is why false-confidence
0.25 is recorded as a floor rather than a measurement. Sizing the set in aggregate (≥40) defended
the wrong location. **Two did not materialise** (I8a pairing; jinja2/argparse containment, never
exercised).

**The seven HIGH findings map to none of the four named risks.** Every one came from an area the
risk table did not contemplate: durability, WAL file naming, chunk-boundary arithmetic, cross-layer
retrievability, vacuous metrics, and the gate mechanism itself. A risk table that enumerates
*algorithmic* risk while the severe defects live in *machinery* is a risk table aimed at the wrong
half of the work.

### Two process rules that eroded silently

Worth stating plainly, because both were written down and both decayed:

1. **The gate** — see #1 above.
2. **Retrospective commits stopped being separate.** The workflow requires findings and fixes as
   their own commit, separate from the implementation. I1–I6, I8a, I8b, I9, I10 and I12 did that;
   **I7, I11, I13, I14 and I15 folded theirs into the implementation commit.** The bisectability
   argument this plan opens with depends on that separation.

### Rules for the next plan

1. **Specify the gate as an artifact:** "`./check.sh` (or `make check`) passes", and any new gate is
   added to it. Never a list of commands in prose.
2. **Add a "trace one datum end to end" pass, distinct from the consistency pass.** The two most
   expensive plannable misses were both producer/consumer name-agreement with column-disagreement,
   and five consistency passes could not see them. For v0.2: trace a word inside a PDF table from
   extraction → chunk text → FTS row → search result; and separately trace every filter dimension to
   a column that is non-null for every row.
3. **Every CI-gating rate ships with its denominator as a co-metric.**
4. **Size golden sets per stratum, not in aggregate,** and never fit and score on the same set
   without saying so up front.
5. **Put test-machinery rules in the ground rules, beside the algorithm rules:** a fake must not
   derive its answer from the input under test; a stand-in must be adversarial in the dimension it
   stands in for; no `or` in an equality assertion; a skip condition is a claim about the
   environment and must check every half of it.
6. **Set test-environment configuration in increment 1** — warnings policy, resource-leak detection,
   determinism seeds. All are plan-time decisions.
7. **Ban inline type suppressions outright.** Reverse this plan's "with a stated reason" allowance.
   When a type checker needs an assertion, the API shape is usually wrong — fix the signature.
8. **Every item in "Verification of the whole" carries an increment number and a test path.** The
   paid-API grep gate had neither and never shipped (see the stale list below); the final
   increment's completion walk checked DESIGN §8's feature list, not this plan's own promises. The
   next plan's last increment walks **both**.
9. **Split exit criteria into repo-verifiable and human-gated.** "A fresh install from PyPI
   reproduces the quickstart" cannot gate an increment when publishing is a human prerequisite —
   and indeed 0.1.0 was never tagged until two days later.
10. **Make retrospectives an input, not only an output.** The absent-vs-empty lesson was repeated by
    its own author one increment later, in the module whose entire contract is not losing what the
    user wrote. Re-read the accumulated lessons at the *start* of each increment, and promote the
    recurring ones into lint rules or `CLAUDE.md`, as the tautology lesson became ruff `SIM222`.
11. **Reuse the I8a/I8b split for any risky algorithm** — a pure core with table-driven tests, then
    the I/O layer as its own increment. With the corollary the build revealed: the pure half
    produced one finding and the I/O half four, all environmental, so **budget review effort at the
    I/O boundary, not at the algorithm.**
12. **Add a durability sweep as an explicit pass:** which writes lose unrecoverable information if
    interrupted?

### What is now stale above this line · **two of its own rows have since gone stale — I15's *PyPI trusted publishing is still unconfigured* (configured 20260728 17:15, `PUBLISH_TO_PYPI` = true; forty-nine versions now carry files, measured 20260825) and I11's *16 checks shipped* (at least 26 named checks in `doctor.py` today)**

Preserved rather than edited, so the plan still reads as reviewed. Do not trust these:

| Where | Says | Actually |
|---|---|---|
| Ground rules | `__version__` stays `0.0.0` until I15 | Version lives in the module only; Hatch reads it from there |
| Decisions table | "the bootstrap stub already uses it" | The stub was replaced in I1 |
| I7 | `--offline` on `sync`, `search`, `serve`, `doctor` | **Not on `doctor`** — it loads models unconditionally offline, so the flag would be a no-op. Defensible, but the divergence was never recorded until now |
| I8a | `DuplicateIdError` among `pair()`'s return values | Raises `DuplicateIdsError` |
| I8b | flag list | `--index-only` also shipped (the post-commit half) |
| I9 / I10 / I13 | "created range", `--created-after/-before` | `mtime`, `--modified-after/-before` |
| I11 | doctor's check list | 16 checks shipped, including four never named here: template drift, recorded failures, calibration validity, link coverage |
| I14 | eval metric list | `confidence_coverage` added — the fix for the vacuous-rate defect |
| I15 | "tag `v0.1.0`"; PyPI exit criterion | Tagged 20260727, two days after the CHANGELOG declared the release. PyPI trusted publishing is still unconfigured; the upload is gated on `PUBLISH_TO_PYPI` |
| Verification of the whole | "grep gate for `anthropic`/`openai` in `src/`" | **Never shipped.** The invariant holds structurally — core deps are jinja2, mcp, numpy, python-ulid, pyyaml — but nothing enforced it until 0.1.1 added the gate |
| Risk table | present tense | All four resolved; see above |
