# Clear the user-facing list, then decide whether to add process

**Drafted as a proposal by a read-only survey session 20260901 11:48 UTC; reviewed, revised and
adopted as a plan by the planner 20260901 11:58 UTC, against `main` at `6ae2a6c`.** § 3 is a build
order and its rows are live. **§ 4 was the user's and is ✅ ANSWERED 20260902 — option E**, so the
two rows that waited on it (row 18 outright, row 15's deferred gate in its body) are unblocked. The
build order never depended on the answer, which was itself part of the argument in § 4.

**Audience: the planner, the coder and the user. Goal: sequence work that is already decided, row one
item nobody has rowed, and put one standing decision to the user.**

**What the planner changed in review**, each verified rather than accepted:

| # | Change | Why |
|---|---|---|
| 1 | § 2's verification grep replaced | The original was a null over a selector never shown able to fire. The replacement names the population |
| 2 | § 3 row 9 keeps its rank, gains a premise correction | `init.py` **does** defend at creation; the row is about drift, and it is stronger when it survives someone opening the file |
| 3 | § 4 gains a third reading and a conflict-of-interest note | Both options assume the ratio measures process appetite. It may measure queue position |
| 4 | § 2's sizing measured, then **the measurement was itself refuted** | The planner's first correction compared battery-file lines against gate-source lines and double-counted mutants. The survey session refuted it; § 2 carries the corrected version and says so |
| 5 | § 2's verb corrected: `check.sh` **names** eleven gates and runs ten | `wheel_import_gate` needs `uv build` and a network resolve; CI's `build` job runs it |
| 6 | § 3 row 3 (cut the release) gained an explicit stop | A session reported the user holding the release; that hold is written in no document, and a tag publishes to PyPI irreversibly |

**M1 was re-run by the planner and its counts are exact** — 11 gates, 4 with a battery, 7 without —
**but its verb was not**: `check.sh` *names* eleven and runs ten (§ 2). A looser sweep of `file =` keys also returns batteries for `mcp_handshake_gate`,
`release_tag_gate`, `review_ledger`, `mutate` and `reachable_ceiling_probe` — correctly excluded,
because `check.sh` does not invoke them.

**What this file does not do.** It re-decides nothing. It opens no finding that
[`20260825_1240-run-pinakes-sweep.md`](20260825_1240-run-pinakes-sweep.md) already carries, and it
does not restate that plan's rows — after the register audit landed in `d441f48`, every open row
there carries its own dated measurement and is better evidence than a summary of it would be. **Only
§ 2 is new.** The rest is order, and one question.

---

## 0. What was measured, and when

**Each row is a measurement I ran, not a claim inherited from a register.** The register audit that
landed at `d441f48` is why this list is short: rows 4–14 of the sweep plan were re-measured at
20260901 11:35 UTC by the planner and I have not duplicated that work.

| # | Measurement | Result | How |
|---|---|---|---|
| M1 | Custom gates `check.sh` invokes, vs. gates with a mutation battery | **7 of 11 have no battery** | `grep -oE 'tools/[a-z_]+\.py' check.sh` ∩ `file =` keys across `tools/batteries/*.toml`, on `c621d08` |
| M2 | Insertions by area, `51a34bb` → `b6be317` | `src/` **928 of 16,909 — 5.5%**; `docs/`+`plans/` 7,677 | `git diff --numstat`, non-merge window since 20260824 04:07 |
| M3 | Register corpus size over the same window | `docs/` 20,110 → 22,786 (+13%); `plans/` 10,883 → 15,631 (**+44%**) | `git show <sha>:<path> \| wc -l` at both ends |
| M4 | Retro fragments ever added, judged by row 12's gate as written on its branch | 107 files, 91 judged, **18 would fail, 0 false positives** | each file read at its own adding commit via `git log --diff-filter=A` |
| M5 | Pending fragments — **pinned to a sha, because this is the one measurement that moves** | **12 at `51e7731`: 3 `changelog.d/` + 9 `retro.d/`**, of which **5 were added by the S4 increment** (1 changelog, 4 retrospectives). It read **16 at `9cc9bc1`** when this plan landed and **11 at `a0dc26d`** in the handover commit `bbdeae1`, which a fourth retrospective then falsified. **This reading was consumed by the landing that wrote it**: the 0.32.1 release cut in the same branch ran `tools/fragments.py --apply`, which splices all twelve and deletes them — so the next reading starts from **0**, and a non-zero count here means fragments have landed since. | `git ls-tree -r --name-only <sha> -- changelog.d retro.d \| grep -v README`, re-run 20260903 09:24 UTC |

**M1 is the only one that produces a work item.** M2–M4 are context for § 4 and nothing depends on
them being acted upon. **M5 is the only one that decays**, because the fragment directories change with
every landing — which is why it names a sha, and why § 3 row 3 now cites it instead of restating its
numbers. Row 3 restated them four times on this file's own landing day and every restatement
disagreed with M5 *and* with the tree: M5 said 11, row 3 said 15 twice and 10, and the tree held 16.
**A register decayed inside the plan about registers decaying, within four hours of landing.**

---

## 1. Why this plan is about order and not about findings

**The product is finished against every plan it has; the queue that keeps it correct is what
stopped.** Every named release has shipped, failed its own measurement honestly and ships off, or
waits behind a written trigger. There is no feature backlog by construction — that is a
finished-product signature, not a stall.

Against that: **the published 0.31.1 carries eight reproduced user-facing defects, found by running
Pinakes on 20260825, decided the same day, and still unbuilt.** They are rows 4, 5, 6, 9 and 10 of
the sweep plan's build order, each re-measured 20260901 11:35 UTC. Two are the sharp kind — S1
indexes *nothing* when one file is unreadable, S4 reports a bricked KB as created with exit 0.

**So the work is already decided, already owned and already rowed. What it lacks is a position in
front of everything else.** That is all § 3 supplies.

---

## 2. ENG-1 — seven of eleven gates have never been mutation-tested

**Recorded in no plan and no queue, which is why it is here rather than in the sweep plan's table.**
I measured it because a survey raised it (M1), and it is the highest-leverage unrowed item in the
repository.

`check.sh` names eleven custom gates and **runs ten of them**: `wheel_import_gate.py` is named at
`check.sh:57-65` and deliberately not run there, because it needs `uv build` and a network resolve, so
CI's `build` job and `release.yml` run it instead. Four of the eleven have a mutation battery. Seven
do not, and the count is unaffected by where the eleventh runs:

| Gate | Battery? | What it enforces |
|---|---|---|
| `paid_path_gate.py` | **none** | the two-entry paid-path allowlist — **the only invariant that costs real money** |
| `traversal_cap_gate.py` | **none** | the link-traversal bound |
| `link_density_gate.py` | **none** | the link-density bound |
| `eval_reproducibility_gate.py` | **none** | that a published eval number can be re-derived |
| `shared_file_overlap.py` | **none** | the pre-merge overlap check every landing is told to run |
| `template_drift_gate.py` | **none** | that the stamped template matches the shipped one |
| `wheel_import_gate.py` | **none** | that the built wheel imports without an extra |
| `fragments.py`, `markdown_link_gate.py`, `release_order_gate.py`, `status_header_gate.py` | ✅ | — |

**This repository's own rule is that a gate is only evidence once something has shown it can kill**
— *"a mutation pass is evidence only if the harness is shown to kill"*, and *"a run with no kills is
a broken harness, not a clean bill"*. By that rule seven of these eleven are unverified until
somebody checks, and nobody has checked.

**A correction to my own framing, because it changes what "do `paid_path_gate` first" means.**
[`docs/INVARIANTS.md:24`](../docs/INVARIANTS.md) says **four** gates enforce the paid path, and that
*"the one that matters runs the whole free path in a fresh subprocess and asserts no paid client
reached `sys.modules`"* — that is `tests/test_paid_path.py` with `tests/free_path_run.py`, not
`tools/paid_path_gate.py`. So the grep gate is not the strongest enforcement and should not be
described as though it were.

**It does not weaken the item; it widens it.** No battery names *any* paid-path file — not the
gate, and not the subprocess test that INVARIANTS calls the one that matters. **The strongest
assertion protecting the money invariant has never been shown able to fail.** Do that one first,
then the gate. **Verify it with a selector that names its population**, not with a grep that returns nothing:

    grep -rln 'PINAKES_ANTHROPIC_API_KEY' src/
      -> src/pinakes/paid.py, src/pinakes/errors.py, src/pinakes/deep/loop.py

Those three files are the paid path. None is a `file =` key in any battery. Stated that way a future
zero carries information; stated as `... | grep -i paid` it cannot, because nothing shows the
selector could ever have fired.

**Scope discipline, because this is the exact shape § 4 wants to freeze.** This is not new process.
It adds no gate, no rule and no register — it asks whether seven gates that already exist do
anything. **Batteries are appended to the target's existing battery file, never started as a second
file** ([`tools/batteries/README.md`](../tools/batteries/README.md)), and
`tests/test_batteries.py` fails if that is got wrong.

**Sizing, measured — and the planner's first version of this paragraph was wrong.** It claimed
`template_drift_gate` at 684 lines was beyond anything ever batteried here, and the survey session
refuted it with one command: **`tools/release_order_gate.py` is 797 lines and carries a full battery**
(458 lines, 45 mutants). 684 is *below* the existing precedent, not beyond it. The planner's mutant
counts were also doubled by an anchor matching both `[[mutant]]` and `name = ` in the same block —
batteries run **128-458 lines and 8-45 mutants** (median 179 and 15; 211 mutants across 11 batteries),
and nothing here has 90.

So the conclusion inverts: **do not split `template_drift_gate` — size it against `release_order_gate`,
the closest and larger precedent.** Roughly 400-450 battery lines, 40-odd mutants, about two sessions.
The other six sit between `status_header_gate` (268 source → 144, 12 mutants) and `fragments.py` (598
→ 207, 20), so one session each holds for them. The original "roughly one session each" was still
wrong for the seventh; it was corrected for a reason that did not survive checking, and the corrected
number stands on the precedent instead.

**The honest expectation is that most of these seven gates kill their mutants, and that the exercise
returns confirmation rather than defects. That expectation is the reason to do it, not a reason to
skip it** — if a gate does not kill, everything it has ever certified is unsupported, including
past releases.

---

## 3. Proposed build order

**Most rows here are coder work — but rows 21 and 22 are planner-owned, and three rows contain a
choice.** Rows **21** (the dogfooding on-ramp's design) and **22** (regenerating `CLAUDE.md`) are
the planner's, so a coder skips them. Row 9 must decide where two `doctor` helpers live; row 23
carries three decisions, including whether `pnk upgrade` fails or degrades; row 25 carries three
more, including where a plain-text guard belongs. **A *Blocked on:
nothing* cell speaks only about external blockers**, so read a row's body before taking it as
decided. Rows 1–6 are the sweep plan's own rows and the *Sweep row* column names their number there
rather than restating them; a row reading **— new** in that column is work this table introduced and
owns. The sweep plan remains the owner of every row it already carries.

*(This paragraph read "**All of this is coder work and none of it needs a decision**" until
20260902 11:48. It was written when the table stopped at row 12 and was never revisited as rows
were appended — the same failure the four stale "Blocked on" cells were, one level up: **a summary
sentence above a table nobody re-reads when the table grows**. **It then failed the same way again
within the day.** That 11:48 rewrite still opened *every row here is coder work*, and rows 21 and 22
— both planner-owned — were appended hours later on the user's own instructions; corrected again
20260903 12:16. `docs/README.md` said *"26 rows as of 20260903, and no longer all coder"* and this
file did not, which is the direction to expect: **the routing table is re-read at every landing and
the summary sentence is re-read at none.**)*

| # | Item | Sweep row | Blocked on | Why here |
|---|---|---|---|---|
| 1 | **✅ BUILT — landed `293d434`, 20260902 09:04 UTC** · Row 14 — teach `markdown_link_gate` the splice destination, both arms; restore the code-spanned links — **four of them, not two** | 14 | — | A correct link **was** degraded on `main` and both fragment READMEs **carried** a caveat contradicting their own instruction — live incoherence in the tree, which is why this was row 1 rather than row 10. **And by 20260902 08:11 it was no longer only a *degraded* link.** Two landed fragments used the **forbidden** filename form — `retro.d/20260902_0734-the-surface-a-later-pass-cannot-fix.md` and `retro.d/20260902_0741-the-census-i-called-correct-was-the-incoherent-one.md`, both naming `20260902_0245-a-method-is-not-a-measurement-point.md` — which `retro.d/README.md` forbids because the path resolves against `docs/` once everything is spliced into one document. **Measured, not argued:** splice, then `make docs` exits **2**, *"Aborted with 2 warnings in strict mode"*, each warning naming that link — while `./check.sh` stays green throughout (`markdown_link_gate`: 382 links, **none broken**), because inside `retro.d/` the relative path hits a real sibling and only becomes wrong at splice. That is the 0.12.0 failure `retro.d/README.md` already records, recurring twice in one night. **Both are fixed at `b4fa8e8`**, rewritten to the code-span form (anchor corrected in `8d71489`) and re-verified — splice **both** streams, then `make docs` **exit 0**, over the **35** fragments present at `8d71489`: 24 in `retro.d/`, 11 in `changelog.d/` — so the release build is unblocked. **This cell first said *25 fragments*, and that number named no population**: it is the number of `retro.d/*.md` paths, which counts `README.md` as a fragment and omits the changelog stream that `--apply` also splices — one non-fragment in, eleven fragments out, in the sentence whose only job was to say how wide the check ran. The run behind it was `--stream retrospectives` alone. Caught by the coder; the numbers above are from a both-streams run repeated here rather than taken on report — **but nothing gates the class.** Whatever row 14 teaches the gate must cover the **filename** case, not only the `#…` anchor case; the anchor case is the one that is merely inconvenient. **The item cell used to say *the two* code-spanned links; measured at `06d8a91` there are four**, all in `retro.d/` — `20260901_0713` line 102 pointing at `20260901_0633`'s heading, and `20260902_0734`, `20260902_0741` and `20260902_0830` all pointing at `20260902_0245`'s. Three of the four were written *after* this row was — two by the fix above, one by the coder's fragment — so the population this row restores grew while the row sat still, and a row that names a count ages differently from one that names a rule. **Control, so the number carries information:** the live-link selector returns **0** across `retro.d/` and `changelog.d/` and **2** inside `docs/RETROSPECTIVES.md`, so it fires, and every cross-fragment reference really is in the degraded form. **Re-measure before restoring; do not restore four because this cell says four.** **Landed `293d434`:** the gate now resolves a fragment's relative links against its *splice destination* rather than its own directory — both arms, so the filename case is gated and not only the anchor case — and all four links are live again. Verified against `origin/main` with `ls-remote`, not against a local ref. |
| 2 | **✅ BUILT — landed `5654cc4`, 20260902 07:55 UTC** · Row 12 — the retro heading + stamp gate | 12 | — | **M4 is the evidence it earns its place:** 18 of 91 historical fragments would fail it and none is a false positive. **This cell said *"finished and pushed on its branch; landing it is cheaper than carrying it"* for the 74 minutes after it landed** — a queue row describing a state the tree had already left, written by the session that watched it land. Nothing here gates that, and the two sessions reading this file in that window both had to be told the row was wrong rather than reading it. |
| 3 | **Cut the release** — ✅ **DONE 20260902 10:07** | — | **nothing — the go-ahead was given and the release is on PyPI.** This column read *an explicit user go-ahead, and nothing else* until 20260902 11:12, one commit after the title was marked done: a reader scanning blockers rather than titles would still have seen a live gate. Rows 1–2 landed 20260902, `5654cc4` and `293d434` | **✅ CUT AND PUBLISHED AS 0.32.0, 20260902 10:07 UTC**, on an explicit user go-ahead (*no hold — cut it*). Verified on the index three ways — installed from PyPI, the `simple/` file listing, and the wheel's own contents — rather than from the release workflow's status, which has twice reported success while shipping nothing. **Everything after this sentence describes the state BEFORE that cut.** It is kept as the record of why the release was held, and it no longer describes the tree: `__version__` is `0.32.0`, not `0.31.1`. **Do not read it as a live instruction to ask again.** **What is pending now is different work and must not be confused with it.** `changelog.d/` holds 2 fragments and `retro.d/` 4, all stamped 20260902, and **no release is due for them**: `git diff v0.32.0..origin/main -- src/ pyproject.toml` is **empty**, so every change since the tag is in `tests/`, `tools/`, `docs/`, `plans/` and fragments. Cutting would publish a package byte-identical to 0.32.0 under a new number, and PyPI never accepts a version twice — the one irreversible act in this file. They ride the next release that touches shipped code. **A non-empty `changelog.d/` is therefore not, by itself, evidence that a release is owed** — run that selector first. **Both paths, not just `src/`**: `pyproject.toml` ships too, and a dependency bump, a new or renamed extra, a changed entry point or a raised `requires-python` each change what a user installs while leaving `src/` untouched — every one of them a PATCH or MINOR under the SemVer table. Raised by the coder against the `src/`-only form before it landed; both selectors read 0 today, so the amendment is for next time. **Do not narrow it to `src/pinakes/*.py`** — `src/pinakes/templates/**` is data that travels inside the wheel, because `pnk init` reads it through `importlib.resources` rather than off the source tree. Verified with a control: 16 files changed since the tag in total, so a selector reading 0 is a measurement rather than a broken probe. Planner's call, taken 20260902 11:05; say so if you want it cut anyway. — The fragments **M5** counted were pending while `[Unreleased]` reads empty, which is itself misleading. **It is a MINOR, not a patch — measured at `6ae2a6c` 20260901 12:05 UTC.** (The number is deliberately not written here: *a version number belongs to a release when it is cut, never before*, and `docs/RELEASING.md` names none either.) Of M5's five `changelog.d/` fragments, two are **Added** (`agent-spend-measures-what-the-agents-cost`, `the-ceiling-probe-takes-the-golden-set-as-an-argument`) and three are **Fixed**, so the SemVer table gives MINOR. **The counts live in M5 and are deliberately not restated here** — an earlier version of this cell restated them four times and disagreed with M5 and with the tree every time. `__version__` is `0.31.1` and `## [Unreleased]` is **empty**, which makes the document say nothing is pending while the tree holds every fragment M5 counts — the exact trap `docs/RELEASING.md` exists for. **Do not cut this without asking.** A session on 20260901 recorded the release as held by the user (*"still uncut, as you set it"*), and that hold appears in no document in this repository — so it cannot be verified from the tree and must not be inferred away by this row. A tag publishes to PyPI and PyPI never accepts a version twice: this is the only row here that cannot be undone. **State plainly in the release notes that this release reaches no user of `pnk`** — the rename crash people would feel is already out in 0.31.1; this is two dev tools, a docs-audit closure and a gate unblock. **The cost of holding, stated because it points the other way:** M5's nine `20260901_`-stamped `retro.d/` fragments are records of that day's own wrong diagnoses — five instances of one failure across three sessions — and they reach `docs/RETROSPECTIVES.md` only at a cut. Every day held is a day the published retrospectives omit the day that produced the most of them. That argues for cutting; it does not argue for cutting without asking. Planner-owned. |
| 4 | **S1** — `PermissionError` aborts the whole walk — ✅ **BUILT 20260902 09:33** | 4 | nothing — landed | **✅ BUILT in six commits, 20260902 09:18–09:33 UTC**: `3f6a3ed` (the fix), `a4ec0f0` (eleven tests, five of them controls), `4926395` (ten mutation rows and a rotted anchor repaired), `512e068` (the same unguarded read in `pnk doctor`, which is where the remedy sends the user), `6e593ef` (fragments and a rebuild case the review turned up), `665ba31`. Verified four ways rather than by reading a marker: `sync.py:684-692` catches `OSError` around the **hash** and not the walk; all six shas are ancestors of `origin/main`; `docs/VERIFICATION.md:1361` carries a 13-row section; `pytest -k "unreadable or held"` → **25 passed**. **This row read `✓ measured still open, 20260901 11:35 UTC` until 20260902 11:30** — true when taken, false 22 hours later, and the planner handed the row to a coder as top priority on the strength of it. **A measurement is a fact about the moment it was taken; the stamp records when it stopped being current, not that it still is** — and the stronger it reads, the less anyone re-runs it. Caught by the coder. The worst failure mode in the list: one unreadable file and **nothing is indexed at all**. |
| 5 | **S4** — escape at render in `template.py` — ✅ **BUILT AND LANDED** | 5 | nothing — landed | **✅ Landed `51e7731`, 20260903, from branch `20260902_1129-s4-escape-at-render`; the branch is removed.** The fix is `finalize=` on the Jinja `Template` in `src/pinakes/template.py`: every interpolated value is escaped for the TOML basic string it lands in, **or refused** where TOML has no representation for it at all. **This cell understated the figures twice — nine, then 33 — so these are the ones that shipped, and they are the last word only because the branch is gone**: **37 tests** (33 red with the hook neutralised, 4 named controls green, re-derived rather than counted), **10 mutants 10/10 killed**, **14 `docs/VERIFICATION.md` rows**, and a fifteenth row in `src-pinakes-init.toml` (**15/15**). **Every kill's *reason* was read against its row's own name** — which is what the third pass turned out to be about: three rows were dying on the wrong exception, so a green battery certified nothing they claimed. Three adversarial passes, and **each one moved these numbers**: pass 1 half-ran (5 of 14 agents died on a session limit); **pass 2 found that S4's fix reproduced S4** — a lone surrogate has no TOML form raw or escaped, and `surrogateescape` mints one from any invalid byte in `argv`; pass 3 first returned a verdict from a vacuous run, and its re-run found four more, including **four escape entries shadowed by the `\uXXXX` fallback** and therefore invisible to every parse and round-trip test in the file. **Three things it learned are carried by other rows now**: the surrogate case is the class row 23 exists for; escaping for TOML discharges nothing a terminal owes, which is row 25; and a commit taken inside a mutation window shipped a live mutant to `origin`, which is row 26. **Rows 25 and 26 were blocked on this landing** — row 25 is unblocked, and row 26 was written in the same branch that flipped this cell. Shipped in **0.32.1**. The defect: silent, permanent, and `init` refuses to repair it — exit 0 on a bricked KB. |
| 6 | **D-37's build** — ⛔ **MERGED INTO ROW 7 (20260902 11:30): it is S6** | 9 | — do not take this row; take row 7 | **⛔ NOT A SEPARATE ROW. D-37's build *is* S6, and S6 is one of the five in row 7** — so two coders taking rows 6 and 7 in parallel collide on the same lines, and a coder taking 7 alone either duplicates this row or silently absorbs it. **The owning plan never split them**: `plans/20260825_1240-run-pinakes-sweep.md:139` defines **S6** as the *"moved without its sidecar, so a new id was minted"* hint firing on ordinary deletion; its § at line 498 is headed *D-37 — what should S6's state mean?*; and its own build-order row reads *"S5–S9 … D-37 is what was stopping it, **at S6**, and D-37 is answered"*. **This table split what the sweep plan had unified, and neither row cross-referenced the other.** The two sites are the same in both readings: `sync.py:406-407` and `pairing.py:499-503`. Scoping defect found by the coder's row-by-row verification, not by a builder hitting the collision — which is where it would otherwise have surfaced. Row 7's own text already tells its builder to decide *one fix or five*, so S6 belongs inside that decision rather than beside it. Fires on **every ordinary deletion, on every `pnk sync`**, claiming an id was minted when none was. Highest frequency of anything in the list. |
| 7 | **S5–S9** — the accept-then-mishandle batch, **including S6 (D-37's build), merged in from row 6** — ✅ **BUILT AND LANDED** | 6 | nothing — landed | **✅ Landed `79e5a88`, 20260903 11:53 UTC**, from branch `20260903_0959-s5-s9-accept-then-mishandle`; the branch and its worktree are removed. Eight commits: `e38539d` (S8 **and** S9 together), `f111c7c` (S5), `dfa23de` (S6), `85ac2d8` (S7), then `8a728f3`, `146a7fa`, `c493c03` and `c8596fd` for the fragments, the battery section and the review passes. **This row asked *one fix or five*; the answer is neither — four.** S8 and S9 are **one** missing check and it belongs at the boundary, which is `e38539d`'s own subject line: the row's question answered in the commit, as the row instructed. S6 was built as D-37 **option E**, gating the move hint on the orphaned sidecar, at the sites this cell already named — `sync.py:406-407`, `pairing.py:499-503`. **It left nine fragments and an uncut 0.32.2**: four in `changelog.d/`, five in `retro.d/`. **This cell read as unbuilt for 23 minutes after the merge, and the planner who said it would flip it is the one who did not.** That is the third instance in this table of a queue row describing a state the tree had already left — rows 2 and 3 record the other two — and the worst of the three, because the documented way to pick work here is *the first unblocked row with no **BUILT** marker*, so it pointed a cleared coder straight at shipped code in `sync.py` and `pairing.py`. **A stale row and an unbuilt row are byte-identical to a cleared session**, which is why no reader caught it: it was found by a fan-out over the handover, not by either session that had just watched it land. |
| 8 | The four Low classes — **three of them; `-k 0` shipped inside row 7** — ✅ **BUILT AND LANDED** | 10 | nothing — landed | **✅ Landed `058df49`, 20260903 13:09:22 UTC**, from branch `20260903_1057-row8-the-low-classes`, in eight commits including three review passes. Three `changelog.d/` and four `retro.d/` fragments arrived with it and are **not** in 0.32.2 — they wait for the next cut. **The three fixes**: a mistyped `--source-type` is a usage error rather than a silent empty result; an empty KB stops attributing its emptiness to filters nobody passed; and a broken symlink is reported rather than skipped. **`-k 0` shipped earlier, inside row 7** (`e38539d`). **The 🛑 on this cell was real and is now discharged by reading the code, not the report (20260903 13:13):** the cell said the fix catches *any symlink `is_file()` rejects*, which would have reported a healthy alias to a real directory as a broken link. The shipped predicate is `candidate.is_symlink() and not candidate.exists()` — `src/pinakes/sync.py:727`, read at `058df49` — so it fires on dangling and looping targets and leaves directory aliases alone. **The narrow half stands and is now row 28**: `pnk sync` reports, `pnk doctor` still does not, because `doctor.py:514` walks only once a row has been retired. That is a design question and the coder correctly declined to decide it |
| 9 | **The `.pinakes/` questions in `doctor`** (D-31/D-32/D-33, option C, unconditional) | exposure 5–6 | nothing | **⚠ A design choice sits INSIDE this row, and "blocked on: nothing" is true only of external blockers.** Whoever builds it must pick the helper's home — import `init.py`'s privates into `doctor.py`, or extract a shared module and disturb ~1100 lines of `init.py` tests. `plans/20260825_1803-open-decisions.md:282` (option **C**) flags it. Verified 20260902: `_tracked_by_git` and `_ignored_by_git` live in `init.py` (lines 122, 177) and are called **only** from `init()` (lines 394, 406) — no caller in `doctor.py`, so the row is genuinely open and the choice is genuinely unmade. Noted here so it is not discovered at build time. Raised by the coder. **Ranks with S1 and S4 on severity, not below them:** since the deep release the index holds users' verbatim questions, only `init` ever asks whether `.pinakes/` is committed, and it asks once. A KB that starts leaking *after* init is never warned while `doctor` reports clean. **Checked before ranking it:** `init.py:122-156` does defend at creation — it writes a `.gitignore` and asks git via `check-ignore` rather than text-scanning, the scan it replaced having been measured wrong in both directions on 20260825. So the exposed population is KBs whose ignore state **drifted after init**, not every KB. That is a lower frequency than S1 or S4 and a worse, irreversible consequence — keep it level with them, not above. **And that defence is itself batteried** (`tools/batteries/src-pinakes-init.toml`, 14 mutants, one of them targeting the `check-ignore` diagnostic), so row 9 is narrower *and* better defended than the draft said — which is the version worth landing, because it is the one that survives being checked |
| 10 | **ENG-1 · `paid_path_gate` battery first**, then the remaining six | — new | nothing | § 2. Split per gate; each is its own commit. |
| 11 | Row 11 — the FX guard, two parts | 11 | nothing | Already scoped, including the sentence the commit must carry. |
| 12 | Row 13 — the vacuous verifier | 13 | nothing | Small; pin the empty-input case as non-zero. |
| 13 | **The BOM arm covers only one stream** — widen it to `changelog.d/`, and nothing else | — new | nothing — row 12 landed | **UNBLOCKED 20260902 11:30.** This cell read `row 12`; that work landed in `5654cc4` at **07:55 UTC**, so the row sat behind a blocker that had cleared hours earlier. One of **four** rows carrying the same wrong cell, all found in one pass. Raised by the coder 20260901, while reviewing row 12, and **outside row 12 as scoped** — recorded, not widened into it. Row 12's BOM check returns early on `stream.name != "retrospectives"`, so a changelog fragment saved with a byte-order mark still splices the mark into `CHANGELOG.md` with no gate seeing it. **The consequence is not cosmetic, which is why this is a row and not a note:** the mark precedes the leading `- `, so `'\ufeff- **claim**'.startswith('- ')` is `False` and the entry's first bullet renders as a **paragraph** — the same failure mode as one leading space in a retro fragment, in the other stream. **Take it right after row 14** while `fragments.py` is open, or later; it blocks nothing and the release does not wait on it. Its own commit and its own review pass — do not fold it into row 12, which is already under review. |
| 14 | **A retro fragment's *second* `## ` heading is spliced unvalidated** — require the form, not the value | — new | nothing — row 12 landed | **UNBLOCKED 20260902 11:30.** This cell read `row 12`; that work landed in `5654cc4` at **07:55 UTC**, so the row sat behind a blocker that had cleared hours earlier. One of **four** rows carrying the same wrong cell, all found in one pass. Found by the coder's row-12 review pass 7 and **deliberately not fixed there**: not a regression, and the obvious rule would have refused a real released fragment. **Corrected 20260902 00:05 — this row first read *116 fragments, exactly one*, and both numbers were wrong.** Re-measured from the object store (`git rev-list <rev> --objects`, `cat-file` on every blob) rather than from the log, because a log-keyed probe reads a consumed fragment at its *deletion* commit and skips it silently: **126 fragment paths, 233 distinct versions, `unreadable: 0`**, and **two** carry two column-0 `## ` headings. **CORRECTED AGAIN 20260902 02:44 — the first correction named a method but no measurement point.** It said `--all`, which is not a corpus: it is every ref any branch happens to hold at the instant it runs, so it counted unlanded work and had drifted to 132 paths / 243 versions within the hour. **The selector is now pinned — `origin/main` at `7751f96`, landed history only** — and both of this row's denominators are the same 126, where they were 129 and 126 from two different populations. The qualitative result is unchanged: exactly **two**, the same two files. **Only one of the two is evidence.** `retro.d/20260805_1907-a-guard-that-could-not-fire.md` stamps its second heading `19:15` against a filename prefix of `1907`, **on purpose**, recording a second moment inside one incident. `retro.d/20260823_0233-a-gate-can-be-green-on-the-list-next-door.md` stamps **neither** heading — and it is *not* pre-convention, which was the obvious way to set it aside and is false: **10 of 126 fragments carry an unstamped first heading, scattered across the whole timeline to 20260831**, so stamping never became universal and there is no cohort boundary to put it behind. It is an instance of the ~8% non-compliance row 12's gate exists to stop, not a deliberate choice about second headings, so it licenses nothing here. **n=1 stands.** **Ruled 20260901, planner:** a second heading must carry a parenthesised `YYYYMMDD HH:MM` stamp ending the heading, in the same spelling as the first; it **need not equal the filename prefix** and **nothing constrains its value**. One instance licenses the *existence* of a later stamp and does not license a monotonicity rule, so none is written. **The 116-and-one measurement was the planner's, taken over the live directory and stated as a fact about the corpus** — the same defect this plan rows against others — and was caught by the coder, who found the second fragment. `retro.d/README.md` gains the sentence at the same time — planner-owned, dictated, not drafted. |
| 15 | `tools/batteries/tools-markdown_link_gate.toml:26` — a bare date where the format reserves a version | — new | nothing | One comment, one line: `# 20260823 · …` conforms to neither form `tools/batteries/README.md` reserves. **✅ BUILT 20260902.** The header fix had already landed in `2914b09`; what this row still owed was the gate, now `tests/test_batteries.py::test_every_section_header_names_the_release_it_belongs_to`. **CORRECTED 20260902 — this row's audit was wrong about its own subject, and its arithmetic said so before anyone re-measured.** It read *30 sections, 19 `unreleased, YYYYMMDD ·`, 8 `X.Y.Z ·`, this the only one matching neither* — and 19 + 8 + 1 is **28**, two short of its own stated total. **Measured at `a4077b5`, the commit that wrote the row** (selector: every comment line *fenced between two rule lines*, rules matched as box-drawing `─` **or** ASCII `-`, read from raw bytes because `tomllib` drops comments): **30 sections — 20 unreleased, 8 version, and TWO matching neither.** The total and the version count were right; *19* and *the only one* were not, and **one header explains both**: `src-pinakes-pairing.toml`'s `# unreleased, 20260831 - S16's residue: …` (written 20260831 in `244f0b2`) has a hyphen where the `·` belongs, and the audit searched for headers *containing* `·`. **A selector defined by the correct form cannot find a violation consisting of that form's absence** — so the invisible header dropped out of exactly two tallies, which is exactly the two the row was short. **On today's tree (`e1a323d`) the same selector gives 36 sections — 26 unreleased, 9 version, 1 matching neither**, the one this row missed; the one it named was fixed in `2914b09`. **The tree grew from 30 to 36; the row's 30 was not stale arithmetic but a correct measurement of a smaller tree** — a first attempt at this correction tabulated today's 36 against the row's 30 and called the total wrong, which turns growth into error and hides the real defect. **A count correction names the commit it was measured at, not only the selector.** **The correct header is `0.30.0 · `, not `unreleased, 20260823 · `** — `tools/markdown_link_gate.py` was added in `6d7c9e3` (20260823 15:44) and **shipped**: `v0.30.0` is the earliest tag containing it and `v0.29.0` does not, so the selector discriminates. The planner instructed `unreleased` here and was **wrong**; writing it would have replaced a bare-date non-conformance with a false claim, in the one file whose job is recording which release shipped which property. Caught by the coder, re-verified independently before the correction was written. **The bare date is both spellings' raw material** — it reads as unreleased-ish precisely because the unreleased form is the one carrying a date, and the version form carries none. **The gate that would catch it was deliberately refused while § 4 was open** — nothing gates the section format and adding a check *is* new process. **§ 4 came back E, which is B plus a trigger, so the refusal is lifted: propose the gate.** Recorded as a refusal rather than an oversight, and now as a refusal that expired. |
| 16 | **`tools/markdown_link_gate.py` and the renderer disagree about what a heading is** — four shapes of five | — new | nothing | `_HEADING = re.compile(r"^\s{0,3}(#{1,6})\s+(.*?)\s*#*\s*$")` implements CommonMark's up-to-three-spaces rule. Python-Markdown 3.10.3 — **the renderer that builds the site**, measured 20260902 via `uv run --no-project --with-requirements requirements-docs.txt` — refuses even one. Only `'## x'` agrees. For `' ## x'`, `'  ## x'` and `'\t## x'` the gate **registers an anchor the site never renders** (paragraph, paragraph, code block), so a link to one **passes the gate** and resolves to nothing; for `'##x'` the reverse — a real H2 the gate cannot see, so nothing may link to it. **Scope is the whole point:** inside `docs/`, `mkdocs build --strict` catches it, but the gate's own docstring says it exists for *"the Markdown the docs site never sees"* — `CLAUDE.md`, `plans/`, `changelog.d/`, `retro.d/` — and **there nothing catches it at all**. Row 12's `^## \S` closes it for retro fragments only, one directory of four. Found while ratifying the row-12 branch's `retro.d/README.md` text, whose own (correct) claim that `'##x'` is invisible to the gate is the half of the divergence that was already known; the other three shapes were not. **Not folded into row 12** — different tool, and that branch was finished. |
| 17 | **One character in a filename silently exempts a fragment from the stamp gate** — narrow the exemption | — new | nothing — row 12 landed | **UNBLOCKED 20260902 11:30.** This cell read `row 12`; that work landed in `5654cc4` at **07:55 UTC**, so the row sat behind a blocker that had cleared hours earlier. One of **four** rows carrying the same wrong cell, all found in one pass. Found by the coder's row-12 pass 8. The stamp arm exempts a fragment when `_STAMP = re.compile(r"^\d{8}_\d{4}-")` fails to match the stem, so `20260901-0710-x.md` — hyphen for the underscore — is **exempted entirely**, reproduced with a control (`20260901_0710-x.md` refused, exit 1; the typo accepted, exit 0). **CORRECTED 20260902 02:04 — the first ruling's basis was false.** It read: *"`changelog.d` already refuses both that shape and a prefix-less one, so `retro.d` is the stream that omits the check."* `changelog.d` has **no prefix gate**; it has a **category** gate, and the decisive case separates them: `fixed-a-thing.md` — no prefix at all, head *is* a category — is **accepted**, while `banana-a-thing.md` is refused. A malformed prefix is refused there only *incidentally*, by shifting the first token out of `(added, changed, deprecated, removed, fixed, security)`. **Nothing in this repo gates a fragment's prefix format.** The planner tested three names, read the category arm's error text — which names the whole convention — as evidence of a prefix arm, and never tested the one case separating the hypotheses. Found by the coder. **§ 4 still does not hold it, on a basis that does not depend on the sibling:** row 12's decided property is *the stamp is the filename's*, a **copy relation**, and an exemption one substituted character triggers means the gate does not check that relation. Closing it implements row 12's decided scope rather than adding scope; § 4 restricts inventing rules, not shipping a decided gate with a known bypass. Row 15's refusal is still not a precedent — that gated battery section format, where there was no defect. **RE-RULED 20260902 02:04, planner:** exempt only a stem that **does not begin with a digit at all**; a stem beginning with a digit is claiming to be dated and must match `^\d{8}_\d{4}-` exactly or be refused naming the prefix. The first ruling said *8 digits*, which the coder improved on evidence. Licensed by measurement over every fragment path that ever existed — `retro.d` 126 total / 17 unprefixed, `changelog.d` 177 / 35, and **0 of those 52 begin with 8 digits**; all are pre-convention names (`l5b-…`, `added-…`). So it breaks nothing in 303 fragments and preserves the prefix-less acceptance `body_of`'s docstring records as deliberate. **The *8 digits* form left a residual and the residual named for it was the wrong string.** `2026090_0710-x` is **already refused today** by `_SLUG` (`^[a-z0-9]+(?:-[a-z0-9]+)*$`), because a failed `_STAMP` strip leaves the underscore in the stem: any malformed prefix keeping its underscore is caught already, and the typo escapes precisely *because* a hyphen removes it. The real residual was the all-hyphen shapes — `2026090-0710-x`, `202-0710-x` — and **the no-leading-digit rule leaves none**. Re-measured over all 303 paths: of the 52 with no canonical prefix, **0 begin with 8 digits and 0 begin with any digit**, control 251 of 303 stems do begin with one. **Its one real cost, chosen rather than discovered:** a name like `5-lessons.md` becomes refused — correct, since every new fragment owes a prefix, and 0 historical files are affected. Pre-existing either way: Python's `\d` matches Unicode decimal digits. |
| 18 | **No fragment gate validates that a filename's date is a date** — was deferred behind § 4, now **UNBLOCKED**: § 4 answered E, 20260902 | — new | nothing | Raised by the coder against row 17: `20261345_9999-x.md` begins with 8 digits and matches `^\d{8}_\d{4}-`, so under row 17's ruling it would demand its heading carry `(20261345 99:99)` — and would pass if it did. **Measured before ruling, four names through `changelog.d`'s format gate:** `20260901_0710` exit 0, `20261345_9999` (month 13, day 45, 99:99) exit 0, `20260230_2500` (Feb 30, 25:00) exit 0, `99999999_9999` exit 0. **Shape only; neither stream validates a date.** So row 17 leaves the two halves consistent rather than disagreeing, and the absurd case is acceptable: the gate's subject is *the stamp is the filename's*, a copy check, not a calendar check — a wrong-but-possible date (`20260901_0710` written on the 2nd) is equally uncaught, and impossibility is that same class made visible. **Was deferred, and the new-process argument did hold — but § 4 answered E on 20260902, so it holds nothing now: propose the calendar check on its merits.** **CORRECTED 20260902 02:04:** this row first justified the split by *"row 17 was exempt because the sibling stream already had the check"* — and that sibling claim was false (see row 17). **The discriminator that survives is a different one: is the check the *decided property*, or a *different* property?** Row 12's decided property is *the stamp is the filename's*, a copy relation, so closing the exemption implements it. Calendar validity is a **different property** — nothing about a copy relation — so it is genuinely new, and § 4 holds it. Same treatment as row 15. Two questions that look identical get opposite answers on that, not on whether the sibling does it and not on whether the change is small. Propose again if § 4 returns B or *neither*. |
| 19 | **An unclosed raw-HTML block in a fragment absorbs every heading after it** — widen the checker beyond headings | — new | nothing — row 12 landed | **UNBLOCKED 20260902 11:30.** This cell read `row 12`; that work landed in `5654cc4` at **07:55 UTC**, so the row sat behind a blocker that had cleared hours earlier. One of **four** rows carrying the same wrong cell, all found in one pass. Found by the coder's row-12 pass 9, and **outside row 12 as scoped**. A fragment carrying an unclosed `<div class="note">` passes every arm: `fragments.py --check` returns **exit 0, *"all well-formed"***, and `--apply` splices it. **Re-measured independently by the planner 20260902 07:50 under `mkdocs.yml`'s own extension list** (`admonition, attr_list, md_in_html, tables, footnotes, pymdownx.*`): two source `## ` headings render as **one** `<h2>`, the following fragment's heading and prose becoming literal text inside the div. **`md_in_html` does not rescue it** — the count is identical under Python-Markdown defaults, so this is not an extension misconfiguration. **The absorbing fragment is itself perfectly well-formed**, which is why no heading arm can ever see it: the damage is to the *next* fragment's rendering, not its own, and row 12's `^## \S` is the wrong shape of check for it. The coder further measured that the protected `## Design review passes` footer is absorbed and that `anchors_of` still mints all four anchors, leaving `markdown_link_gate` green on two headings the site does not have — **that half is the coder's measurement; the planner's fixture carried no footer and did not reproduce it.** Its own commit and its own review pass. |
| 20 | `tools/batteries/src-pinakes-pairing.toml:369` — a header conforming to neither form, on ASCII rules — ✅ **DEAD 20260902 11:24** | — new | **nothing — already cured.** `c806b13`, row 15's own commit, modified this file, and `tests/test_batteries.py::test_every_section_header_names_the_release_it_belongs_to` **passes on `main`** — a gate that refuses a third form cannot pass while a header matching neither exists. Verified by running that test alone (0.04s), not by reading the file. **The row was never struck**, so it sat in the queue as live work: the third stale row found in this plan on 20260902, after row 3's title and row 3's blocker column. **A fix's side effects close rows nobody thinks to look at** — row 15 was about the markdown-link battery and cured this one in passing. Read a row's body and check the tree before building it. thing | Sibling of row 15, found by the coder 20260902. The header reads `# unreleased, 20260831 - S16's residue…` — a hyphen where the convention has `·` — and its rules are ASCII `# ----` rather than `# ────`. **Verified by the planner: across `tools/batteries/`, 56 box-drawing rules against 6 ASCII**, so this file is the outlier on both counts. **This is why row 15's census needed a selector at all** — a `─`-delimited census cannot see this section, which is exactly how one tree yields 30 sections and 32 sections with neither count being wrong. Take it with row 15 or after it; blocks nothing. |
| 21 | **Build the dogfooding on-ramp — a checklist and a one-command start** — 🧭 **owner: planner, then coder** | — new | nothing | **Instructed by the user 20260902**, in the same session that cut 0.32.0: *build the on-ramp this week — a checklist and a one-command start*. **This supersedes a classification, which is why it is a row and not a note.** `plans/20260825_1252-plans-sweep-findings.md` row 24 filed the dogfooding KB as **not queueable by an agent — needs the user's material**, and that was correct until the instruction arrived: an on-ramp is buildable without any KB content, because it is the path *to* first use rather than the use itself. The owning plan is [`plans/20260801_0749-realism-corpus.md`](20260801_0749-realism-corpus.md) § *The dogfooding KB*; `pinakes-kb` was created 20260804 and has one commit, an empty `docs/` and no real content, which is the condition the on-ramp exists to end. **Split at the ownership boundary**: the checklist is a document and therefore planner-owned; the one-command start is code and is the coder's. **Neither half is designed yet** — no plan states what the command does, what the checklist checks, or what "started" means, and § *Working mode* forbids guessing what the plans have not decided. So the first increment is the planner writing that design, not either half of the build. **"This week" is the user's word and is recorded, not converted into a date** |
| 22 | **Regenerate `CLAUDE.md` fresh** — 🧭 **owner: planner** | — new | nothing | **Authorised by the user 20260902**: *Drop, and regenerate fresh* — asked and answered when an extraction branch was on the table, and the answer chose regeneration over extraction. The file is **~295 lines against the ~150-line guardrail** in the global CLAUDE.md hygiene rules, whose own remedy is *extract sections and leave pointers*, not delete. **Show the diff before applying** — the global rule *Non-trivial operations → show the change, then ask* covers instruction-file changes explicitly, and the authorisation is to regenerate, not to land unseen. **Two known hazards, both measured today.** (a) The live status block in this file has been wrong three separate ways in two days — it is the single most falsified region and a regeneration must rebuild it from the tree rather than carry it across. (b) A cleared session's *only* pointer to the live build order is that block, so a regeneration that drops or mis-links it strands every queue in `plans/`. **Tell live peers before editing** — `CLAUDE.md` is the most-shared file here and the injected copy in any session has no knowable age |
| 23 | **A check that does not go through the escaper** — a post-render `tomllib.loads` in `template.py` `_render` | — new | row 5 | **Proposed by the coder while building row 5, 20260902 11:44, and ruled by the planner as its own increment rather than folded in.** Escaping makes a value safe inside a TOML **basic** string; it cannot reach a literal string, a bare key or a number — so all nine of S4's rows go through the escaper, and **none of them can see a template that interpolates outside a quoted string**. That is a seam defining a region no test reaches, and the remedy is a check that does not go through it. **Scope is settled: `_render` is manifest-only** — both callers pass `MANIFEST_TEMPLATE` — so parsing its output imposes TOML on nothing else, and it really is one call. **It is not one line.** `_render`'s two existing arms carry deliberately *different* remedies (*reinstall* for bad Jinja, *written for a different build* for a missing variable); a raw `TOMLDecodeError` escaping past them is the exact class S4 exists to remove. And it reaches **`render_archived`**, so `pnk upgrade` gains a way to hard-fail where it used to show a diff — **decide there whether it fails or degrades**, and give that surface its own row. Kept out of S4 for a second reason: a parse sitting behind those nine rows satisfies *renders a manifest that parses* on its own, and the battery would then kill mutants for the wrong reason. **Not a re-opening of *escape at render, not reject at `init`*** — that ruling was about user **names**, and this refuses no name the escaper handles; it rejects a broken **template**. **Three decisions sit inside this row and none is taken:** (i) the remedy's wording, (ii) whether `pnk upgrade` **fails or degrades** when an archived template renders unparseable, and (iii) — added 20260902 12:00, out of a failure in the S4 increment itself — **the remedy must not name an example that lacks the property it demonstrates.** A sentence dictated into `tools/batteries/README.md` claimed `C:\notes\kb` renders valid TOML without the backslash escape; `tomllib` rejects it, because `\k` is not an escape. The general form is in [`docs/RETROSPECTIVES.md` § *An example I reasoned about instead of running*](../docs/RETROSPECTIVES.md#an-example-i-reasoned-about-instead-of-running-20260902-1200): **a value named to demonstrate a property is run, not reasoned about.** **A worked case, found 20260903 and measured on both sides:** the shipped template already interpolates outside a parsed string — `templates/notes/pinakes.toml.j2:39` is `# fitted_for = "{{ rerank_model }}@<revision>"`, a **comment**, identically in `_versions/1.1` and `1.2`. TOML never parses it, so the quotes are decorative and it is a **third interpolation position** that neither the escaper's own bound (*literal string, bare key, number*) nor any of S4's thirteen rows names. Rendered with a raw newline in `rerank_model`, the comment breaks and `reranker@<revision>"` lands on a **live line**; `tomllib` then rejects the manifest — **so this row's check catches it and nothing else does.** That makes row 23 a fix for a position in the wheel today, not a hardening against a hypothetical third-party template. What holds there now is a single entry of `_TOML_ESCAPES`, which was itself unobservable until the S4 review, because the `\uXXXX` fallback shadows it. |
| 24 | **Anchor a status claim so a gate can falsify it** — `tools/`, one new checker | — new | nothing | **Raised by the planner 20260902 11:52, from this morning's own evidence rather than from any plan.** Today four cells read `Blocked on: row 12` for **three and a half hours** after row 12 landed at 07:55; row 8 read `rows 4–7` after row 4 landed at 09:33; row 4 read LIVE in **two** registers after six commits had landed. **Every one of those is mechanically falsifiable and nothing checks any of them** — `CLAUDE.md` already records that *a status claim is never gated, not a `##` heading, not a table cell*, and treats it as a fact to work around rather than a defect to remove. **Shape:** a cell claiming built carries a sha and the gate asserts `git merge-base --is-ancestor <sha> origin/main`; a `Blocked on: row N` cell is checked against row N's own status; a row carried in two registers is linked by id and the gate is red when they disagree. **Its costs, stated rather than discovered:** it constrains the form a row may be written in and is a migration across existing plans; it can check only what is **anchored**, so it would *not* have caught the rows 6/7 scope overlap or the design choice sitting inside row 9 — **the two of today's eleven corrections that actually mattered**; and a gate never shown to fail is a claim, so it ships with a known-catchable case or not at all. **Provenance, so nobody mistakes this for a decision that has been taken:** sharpened while answering an advisory question from the session `pinakes-three-agent-architecture` about whether to add a third resident agent. **This row is not an answer to that question and does not pre-empt it** — that one is the user's. |
| 25 | **`pnk budget` prints a KB name raw — an ANSI escape reaches the terminal, a newline breaks the line contract** | — new | nothing — row 5 landed `51e7731` | **Found by the coder during S4's review 20260902 and re-measured by the planner rather than taken on report.** `\u001b` is six characters in the file and **legal TOML**; it decodes to a live ESC (`1b 5b 33 31 6d` in the rendered bytes), and `budget/summary.py:193` interpolates `summary.kb_name` straight into a plain-text line. A newline in the name breaks `render()`'s one-entry-one-line contract outright — `budget — Bob` then `KB (01M1…)` on the next line. **S4's fix did not create this and a review agent's *previously unreachable* was wrong** — a hand-written manifest always parsed and always printed raw, on `main` too. What S4 changed is the **route**: from *hand-edit your TOML* to *pass a flag*. **The narrower claim is the defensible one, and the coder corrected its own reviewer to reach it.** **Scope measured, not estimated:** `summary.py:193` is the only plain-text consumer of `kb.name` in `src/` — the other reads are `serve.py` JSON payloads and in-memory comparison, plus `cli.py:1255` which feeds `Summary` and `template.py:197` which is the render itself. **Three decisions sit inside it and none is taken:** which characters (C0 only? DEL? every `Cc`?), what replacement (drop, `\xNN`, a placeholder), and **where the guard belongs** — at `render()`, at the `print`, or at the `Summary` boundary, which is the only one of the three that also covers a future plain-text consumer. The sweep plan bounds its control-character nicety to *"only if it stays cheap and does not grow a decision of its own"*; **it grew one, so it is a row and not a nicety.** |
| 26 | **Write the other half of *commit before mutating*** — `tools/batteries/README.md`, prose only — ✅ **BUILT** | — new | nothing — landed | **A live mutant reached `origin` on 20260903.** `7193983`, under the subject *"Retro: the review harness reported a clean pass while nobody had looked"*, carried `elif False:` in `_toml_basic`'s control-character arm — verified by the planner in the commit itself, and `9e1de79` restores it byte-identical to `c29cbc0`. **Every instrument was green and each was right to be:** the battery reported 9/9 because it mutates a *committed* target and has no opinion about what else is committed while it works; `--check-anchors` was green because the anchor was present and only the arm was gone; `./check.sh` had been killed at ten minutes under load. The one thing that could have caught it was reading `src/pinakes/template.py \| 2 +-` in the stat of a commit whose subject promised a fragment. **`CLAUDE.md` already says *commit before mutating*; the missing direction is the other one.** The rule to write, the coder's formulation and it is the right one: **while a battery is running, that worktree has no committable state** — not for the files the battery names, and in practice not at all, because `git add -A` does not know which those are. **Blocked on row 5 only because that branch holds this same README** — this is prose, not a gate, and the increment that produced the incident is the one that must not be made to conflict. **✅ Written 20260903 as `tools/batteries/README.md` § *While anything is reverting a file, the worktree has no committable state***, in the same branch that flipped row 5 — which is what unblocked it. **The heading was widened at 12:26, hours after it was written**: it named the battery, and a review lens that reverts a source file to ask whether a test pins the fix opens the identical window. The rule this row exists for was **narrower than the class it was drawn from**, and the second instance came from a different tool the same day. `docs/RETROSPECTIVES.md` keeps the original wording, as a record written when it was written. |
| 27 | **Cut 0.32.2** — 🧭 **owner: planner** — ✅ **DONE 20260903 12:59** | — | nothing | **The second release row in this queue, and it exists for the same reason row 3 does**: a cut is planner work, so an implementer that finishes an increment leaves it here rather than doing it. Nine fragments — 4 `changelog.d/`, 5 `retro.d/` — from row 7's four fixes and the documents that followed them. **A PATCH**, and the `-k 0` refusal inside it is a breaking CLI change ruled the same way at 0.7.1 and 0.20.1. **`prices.toml` was re-read, not re-stamped**: the ECB had not published 20260903 yet, so the 2026-09-02 fixing of `1.1578` stands and only `as_of` moved. **✅ Published 20260903 13:08 and swept**: the hold ran 47 seconds, the wheel carries all four fixed modules across 78 entries, and the control fires — `pnk search -k 0` exits 2 at the parser on the published 0.32.2 and 1 on *no pinakes.toml found* on the published 0.32.1. **The sweep also corrected `docs/RELEASING.md`**, which had told planners to write ROADMAP's standing prose in the release commit: that block counts git tags and never increments them, and at the release commit the tag does not exist |
| 28 | **`pnk doctor` is silent about an unresolvable symlink that `pnk sync` names** — 🧭 **owner: planner** | 10 | nothing — the crash half landed `934190c` | **Filed by the coder 20260903 13:13. Re-scoped twice by the planner, 20260903 14:20, and the second re-scope is a correction of the first.** The row first said the choice was *when does `doctor` walk at all*, on the reasoning that widening the trigger costs every KB a traversal. **Two things falsified that, and only one of them was in the code.** First, `_sidecars` (`doctor.py:400`) already `rglob`s the whole KB root **unconditionally** from `doctor.py:162`, so the walk is not the cost being protected; the gated walk is the *other* one, `_retired_documents` (`doctor.py:463`), whose own docstring gives its reason. Second — **and this is the part I got wrong** — I then wrote that `doctor` does not crash on 3.13, having watched the published 0.32.2 complete on both interpreters. **That was a true measurement of the wrong population.** The KB I probed had no sidecar for the unreachable document, so `_sidecars` never reached its loop body, and the loop body is `document_for(path).is_file()` at **`doctor.py:420`** — the raising call. Re-run against a KB where the document *had* been indexed, the published 0.32.2 `pnk doctor` dies at that exact line on 3.13 while 3.14 completes. **`rglob` is safe on both** (measured: no raise over a `0o000` directory on either), which is why the walk looked safe and the stat inside it was not. **Both halves are fixed on `main` and I verified them there on 3.13 rather than reading the diff**: `pnk doctor --kb` exits 0, and the present-but-unreachable document is no longer reported as an orphan — so `--prune` is no longer offered a permanent ULID. **What is left for this row is the silence only.** `sync` reports the unresolvable link; `doctor` completes and says nothing. The choice is **which check owns that report and what `doctor` exits when it fires** — not whether walking is affordable, and no longer anything about a crash |
| 29 | **A gate that refuses the `pathlib` spelling on a corpus path** — `tools/`, one checker, plus the `minimum-python` leg's own blind spot | — new | nothing — `paths.py` landed `934190c` | **Filed by the planner 20260903 14:24, and this is the row that stops the class recurring rather than fixing another instance of it.** `src/pinakes/paths.py` now owns `is_regular_file` and `resolves`, but **nothing stops the next caller writing `Path.is_file()` again** — and the first fix for this defect did exactly that in reverse: two private helpers in `sync.py`, which `doctor.py` and `linkscan.py` could not reach, so the identical crash survived in both until an adversarial review found it. **Three sites, one class, one review away from shipping again.** Build a checker that refuses `Path.is_file()`, `Path.exists()` and `.stat()` on a path that came from the corpus or from a user argument, directing the author to `paths.py`. **The hard part is the discriminator, not the grep**: the same spellings are perfectly correct on paths Pinakes itself wrote — `.pinakes/`, a template file, the manifest — so a checker that refuses them everywhere will be suppressed into uselessness on its first run. Decide the rule (an allowlist of safe roots? a naming convention on the variable? a per-line opt-out that must carry a reason?) and say what would falsify it. **Second half, smaller and independent**: `minimum-python` runs one leg, `--extra light`. It cannot see a 3.13 difference that needs `[pdf]` or `[claude]` — the coder's note argues no mechanism connects an SDK's presence to interpreter stat semantics, **and that argument is worth keeping only while it stays written down where the next person finds it**, so put it in the job's comment if it is not already there and state what would break it. **Also worth one line**: `docs/RETROSPECTIVES.md:2218` says a developer machine differs from CI by *“different OS, different Python”* — a dated record, so it stays as written, but do not cite it |

**Rows 4–9 are the list § 4 refers to as "the user-facing list".** It is empty when those six land.

**What this order deliberately puts last:** the G5 re-run, the ROADMAP review, X7 layer 3, D-36,
`_toml.py`'s message and `pageyield.py`'s example. Each is real; none reaches a user of `pnk`.

---

## 4. The decision for the user — a process moratorium · ✅ ANSWERED 20260902: **option E**

**The user answered E — no freeze, plus a written re-measure trigger — 20260902, in their own
session and their own words.** Nothing in this repository is frozen; every addition is still judged
on its merits. What changes is that the ratio below is **re-measured at a named moment and comes
back to the user if it has not moved**.

**Options D and E were not in this file when the decision was taken.** Both were raised in the
session that put the question and lived only in that conversation until now. They are written out
below because **a file offering three options while recording that a fifth was chosen is a register
disagreeing with its own decision** — the exact failure this plan rows against other documents.

The analysis that follows is left standing as the reasoning the decision was taken against, with
its recommendation replaced by what was actually chosen.

**The evidence, stated with its selector.** Over `51a34bb` → `b6be317` (20260824 04:07 → 20260901
11:16 UTC), `src/` took **928 of 16,909 inserted lines — 5.5%** (M2), while `docs/` grew 13% and
`plans/` grew **44%** (M3). Both halves are true simultaneously: individual findings genuinely
closed, *and* the corpus grew 7,424 lines net. **A count of commits is not offered** — a commit
tally varies with how finely a session slices its work, and a path-filtered `git log` returns a
different number again through history simplification.

**M2's denominator does not reproduce, and the trigger below therefore uses one that does.**
Re-run 20260902 against the same window: the numerator is exact — `src/` really did take **928**
inserted lines — but no reading of M2's stated selector (*"`git diff --numstat`, non-merge window"*)
returns 16,909. Net tree diff gives **928 of 18,028 — 5.1%**; per-commit churn
(`git log --no-merges --numstat`) gives **1,115 of 21,434 — 5.2%**; net diff excluding the two
fragment directories gives **928 of 17,489 — 5.3%**. All three agree with M2's conclusion and none
agrees with its arithmetic. **M2 is left as written** — it records what was measured that day and
its 5.5% changes no conclusion — but a trigger has to be re-runnable, so the baseline below is the
first of the three, whose command is written out.

**A third reading, which neither option below states.** The ratio may be measuring **queue position,
not process appetite.** Across that window the code work was decided-but-unstarted and the document
work was unblocked, and agents write what is available to write. If that is what happened, freezing
the register layer does not cause S1 to be built — only building S1 does — and the ratio corrects
itself when § 3 rows 4-9 land. This reading argues that **§ 3 is sufficient and § 4 is unnecessary**.
It is not offered as a fourth option because it is an argument about whether to decide at all, and
because it is not free of assumption either: it predicts the ratio falls once the queue drains, which
nobody has tested.

**Conflict of interest, stated because the recommendation is not neutral.** Option A validates the
measurement its author spent the morning producing, and the author said so when handing the draft
over. That does not make A wrong — it is genuinely the only zero-cost reversible option — but the
user should weigh it knowing the recommender benefits from its adoption.

### The options

| | **A · Freeze the register layer** | **B · Freeze nothing** | **C · Actively consolidate** |
|---|---|---|---|
| **What it means** | No new register, no new rule in `CLAUDE.md`, no new gate that is not closing a defect that already shipped — until § 3 rows 4–9 are done | Carry on; judge each addition on its merits as now | Collapse the four queues into one as a scheduled piece of work |
| **For** | Costs nothing, reversible in a sentence, and directly targets the measured ratio. Leaves every existing gate running | Nothing is refused on a rule rather than on merit. The newest process — `RELEASING.md` step 3 — caught a stale FX rate that had shipped in a wheel for a month | Attacks the actual cause of the disorientation, which the repo has already diagnosed precisely |
| **Against** | A freeze is blunt: it would have refused step 3, which paid for itself. Needs an explicit carve-out for a gate closing a shipped defect | Is the status quo, and the status quo produced the ratio. Nothing changes | More writing, into the corpus that is already up 44%. **The last attempt produced a fifth register rather than one.** Spends a week on the map while the territory is stalled |
| **Cost** | zero | zero | ~1 week, planner |
| **Reversible** | yes, immediately | n/a | no |

### Two options that were missing, and the one that was chosen

Neither D nor E was in this file when the question was put. **E is what the user chose.**

| | **D · § 4 is unnecessary** | **E · B plus a written re-measure trigger** ✅ **CHOSEN** |
|---|---|---|
| **What it means** | Delete the question. The ratio measures **queue position, not process appetite** — the code work was decided-but-unstarted, the document work was unblocked, and agents write what is available to write | No freeze and no new rule. Build § 3 rows 4–9. Re-measure the ratio at the next cut and report it. If it has not risen, **A returns to the user** |
| **For** | It is the third reading already written into this section, promoted to an option rather than left as an aside. Costs nothing, and refuses to build machinery around an untested inference | Keeps B's zero cost and its judge-on-merit rule while removing B's one real weakness — that nothing changes and nobody looks again |
| **Against** | It *predicts* the ratio corrects once the queue drains, and **nobody has tested that prediction**. If it is wrong the question returns in a month with a worse ratio and no baseline to compare against | One more written thing, in a corpus the measurement says is already growing too fast. The trigger must be honoured by whoever cuts the next release, and **nothing gates it** |
| **Cost** | zero | near zero — one measurement, at a moment that already exists |
| **Reversible** | yes | yes |

### ✅ DECIDED — **E**

**Nothing is frozen. Build § 3 rows 4–9. Judge every addition on its merits, as before.**

**The trigger, written so it can be run rather than remembered.** It fires at **the first release
cut after the one taken 20260902**. At that cut, run:

    git diff --numstat <baseline>..<sha being tagged> |
      awk '$1!="-"{split($3,a,"/"); t+=$1; if(a[1]=="src") s+=$1}
           END {printf "src/: %d of %d = %.1f%%\n", s+0, t, (s+0)*100/t}'

`<baseline>` is the sha tagged on 20260902. **Report the number in that release's own record.** If
`src/`'s share has **not risen above 5.1%** — the reproducible baseline established above, net tree
diff, all paths — then **option A returns to the user as a live decision**. It does not become
policy by default and it does not lapse by inattention.

**Where the ratio stood when the decision was taken, because it points the other way.** Over
`b6be317..67a5718`, `src/` took **0 of 2,883 inserted lines — 0.0%**, and `src/pinakes/` was
**byte-identical to `v0.31.1`**. That is worse than the number that prompted the question, and it is
the strongest single argument for A. The user chose E knowing it. **S1 landed into the same release**,
which is the first `src/` change in that window and the reason the next measurement will not start
from zero.

**What the two rejected-for-now options still contribute.** **C** — consolidation — becomes the right
answer once § 3 rows 4–9 are empty, and its objection stands until then: acting on the diagnosis
while the engine is stalled grows the map again. **A**'s carve-out survives as a useful test even
without the freeze: *a gate that closes a defect which already shipped is not new process.* Row 1
passed it, `RELEASING.md` step 3 would have passed it, and ENG-1 passes it.

**Two rows were blocked on this section and are now unblocked** — row 18 outright, and row 15's
deferred gate, which its body held behind a B-or-*neither* outcome. E is B plus a trigger, so both
may be proposed.

---

## 5. What comes after — and why it is not scheduled here

**Dogfooding is the highest-yield activity this repository has on record, and it cannot be
scheduled by an agent.** Every user-facing defect in § 3 came from one sweep of *running* Pinakes on
20260825. Two of the three gated futures need the same input to fire: T6 wants "a KB that is
actually queried" past ~50,000 chunks with felt latency, and the dogfooding KB `pinakes-kb` has one
commit, dated 20260804, untouched since.

**It is the user's material, so the user is the trigger.** It is named here so that an empty build
order is not misread — [`docs/README.md`](../docs/README.md) already warns that an empty list means
*the next thing to build has not been planned yet*, never *nothing to do*.

**One honest note about the gated futures, because it changes what re-measuring is worth:** the G5
re-run will not revive the graph channel. PPR's own gate requires a channel shipping default-on, and
the plan predicts another null in advance — so a null re-run leaves PPR exactly where it is.
Reviving it needs a different mechanism or corpus, not another measurement. The re-run is still
worth doing if the goal is to retire the question honestly; it is not worth doing if the goal is to
unblock the graph work.

---

## 6. Also owed by the user, and not by anyone here

Listed for completeness; none blocks § 3.

| Decision | State |
|---|---|
| The `CLAUDE.md` extraction (`475b452`) | Pushed, unmerged, marked *do not land without the user*. **Measured 20260901 12:35 UTC: `main` is 114 commits ahead, and `git merge-tree` reports a real content conflict in `CLAUDE.md`** (`docs/README.md` merges clean). A rebase is not optional and not mechanical — the conflict is the status block the proposal deletes, which `main` has since rewritten |
| ~~`pnk adopt` — which release owns it, or drop it~~ **REASSIGNED TO PLANNER 20260902 07:41** | **Corrected 20260901 12:35 UTC: it *does* appear in a top-level routing document** — [`docs/README.md:29`](../docs/README.md) names it, correctly, as part of what "is still a proposal". The earlier claim that it appears in none was wrong. **This row was misfiled and sat here 29 days for no reason.** The decision's own founding sentence ([`plans/20260804_1016-template-release.md:523`](20260804_1016-template-release.md)) reads *"Flagged so the planner can either drop the APPROACH row or add the work"* — it names the planner, and the same file's line 304 puts D-8 among *"recommendations the planner may still overrule"*, in contrast to D-9 beside it which does say *"it is the user's"*. Both actions it names are edits to `docs/**`, which the ownership table already makes planner-only. What is true: no release owns it, and it is unimplemented — `grep -rn '"adopt"' src/` returns 0 where the same selector returns `cli.py:1799` for `"upgrade"` |
| ~~The `fable` clause in the user's global `~/.claude/CLAUDE.md`~~ | **CLOSED — verified present 20260901 12:35 UTC**, `~/.claude/CLAUDE.md:42`: *"nothing reaches Fable either, which bills at 2x Opus"*. It was reported done and the report was right; this row existed because nobody had opened the file |
| ~~`--autocompact 150000`~~ **STRUCK 20260902 07:41 — not a decision** | **Unverifiable from this repo**, and the flag does not exist under that name. One mention in `RESUME.md`, no plan, no commit. The live setting is `autoCompactWindow` in `~/.claude/settings.json` and reads **300000** — measured 20260901 12:35 UTC, twice the number this row is named for. Whatever decision this row records, it is not about a value anything currently reads. **So it is not the user's and not anyone's: there is nothing to decide.** Kept struck rather than deleted so the row is not re-derived from the same absent source a third time |

---

## 7. What this plan does not claim

- **It does not claim § 3's order is the only correct one.** Rows 4–9 could be built in any order;
  what is argued is that they precede rows 10–12, and that row 9 belongs with rows 4–5 rather than
  below them.
- **It does not restate the sweep plan's evidence.** Where this file and that one disagree about a
  row's state, **the sweep plan wins** — it carries the dated measurement and this file carries a
  summary of it.
- **It adds no finding except ENG-1.** Everything else here has an owner already.
- **Its own status claims decay.** Every row states where to check rather than asking to be
  believed; a register that is read is the one that stays true.
