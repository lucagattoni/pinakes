# Every open decision, evaluated — and none of them taken

**Written 20260825 18:03 UTC against `main` at `e6d62e3`.** Eight decision clusters, fifteen questions.
Each was analysed against the tree and then handed to a **dedicated adversary** told to check the evidence,
hunt for a missing option, and attack the recommendation.

> ## 🛑 NOTHING HERE IS DECIDED. Every recommendation below is a proposal to the user.
> The repository's rule is that **an undecided question is a stop, not a guess**. This file exists so the
> stop is *informed*, not so it can be skipped.

## ✅ ALL EIGHT TAKEN BY THE USER, 20260825 18:16 UTC

**The user accepted every recommendation in this file.** The blockquote below it — *"nothing here is
decided"* — described this file between 18:03 and 18:16 and is left as written, because the analysis was
produced under it and the record of that matters.

| # | Decision | Taken | Owner |
|---|---|---|---|
| 1 | **Tag `0.30.3`** | **Tag now — with D-35 layer 2 gating the marker.** Layer 2 is built **first**, so the hold marker's removal is enforced rather than remembered | planner → coder |
| 2 | **D-31 / D-32 / D-33** | **Option C — `pnk doctor` asks *both* questions** (tracked *and* ignored), **unconditionally** | planner → coder |
| 3 | **D-34** | **Promises only, ratified** — plus **one bounded audit** of the residue | planner → coder |
| 4 | **D-36** | **Option E** — derive the bound from a **generative round-trip corpus**, and set the free `ruamel` options. **This option did not exist before the adversarial pass invented it** | planner → coder |
| 5 | **D-37** | **Option E** — gate the move hint on the **orphaned sidecar**, not the mint count. **Also invented by the adversarial pass** | planner → coder |
| 6 | **`requires_pinakes` floor clause** | **E+F** — fold into `docs/KB-UPDATES.md` §8, fix the unknown-key remedy, one line in the sharing section | planner → coder |
| 7 | **Arity req 3 + the G5 gate re-run** | **Split them.** Close arity requirement 3 **now** — its antecedent is measured **false** on every real corpus. Run the gate re-run as **its own three-leg gate**, later | planner → coder |
| 8 | **The two ownerless deferrals** | **Close the mutation fields as declined.** **Keep the re-extraction loop deferred** — with its trigger written **in the code that owns the argument**, not in a plan | planner → coder |
| 9 | **The three residues** | Rule question 1 **without re-enumerating**; **cite `docs/ROADMAP.md`** for question 2 (already ruled there); defer question 3 behind a trigger that **names the cheap implementation** | planner |

### Three things about this record that must not be lost

1. **Two of the taken options were invented by the adversary, not the analyst.** D-36 E and D-37 E did
   not exist in the first pass. Anyone implementing them must read those sections rather than a memory
   of the original options — **the originally-recommended D-36 option was overturned.**
2. **The tag has an ordering constraint, and it is the whole point of the option.** *Tag now* does
   **not** mean tag next. **D-35 layer 2 is built first**, and only then is the tag pushed. Tagging bare
   was a distinct option (`Tag 0.30.3 now, bare`) and it was **not** the one taken.
3. **`git tag` + push is irreversible and outward-facing** — PyPI never accepts a version twice. The
   decision is taken; **the execution is still confirmed at the moment of pushing**, per this repository's
   own rule that a release is verified rather than assumed.

### What this unblocks, and in what order

**D-37 was the only decision blocking queued work** — the coder reaches **S6** in the sweep's S5–S9 batch
and stops. It is now answered, so that batch runs to completion.

The synthesis's ordering (§ *The order to take them in*) still holds and is **not** superseded by this
banner: the cheap closes (arity req 3, the residues, D-34, the deferrals) are records rather than builds
and can land in one planner pass; **D-31/32/33 is the largest genuine build in the set**; the G5 gate
re-run is last and blocks nothing.

**Six of the nine rows above block nothing.** Four high-severity defects — S2, S3, S1, S4 — remain
decided, unblocked and unbuilt, and are **already live in the 0.30.2 wheel PyPI is serving**. Nothing in
this table outranks them.

## What the set looks like

Eight decisions, and six of them block nothing. Only D-37 blocks work already queued and assigned, while the rest compete for sessions against four unblocked, already-decided coder fixes (S2, S3, S1, S4) that are live on PyPI in 0.30.2 right now. Every one of the eight briefs had a load-bearing claim overturned by its adversary, and four of the eight decisions turned out to be partly or wholly answered already on `main` — the dominant failure in this set is not *undecided*, it is *decided where nobody looks*.

**Seven of the eight recommendations changed after adversarial review.** One survived intact. That ratio is
the argument for the second pass, and it is recorded here rather than smoothed over.

## Read this before acting on any row

- FOUR OF EIGHT WERE ALREADY ANSWERED SOMEWHERE ON `main`. Residues Q2 is ruled verbatim at docs/ROADMAP.md:2528 (verified). The requires_pinakes clause has its remedy at docs/GUIDE.md:797 and its general question at docs/KB-UPDATES.md §8 since 20260728. D-34 was taken in db7d1c1 on 20260804, and its key sentence was written into the preamble by c770c0f at 20260825 13:51 — hours before the brief arguing for it. The tag question was half-settled by D-35 at 12:37. The bottleneck in this repository is not decision-making. It is that a taken decision does not reach the page the next reader opens.
- READING PRODUCES PLAUSIBLE WRONG CLAIMS; RUNNING PRODUCES CORRECT ONES — at a measured rate. The sweep that generated four of these items says it in its own title: fifteen defects found by running, "three high, none found by reading". Every adversarial overturn in this set came from executing something the analyst had read about: 30 sidecars through the real write path, 300 documents re-chunked, survey_free_yield over 18 fixtures, a 100k-entry git index timed, `pnk doctor` actually run. This is the repo's named failure mode — a sound argument over an unexamined domain — reproduced eight times in one sitting, including by the adversaries' own first passes.
- HAND-ENUMERATION HAS A 0-FOR-N RECORD HERE, AND THREE OF THESE DECISIONS INDEPENDENTLY INVOKE THE SAME REMEDY. The sidecar bounds table: four failed enumerations. STATUS line 3: missed by four consecutive release sweeps. The Published versions row: four releases behind, twice. The hold marker: 0-for-2. In each case the project's own doctrine — a convention that misses repeatedly becomes a gate — is the answer, and D-35 layer 2, D-36 option E and the D-34 audit are all instances of it. The generalisable rule: when a decision's output is "someone must remember to X", the decision is not finished.
- AGE MEASURED NOBODY'S ATTENTION, NOT THE WORK'S VALUE. The three oldest items — decision 12 / I7c (20260727), the requires_pinakes clause (20260805), arity requirement 3 (20260804) — are the three the adversaries found cheapest to close and least valuable. The loop cannot fire on this corpus. Arity's antecedent is false. The floor clause's Test is already discharged and pinned. Meanwhile four HIGH/MEDIUM user-facing defects were found in a single day by running the tool. Do not read "open since July" as "important".
- THE AGEING MECHANISM IS AN OWNERSHIP SEAM, NOT NEGLECT. The gate re-run was handed to the planner by a retrospective and no planner document picked it up in 21 days. Decision 12 has no ROADMAP row, no STATUS row and no owner — `grep -rn 're-extraction loop'` hits one file. The generalisable fix is the deferrals adversary's: write a trigger where the person who would ACT on it looks (audit.py's docstring), not where a sweeper looks (a ROADMAP row). Every deferral in this set should be placed by that test.
- SIX OF EIGHT BLOCK NOTHING, AND THE REAL COMPETITION IS NOT INSIDE THIS SET. S2 (silent index loss behind a green doctor), S3 (serve's cross-thread connection), S1 (one unreadable file builds no index at all) and S4 (a KB name bricked at creation) are already decided, unblocked, assigned to the coder, and — verified by diff — byte-identical in the 0.30.2 wheel PyPI is serving today. Any session spent on the six non-blocking decisions is a session not spent there. Only D-37 has a claim on precedence, because S6 sits in the coder's queue behind it.

## The order to take them in

Ranked by the synthesis. **Six of the eight block nothing** — the ordering is about cost and about what is
waiting, not about importance.

| # | Decision | Why here | Blocks |
|---|---|---|---|
| 1 | `g5-gate-rerun-and-parent-child-arity — (a) arity requirement 3 only` | Cheapest item on the board and it is free. Requirement 3 is a conditional — "IF the ceiling is alarming" — and the antecedent is measured false on every real corpus (4.95 rows/chunk here, 3.80 on 300 RFCs, 0 of 300 reaching the synthetic 53.42). Closing a conditional whose antecedent is false decides nothing about retrieval; transitive stays as built. Zero CPU, zero src/ change, one planner sentence. The adversary broke this away from the gate re-run, which is the single most useful structural change in the whole set — the original brief bundled them and argued you could not close one without paying for the other. | nothing |
| 2 | `residues-tripwire-t6-widening` | One planner ruling, minutes. Question 2 needs no disposition at all — docs/ROADMAP.md:2528 already states D-13's trigger and rules on it (I read the lines: "A corpus above the threshold is not enough; one exists and nobody searches it interactively"). Question 1 is a one-line purposive ruling. Question 3 defers. Take it early because it is the item most likely to be re-opened by the next sweep if left, and because the ruling also settles whether "the graph release, staged" belongs in CLAUDE.md's unbuilt-work table. | a CLAUDE.md table entry, and nothing else |
| 3 | `D-34` | Ratify promises-only now, because the cost of leaving it open is measured and recurring: every fresh reader who counts the tree re-derives the question, and it has already been taken once (db7d1c1, 20260804) without reaching VERIFICATION.md's preamble. Cheap to record. The bounded audit the adversary attached is real work but it is time-boxed and can trail the ruling. | nothing — but leaving it open regenerates itself |
| 4 | `deferrals-with-no-owner-reextraction-loop-and-mutate-fields` | Two records, no build. The mutation fields decline is unambiguous (expect_green exists in two commits and no code; 0 of 136 mutants asks for a green control). The loop stays deferred, and the adversary's finding makes the trigger's first term free to evaluate — nobody has to spend a euro to test it. Take it here because 8f05e73 already landed the dispositions the original brief budgeted an hour to write, so what remains is a trigger and an owner. | nothing |
| 5 | `D-36 + D-37 — (a) D-37 half` | THE ONLY DECISION IN THIS SET THAT BLOCKS QUEUED WORK. S6 sits inside the sweep's medium batch (build order item 5, coder, unblocked), and D-37 is item 7 — so the coder reaches S6 and stops. Answering it releases the batch. It is also a one-line coder change with a discriminator the adversary measured at 5/5 against the original's 3/5. | S6, inside the sweep's S5–S9 batch — i.e. live coder work already assigned and in flight |
| 6 | `D-36 + D-37 — (b) D-36 half` | Same file family and same session as D-37, so pairing them costs one context rather than two. But it blocks nothing queued, and its recommended option (derive the bound from a generative round-trip corpus) is a build, not a ruling. Take the decision here; schedule the build against the S-queue. | nothing queued; it does touch the sidecar byte-identity invariant, so it gates any future MANIFEST bounds edit |
| 7 | `tag-v0.30.3-now-or-hold-for-sweep-fixes` | Order-independent and takes minutes, so it slots wherever convenient — I place it after D-36/D-37 only because that is where the release cadence gets clean. The work is complete: `[Unreleased]` is empty, changelog.d/ and retro.d/ hold nothing but their READMEs, `__version__` is 0.30.3, and only 4 files differ from v0.30.2. The sweep half of the question dissolves — sync.py, doctor.py, serve.py and template.py are byte-identical to the published 0.30.2, so holding protects nobody. If you want the X7 rider, build it before the tag; see the honest limit in do_not_lose. | nothing — but every subsequent release is queued behind it, and S1/S3 (PATCH) then S2 (MINOR, a new doctor report) are the natural next two cuts |
| 8 | `D-31 / D-32 / D-33` | The largest genuine build in the set and the one with the strongest reason to happen: docs/INVARIANTS.md:22 promises a deep-run transcript "never leaves .pinakes/" and justifies it with "the state that is already gitignored" — a premise M1 falsifies, with nothing anywhere that checks it. It sits here rather than higher because it is medium-cost coder work that must queue behind the four high-severity sweep fixes, and because it needs one planner answer first (the helper-home question) that no document decides. | rows 5 and 6 of the exposure plan's build order — the recurring doctor check and D-33's detail line |
| 9 | `20260805_1313-requires-pinakes-floor-clause` | Cheap (one error-message string, one test, one doc fold) but low value: docs/GUIDE.md:797 already carries the correct remedy in the Troubleshooting table, and D-11's accepted-cost paragraph already had the user accept this harm on 20260804. It is forward-only — it reaches no already-shipped build. Low urgency, low risk, do it when a planner is already in those files. | nothing |
| 10 | `g5-gate-rerun-and-parent-child-arity — (b) the gate re-run` | Last: the most expensive item in the set (~2.4 h CPU derived from RETROSPECTIVES.md:4203, plus two sessions), it blocks nothing, its expected result is another null, and it removes only one of the two bounds STATUS.md:585 names — [retrieval.confidence] is still commented out in the committed corpus manifest, so clauses 3 and 4 stay vacuous either way. Worth doing because a shipped default (`expand = off`) rests on an index where three of seven edge kinds derived zero. If session time rather than CPU is the binding constraint, decline it; the adversary said it would not argue. | nothing |

## Where the adversary overturned the analyst

- FOUR of eight changed the recommended OPTION. (1) requires_pinakes floor: D (withdraw the clause + close a doc gap) → E+F (fold into docs/KB-UPDATES.md §8, fix the unknown-key message). The reversal turned on the adversary opening docs/GUIDE.md:797, which the original asserted "misses this case exactly" — it does not; it is exactly this case, correctly remedied. (2) D-36 and D-37: BOTH recommended options replaced with new ones the original never considered. D-36 B (widen the bound by hand) → E (derive it from a generative round-trip corpus), because 30 anchor-free sidecar shapes produced 12 byte-identity failures of which one is documented, and hand-enumeration of this bound has now failed four times including the adversary's own first pass. D-37 A (gate on the Mint count) → E (gate on the orphaned sidecar), because the Mint discriminator fails on delete-one-add-one — the ordinary git-hook batch shape — and on an ambiguity branch the original never found. (3) D-31/32/33: C-with-ls-files-first → C-unconditional. The original's one design contribution was a regression: the shortcut silently downgrades a WARN to OK on exactly the loose-folder KB where `pnk init` had just printed the warning, and that case is its ONLY behavioural effect. (4) g5/arity: "run the bundled gate re-run" → "split — close arity requirement 3 free, run the re-run on its own merits". The original's deciding quotation was misread: the plan's "deciding a retrieval question on a cost argument" forbids ADOPTING immediate-parent-only, not declining to measure it.
- FOUR of eight kept the option and replaced the reasoning underneath it. Tag 0.30.3: ship upheld, but the hygiene argument was superseded by D-35 — a user decision taken 20260825 12:37 that defines `__version__` as *landed* and makes the hold marker REQUIRED in exactly the current state. The hold is sanctioned, not anomalous; the urgency evaporates and the X7 rider replaces it. D-34: promises-only upheld, but the category claim ("the unrowed population is not debt") was falsified at n=1 by running the sample the original named and skipped — tests/test_serve.py::test_get_refuses_anything_that_is_not_a_known_id is a genuine unrowed promise. Deferrals: decline-and-defer upheld, but the deciding fact was replaced (the loop is unreachable because SCANNED_PAGE_FRACTION and the audit's exemption select near-disjoint page sets) and the trigger moved from docs/ROADMAP.md into audit.py. Residues: the shape upheld, but question 2 dissolved entirely (already ruled in ROADMAP) and question 1's proposed repair would have reintroduced a defect patched in the same file four hours earlier.
- Two overturns prevented a false fact being written into a planner-owned, published document. The residues brief's A(ii) would have recorded "the only KB with built state on this machine holds 43,353 chunks" — a 106,806-chunk KB sits at depth 5 on the same disk, above D-13's threshold, named by the trigger itself. The D-31 brief quoted docs/DESIGN.md as saying "stated rather than mitigated"; that phrase is not in DESIGN.md at all, it is in a plan about edge arity, and the real DESIGN.md:1367 says the opposite.
- Three overturns were arithmetic or provenance, not judgement, and all three were in briefs that were otherwise well-measured: VERIFICATION.md has 890 rows not 923 (923 counts 33 table headers the repo's own gate skips); the "67 modules, measured 20260825 12:49 on c23359f" stamp is impossible because c23359f was committed at 13:42; and `make release-check` runs no gate at all (Makefile:102-106 prints the version and echoes the tag command), so the assurance behind the tag is CI-by-SHA, not release-check.

---

# The decisions

## tag-v0.30.3-now-or-hold-for-sweep-fixes

**The question.** v0.30.3 is landed on `main` but untagged and unpublished — do we tag and publish it now, and if so, do we first build the already-decided gate (D-35 layer 2) that would force the hold marker's removal at tag time rather than relying on someone remembering?

**Status of the argument.** I UPHOLD the original brief's direction — ship, do not hold — and OVERTURN its reasoning and its bare form. The conclusion survives; three of the arguments carrying it do not.

WHAT I PERSONALLY VERIFIED (not relayed): I re-ran every release-state command (no v0.30.3 tag local or remote, no GitHub release, PyPI latest 0.30.2, tag page 404). I ran the full-tree diff `git diff --stat v0.30.2 HEAD -- src tests tools pyproject.toml` — only 4 files, so every other module in `src/` is byte-identical to the published 0.30.2, which is stronger evidence than the brief's targeted per-file diffs. I performed the hunk-range audit the brief ADMITTED SKIPPING: I located every file cited by all 15 findings and checked each against the six 0.30.3 hunks. None falls inside one. I executed 0.30.3's fix in scratch git repos — it works — and I independently reproduced the "bare `Then:`" wart. I executed the reach test: `pnk init` refuses on an existing KB, and `pnk doctor` prints no ignore/tracked row at all. I confirmed CI run 32854256771 ran against `e6d62e33` = HEAD exactly, and mapped 9 of check.sh's 11 gates to their own CI jobs. I fetched the live docs page and it does carry the ⏸ marker — closing one of the brief's unverified items in its favour.

WHAT I OVERTURNED:
(1) The brief's central hygiene argument is superseded by a user decision it read but did not weigh. D-35 was taken BY THE USER at 20260825 12:37 — four hours AFTER the hold began and AFTER the sweep — and decides that `__version__` means *landed on `main`*, that line 3 keeps naming it, and that a new offline relation makes the marker REQUIRED exactly when line3 > R. Right now line3 = 0.30.3, R = 0.30.2, marker present: the current state is precisely what the user sanctioned nine hours ago. So "an untagged `__version__` is an anomalous state that must be ended" is not this repo's position; the repo's position is that it is a legitimate state with a specified enforcement. The brief cites D-35 only as proof the protection is unbuilt and never notices it legitimises the hold. This does not reverse the direction — it removes the urgency.
(2) "`make release-check` was run against this tree — no re-verification owed" is wrong about what that target does. Makefile:102-106 runs NO gate: it prints the version and echoes the tag command. The assurance is CI, which I verified by SHA; it is not release-check.
(3) "0.6.0 is the last release whose notes lead with a new `pnk doctor` report" is false. 0.18.0 (20260807 22:37) opens with one, as does 0.17.0. Precedent is WORSE for a PATCH than the brief said, which makes the fold-S2-in options riskier, not safer.
Plus two smaller corrections: "the one sweep finding that shares a FILE with 0.30.3" is wrong (`-k` at cli.py:333 and `--source-type` at cli.py:319 put S8, S9 and two lows in cli.py too — line-disjoint, so the conclusion holds); and "18 commits" is 21.

WHAT IS ACTUALLY STILL OPEN, and why the question is half mis-posed: the framing "held until some or all of the sweep's 15 defects are fixed" rests on a premise I have now falsified exhaustively rather than by sampling — 0.30.3 shares no line with any finding, and all 15 are already live in the 0.30.2 wheel PyPI is serving. The sweep is simply irrelevant to this tag, so that half of the question dissolves. What genuinely remains is narrower and the brief never asked it: the marker must be removed by hand at tag time, D-35 measured that a stale marker is "green by construction, forever" in every existing gate, and the remedy is decided, unblocked, assigned to the coder, and sitting at item 9 of the build order. Build it first and the gate turns RED if the marker is left behind; tag bare and a human must remember. That is the open decision.

### What was established

- **No v0.30.3 tag exists locally or on origin, no GitHub release exists, and PyPI's latest is 0.30.2. The v0.30.3 tag page 404s while v0.30.2 returns 200. `__version__` on main is "0.30.3".**
  - *Source:* `git tag --sort=v:refname | tail -6` (ends v0.30.2); `git ls-remote --tags origin | grep v0.30` (0.30.0/1/2 only); `gh release list --limit 5` (top row v0.30.2, 2026-08-25T01:04:22Z); `curl -s https://pypi.org/pypi/pinakes/json` → latest: 0.30.2; `curl -o /dev/null -w %{http_code}` on both tag URLs → 404 / 200; `grep -n __version__ src/pinakes/__init__.py` → line 7
- **0.30.3 changes exactly four files against v0.30.2 across ALL of src/, tests/, tools/ and pyproject.toml: `__init__.py` (2 lines), `cli.py` (+32), `init.py` (+87), `tests/test_init.py` (+167). Because the diff is scoped to the whole tree rather than to named files, every other module in src/ — sync.py, doctor.py, store.py, serve.py, template.py, deep/estimate.py — is byte-identical to the published 0.30.2. This is a stronger form of the brief's claim than the brief established.**
  - *Source:* `git diff --stat v0.30.2 HEAD -- src tests tools pyproject.toml` → exactly those 4 rows, 279 insertions
- **THE AUDIT THE BRIEF ADMITTED SKIPPING, NOW DONE: no sweep finding's cited line falls inside any 0.30.3 hunk. The six hunks are cli.py new-lines 150-181 and init.py 160-207, 302-313, 379-389, 400-407, 426. Findings cite sync.py:519 (S1), sync/store/serve (S2, S3), init.py:355 and template.py (S4), sync (S5, S6), doctor (S7), search (S8), deep/estimate.py:456 (S9), plus the five lows and D-36 in sidecar/link paths. init.py:355 sits in the untouched gap between hunks 302-313 and 379-389.**
  - *Source:* `git diff -U0 v0.30.2 HEAD -- src/pinakes/init.py src/pinakes/cli.py | grep '^@@'` for the ranges; `grep -nE 'init\.py|cli\.py' plans/20260825_1240-run-pinakes-sweep.md`; `sed -n '28,110p'` of the sweep for every finding's file
- **CORRECTION to the brief: S4 is NOT the only finding sharing a file with 0.30.3. `-k` is parsed at cli.py:333 and `--source-type` at cli.py:319, so S8, S9 and two of the five low findings also live in cli.py. All are outside the hunk (150-181), so the line-disjointness conclusion survives — but the brief's stated evidence was inaccurate.**
  - *Source:* `grep -rn '"-k"' src/pinakes/` → cli.py:333, eval.py:781; `grep -rn 'source-type' src/pinakes/cli.py` → line 319
- **Nothing in src/, tests/ or tools/ has changed since the 0.30.3 release commit. 21 commits (not 18, as the brief stated) touch only CLAUDE.md (1), docs/ (9) and plans/ (22 files).**
  - *Source:* `git diff --stat d3a8f68..HEAD -- src tests tools pyproject.toml` → empty; `git log --oneline d3a8f68..HEAD | wc -l` → 21; `git diff --name-only d3a8f68..HEAD | sed 's#/.*##' | sort | uniq -c`
- **⚠️ TRUE WHEN WRITTEN, FIXED 20260826 — `674eda6` made it four real legs; the analysis below is left as it stood because the decision rested on it.** **CORRECTION to the brief: `make release-check` runs NO gate. Its entire body prints the package version, prints the tag to push, and echoes the manual tag command. It verifies nothing — not tests, not types, not even that a tag exists. The brief's pro "the tree `make release-check` was run against — no re-verification owed" rests on a target that does not verify.**
  - *Source:* `grep -n 'release-check' -A 15 Makefile` → lines 102-106, three `echo` statements and one `uv run python -c 'import pinakes; print(...)'`
- **CI's last run DID cover HEAD exactly, which the brief asserted but did not check by SHA. Run 32854256771 ran against e6d62e33, and `git rev-parse HEAD` is e6d62e339667f218c41f068258aac3f92737e96d. Success at 2026-08-25T13:35:31Z. Further, 9 of the 11 gates check.sh invokes have their own ci.yml jobs; the two that do not (fragments.py, shared_file_overlap.py) are a splicer and an overlap reporter, not correctness gates.**
  - *Source:* `gh run list --limit 12 --json headSha,conclusion,...`; `git rev-parse HEAD`; loop grepping each `tools/*.py` from check.sh against .github/workflows/ci.yml
- **D-35 was taken BY THE USER at 20260825 12:37 — after the hold began (08:27) and after the sweep (12:40) — and it SANCTIONS the landed-but-unreleased state. It decides `__version__` means "landed on main", that line 3 keeps naming it, and specifies layer 2: R = the Published versions row's tail; line3 > R → marker REQUIRED; line3 == R → marker FORBIDDEN; line3 < R → always red; row unreadable → hard fail. The current state (line3 0.30.3, R 0.30.2, marker present) is exactly the sanctioned configuration.**
  - *Source:* `sed -n '451,500p' plans/20260825_0749-exposure-and-silent-status.md` ("**Taken by the user 20260825 12:37.**" and the three-layer table); `grep -n 'Published versions' docs/STATUS.md` → line 1207, list ends "0.30.1 and 0.30.2"
- **D-35's layer 2 is decided, UNBLOCKED, and assigned — it is item 9 of the sweep plan's build order, owner `coder`, Blocked-on column reading "**nothing — unblocked**". D-35 also measured why it is the layer that matters: "X7 can enforce writing the marker. It can never enforce removing it", and a stale marker after publishing is "green by construction, forever" — green in status_header_gate.py (whose SHAPE has no `$`) and in release_order_gate.py (none of whose seven Sequence patterns reads line 3's tail).**
  - *Source:* `sed -n '535,565p' plans/20260825_0749-exposure-and-silent-status.md` (build order table row 9); `sed -n '451,470p'` same file for the enforcement analysis; `sed -n '44,47p' tools/status_header_gate.py` confirming SHAPE = `^\*\*Latest release: (\d+\.\d+\.\d+)\*\*` with the tail "deliberately unconstrained"
- **NUANCE the brief missed: layer 1 IS gated in CI and is green. `status_header_gate.py` has its own ci.yml job at line 283 plus a negative check proving the gate can still fail. So line 3's VERSION is enforced on every push; only the ⏸ MARKER is unenforced. "Protected by a hand-maintained line with no gate" is half right.**
  - *Source:* `grep -n 'status_header' .github/workflows/ci.yml` → lines 283, 291, 295; `grep -n 'status_header_gate' check.sh` → line 160
- **A STALE DOCSTRING that nearly trapped me, and which is planner-owned drift created by the same day's work: tools/status_header_gate.py still says "**The invariant holds with no exception window.** On `main`, `__version__` *is* the latest release … this gate never goes red on a correct tree, and a red run means the tree is wrong." D-35 redefined `__version__` as *landed*, so that paragraph is now false. I began building an argument on it before checking D-35.**
  - *Source:* `sed -n '10,16p' tools/status_header_gate.py` versus `sed -n '451,455p' plans/20260825_0749-exposure-and-silent-status.md`
- **0.30.3's fix works, verified by execution, AND the "bare `Then:`" wart reproduces. In a scratch git repo where `sub/.pinakes/state.json` was committed, `pnk init sub` printed the tracked warning with the absolute `git rm -r --cached "…/sub/.pinakes"` remedy — and printed a bare "      Then:" with no preceding step, because the "Add the line above first" line is conditional on `gitignore_unprotected` while "Then:" is unconditional.**
  - *Source:* Live runs under the scratchpad using /Users/luca/Code/repos/github_lucagattoni/Pinakes/.venv/bin/pnk; `sed -n '145,185p' src/pinakes/cli.py` for the conditional structure
- **0.30.3's reach is narrow, confirmed by execution rather than by reading. Re-running `pnk init sub` on the KB just created errors "is already a KB", and `pnk doctor` inside that KB prints no ignore or tracked row at all — its only git line is "WARN git hooks: not a git repository". So the new warning reaches only someone creating a NEW KB in a directory where git already tracks that root's `.pinakes/`.**
  - *Source:* Live `pnk init` re-run and `pnk doctor | grep -iE 'ignore|track|git'` in the scratch KB; `grep -rln 'check-ignore|ls-files|pinakes_tracked' src/pinakes/` → only cli.py and init.py; `grep -n 'already a KB' src/pinakes/init.py` → _check_target at 430-432
- **TAGGING IS STRUCTURALLY NEAR-REVERSIBLE, a pro nobody stated. release.yml puts tag/version agreement, `uv build`, an isolated wheel smoke test, `wheel_import_gate.py --require pinakes.serve --min-modules 50` and a 300 s MCP handshake gate ALL IN FRONT of `uv publish`, by explicit design: "a failure costs a deleted tag and nothing else." The GitHub release step runs after the upload. PUBLISH_TO_PYPI is `true`, so a tag really does publish.**
  - *Source:* `cat .github/workflows/release.yml` lines 30-130; `gh variable list` → PUBLISH_TO_PYPI true, set 2026-07-28
- **CORRECTION to the brief: "0.6.0 (MINOR) is the last release whose notes lead with a new `pnk doctor` report" is false. 0.18.0 (20260807 22:37) opens its notes with "**`pnk doctor` now says *how far* your template has drifted**" under ### Changed, and 0.17.0 also leads with a doctor WARN. Every doctor-facing addition I mapped landed in a MINOR: 0.2.0, 0.3.0, 0.4.0, 0.14.0, 0.17.0, 0.18.0. Precedent is worse for a PATCH than the brief said.**
  - *Source:* `grep -nE '`pnk doctor` (now |gains|adds|reports)' CHANGELOG.md` then an awk pass mapping each hit line to its enclosing `## [x.y.z]` heading; `sed -n` on the 0.18.0 heading at CHANGELOG.md:1132
- **The S2 branch exists, is disjoint from 0.30.3's files, and its author has already categorised it PATCH. Two commits (3a8ad62 code+tests 12:55 UTC, 02bedeb fragments 13:01 UTC), touching sync.py (+22), doctor.py (+50, a new `_index_covers_its_sidecars` check) and 114 lines of tests. Its changelog fragment is named `20260825_1258-fixed-…`, and changelog.d/README.md says the category "lives in the filename so it cannot drift from the content" — so the implementer has called it Fixed, i.e. PATCH.**
  - *Source:* `git worktree list`; in that worktree `git log --format='%h %ci %s' -3` and `git diff --stat main...HEAD`; `head -12 changelog.d/20260825_1258-fixed-retired-row-blocked-its-own-path.md`; `head -30 changelog.d/README.md`
- **The hold's public description is currently ACCURATE and live — closing an item the brief left unverified. The published site serves "Latest release: 0.30.3" with the ⏸ qualifier. The docs workflow is path-filtered on docs/**, and dd249f9..HEAD touched only plans/, so no docs deploy was owed and the deployed content matches the tree byte-for-byte at line 3.**
  - *Source:* `curl -s https://lucagattoni.github.io/pinakes/STATUS/ | grep -oE 'Latest release[^<]*'` → "Latest release: 0.30.3"; `sed -n '1,30p' .github/workflows/docs.yml` for the path filter; `git diff --stat dd249f9 HEAD` → plans/ only; `git show dd249f9:docs/STATUS.md | sed -n '3p'` identical to HEAD's
- **The current state is the shape of a recorded historical failure, which strengthens the case for ending it — an argument the brief never made. RELEASING.md: "A CHANGELOG entry and a `__version__` are only claims: v0.1.0 had both for two days with no tag, no release and nothing published." RETROSPECTIVES I15: "The 0.1.0 release existed in every artifact except the ones that matter." The hold is now ~9h20m old, against corrective releases cut 58 minutes apart (0.30.1 00:06 → 0.30.2 01:04).**
  - *Source:* `grep -n 'no tag, no release' docs/RETROSPECTIVES.md` → line 38; `sed -n '463,465p' docs/RETROSPECTIVES.md`; `sed -n '95,99p' docs/RELEASING.md`; `gh release list`; `date -u '+%Y%m%d %H:%M UTC'` → 20260825 17:47
- **The release-window mechanism's PRECONDITION is not met, which is what actually dissolves half the question. RELEASING.md § "A fragment that arrives after the release commit but before the tag" exists because 0.30.2's window let "a defect found in *landed* code be folded into the untagged section" — a defect in the code being released. None of the 15 findings is in 0.30.3's code, so there is nothing the window exists to catch.**
  - *Source:* `grep -n 'fragment that arrives after' -A 30 docs/RELEASING.md` → lines 70-93; cross-read with `sed -n '1116,1122p' docs/STATUS.md`
- **Release readiness by the procedure's own checklist holds: `## [Unreleased]` is empty (immediately followed by `## [0.30.3] — 20260825 08:27`), the 0.30.3 footer link and repointed Unreleased compare are both written at CHANGELOG.md:4012-4013, and no fragments are pending on main.**
  - *Source:* `sed -n '1,22p' CHANGELOG.md`; `grep -nE '^\[Unreleased\]|^\[0\.30\.[123]\]' CHANGELOG.md`; `ls -A changelog.d/ retro.d/` → README.md only in each

### The options

| Option | What it means | Pros | Cons | Work |
|---|---|---|---|---|
| **Tag 0.30.3 now, with D-35 layer 2 gating the marker** ⭐ | Do the tag, but make the marker's removal enforced rather than remembered. Coder builds D-35 layer 2 into `tools/status_header_gate.py` + `tests/test_status_header_gate.py`: read R from docs/STATUS.md's *Published versions* row tail, then line3 > R → marker required; line3 == R → marker forbidden; line3 < R → red; row unreadable → hard fail. Built during the hold it is green as-is (0.30.3 > 0.30.2, marker present). Planner then does the release sweep — append 0.30.3 to the *Published versions* row and the *Published on PyPI* prose, strip the ⏸ from line 3 — and the gate goes RED if the marker is left behind. Then `git tag -a v0.30.3`, push, and verify the artifact per RELEASING.md. No change to src/pinakes/. | Removes bare-tagging's only real cost using work that is already decided by the user, already unblocked, already assigned, and already item 9 on the build order — not new scope invented to justify a delay. · The gate is red-by-construction if the marker is forgotten, so the tag-time cleanup stops depending on a human. D-35 measured that nothing else can ever catch a stale marker: no `$` in status_header_gate's SHAPE, and none of release_order_gate's seven sequences reads line 3's tail. · Meets this repository's own written threshold for replacing a convention with a gate — the same threshold cited in status_header_gate.py and release_order_gate.py — which the marker has already earned at 0-for-2 on being produced by the procedure. · Ships the same disjoint, CI-green tree as bare tagging, with the same near-zero user risk. · Gives the idle `coder` session a bounded, fully-specified increment instead of leaving layer 2 to be remembered later. | It delays the tag by one coder increment — plausibly hours — and I did NOT measure how long layer 2 takes to build. That is a real reintroduction of exactly the drift this decision is trying to end. · It makes a planner action depend on coder work, requiring coordination with `coder [8b4a28]` and creating a queue behind S2 if that lands first. · The risk it removes is UNMEASURED, not measured-bad. The marker has been written once and removed zero times; the 0-for-2 record is about WRITING the marker during a hold, not about removing it after. I checked this specifically and it weakens my own case. · Strictly more moving parts than tagging bare, in a decision whose user-facing stakes I have shown are near zero either way. · Still ships the bare "Then:" wart, and still ships a fix that reaches almost nobody. | Medium, coder-then-planner. Coder: one gate function plus tests, mutation and adversarial review per the standing increment shape — small but not trivial, and unmeasured by me. Planner: the same ten-to-twenty-minute release sweep, tag, push and artifact verification as bare tagging. The sequencing is flexible: layer 2 before the tag is strictly safer; layer 2 immediately after is acceptable if the planner removes the marker as step 1 of the sweep with attention on it. |
| **Tag 0.30.3 now, bare** | Strip the ⏸ from docs/STATUS.md line 3, append 0.30.3 to the *Published versions* row and the *Published on PyPI* prose in the same commit (the release-order gate's relation requires the row not to lag the prose), `git tag -a v0.30.3`, push, verify the wheel, `gh release view` and PyPI's simple index. Layer 2 goes on the build queue as normal. No src/ change. | Cheapest path that ends the untagged state: about ten to twenty minutes of planner work, no coder involvement, no coordination. · Ships a tree that is code-disjoint from all 15 findings at hunk level — verified exhaustively, not sampled — and that CI went green on at the exact HEAD sha. · Structurally near-reversible: release.yml puts tag/version agreement, build, wheel smoke test, wheel_import_gate and the MCP handshake gate all in front of `uv publish`, so a bad tag costs a deleted tag, not a burned version. · Ends a state that matches a recorded historical failure (v0.1.0 held a CHANGELOG entry and a `__version__` for two days with nothing published). · Keeps 0.30.3 an unambiguous PATCH and leaves the sweep's fixes free to cut their own release. | Walks straight into the one failure D-35 says nothing can see: the ⏸ marker must be removed by hand, and a marker left behind is green in every existing gate, publicly false, and undetectable until layer 2 eventually lands. · The urgency argument carrying it is much weaker than the original brief presented. The user sanctioned this exact state at 12:37 via D-35, and I fetched the live site — the public description is currently ACCURATE. The risk is prospective, not a present harm. · Users get a release that helps almost none of them: verified by execution that `init` refuses on an existing KB and `doctor` has no such check, so the warning reaches only new-KB creation over an already-tracked `.pinakes/`. · Forfeits batching — S2 is built today, S3 and S1 are unstarted, so a user who installs 0.30.3 now installs again shortly. · Ships the bare "Then:" wart in two of the warning's three branches, which I reproduced by running it. | Small and entirely the planner's: one commit to docs/STATUS.md (line 3 marker, *Published versions* row, *Published on PyPI* prose), then tag, push and the artifact verification RELEASING.md requires. Ten to twenty minutes. |
| **Bounded hold — land S2, fold it in, tag today** | Coder runs `./check.sh`, lands the S2 branch via `tools/land.py`, watches CI. Planner splices the fragment and moves the entry down into the untagged `## [0.30.3]` per RELEASING.md's window procedure, settles PATCH-vs-MINOR on the new `doctor` check, then tags. S3, S1 and the rest go to the next release. | Ships the sweep's #1-ranked finding — silent index loss behind a green diagnostic, which the sweep calls this repository's worst recorded shape — in the same release. · Gives the hold a stop condition measured in hours against work that already exists on disk, rather than an open-ended one. · File-disjoint from 0.30.3 (sync.py + doctor.py versus init.py + cli.py), so no merge risk. · The implementer has already categorised it Fixed in the fragment filename, where changelog.d/README.md says the category cannot drift from the content — so the PATCH call is less open than it looks. | The MINOR risk is WORSE than the original brief stated. I checked the precedent it cited and it is wrong: 0.18.0 and 0.17.0 both lead their notes with `pnk doctor` changes, and every doctor-facing addition I mapped (0.2.0, 0.3.0, 0.4.0, 0.14.0, 0.17.0, 0.18.0) shipped in a MINOR. Deciding MINOR means withdrawing 0.30.3's number from CHANGELOG.md, docs/STATUS.md, docs/ROADMAP.md and the CHANGELOG footer links. · The S2 branch has never been through a gate: no `./check.sh` run, no CI run. Tagging the same day compresses exactly the verification the window exists to provide. · Uses the release-window mechanism outside its precondition. That mechanism exists for a defect found in the code BEING released; S2 is in files 0.30.3 does not touch. · Still leaves S1 and S3 live on PyPI, so it shrinks the follow-up release rather than avoiding it. · "Today" is a soft deadline with nothing enforcing it — the same drift that turned 40 minutes into nine hours applies again. · Requires coordination with `coder [8b4a28]` before the planner can act. | Medium, planner-then-coder, plausibly a few hours. Coder: run `./check.sh`, land S2, watch CI. Planner: splice-and-move in its own commit, settle PATCH-vs-MINOR, then the same release sweep and tag. |
| **Open hold until S1, S2 and S3 land** | Leave main untagged. Coder lands S2, then builds S3 and S1 from scratch. Planner splices and moves a fragment at each landing. Tag once all three are in, renumbering if the SemVer call goes MINOR. | One upgrade instead of three or four for every user. · The three highs are the findings that actually cost a user something: no index at all after one unreadable file (S1), a document silently unfindable behind doctor exit 0 (S2), `pnk serve` failing on any pause over ~10 s (S3). · The eventual release notes tell one coherent story about running the tool. | Unbounded by construction, and I verified why: only S2 exists, S3 and S1 are unstarted, and two of the sweep's eight build items (D-36, D-37) are user decisions not yet taken. · Rests on a premise I falsified exhaustively: 0.30.3 shares no line with any of the 15, and all 15 are byte-identical in the wheel PyPI is serving right now. Holding protects nobody from S1, S2 or S3. · Compounds the MINOR/renumber risk across three landings instead of one. · The 0.30.3 heading's timestamp (20260825 08:27) drifts further from the content beneath it with each fold. · Recurring planner cost: a splice-and-move commit per landing, three times. | Large and spread over days, split across both roles. Coder: land S2, then build S3 and S1 from scratch with tests, mutation and adversarial review each. Planner: a splice-and-move commit per landing, then the release sweep and tag. Unbounded in wall-clock. |
| **Deliberate do-nothing — leave the hold, revisit next planner session** | Change nothing today. Record in the plan that the hold is deliberate, has no fix-dependency, and will be revisited at the next planner session. The ⏸ marker stays; line 3 stays truthful; nobody tags. | Genuinely near-costless right now, which nobody established: I fetched the live site and the public description is accurate, and the release being withheld reaches almost no user. · It is the state the user explicitly sanctioned at 12:37 via D-35 — doing nothing is not drift here, it is the decided configuration. · Costs zero minutes of anyone's time and forecloses nothing. · Leaves room for layer 2 to land first on its own schedule, which is the safest tag-time posture. | Has no stop condition, which is the exact defect that made this decision necessary — it converts an unrecorded hold into a recorded one without ending it. · Every extra day is a day matching the shape of a recorded failure (v0.1.0's two days with a CHANGELOG entry, a `__version__`, and nothing published). · Leaves complete work sitting untagged against the standing rule that it never should, and against a repo that cuts corrective PATCH releases 58 minutes apart. · Depends on someone actually revisiting it — a human obligation with nothing scheduling it, in a plans/ directory the 20260825 sweep found riddled with items sitting unworked. · Requires a planner commit to record the decision anyway, so it is not literally free. | Trivial, planner-only: one plan or STATUS note recording that the hold is deliberate and unconditioned. Minutes. |

> ### ⭐ Recommended: **Tag 0.30.3 now, with D-35 layer 2 gating the marker**
>
> The single deciding fact is not the one the original brief named. Code disjointness is real — I verified it exhaustively at hunk level across all fifteen findings rather than sampling one, which is the audit the brief admitted skipping — and it settles the sweep half of the question: holding protects no user from S1, S2 or S3, because all fifteen are byte-identical in the 0.30.2 wheel PyPI is serving. That half dissolves.

What it does NOT settle is what remains, and here the brief argued from a position the user had already overruled. D-35, taken by the user at 12:37, defines `__version__` as *landed* and makes the ⏸ marker required exactly in the state we are in. The untagged tree is not an anomaly to be escaped; it is a sanctioned configuration with a specified enforcement that has not been built. So the reason to act is not urgency — I fetched the live docs page and its description is accurate today — it is that the one step the tag requires of a human is the one step D-35 measured as permanently invisible if skipped: a marker left on line 3 after publishing is green in status_header_gate.py (no `$` in SHAPE) and green in release_order_gate.py (no sequence reads line 3's tail), forever.

Layer 2 fixes that, it is already decided by the user, unblocked, assigned to the coder, and sitting at item 9 of the build order. Built during the hold it is green as-is; at tag time, appending 0.30.3 to the *Published versions* row flips it to marker-forbidden and the gate goes RED if the marker is still there. That converts the only real cost of shipping into a red build. This repository's own doctrine — cited in both gates' docstrings — is that a convention which misses repeatedly becomes a gate, and the marker is 0-for-2.

I want to be explicit about three things that cut against me. First, the risk layer 2 removes is unmeasured: the marker has been written once and removed zero times, so 0-for-2 is a record about writing, not removing — I checked that specifically and it weakens my case. Second, I did not measure how long layer 2 takes to build, so the delay I am recommending is unquantified, and delay is the failure mode this decision exists to end. Third, on user benefit this is close to a coin toss and I am not pretending otherwise: 0.30.3 reaches only someone creating a new KB over an already-tracked `.pinakes/`, verified by running it. If the user wants the simplest defensible thing, "Tag 0.30.3 now, bare" gets the direction right and I would not call it wrong — my objection is to its reasoning and one missing safeguard, not to its conclusion. What I would not do is hold for the sweep: that option rests on a premise I have falsified.

**The first pass recommended _Tag v0.30.3 now_ and the adversary moved it.** The reasoning above is the revised one.

**What would change this.** Five falsifiable tests, in order of force.

(1) SHOW ME THAT LAYER 2 IS NOT SMALL. If the coder estimates layer 2 at more than roughly half a day — for instance because parsing R out of the *Published versions* row proves as fragile as release_order_gate's `within` anchor was, which its own docstring says drops silently when one comma becomes an em dash — then the delay costs more than the risk it buys and I switch to "Tag 0.30.3 now, bare" with the marker removed as step 1 of the sweep, attention on it.

(2) SHOW ME A FINDING INSIDE A 0.30.3 HUNK. New-file init.py 160-207, 302-313, 379-389, 400-407, 426, or cli.py 150-181. I audited all fifteen against those ranges and found none, so this now takes a SIXTEENTH finding rather than a re-read. If one appears, 0.30.3 ships on top of an unfixed defect in its own file and holding becomes right.

(3) SHOW ME `./check.sh` RED ON MAIN. I did not run it — the task is read-only and it writes caches. I relied on CI run 32854256771 against e6d62e33 = HEAD, and I checked that 9 of check.sh's 11 gates have their own ci.yml jobs. If the two uncovered ones, or check.sh's sequencing, turn it red, nothing gets tagged until that is fixed.

(4) SHOW ME THE PLANNER JUDGING S2's NEW `doctor` CHECK A PATCH, with `./check.sh` and a CI run green on that branch. Then "Bounded hold — land S2, fold it in, tag today" gains the sweep's #1 finding at almost no cost and I switch to it. Note this test got HARDER, not easier, once I checked the precedent: 0.18.0 and 0.17.0 both lead with doctor changes in MINOR releases, so the burden is on whoever calls it PATCH.

(5) SHOW ME SOMEONE WAITING ON THE TRACKED-`.pinakes/` WARNING — an issue, a user, a peer. The reach argument I verified by execution is what makes the user benefit near zero on both sides; if it collapses, shipping becomes urgent and bare tagging wins on speed.

**Cost of getting it wrong:** low. **Owner:** planner-then-coder.

**Not established — do not treat these as settled:**

- I did NOT run `./check.sh` or the test suite, on main or on the S2 branch — the task is read-only and both write caches into the repo. Release readiness rests on CI run 32854256771 against e6d62e33 = HEAD, plus my mapping of 9 of check.sh's 11 gates to their own ci.yml jobs. The two unmapped (fragments.py, shared_file_overlap.py) are a splicer and an overlap reporter, but I did not confirm that check.sh's sequencing adds nothing CI misses.
- I verified S4 and 0.30.3's own behaviour BY EXECUTION, and S1/S3 by code reading (sync.py:518-519 `return hash_bytes(path.read_bytes())` with no error handling; store.py:225 `sqlite3.connect(target, uri=not writable)` with no `check_same_thread=False`; serve.py:109/132-137 caching one `_connection`). S2, S5, S6, S7, S8, S9 and the five lows I verified only far enough to locate their FILES for the hunk audit — I did not reproduce them. I am relying on the sweep's own verifier for those, and the sweep records that its verifier returned no verdict once and was nearly buried.
- I did NOT install the published 0.30.2 wheel and diff it against the repo. The claim that all 15 defects are live on PyPI is inferred from `git diff v0.30.2 HEAD -- src` touching only three files — sound, but it assumes the v0.30.2 tag is what was actually uploaded. STATUS.md records that assumption being checked for 0.30.2 by parsing the wheel with `ast`; I did not repeat it.
- I did NOT measure how long D-35 layer 2 takes to build. That is the load-bearing unknown in my own recommendation, and it is the coder's to answer, not mine. D-35's own record that release_order_gate silently drops a sequence when one comma becomes an em dash suggests parsing R out of that table cell may be more fragile than the four-rule spec makes it look.
- Whether S2's new `pnk doctor` check is MINOR or PATCH under this project's SemVer table. The fragment filename says `fixed-`, and changelog.d/README.md says the category cannot drift from the content — but every doctor-facing addition I mapped shipped in a MINOR. It is a planner call and I did not take it.
- I did NOT verify that the docs site would redeploy correctly after a tag-time STATUS.md edit, nor run `make docs` (`mkdocs build --strict`). The docs workflow is path-filtered on docs/**, so a STATUS.md change does trigger it, but I did not exercise the build.
- I reproduced the brief's unreported stray observation and it survives: `pnk doctor` run in a KB at `t1/sub`, INSIDE a git repo rooted at `t1`, prints `WARN git hooks: not a git repository`. Two independent runs now show it. It may be deliberate (hooks want the KB at the repo root) or a sixteenth finding. It has no bearing on this decision; I record it because I saw it and because two agents have now seen it and neither chased it.
- I did not look at PyPI download figures and nobody should use them to size this — they cannot separate humans from mirrors and scrapers. My reach argument rests on executed behaviour (`init` refuses on an existing KB; `doctor` has no such check), not on usage.
- The stale docstring in tools/status_header_gate.py ("the invariant holds with no exception window") is planner-owned drift I found while nearly arguing from it. I did not check whether the same superseded claim appears elsewhere in docs/ after D-35 — a neighbourhood audit is owed and I did not run it.

---

## D-31 / D-32 / D-33 — a recurring `.pinakes/` exposure check in `pnk doctor`

**The question.** Should `pnk doctor` carry a recurring `.pinakes/` exposure check — and if so, does it ask the tracked question, the ignored question, or both; is a finding a WARN or an OK-with-a-note; and does it also say when protection is local to this checkout only?

**Status of the argument.** Genuinely undecided. D-31/D-32/D-33 sit in `plans/20260825_0749-exposure-and-silent-status.md:202-266` as three `✅ Recommendation:` blocks with no user answer; `plans/20260825_1252-plans-sweep-findings.md:55` classifies the blocking open-corrections item **BLOCKED** on exactly those three and marks it `user-decision`. They block rows 5 and 6 of that plan's build order (rows 2-4 and 7 are now BUILT, so the sweep's "rows 2-6" at line 561 is stale; the brief's "rows 5 and 6" is right).

**I am UPHOLDING the original brief's substance and OVERTURNING its headline mechanism.** Its conclusion — doctor carries the check, asks both of X1's questions, WARN only where a printed command clears it, FAIL ruled out, D-33 stays at A — survives everything I threw at it. Its one original design contribution, "ask `git ls-files` first and skip the ignore question when it returns `None`", is a **regression** and I recommend against it.

**What I verified myself, not from the brief.** I re-ran every measurement. Reproduced M1/M2 in a purpose-built repo (`ignored=True`, `tracked=True` simultaneously). Ran `pnk doctor` on a nested KB: 33 rows, 5 WARN, 2.32-2.40 s, and the false `WARN git hooks: not a git repository`. Read `_ask_git`, `_all_probes_ignored`, `_ignored_by_git`, `_index_pathspec`, `_tracked_by_git`, `init()`'s adopted gate, `cli.py:134-179`, `doctor.py`'s `_hooks`, `run_doctor`, `ci.py`'s WORKFLOW, `hooks.py`, `sync._git`/`_staged_paths`, D-31/32/33 whole, the open-corrections `## Closed` table.

**Three things the brief got wrong, all checked against the file.** (1) Its ls-files-first refinement silently downgrades a WARN to OK on a loose folder — measured side by side, on the same KB where `pnk init` had just printed that warning. (2) Its claim that `git check-ignore -v` breaks `_all_probes_ignored`'s parse is **false** — I ran the function's own logic with and without `-v` and got identical verdicts, so option D is cheaper than the brief costed it. (3) Its DESIGN.md quotation is **fabricated**: "stated rather than mitigated" appears nowhere in `docs/DESIGN.md`; it is in `plans/20260804_1844-decision-parent-child-arity.md:72`, about edge arity. The real `DESIGN.md:1367` says exposure *is* "Mitigated by shipped `.gitignore`" — a claim M1 falsifies, which makes it stale planner-owned rationale the brief left off its doc-debt list.

**One argument the brief missed that strengthens its own case.** `docs/INVARIANTS.md:22` makes "a deep-run transcript … never leaves `.pinakes/`" an **invariant**, justified by "the state that is already gitignored". M1 shows that premise can be false while `_ignored_by_git` says `True`, and nothing in the repo checks it. Per CLAUDE.md, invariants are the list of things that "fail *silently* when broken". That is the strongest single argument against option A, and it is not in the brief.

**Two of the brief's admitted unknowns I closed, both in its favour.** A 100 001-entry git index costs 10.7-11.1 ms (`ls-files`) and 13.1-13.9 ms (`check-ignore`) — its own #1 falsifier was a 300 ms threshold. And a linked worktree returns `tracked=True`, a submodule `tracked=False` — no silent degradation to "not checked" inside an ordinary repository.

### What was established

- **`pnk doctor` reports nothing about `.pinakes/` exposure and runs no subprocess at all. A full run on a synced 30-document KB prints 33 rows, 5 of them WARN, in 2.32-2.40 s wall.**
  - *Source:* `grep -n subprocess src/pinakes/doctor.py` → no output. Ran `uv run pnk doctor --kb <scratch copy of tests/demo-kb>`; counted 33 status rows; `/usr/bin/time -p` twice → real 2.40, real 2.32.
- **M1/M2 reproduced independently. Built `repo/` with `kb/.pinakes/{deep/op1.json,index.db,index.db-shm,index.db-wal}` committed, then added a root `.gitignore` holding `.pinakes/`. `git ls-files -- kb/.pinakes` returns 4 rows. The shipped detectors report `_ignored_by_git → True` and `_tracked_by_git → True` at the same time.**
  - *Source:* Scratch repo under the scratchpad; `uv run python -c "from pinakes.init import _ignored_by_git, _tracked_by_git; ..."`
- **`docs/INVARIANTS.md:22` states as an invariant that "A deep-run transcript is KB-local and never leaves `.pinakes/`", justifying it as "it lives with the state that is already gitignored". M1 falsifies that premise — `.pinakes/` can be in git's index while `check-ignore` says ignored — and nothing in the repo checks it. CLAUDE.md: invariants "fail *silently* when broken".**
  - *Source:* `grep -n "gitignore\|exposure" docs/INVARIANTS.md` → line 22; the brief never cites INVARIANTS.md.
- **The brief's DESIGN.md quotation is fabricated. `grep -rn "stated rather than mitigated" docs/ plans/` matches only `plans/20260804_1844-decision-parent-child-arity.md:72`, about parent-child edge arity. `docs/DESIGN.md:1367` actually reads: "Accidental publication | … Mitigated by shipped `.gitignore`, an index/ledger that never leaves the machine, and explicit docs — not by anything the engine can enforce". M1 falsifies "mitigated by shipped `.gitignore`", so that row is stale planner-owned rationale and belongs in the doc-debt list.**
  - *Source:* `grep -rn "stated rather than mitigated" docs/ plans/`; `sed -n '1363,1370p' docs/DESIGN.md`
- **DECISIVE AGAINST THE BRIEF'S REFINEMENT. `pnk init` on a loose folder carrying a `.gitignore` of `*.log`/`node_modules/` prints "⚠️ your .gitignore does not ignore `.pinakes/`". On that same KB, the brief's ls-files-first ordering yields `_tracked_by_git → None` and prints **OK — not checked** in 7.6 ms; the plan's unconditional ordering yields `_ignored_by_git → False` and prints **WARN — not ignored** in 42.0 ms. Same KB, same machine, minutes apart: init warns, the recommended doctor is silent.**
  - *Source:* `uv run pnk init <scratch>/loosekb` (output quoted above); then a harness running both orderings over four roots side by side.
- **The refinement's only behavioural effect is that case. When `_tracked_by_git` is `True` the plan still needs the ignore answer (it orders the remedy ignore-line-first when the rule is also missing); when `False` it needs it for the verdict. So the ordering skips a call only when `tracked is None` — exactly the not-in-a-repo state where the ignore answer is the only answer available. Its entire benefit is its entire cost.**
  - *Source:* `plans/20260825_0749-...md:221-243` (D-32's four-row table); measured verdict/timing matrix across `loosekb`, `repo/kb`, `outer/mod/kb`, `tests/demo-kb`.
- **The refinement also contradicts the plan's own stated intent. D-32's table gives the "**not ignored**, not tracked → `WARN`" row the reason "Same as `init` reports today." The refinement makes doctor differ from init on precisely that row.**
  - *Source:* `sed -n '221,243p' plans/20260825_0749-exposure-and-silent-status.md`
- **The brief's cost claim against option D is false. `git check-ignore -v` does NOT break `_all_probes_ignored`'s parse: the function counts distinct non-empty stdout lines against `len(probes)`, and `-v` still emits exactly one line per matched probe. Ran the function's own logic with and without `-v` on two repos: identical verdicts (4/4 → True; 0/4 → False) in both directions.**
  - *Source:* `uv run python` harness calling `_ask_git(["check-ignore", *flags, *probes], cwd)` and applying `_all_probes_ignored`'s exact set/len logic.
- **D-33's mechanism verified across all four sources. `.git/info/exclude:7:kb/.pinakes/`; a committed `.gitignore:1:.pinakes/` (ls-files → 1 row); an **untracked** `.gitignore:1:.pinakes/` — byte-identical shape (ls-files → 0 rows); `core.excludesFile` as an absolute path. Also: with cwd at `sub/kb`, the source column is relative to the repo top-level while the pathname column is relative to cwd — two bases in one line.**
  - *Source:* Four scratch repos `ci/{a,b,c,d}` exercising each source, with `git ls-files -- .gitignore` as the tracked-ness oracle.
- **CLOSES THE BRIEF'S #1 FALSIFIER, in its favour. On a synthetic 100 001-entry git index, `_tracked_by_git` = 10.7-11.1 ms and `_ignored_by_git` = 13.1-13.9 ms over four runs. The brief's stated threshold for weakening the cost argument was ~300 ms.**
  - *Source:* Built `big/` with 100 000 files across 200 directories plus `kb/.pinakes/index.db`, `git add -A -f`; timed both detectors four times.
- **CLOSES THE BRIEF'S UNVERIFIED ITEM (3). A linked worktree returns `tracked=True` (7.8 ms); a git submodule returns `tracked=False`, `ignored=False` (7.7 / 7.5 ms). Neither degrades to `None` inside an ordinary repository. `tracked=None` occurs only outside a repo (6.7 ms).**
  - *Source:* `git worktree add`; `git -c protocol.file.allow=always submodule add`; harness over five roots.
- **UNSTATED COST OF EVERY OPTION THAT ADDS THE ROW. `doctor._hooks` decides git-ness with `(manifest.root / ".git").exists()` — a directory-local filesystem test. On `repo2/kb` it prints `WARN git hooks: not a git repository → …a loose folder needs manual or cron pnk sync`, while a repo-aware exposure row in the same report would say `.pinakes/` is tracked by git. Adding the check makes `pnk doctor` print two flatly contradictory rows in one screen.**
  - *Source:* `src/pinakes/doctor.py:1350-1358`; `src/pinakes/hooks.py:85-102`; the 33-row run on `repo2/kb`.
- **UNSTATED COST OF THE UNCONDITIONAL ORDERING. pytest temp dirs are not repositories (`/var/folders/...` → `tracked=None`), so under the unconditional ordering every `diagnose()` in the suite pays `_ignored_by_git`'s temp-repo fallback: ~36.2 ms vs ~6.7 ms, a ~29.5 ms delta. `tests/test_doctor.py` has 92 `checks(kb…)` call sites plus 23 `diagnose(` across `tests/`, and `checks()` calls `diagnose`. Derived: ~115 × 29.5 ms ≈ 3.4 s added to the suite.**
  - *Source:* `tempfile.TemporaryDirectory()` + `_tracked_by_git`; `grep -c "checks(kb" tests/test_doctor.py` → 92; `grep -rn "diagnose(" tests/*.py | wc -l` → 23; `tests/test_doctor.py:82-84`.
- **MISSING OPTION, verified mechanism. `sync._staged_paths` already runs `git diff --cached --name-only --diff-filter=ACMR`, and the `pre-commit` hook is `sync --sidecars-only --stage --quiet --extract=pypdfium2`. A staged-`.pinakes/` check there costs zero additional subprocesses and fires at the exact instant of the mistake. Its fatal limit: `hooks_dir(root)` raises unless `root/.git` exists, so pinakes hooks cannot exist for a nested KB — the case this decision is mostly about — and hook doctrine is exit 0, never fail the commit.**
  - *Source:* `src/pinakes/sync.py:1608-1612`; `src/pinakes/hooks.py:51-55, 85-102`; `SCRIPT` comment "Exits 0 even when pnk is missing".
- **The exposure is caught while it is still free to fix: `git add -f .pinakes` with **no commit** yields `_tracked_by_git → True`. A WARN fires before the commit that publishes anything.**
  - *Source:* Scratch repo `staged/`: `git add -f .pinakes`, then `_tracked_by_git` → True.
- **The payload is verified. `src/pinakes/deep/transcript.py:183` writes `"question": question` into `.pinakes/deep/<operation_id>.json`, and the module docstring at line 13 claims "**KB-local, and it never leaves `.pinakes/`.**" Separately, `chunks.text` holds verbatim document text: a read-only query returned `'# Access restrictions\n\nRestrictions are applied at item level and reviewed annually. A res…'`.**
  - *Source:* `sed -n '175,190p' src/pinakes/deep/transcript.py`; `sqlite3 "file:…/index.db?mode=ro" "select substr(text,1,90) from chunks limit 1;"`
- **`pnk init` structurally cannot re-ask, and its ignore check is narrower than the brief says: `_check_target` raises `InitError(f"{root} is already a KB.")` on an existing `pinakes.toml`, and the ignore verdict runs only `if gitignore in adopted` — i.e. only when a `.gitignore` already sat in the KB root. When `init` writes its own, it is never re-examined. `pinakes_tracked` sits outside that gate, deliberately.**
  - *Source:* `src/pinakes/init.py:373-406, 430-436`, including the comment "The ignore verdict is only for a `.gitignore` that was already here" and `test_a_gitignore_written_by_init_is_not_re_examined`.
- **D-33's third local-only source confirmed: `pnk init` writes `.gitignore` with `gitignore.write_text(GITIGNORE, encoding="utf-8")` and never stages it, so a freshly-initialised KB inside a repo is protected only locally until the user commits. The repository is its own instance of the durable case: `.git/info/exclude:18` holds `RESUME.md`, and `git check-ignore -v RESUME.md` prints `.git/info/exclude:18:RESUME.md`.**
  - *Source:* `src/pinakes/init.py:377`; `cat -n .git/info/exclude`; `git check-ignore -v RESUME.md`.
- **FAIL is ruled out by mechanism. `run_doctor` returns `EXIT_FAILURE if report.worst is Status.FAIL else EXIT_OK`, and `ci.py`'s generated `WORKFLOW` runs `pnk doctor` as its `Health check` step under `on: push: branches: [main]`. `doctor.py:1276` already records "**Nothing here is FAIL.**"**
  - *Source:* `src/pinakes/cli.py:1074-1094`; `src/pinakes/ci.py:27-57`; `src/pinakes/doctor.py:1276`.
- **D-32's precedent is real and is a user decision, and there are two more beside it in the same table. Heading coverage: "WARNed forever on `code` and `pdf` … a remedy amounting to *a limit of the tool*" → narrowed "20260805 21:56, as the user decided", everything else "OK with a note". Also D-15 (cold cache → "OK with a note rather than WARN, since nothing is broken") and `titles` ("**Always OK, never a warning**, and that is the decision rather than timidity").**
  - *Source:* `plans/20260731_1202-open-corrections.md` `## Closed` table (heading-coverage row, D-15 row, titles row).
- **NO DOGFOOD FALSE POSITIVE. The repository's own two corpora report clean under the proposed check: `tests/demo-kb` and `tests/partner-kb` both give `tracked=False`, `ignored=True` → a single OK row. Neither has its own `.gitignore`; the root `.gitignore:2` is `.pinakes/`.**
  - *Source:* Harness over both paths; `grep -n pinakes .gitignore`.
- **The helper-home question is genuinely undecided and there is no shared module. `src/pinakes/` has no git module; `doctor.py` does not import `init`; the only importers of `init` anywhere are `cli.py:114` (`init` itself, lazily) and `tools/template_drift_gate.py:68` (two constants). The two existing helpers differ in hardening: `init._ask_git` (5 s timeout, scrubbed `GIT_*`, `stdin=DEVNULL`, `errors="replace"`) vs `sync._git` (`check=True`, no timeout, strict `text=True`, raises `SyncError`). Adding a git subprocess does not touch `.paid-path-allowlist`, which gates paid-client imports only.**
  - *Source:* `ls src/pinakes/*.py`; `grep -rn "from pinakes.init import" src/ tools/`; `src/pinakes/init.py:81-104`; `src/pinakes/sync.py:1613-1631`; `cat .paid-path-allowlist`.
- **The gate a new check must pass exists, and the fixture it needs does not. `tests/test_doctor.py::test_every_doctor_check_is_exercised_by_a_test` (line 1010) asserts every name `diagnose` can produce is named in that file, with a `conditional` escape map at 1028. But `tests/test_doctor.py` fakes git with `(kb / ".git").mkdir()` at line 699, while `tests/test_init.py` already drives real `git init`. `docs/VERIFICATION.md:579` opens "`pnk doctor`, check by check"; `docs/STATUS.md:22` lists doctor's surface without any git/exposure term.**
  - *Source:* `tests/test_doctor.py:699, 1010-1040`; `sed -n '575,600p' docs/VERIFICATION.md`; `sed -n '20,24p' docs/STATUS.md`.
- **MINOR CORRECTION to the brief's framing. It repeats CLAUDE.md's "everything remaining there is a stop, not a task" for the exposure plan. Build-order row 9 is `**X7** — line 3's three layers (D-35 **answered** 20260825 12:37) | **nothing — unblocked** | coder`. So unblocked coder work does remain in that file. It does not bear on this decision.**
  - *Source:* `grep -n "X7" plans/20260825_0749-exposure-and-silent-status.md`; line 553.

### The options

| Option | What it means | Pros | Cons | Work |
|---|---|---|---|---|
| **A — leave it at `pnk init` (status quo)** | Nothing changes. `src/pinakes/doctor.py` is untouched; the only exposure check stays X1's, in `src/pinakes/init.py`, printed once at creation time by `src/pinakes/cli.py:134-179`. D-32 and D-33 become moot. The planner records the answer and closes the open-corrections item as *decided: no*. | Zero new code, tests, doc debt or runtime cost. `doctor.py` keeps its property of running no subprocess at all — no timeout, no scrubbed environment, no third git convention. · No new row can nag, and doctor's WARN budget stays put (5 of 33 on a healthy demo KB). · No self-contradiction: doctor's directory-local git model (`(root/'.git').exists()`) stays internally consistent because nothing repo-aware sits beside it. · The `init` warning still fires when the user is thinking about setup, which is when a remedy is most likely acted on. It also covers the loose-folder case, which every doctor-side option handles less well or not at all. | It leaves a measured hazard permanently unreported. Verified: a KB whose `.pinakes/deep/op1.json` and `index.db-wal` are in git's index prints 33 doctor rows, none about it — and doctor's one git-shaped row, `WARN git hooks: not a git repository`, is false for that KB. · **It leaves a documented invariant unchecked.** `docs/INVARIANTS.md:22` promises a deep-run transcript "never leaves `.pinakes/`" *because* that state "is already gitignored". M1 shows the premise can be false while `_ignored_by_git` returns `True`. Invariants are the repo's list of things that fail silently; this one has no check. · The failure is silent and unrecoverable — a push cannot be taken back, and the payload is verbatim questions (`transcript.py:183`) plus verbatim document text (`chunks.text`). · `init` cannot cover it even in principle: `_check_target` refuses on an existing `pinakes.toml`, and `.pinakes/` does not exist when `init` runs. Its ignore check is narrower still — it runs only when a `.gitignore` was already present (`if gitignore in adopted`). · It leaves `docs/DESIGN.md:1367` asserting the exposure is "Mitigated by shipped `.gitignore`", which M1 falsifies. Choosing A does not remove that doc debt; it just leaves it unowned. | None for the coder. Planner: record the decision, close the open-corrections item, and still fix `docs/DESIGN.md:1367`. ~1 hour. |
| **B — `doctor` asks the tracked question only** | One new `Check` in `src/pinakes/doctor.py` calling `_tracked_by_git(manifest.root)` and nothing else. Three rows: tracked → WARN with `git rm -r --cached <abs>` plus commit; not tracked → OK; git did not answer → OK with "not checked". No `check-ignore`, no temp repo. | Cheapest form that covers the unrecoverable case: one subprocess, measured 6.7-11.1 ms (including a 100k-entry index) against a 2.3-2.4 s run. · Covers the one question `init` is structurally incapable of asking, and fires on staged-but-uncommitted (measured `True`), so it lands before the publishing commit. · No temp directory and no `git init` side effect, so doctor's "Nothing here changes anything" docstring needs no reinterpretation. · Smallest test-suite impact: ~7 ms × ~115 `diagnose()` call sites ≈ 0.8 s, versus ~3.4 s for the unconditional both-questions form. | It reproduces X1's own defect one layer up. An `OK` row that answered only the index question is silent for a repo whose `.gitignore` stopped covering `.pinakes/` after `init` ran, and reads as reassurance about exposure generally. · The ignore question genuinely goes stale after `init` — a rewritten `.gitignore`, an added `!.pinakes/`, a KB moved under a repo whose rules do not cover it — and nothing re-asks it until a `git add` has already put files in the index. · It is silent on the submodule case I measured (`tracked=False`, `ignored=False`) and on the loose-folder case, both of which are real WARN states. · The saving over the unconditional form is one subprocess, ~7-14 ms of a 2.3 s run. The cost argument for stopping at one question is weak on the numbers. · It still needs the full apparatus: a new name gated by `test_every_doctor_check_is_exercised_by_a_test`, real git fixtures new to `tests/test_doctor.py`, VERIFICATION rows, `docs/STATUS.md:22`. · It still creates the `_hooks` contradiction — a repo-aware row beside `WARN git hooks: not a git repository`. | Coder: ~40 lines plus docstring in `doctor.py`, the helper-home decision, 4-5 tests on real `git init` fixtures (pattern exists in `tests/test_init.py:965-1090`), VERIFICATION rows. Planner: the printed remedy text, `docs/STATUS.md:22`, the `pnk doctor, check by check` section, `docs/DESIGN.md:1367`. Half a day. |
| **C — `doctor` asks both questions, unconditionally (the plan's own D-31/D-32 recommendation)** ⭐ | One new `Check` in `src/pinakes/doctor.py` calling both `_tracked_by_git(manifest.root)` and `_ignored_by_git(manifest.root, manifest.root/'.gitignore')`, with no ordering shortcut. Exactly D-32's four rows: tracked → WARN (`git rm -r --cached <abs>` then commit, ignore-line-first when the rule is also missing); not ignored and not tracked → WARN (add `.pinakes/`); ignored and not tracked → OK; git did not answer → OK with "not checked". D-33 stays at A — one notion of "protected", no source detail. In the same change, `_hooks`' "not a git repository" wording is corrected so the report does not contradict itself. | Answers both halves, so no `OK` row is true-about-the-index and read as reassurance about exposure. This is the difference from B. · Measured 13.1-16 ms inside a repository — 0.6% of a 2.32-2.40 s run — and 25 ms even against a 100 001-entry index, well inside any plausible budget. · **Matches `pnk init` on the same KB.** D-32's own table gives the not-ignored row the reason "Same as `init` reports today", and I measured the loose-folder case where the ls-files-first variant does not: init WARNs, C WARNs, the variant says OK. · Both functions exist, are hardened (`_ask_git`: 5 s timeout, scrubbed `GIT_*`, `stdin=DEVNULL`, `errors="replace"`) and are heavily tested in `tests/test_init.py`. This is wiring, not new logic. · Correct on every layout I measured: nested KB, linked worktree, submodule, loose folder, and both of the repo's own corpora (which report a clean single OK — no dogfood false positive). · It gives `docs/INVARIANTS.md:22`'s promise its first and only check. · A WARN cannot redden the workflow `pnk init --ci` generates, because `run_doctor` exits non-zero only on FAIL. · Reversible: if the row proves noisy, deleting or downgrading it is a patch. | **It gives `doctor` its first subprocess ever, and two of them.** Worst case is `GIT_TIMEOUT_SECONDS = 5` per call — up to ~10 s added behind a wedged `core.fsmonitor`/Watchman, which `init.py`'s own docstring records as a real hang. I read the constant; I did not reproduce a hang. · **On a loose folder it pays ~36 ms and spawns `git init` in a temp directory** to answer the ignore question. That is 1.5% of a run, and it is a write outside the KB from a command whose docstring says "Nothing here changes anything" (the KB is untouched, so this is a reading, not a violation). · **Test-suite cost, derived not measured: ~3.4 s.** pytest temp dirs are not repositories, so every `diagnose()` takes the temp-repo fallback: ~29.5 ms × ~115 call sites. B or the ls-files-first variant avoids most of it. I did not measure the suite's total runtime, so I cannot say what fraction that is. · **It needs a home for the two git questions and none is decided.** Importing `init`'s privates into `doctor.py` is cheap and ugly; extracting a shared module is clean and touches `init.py`, which carries ~1100 lines of tests. Nothing in `plans/` or `docs/` decides it, and `src/` already carries two unrelated git helpers with different hardening. · **It forces the `_hooks` fix into scope**, or ships a self-contradictory report. I verified both rows fire on the same nested KB. That is planner-owned printed text plus a coder change, and the brief costed neither. · One more row in a 33-row report on a command that already shows 5 WARNs on a healthy demo KB. The dilution is real, though that 5-of-33 ratio is one synthetic corpus with a deliberately unresolvable linked KB and an out-of-date template. · `tests/test_doctor.py` has no real git fixture today (it `mkdir`s a fake `.git` at :699), so this introduces a new fixture pattern into that file. · It does not surface D-33's fact: a KB protected only by `.git/info/exclude` still reads as a plain `OK`. | Coder: ~60-70 lines with docstring in `doctor.py`, the helper-home decision once the planner answers it, ~6-7 tests on real `git init` fixtures covering nested/worktree/submodule/loose, VERIFICATION rows for each, plus the `_hooks` wording change. Planner: the four rows' printed content, the `_hooks` replacement wording, `docs/STATUS.md:22`, `docs/VERIFICATION.md` § *`pnk doctor`, check by check*, `docs/DESIGN.md:1367`, possibly `docs/GUIDE.md`. One day. |
| **C-lsfirst — both questions, `ls-files` first (the original brief's recommendation)** | As C, but `_ignored_by_git` is called only when `_tracked_by_git` returned a real answer. When it returns `None` — which happens if and only if the KB is not inside a git repository — the row prints "OK — not checked" and the ignore question is never asked. | Avoids the temp-repo fallback entirely: 7.6 ms and one subprocess on a loose folder, versus 42.0 ms and a `git init` in a temp directory. Measured. · Cuts the derived test-suite cost from ~3.4 s to ~0.8 s, because pytest temp dirs are exactly the `tracked is None` case. · Removes the worst-case double timeout on a machine with a wedged fsmonitor: one 5 s call rather than two. · Identical to C on every in-repository layout I measured — nested KB, linked worktree, submodule, and the repo's own corpora all produce the same verdict. | **It converts a WARN into a silent OK on the one state where the ignore question is the only question available.** Measured on the same KB, minutes apart: `pnk init` prints "⚠️ your .gitignore does not ignore `.pinakes/`"; this variant prints "OK — not checked". That is the same true-about-the-wrong-question `OK` used to reject option B, reproduced one case over. · **Its entire benefit is its entire cost.** `tracked is None` means "not in a repository", and that is the only branch where the shortcut changes anything — in both `True` and `False` the ignore answer is needed anyway for the verdict or the remedy ordering. · **It contradicts the plan's stated reason for that row**, which is "Same as `init` reports today" (D-32's table). Choosing it silently amends D-32 rather than answering it. · The loose folder is not an exotic state — "make a KB in a folder, `git init` later" is the ordinary path to the exposure this whole plan is about, and it is the one moment when the fix is a single line and costs nothing. · It buys ~34 ms of a 2 400 ms command. The trade is a real diagnostic for 1.4% of one run. | Identical to C, minus the loose-folder tests it makes unnecessary. Coder: ~60 lines, ~5-6 tests, VERIFICATION rows. Planner: same doc set. One day. |
| **D — C plus D-33's local-only-ignore-source note (the plan's full recommendation)** | Everything in C, plus: when protection comes only from an uncommitted source, the `OK` row says so — `ok — ignored via .git/info/exclude (local to this checkout)`. Requires `git check-ignore -v` and reading its source column. | Names a real exposure invisible today: protected-for-me, exposed-for-every-collaborator who clones. The repository is its own instance — `.git/info/exclude:18` holds `RESUME.md`, invisible to every other checkout. · **Cheaper than the brief claimed.** I verified `-v` does *not* break `_all_probes_ignored`: it emits one line per matched probe either way, and the function's length check returns identical verdicts with and without the flag. Extracting the source is a TAB split, not a rewrite of a matrix-pinned parser. · Mechanically available and measured for all three named sources: `.git/info/exclude:7:kb/.pinakes/`, `.gitignore:1:.pinakes/`, and an absolute path for `core.excludesFile`. · No new severity and no new verdict — a detail on an existing `OK` row, so it cannot become an unclearable WARN. · Runtime cost is a flag on a subprocess C already runs, not an extra process. | **It catches two of three local-only sources.** An untracked `.gitignore` is reported by `-v` identically to a committed one (measured: same line shape, `ls-files` 0 rows vs 1), and `pnk init` writes `.gitignore` without staging it. So a freshly-initialised KB — protected only locally, in exactly D-33's sense — gets no note at all. The row meant to end an invisibility is systematically blind in its commonest instance. (This is a missed note, not a false statement: the recommended text prints a source only for the sources it recognises.) · Closing that hole means testing whether the source itself is tracked, which needs `git rev-parse --show-toplevel` — the source column is relative to the top level while the pathname column is relative to cwd, measured — plus a `git ls-files -- <source>`. Two more calls, and then the note fires on nearly every fresh KB until the user commits `.gitignore`. · `core.excludesFile` for a solo user is noise by the plan's own admission, and nothing decides what the row should say there. · It adds a second concept — "protected here" vs "protected for everyone" — to a report the user reads quickly. · The plan itself offers the exit: "If this is judged scope creep, cut it and keep A; D-31 and D-32 do not depend on it." · Everything in C's cons still applies. | C's work, plus: the `-v` source extraction, an optional 2 extra git calls if source tracked-ness is checked, 3-4 tests covering `.git/info/exclude`, `core.excludesFile` and an untracked `.gitignore`, and a planner answer for what the note says when the source is an uncommitted `.gitignore`. Adds roughly half a day and one open sub-question. |
| **E — C, but a tracked `.pinakes/` is FAIL rather than WARN** | Identical to C except `Status.FAIL` on the tracked row, so `pnk doctor` exits 1. | The strongest possible signal for the most serious thing doctor can find — verbatim questions and full document text republished on every commit. · Cannot be scrolled past: a non-zero exit is visible to scripts and to CI. | **It breaks the user's own CI.** `src/pinakes/ci.py`'s generated workflow runs `pnk doctor` as its `Health check` step under `on: push: branches: [main]`, and `run_doctor` returns `EXIT_FAILURE` only on FAIL. Every push to an affected repo goes red until the user untracks — and a red build caused by a diagnostic is how people delete the diagnostic. · It breaks the file's own convention. FAIL in `doctor.py` means the KB does not work (missing FTS5, unreadable sidecar, duplicate ids, model mismatch); `doctor.py:1276` already states "Nothing here is FAIL." An exposed KB works perfectly. · The remedy is not always immediately available — a shared repo may need a coordinated commit — so FAIL can persist for days on a tree nobody can fix that hour. · Everything in C's cons still applies. | Same as C. A one-line severity difference; the cost is entirely downstream. |
| **F — catch it at commit time in the pre-commit hook instead of (or as well as) `doctor`** | `src/pinakes/sync.py`'s `--stage` path already runs `git diff --cached --name-only --diff-filter=ACMR` in `_staged_paths`. Add a check there (or in the `pre-commit` hook wrapper) that prints a warning when any staged path falls under the KB's `.pinakes/`. No new subprocess. Fires at the instant of the mistake, before the commit exists. | Zero marginal git cost — the staged-path query already runs on the hot path it would use. · Best possible timing: the warning lands on the commit that would publish, when `git restore --staged` still fixes it for free. · Precise: it names the staged paths rather than answering a yes/no about the index. · Complements rather than competes — it could ship beside any of A-D. | **It cannot cover the case this decision is mostly about.** `hooks_dir(root)` raises unless `root/.git` exists, so a KB at `repo/kb` — the nested docs-KB where the exposure is most likely — can never have pinakes hooks at all. · Hooks are opt-in (`pnk install-hooks`); a user who never installed them gets nothing. · The hook runs `sync … --quiet`, and hook doctrine is explicitly "exits 0 even when pnk is missing … a hook that fails every commit only teaches you to use `--no-verify`". So the warning must bypass `--quiet` without ever failing the commit — fiddly, and easy to make either invisible or hated. · It catches the *new* mistake, never the *existing* one: a KB already committed with `.pinakes/` tracked never re-stages those files, so this stays silent forever on M1's own reproduction. · It is not what D-31 asks. Choosing it alone answers a different question and leaves the open-corrections item open. | Coder: ~25 lines in `sync.py` plus the quiet-bypass decision, 3-4 tests. Planner: the printed text and a `docs/CLI.md`/`docs/GUIDE.md` line. Half a day — but only as an addition to a doctor-side answer, not a substitute for one. |

> ### ⭐ Recommended: **C — `doctor` asks both questions, unconditionally (the plan's own D-31/D-32 recommendation)**
>
> **The single deciding fact is one the original brief did not cite: `docs/INVARIANTS.md:22` promises that a deep-run transcript "never leaves `.pinakes/`", and justifies it with "it lives with the state that is already gitignored" — a premise M1 falsifies, with nothing anywhere that checks it.** CLAUDE.md's own framing is that invariants are the list of things that "fail *silently* when broken, which is why it is a list and not a convention". I reproduced the break: a KB reading `ignored=True` and `tracked=True` at the same instant, with `deep/op1.json` in git's index, and `pnk doctor` printing 33 rows about it and not one that mentions it. That converts this from "should doctor gain a nice-to-have row" into "the only recurring surface that can detect a broken invariant". Option A's error is silent, permanent and unrecoverable; C's error is one row someone changes in a patch.

**Against B**, asking only the index question produces an `OK` that is true about the index and reads as reassurance about exposure — the same shape X1 was built to remove — and the saving is one subprocess of ~7-14 ms on a 2 320-2 400 ms command. I measured B silent on the submodule case (`tracked=False`, `ignored=False`) that is a real WARN state.

**Against C-lsfirst, which is what I am overturning.** The brief's own design contribution converts a WARN into a silent `OK` on a loose folder — and I established by running both that this is the exact KB on which `pnk init` had just printed "⚠️ your .gitignore does not ignore `.pinakes/`". Worse, the shortcut's only behavioural effect *is* that case: `tracked is None` means "not in a repository", and in both other branches the ignore answer is needed anyway. Its entire benefit is its entire cost. And D-32's table gives that very row the reason "Same as `init` reports today" — so the shortcut silently amends the decision instead of answering it. It buys ~34 ms of a 2 400 ms run and a derived ~2.6 s of test-suite time. That is not worth a diagnostic that goes quiet at the one moment the fix is one line.

**Against D**, `check-ignore -v` reports an untracked `.gitignore` identically to a committed one, and `pnk init` writes `.gitignore` without staging it — so D's note is blind in the commonest instance of the thing it exists to reveal. I *correct* the brief here: `-v` does **not** break `_all_probes_ignored`'s parse (verified, identical verdicts with and without), so D is cheaper than it was costed. The objection that survives is coverage, not cost. D-33 should stay at **A** and become its own item once the uncommitted-`.gitignore` case has an answer.

**Against E**, FAIL is ruled out by mechanism: `run_doctor` exits non-zero only on FAIL and `ci.py`'s generated workflow runs `pnk doctor` on every push to `main`. **Against F**, `hooks_dir` requires `root/.git`, so hooks cannot exist for a nested KB — F is a good addition and a bad substitute.

**Two things I would add to the increment that the brief did not cost.** First, `doctor._hooks` decides git-ness with `(root/'.git').exists()`, so a repo-aware exposure row would print beside `WARN git hooks: not a git repository` on the same nested KB — I saw both. Fix that wording in the same change or ship a self-contradictory report. Second, `docs/DESIGN.md:1367` still says the exposure is "Mitigated by shipped `.gitignore`", which M1 falsifies; it belongs on the doc-debt list whichever option is chosen, including A.

**On D-32 the precedent is decisive and is already the user's**: the heading-coverage narrowing of 20260805 21:56, plus D-15 and `titles` in the same table. WARN only where a printed command clears it; OK-with-a-note when git could not answer.

**Ownership splits.** The four rows' printed content, the `_hooks` replacement wording, `docs/STATUS.md:22`, `docs/VERIFICATION.md` § *`pnk doctor`, check by check* and `docs/DESIGN.md:1367` are the planner's. The check, its tests and the VERIFICATION rows those tests require are the coder's. The helper-home question — import `init`'s privates versus extract a shared module — must be answered by the planner **before** the coder starts, because either answer touches `init.py`, which carries ~1100 lines of tests, and nothing in `plans/` or `docs/` decides it.

**The first pass recommended _C — `doctor` asks both questions, `ls-files` first_ and the adversary moved it.** The reasoning above is the revised one.

**What would change this.** Concretely and falsifiably:

1. **If the planner rules that `pnk init`'s loose-folder ignore warning was itself a mistake** — that answering "would `.pinakes/` be ignored if you `git init` here" via a scratch repo is a hypothetical doctor should not report — then my objection to C-lsfirst collapses and it becomes the better option. That is a real judgement, not a measurement, and it is the planner's to make. It would also mean amending `init`, since the two commands must not disagree about the same KB.

2. **If the test suite's total runtime turns out to be under ~30 s**, the derived ~3.4 s temp-repo cost is >10% and stops being negligible. The fix is then a real-git fixture for `tests/test_doctor.py`'s base `kb`, not a shortcut in the product — but the trade would need stating. I did not run pytest (read-only task), so I have the ~29.5 ms per-call delta and the ~115 call sites, and not the denominator.

3. **If someone reproduces a wedged `core.fsmonitor`/Watchman hang and shows `pnk doctor` taking ~10 s**, the "0.6% of a run" framing is wrong for the population that matters and I would want the check gated (behind `.pinakes/` existing, or a `--git` flag) rather than unconditional. I read `GIT_TIMEOUT_SECONDS = 5` and `init.py`'s account; I did not reproduce a hang.

4. **If an ordinary, common state produces the not-ignored-and-not-tracked WARN with no command that clears it** — a vendored KB inside a repo whose `.gitignore` the user cannot edit and where `.git/info/exclude` is somehow unavailable — then D-32's own precedent (heading coverage) flips that row to OK-with-a-note and my recommendation for it changes. I found no such state: `.git/info/exclude` is always writable by the person running `pnk doctor`.

5. **On D-33: if the note is scoped so an uncommitted `.gitignore` is detected too** (source tracked-ness checked via `rev-parse --show-toplevel` + `ls-files`) **and the resulting fresh-KB noise has an answer the planner is happy with**, D becomes better than C and I switch. Given `-v` does not break the existing parse, D is cheaper than the brief said, so this is a closer call than it was.

6. **If `_hooks`' "not a git repository" wording is judged out of scope**, then C ships a self-contradictory report and I would rather have B — one repo-aware row is easier to reconcile with a directory-local one than two.

7. **If Windows or a non-macOS platform returns `None` from `_tracked_by_git` inside an ordinary repository**, C degrades to "not checked" in exactly the case it exists for, and I would want that pinned by a test before shipping. I closed the linked-worktree and submodule cases on macOS; I could not test Windows.

**Cost of getting it wrong:** medium. **Owner:** planner-then-coder.

**Not established — do not treat these as settled:**

- **The test suite's total runtime.** I did not run `./check.sh` or pytest (read-only task, and pytest writes `.pytest_cache`). The ~3.4 s figure is arithmetic: a measured ~29.5 ms per-`diagnose()` delta on a non-repo temp dir × ~115 call sites (92 `checks(kb…)` in `tests/test_doctor.py` plus 23 `diagnose(` across `tests/`). I have no denominator, so I cannot say whether that is 1% or 15%.
- **The wedged-`core.fsmonitor` worst case.** I read `GIT_TIMEOUT_SECONDS = 5` and `init.py`'s docstring recording a Watchman hang; I did not reproduce one. "Up to ~10 s added to a doctor run" is derived from the constant.
- **Windows and every non-macOS platform.** All measurements are macOS 25.6 with the git on this machine. Linked worktree and submodule are now verified *here*; nothing about Windows path handling or a case-insensitive `:(literal)` pathspec is.
- **How often real users run `pnk doctor`.** No telemetry and none is possible. The recurring-check argument assumes doctor is the command people reach for, which is design intent (`docs/GUIDE.md` routes troubleshooting through it) rather than a measurement. This is the load-bearing unverified premise of every option except A and F.
- **`core.excludesFile` on a real user's machine.** `git config --global --get core.excludesFile` returns nothing here, so I exercised that path only by passing `-c core.excludesFile=…` explicitly.
- **Whether `git init` in a temp directory is ever surprising in practice.** A user with `init.templateDir` configured would have that template's files copied into the scratch repo. I did not test it, and I judge it negligible — but I did not measure it.
- **Whether a shared git module or an import of `init`'s privates is the intended home.** I confirmed nothing decides it: no git module in `src/pinakes/`, `doctor.py` does not import `init`, and the only importers anywhere are `cli.py:114` and `tools/template_drift_gate.py:68`. The absence is verified; the intent is not.
- **Whether the demo KB's 5-of-33 WARN ratio represents a well-kept real KB.** It is one synthetic corpus with a deliberately unresolvable linked KB and an out-of-date template, so the dilution argument against a sixth potential WARN is weaker than the ratio suggests. Note the repo's own two corpora report a clean single OK under the proposed check.
- **Everything I ran was `uv run` against the working tree at `e6d62e3`, in scratch directories outside the repo, on free paths only** — no `--deep`, no `claude-vision`, no `PINAKES_ANTHROPIC_API_KEY`. `git status` is clean and HEAD is unchanged. Nothing spent money.

---

## D-34

**The question.** Should docs/VERIFICATION.md carry a row for every test in tests/, or only for the promises the project makes — and is the 929-test unrowed population debt to pay down, or a category that was never in scope?

**Status of the argument.** I UPHOLD the original direction (promises-only) and OVERTURN three claims it rests on. The recommendation changes shape: promises-only ratified WITH a bounded audit, not closed as "not debt by category".

WHAT I VERIFIED BY RE-RUNNING (not reading): every population figure reproduces EXACTLY at HEAD e6d62e3 — 74 modules, 63 named, 11 zero-row (identical list), 2032 `def test_`, 1120 raw refs, 1104 pairs, 1103 on-disk, 929 unrowed, the 175/754 split, all four worst-case modules. Also confirmed: 2282 collected; 12 indented `def test_` split 6 real TestCorpusGate / 6 test_mutate string fixtures; the six 20260725 module dates against the table's 20260729 05:24; all five unnamed modules test tools/; churn 140 (2nd behind STATUS.md 178); MAX_VERIFICATION_LAG unrelated; gate green (4 passed). I re-ran the token-overlap measurement independently and confirm its direction (43.5% vs their 47%; difference is stopword choice). This is an unusually well-measured brief and that deserves saying before the objections.

WHAT DOES NOT SURVIVE:

(1) "923 rows" is wrong — there are 890. 923 counts every non-separator table line including all 33 headers. The repo's own gate skips headers by first-cell match, so the codebase already treats them as not-rows. The brief caught one wrong number in that sentence and REPRODUCED a second from the same sentence, then built its cost model on it (203 bytes/row is the header-diluted mean; over real rows it is 208.7).

(2) The provenance stamp is impossible — stronger than "I could not reproduce 67". "Measured 20260825 12:49 UTC on c23359f", but c23359f was authored and committed at 13:42, 53 minutes later. And the tree has had 74 modules continuously since 20260823 15:44; I found no commit with 67 between 20260814 and HEAD. So 67 is not a glob artifact, it is a stale figure restated under a fresh measurement stamp. That single sentence carries three defects, not one.

(3) "Nobody ever recorded that the unrowed population is not debt" is FALSE. Commit c770c0f (20260825 13:51) already put it in the preamble: "Whether either gap is *work* is undecided, and this paragraph must not be read as saying it is... a large unrowed population is **not** evidence of a hole". The regeneration mechanism the brief's headline PRO claims to kill was already patched hours before the brief was written. What remains is only converting "undecided" to "decided" — real, but far smaller than advertised.

(4) "Not debt by category" is FALSIFIED at n=1. I ran the falsification test the brief named and skipped. Seeded random sample of 50 unrowed tests; I checked row-coverage for the 6 that looked most promise-bearing (a deliberately upward-biased subset). One is a genuine unrowed promise: tests/test_serve.py::test_get_refuses_anything_that_is_not_a_known_id, whose docstring reads "No tool argument is ever a path: that is the whole server boundary" and which asserts the MCP surface refuses `../../etc/passwd`. Nothing in the 1,127-line file covers traversal at the MCP boundary; line 189 rows a weaker, different property via a different test. Two others turned out to be redundant per-surface assertions of already-rowed promises. Three were internal validation. So the brief's direction is right — the 929 is overwhelmingly internal units plus redundancy — but the category claim is one I broke in a single sitting.

WHAT I DID NOT ESTABLISH: I checked row-coverage for 6 of the 50, chosen because they looked most promise-bearing, so 1-in-6 is an upper bound by construction and says little about the rate across all 929.

### What was established

- **Every population figure in the original brief reproduces exactly at HEAD e6d62e3: 74 test modules on disk, 63 named by the table, 11 with zero rows (identical list), 2,032 top-level `def test_` functions, 1,120 raw `::` references, 1,104 distinct (file,test) pairs, 1,103 distinct on-disk tests named, 929 unrowed. Per-module worst cases also match: test_sync.py 76/111, test_doctor.py 60/113, test_extract_layout.py 39/44, test_fragments.py 36/40. Split: 175 unrowed sit in zero-row modules, 754 inside already-rowed modules.**
  - *Source:* Python script over docs/VERIFICATION.md and tests/test_*.py using the gate's own REFERENCE regex, run at HEAD; `ls tests/test_*.py | wc -l` = 74
- **CORRECTION: the document's stated '923 rows' is wrong; there are 890 data rows. 923 is the count of every non-separator table line, which includes all 33 header rows ('| What must be true | Increment | ... |' x26 plus '| ... | Test |' x7). 956 lines start with '|', 33 are separators, 923 are non-separators, 33 of those are headers, leaving 890. The repo's own gate treats headers as not-rows: `if cells[0].lower() in header_words: continue`. The original brief inherited this error while catching a different one in the same sentence, and used it in its cost model (avg 203 bytes/row is header-diluted; over the 890 real rows it is 208.7).**
  - *Source:* python3 over docs/VERIFICATION.md counting pipe-lines, separators, and lines immediately preceding a separator; tests/test_verification.py:66-70
- **CORRECTION, stronger than the original's: the scope paragraph's provenance stamp is impossible, not merely unreproducible. It says 'Measured 20260825 12:49 UTC on `c23359f`', but c23359f was authored AND committed at 20260825 13:42 — 53 minutes after the claimed measurement. Separately, the tree has held 74 test modules continuously since 20260823 15:44, and no commit between 20260814 and HEAD has 67. So '62 of the 67' is a stale figure restated under a fresh measurement stamp, not a glob artifact. The sentence carries three defects: wrong module count, wrong row count, impossible provenance.**
  - *Source:* `git log -1 --format='author:%ad commit:%cd' --date=format:'%Y%m%d %H:%M UTC' c23359f` -> both 20260825 13:42 **[⚠️ that command's `--date=format:` formats in the committer's offset, not UTC, despite the `UTC` in the format string — `c23359f` is `+01:00`, so the true UTC is **12:42**. Corrected 20260831; the measurement it supports is unaffected]**; loop over `git ls-tree --name-only <c> tests/ | grep -c '^tests/test_.*\.py$'` across all commits touching tests/ since 20260814
- **CORRECTION: the original brief's claim that 'nobody ever recorded that the unrowed population is not debt' is false. Commit c770c0f (20260825 13:51) already added to the preamble: 'Whether either gap is *work* is undecided, and this paragraph must not be read as saying it is... Until it is answered, a large unrowed population is **not** evidence of a hole — that inference was made once here, by a planner, hours after the lesson that named it.' The same commit introduced the D-34 pointer. So the anti-regeneration text existed hours before the brief was written; only the conversion from 'undecided' to 'decided' is missing.**
  - *Source:* `git log --oneline -S 'not** evidence of a hole' -- docs/VERIFICATION.md` -> c770c0f only; `git log -1 --format='%ad'` c770c0f -> 20260825 13:51; docs/VERIFICATION.md lines 33-38
- **CORRECTION, and the decisive one: 'not debt by category' is falsified. I ran the sample the brief admitted skipping — seeded random sample of 50 of the 929 unrowed tests — and checked row-coverage for the 6 most promise-looking. `tests/test_serve.py::test_get_refuses_anything_that_is_not_a_known_id` asserts the MCP server refuses '../../etc/passwd', 'docs/b.md' and a bare ULID, with docstring 'No tool argument is ever a path: that is the whole server boundary.' No row in docs/VERIFICATION.md covers it. All 16 test_serve.py rows name other tests; line 189 rows the weaker, different property 'an unknown document is refused with a remedy'. Grep for traversal/'not a path'/passwd across the whole 1,127-line file returns nothing at the MCP boundary. This is a security-relevant, user-visible guarantee with no row, found in under an hour.**
  - *Source:* seeded random.sample(unrowed, 50); tests/test_serve.py:117-122; `grep -n 'test_serve\.py' docs/VERIFICATION.md` (16 hits); `grep -n -i 'traversal|never a path|not a path|passwd' docs/VERIFICATION.md`
- **NEW, and it cuts FOR promises-only: two of the six sampled candidates are not gaps but redundant per-surface assertions of already-rowed promises. `test_extract_claude.py::test_an_ambient_anthropic_api_key_is_not_enough` — the promise is rowed twice, at :481 (test_paid.py) and :1053 (test_deep_client.py); three modules define a test of that exact name. `test_extract_claude.py::test_a_call_that_would_breach_a_cap_is_never_made` — rowed at :423 via test_budget_core.py. Three more sampled tests were internal validation (ledger naive-timestamp, budget zero-pages, manifest boolean typing). So the 929 is dominated by internal units plus redundancy, with a small residue of genuine promises.**
  - *Source:* `grep -n -i 'ambient|ANTHROPIC_API_KEY' docs/VERIFICATION.md` -> :481, :1053; `grep -rn 'def test_an_ambient_anthropic_api_key_is_not_enough' tests/*.py` -> 3 modules; docs/VERIFICATION.md:423
- **NEW: the document's own reasoning argues against leaving that redundancy unrowed. Row :1053 states the key rule is 'asserted again on the second entry point, because "the extractor refuses it" says nothing about a new module' — yet the deep client's entry point has a row and the extractor's does not, while docs/INVARIANTS.md § The paid path's key is its own says the rule is 'bound to its own surface by each of the two entry points (extract/claude.py, deep/client.py)'. The asymmetry is in the table, not in the tests.**
  - *Source:* docs/VERIFICATION.md:1053; docs/INVARIANTS.md:50-53
- **CONFIRMED and decisive against the module-accounting option: tests/test_sync.py already carried 18 `::` references BEFORE db7d1c1, so 0.7.1's seventeen containment tests landed inside a module the table already covered. db7d1c1 added 15 row-lines naming test_sync.py 16 times. A module-level gate is blind to this by construction — and 754 of the 929 unrowed tests live in already-rowed modules.**
  - *Source:* `git show db7d1c1^:docs/VERIFICATION.md | grep -o 'tests/test_sync\.py::' | wc -l` -> 18; `git show db7d1c1 -- docs/VERIFICATION.md | grep '^+' | grep -c '^+|'` -> 15
- **CONFIRMED verbatim: db7d1c1 (20260804 04:23) decided the one-directionality question and recorded it only in a commit message: 'tests/test_verification.py is one-directional by construction: it walks from this document to the tests, proving no row is fiction. It cannot prove no shipped guarantee is un-rowed — there is nothing to walk from. That is not a defect in the gate, it is the shape of the problem, so the answer is procedural: docs/README.md's landing checklist gains a VERIFICATION.md step.' That step exists today as docs/README.md item 7, which also records the incident inline.**
  - *Source:* `git show db7d1c1 --format='%b'`; docs/README.md:118-121
- **CONFIRMED: the gate is green and structurally one-directional. REFERENCE = re.compile(r"`(tests/[\w/]+\.py)::(\w+)`") admits exactly one `::`, and _defined_in uses `^def (\w+)` which cannot see an indented method. tests/test_extract_quality.py::TestCorpusGate has 6 collected methods addressed as file::Class::test, so a row naming one would turn the gate red. Separately, 6 more indented `def test_` lines live inside triple-quoted fixtures in tests/test_mutate.py (PINNING_TESTS, SKIPPABLE_TEST) and are never collected. An 'every test' gate must be rewritten, not extended.**
  - *Source:* `uv run pytest tests/test_verification.py -q` -> 4 passed in 0.31s; read tests/test_verification.py in full; `grep -rn '^    def test_' tests/*.py` -> 12 hits in exactly 2 files; `uv run pytest tests/test_extract_quality.py --collect-only -q | grep TestCorpusGate` -> 6; tests/test_mutate.py:76-143
- **CONFIRMED: the two unrowed categories are real and explainable. The six zero-row modules (test_chunk, test_ids, test_lock, test_pairing, test_uri, test_embed) were all added 20260725 14:00-14:52; docs/VERIFICATION.md was created 20260729 05:24 — four days later. The five modules named by no row all test tools/: test_build_rfc_corpus (docstring line 1: '`tools/build_rfc_corpus.py` — the title curation and the chunking reserve, driven offline'), test_deep_reservation, test_measure_sync_cpu, test_rfc_golden_set (tools/verify_rfc_golden_set.py), test_two_leg_gate — added 20260805 to 20260821, all after the table.**
  - *Source:* `git log --diff-filter=A --date=format:'%Y%m%d %H:%M'` on each of the 11 modules and on docs/VERIFICATION.md; head -20 tests/test_build_rfc_corpus.py; grep for tools/ path constants in each
- **NEW: the demonstrated recurring failure in this document is wrong published numbers, not unrowed promises. The document itself records four prior errors ('stopped at 0.12.0', 'the gap is four releases wide', test_init.py listed absent, test_eval.py listed absent). I found three more live today (62-of-67, 923-rows, the c23359f stamp). That is seven, against one recorded unrowed-promise incident (0.7.1) plus the one I found. And the paragraph states the rule 'Do not restate a release count here — it goes stale silently' and then restates a module census one clause later. Its two self-corrections ARE right: test_init.py carries 27 rows, test_eval.py carries 32.**
  - *Source:* docs/VERIFICATION.md:26-32; `grep -c 'tests/test_init\.py::' docs/VERIFICATION.md` -> 27; `grep -c 'tests/test_eval\.py::' docs/VERIFICATION.md` -> 32
- **CONFIRMED: cost and consumer facts hold. docs/VERIFICATION.md is 1,127 lines / 196,251 bytes, published as nav entry '2.3 Verification' (mkdocs.yml:115). It is the second-highest-churn document in the repo at 140 commits in 30 days, behind docs/STATUS.md at 178. tests/test_verification.py is the only machine consumer; MAX_VERIFICATION_LAG in tools/release_order_gate.py bounds release-verification lag and that file never reads VERIFICATION.md.**
  - *Source:* `wc -c -w -l docs/VERIFICATION.md`; `grep -n -i verification mkdocs.yml`; `git log --since=30.days --name-only --pretty=format: -- docs/ CLAUDE.md README.md CHANGELOG.md | sort | uniq -c | sort -rn`; repo-wide grep for VERIFICATION in .py/.sh/.yml/.toml
- **CONFIRMED against my own expectation: the rows are not restatements of test names. Re-running the measurement independently over the 885 machine-parsable rows gives 43.5% average prose words absent from the test name, 44% of rows above 50% new, 31% near-restatement (<25% new). The original brief reported 47/49/27; the gap is stopword-list choice. Direction confirmed — a backfill would document real properties, so the case for promises-only rests on scope and cost, not on the rows being empty.**
  - *Source:* independent token-overlap script over the rows' first and last cells, own stopword list, run at HEAD
- **CONFIRMED: three governing files already state the promises-only reading. VERIFICATION.md's preamble ('this file is the resolved mapping: what must be true, and the test that actually holds it'); docs/README.md item 7 ('if the increment shipped a test that holds a promise, it gets a row'); CLAUDE.md's narrow exception ('add **only** the row a test you wrote requires'). The plan itself flags the paragraph fix as independent of the decision: 'Independent of that decision, one thing is simply wrong and should be fixed either way.'**
  - *Source:* docs/VERIFICATION.md:1-16; docs/README.md:118; CLAUDE.md ownership table; plans/20260825_0749-exposure-and-silent-status.md:327-334

### The options

| Option | What it means | Pros | Cons | Work |
|---|---|---|---|---|
| **Promises only, ratified — plus one bounded audit of the residue** ⭐ | Everything the original Option A does, minus the claim I falsified, plus the audit that replaces it. In docs/VERIFICATION.md: define what qualifies as a promise (user-visible guarantee, named invariant, or a gate's own correctness — as distinct from a unit test of an internal primitive, and as distinct from a per-surface re-assertion of a promise already rowed); state that the unrowed population is not systematically debt; name the two categories by their real reasons (six primitive-unit modules predating the table by four days, five modules testing tools/); and fix all THREE defects in the scope sentence — 63 of 74 not 62 of 67, 890 rows not 923, and a provenance stamp that names a commit which existed when the measurement was taken. Fold db7d1c1's one-directionality finding into the preamble so it stops being reachable only from a commit message. Then run ONE time-boxed sweep of the 929 to surface genuine promises like the MCP path boundary, and row what it finds. Optional rider, cheap and independent: have tests/test_verification.py recompute the census it states and assert it, or drop the census from the prose entirely. In plans/20260825_0749: mark D-34 closed with its disposition and a named re-open trigger. | Ratifies what three governing files already say, rather than inventing a fourth answer. · It is the only option that lets the closing note say *checked* rather than *by category* — I falsified the category claim at n=1 in one sitting, so closing on it now would put a known-false sentence into a published document. · The audit is finite and it ends. It is not the 929-row obligation; it is a read pass that yields perhaps a dozen rows. · Fixes three live factual errors in the sentence the plan itself calls 'the first thing anyone deciding D-34 would read' — one of which survived a correction pass made the same day, and one of which the original brief propagated. · Keeps the table's signal density: 43.5% of row prose is absent from the test name, so the curated half is real and a population-sized table would dilute it. · The census rider targets the failure this document actually keeps having — seven wrong published numbers against two unrowed-promise incidents. | More work than closing it flat, and the audit has no natural stopping rule beyond a time box — a bounded sweep can still miss the promise it was run to find. · Leaves the one-directional blind spot open permanently. Nothing catches a promise-bearing test shipping with no row; 0.7.1 stays repeatable by construction. · 'Promise' stays a judgment call. A prose definition is not a gate, and the same judgment that missed seventeen containment tests is what the project keeps relying on. · The audit needs a reader who can tell a promise from a unit test across 929 tests — that is exactly the judgment the definition is admitting it cannot mechanise. · The census rider adds a gate to a file that already has one, and a gate asserting a number in prose is brittle to reformatting. | Medium; planner-then-coder. Planner: one editing pass over the preamble (definition, three number fixes, both categories, the db7d1c1 fold-in), one disposition line in the plan. Audit: one time-boxed session reading the 929 by name with spot-checks — a coder or a cheap model can gather candidates, a planner classifies and writes rows. Coder, only if the rider is taken: ~15 lines in tests/test_verification.py, watched failing. |
| **Promises only — record the rule and close D-34** | The original brief's recommendation as written: define 'promise', state that the unrowed population is not debt, correct the module count, name the two categories, close D-34. No code changes, no audit. | Cheapest option that ends the regeneration — one planner session, no implementer. · Ratifies the reading three files already operate on. · Gate stays green and untouched. · Preserves the table's signal density. | Its central closing claim is one I falsified: an MCP path-traversal boundary with no row, found in a 50-sample in under an hour. Closing on 'not debt by category' writes a known-false sentence into a published site page. · Its headline benefit is overstated. c770c0f already added 'a large unrowed population is **not** evidence of a hole' to the preamble on 20260825 13:51, so the regeneration mechanism was substantially patched before the brief was written; what remains is only 'undecided' to 'decided'. · Its 'fix the wrong count' work item is understated — three defects in that sentence, not one, and the brief itself propagated the 923-row error. · Depends entirely on a human applying an undefined word. After it ships, the serve boundary is still unrowed, because writing a definition audits nothing. · Leaves the blind spot open, and leaves the 754 unrowed tests inside already-rowed modules invisible — exactly where the one recorded incident occurred. | Small; planner-only. One editing pass plus a disposition line. |
| **Fix the three wrong numbers, defer D-34** | The genuine do-nothing. Correct the scope sentence (63 of 74, 890 rows, an honest provenance stamp), leave the existing 'undecided / not evidence of a hole' paragraph exactly as c770c0f wrote it, and leave D-34 open and unscheduled. Optionally add the census rider so the numbers cannot rot again. | Minimum write on a planner-owned, second-highest-churn document, which is worth something on a repo with concurrent agents. · The plan itself licenses exactly this: 'Independent of that decision, one thing is simply wrong and should be fixed either way.' · The harmful inference is already blocked by existing text, so the acute risk is handled without taking any decision. · Fully reversible; commits the project to nothing. · Honest — it does not assert a category claim nobody has checked. | The question keeps regenerating as 'someone should decide this', which is what D-34 sitting open produces. It costs a message every time a fresh session counts the tree. · Leaves a real, security-relevant unrowed promise (the MCP path boundary) unrowed and now known — deferring after being told is worse than deferring in ignorance. · Postpones a decision whose evidence is already gathered, which is its own kind of waste. · Two agents have already been within one message of building against this ambiguity (20260824, 20260825, per CLAUDE.md); leaving it open keeps that live. | Very small; planner-only. Three number corrections in one sentence. Under an hour. |
| **Every test — invert the gate and backfill the population** | Redefine the document as a complete map of tests/. Coder rewrites tests/test_verification.py to enumerate from pytest collection or AST, extends the reference form to file::Class::test for the six TestCorpusGate methods, and excludes the six test_mutate.py string-fixture false positives. Planner writes ~929 new rows. CLAUDE.md's narrow exception widens to 'a row for every test you write'. | Closes the blind spot completely and permanently — a shipped guarantee with no row becomes impossible. No other option achieves this. · Removes the definitional argument entirely; nobody adjudicates what counts as a promise. · Would have caught both known incidents (0.7.1's seventeen, and the MCP path boundary I found). · The prose measurement supports real value: 43.5% of row content is absent from the test name, so a backfill documents genuine properties, not only ceremony. | Requires a unit nobody has agreed on: 2,032 functions or 2,282 collected cases. The repository has already produced three mutually inconsistent counts of this same population, and the document's own current count is wrong by 33. · Buys 929 new existence assertions and zero new verification — the document's own 'Two limits' block says a row is a strong pointer, not a proof. · Roughly doubles a published 196KB site page. · Turns the second-highest-churn document into a file every test-writing commit must touch, on a project that built changelog.d/ and retro.d/ specifically to stop that pattern. · My sample shows a large fraction of the 929 are per-surface re-assertions of already-rowed promises — rowing those adds noise to the table, not coverage. · Several sessions of planner prose for tests whose names are already property-shaped. | Large; planner-then-coder. Coder: a real gate rewrite (collection-based enumeration, two-separator node ids, fixture-string exclusion), a day, and it must be watched failing to count. Planner: ~929 rows, realistically several sessions. |
| **Promises only, plus a module-accounting gate** | Keep promises-only, but extend tests/test_verification.py so every test module either carries at least one row or appears on a committed exemption list with a stated reason. The eleven zero-row modules become eleven exemption entries. | Cheap — about eleven lines plus a small gate extension. · Makes the two unrowed categories machine-checked rather than prose, so a new module cannot silently join them. · Preserves promises-only and the table's signal density. · Forces the categories to be named explicitly. | VERIFIED blind to both known incidents. 0.7.1's seventeen tests landed in test_sync.py, which already carried 18 references before db7d1c1; the MCP path boundary I found is in test_serve.py, which carries 16 rows. A module-level gate cannot see either — and 754 of the 929 unrowed tests live in already-rowed modules. · Adds an exemption list, another artefact that rots silently — a failure mode this repository has recorded repeatedly. · Its one real catch (a promise-bearing new module shipping with zero rows) has never been observed here. · Spends implementer time on a gate with near-zero expected yield — the 'ceremony that looks like assurance' pattern this project keeps flagging. | Small; planner-then-coder. Planner writes the exemption list; coder extends the gate and must watch it fail. Under half a session each. |
| **Promises only now, every new test rowed from here** | Grandfather the 929-test backlog, but require every newly added `def test_` to gain a row or an explicit exemption in the same commit. Coder builds a diff-aware gate against the merge base. CLAUDE.md's narrow exception widens for all new tests. | Fixes the blind spot going forward — the only cheap option that would have caught 0.7.1's seventeen tests. · Avoids the 929-row backfill; cost is incremental and paid by whoever creates the obligation. · Extends existing practice rather than inventing one. | Converges to the every-test end state at this repo's test-writing rate, so rejecting that end state means rejecting this too. · Needs an exemption hatch, and an easy hatch becomes the default — the `false_abstain: 0.0` degradation tests/test_verification.py's own docstring names as a failure this project keeps finding in its own gates. · A diff-aware gate is the most fragile kind here: it needs a merge base, so it behaves differently locally and in CI, and it must itself be watched failing to count. · Puts every test-adding commit into a planner-owned, highest-churn document, maximising parallel-agent contention. · Leaves the backlog permanently unrowed — including the MCP path boundary — so the coverage claim is never true. · Would not have caught the gap I found: test_serve.py's path-refusal test is old, not new. | Medium and ongoing; planner-then-coder. Coder: diff-aware gate plus a machine-readable exemption store plus a watched-failing demonstration. Then a permanent per-commit tax on every implementer. |

> ### ⭐ Recommended: **Promises only, ratified — plus one bounded audit of the residue**
>
> The single deciding fact is unchanged from the original and I verified it independently: this decision was already taken on 20260804 in db7d1c1, which confronted the identical complaint and concluded that one-directionality "is not a defect in the gate, it is the shape of the problem, so the answer is procedural." That decision produced docs/README.md item 7 and has governed the file since; it never reached VERIFICATION.md's preamble, so every fresh reader who counts the tree re-derives the question. Promises-only is right, and the original brief earned that conclusion honestly — including reporting a measurement that cut against its own framing.

What I changed is the closing note, and I changed it because I ran the falsification test the brief named as its own #1 gap and skipped. A 50-test random sample produced tests/test_serve.py::test_get_refuses_anything_that_is_not_a_known_id — the MCP surface refusing '../../etc/passwd', with a docstring calling it "the whole server boundary" — and nothing in the 1,127-line table covers it. The brief's own stated falsification bar was ">10% of a 50-sample promise-bearing". I did not reach that bar, because I only checked row-coverage for the 6 most promise-looking of the 50; but I did establish existence, and existence is enough to make "the unrowed population is not debt" a sentence the project should not publish. Closing on an unchecked category claim is precisely this repository's recorded failure mode — a sound argument over an unexamined domain — and it would be committed to a page that is published on every push to main.

Two further facts move the original's cost/benefit rather than its direction. First, its headline benefit is smaller than stated: c770c0f already wrote "a large unrowed population is **not** evidence of a hole" into the preamble at 20260825 13:51, so the regeneration mechanism was mostly patched before the brief existed; only "undecided" to "decided" remains. Second, its cheapest work item is larger than stated: the scope sentence has three defects, not one, and the brief propagated the second of them ("923 rows" is 890 — 923 counts 33 headers, which the repo's own gate skips).

The sample also supplies the strongest argument FOR promises-only, and it is one nobody had: two of the six candidates I checked were not gaps at all but per-surface re-assertions of promises the table already rows twice. That replaces an argument from category with a measurement — the 929 is dominated by internal units and redundancy — and it is why the audit should be a bounded sweep, not a backfill.

Its true cost, stated plainly: the blind spot stays open forever, "promise" remains an ungated judgment call, the audit has no stopping rule beyond a time box, and after it ships nothing prevents the next promise-bearing test from shipping unrowed.

**The first pass recommended _Promises only — record the rule and close D-34_ and the adversary moved it.** The reasoning above is the revised one.

**What would change this.** In descending force.

(1) COMPLETE THE SAMPLE. I checked row-coverage for 6 of my 50, chosen because they looked most promise-bearing, so my 1-in-6 is an upper bound by construction and near-worthless as a rate. Check all 50 properly — for each, ask not "is this test rowed" but "is the property it asserts rowed anywhere, by any test". If more than ~10% are genuinely unrowed promises, the residue is not a residue and I would move to "Promises only now, every new test rowed from here". If the answer is 1 or 2 in 50, my bounded-audit recommendation is right-sized. This is cheap, decisive, and neither the original analyst nor I did it. It is the single thing that should happen before this decision is taken.

(2) A SECOND RECORDED INCIDENT WITH CONSEQUENCES. 0.7.1 is an anecdote remediated in hours; my MCP finding is a gap with no known consequence. Find one case where a promise-bearing test's absence from the table let a real defect through or delayed a diagnosis, and the procedural answer stops being adequate — I would switch to the diff-aware option despite its fragility.

(3) SHOW THE MCP FINDING IS NOT A GAP. If someone points at a row I missed that covers "no tool argument is ever a path" — in a section I did not grep, or under wording I did not anticipate — my central objection collapses and the original Option A is correct as written. I grepped test_serve.py rows (16), traversal, "not a path", "never a path", and passwd; I did not read all 890 rows.

(4) A DEFINED GLOB PRODUCING 67. I could not reproduce 67 at any commit between 20260814 and HEAD, and the "measured on c23359f" stamp names a commit created 53 minutes after the measurement. If someone reproduces 62-of-67 under a stated rule, my reading of that sentence weakens — though the 923-vs-890 error and the impossible timestamp stand independently.

(5) AN EXTERNAL CONSUMER NEEDING FULL TRACEABILITY — an audit, a compliance claim, a published assurance that every test maps to a stated property. That makes completeness the point and justifies the backfill regardless of cost.

**Cost of getting it wrong:** medium. **Owner:** planner-then-coder.

**Not established — do not treat these as settled:**

- THE CENTRAL GAP, AND IT IS THE SAME ONE THE ORIGINAL ADMITTED — narrowed, not closed. I sampled 50 of the 929 unrowed tests at random (seeded) but checked row-coverage for only 6, and I chose those 6 because they looked most promise-bearing. So '1 genuine unrowed promise in 6' is an upper bound by construction and does NOT support any rate estimate over the 929. What I established is existence, not prevalence. The right next step is to finish the 50 properly.
- Whether the MCP path-boundary property is rowed somewhere I did not look. I grepped all 16 test_serve.py rows, plus 'traversal', 'not a path', 'never a path', 'passwd'. I did not read all 890 rows, and the table's wording is idiosyncratic enough that a row could describe this property in words I did not guess.
- Whether the 890 existing rows' prose is ACCURATE. The gate cannot check it and I did not re-read them. The document itself records that the I9 review found one row mapped from a name alone and wrong. My token-overlap re-run shows the prose is substantive, not that it is correct.
- The provenance of the document's '62 of the 67'. I proved it is not reproducible at any commit since 20260814 and that its measurement stamp predates the commit it names by 53 minutes, but I cannot say what the author actually counted.
- Whether '923 rows' was intended to include headers. I show it equals 890 data rows + exactly 33 headers and that the repo's own gate excludes headers, which makes the header-inclusive reading inconsistent with the codebase — but I did not find the script that produced it.
- Whether pytest's 2,282 collected count is stable across CI's three-leg extras matrix ([light], [light,pdf], [light,pdf,claude]). I ran one local resolve only. If collection varies by installed extras, the 'every test' unit is even less stable than represented.
- Whether the six TestCorpusGate methods are the only class-based tests that would ever need rows. I measured today's tree; nothing prevents more, and no convention discourages the form.
- Whether the census rider I propose is actually implementable without brittleness. I did not write it or estimate how it behaves under reformatting of the scope paragraph.

---

## D-36 + D-37

**The question.** Two calls. D-36: is the sidecar rewrite's comment/formatting drift a defect to fix, a bound to write into MANIFEST.md plus a pinning test, or a bound to *derive* from a generative round-trip test? D-37: should `pnk sync`'s vanished-path line read as a deletion or a possible move — and what should gate the move hint?

**Status of the argument.** I am UPHOLDING the brief's two central conclusions and OVERTURNING both of its recommended options.

Upheld, verified myself: D-36's defect is real and reproduces through the real `sidecar.read()`/`write()` path with no CLI; its root cause is a ruamel *loader* misfiling (I read `ca.items` — slot 2 for a plain key, slot 1 of the NEXT key for an alias); the `pnk link` misattribution is real and I reproduced it (the comment lands at the bottom of the file under `rel: cites`); D-37's message is false in every clause on an ordinary deletion (I ran it: "0 indexed", no sidecar on disk, yet it says "a new id was minted"); and DESIGN 6.4 already fixes the *meaning* as deletion-with-a-hedge. The brief's downgrade of D-37 and upgrade of D-36 both stand.

Overturned, on measurement:

(1) D-36's population is far wider than the brief's own "widened" population. I ran 30 constructed sidecar shapes containing **no anchors and no aliases at all** through `sidecar.read()`/`write()`: **12 broke byte-identity**, and only one of those is on MANIFEST's bounds table. Undocumented casualties include column alignment (`title:   Alpha` → `title: Alpha`), flow-comma spacing (`tags: [a,b]` → `tags: [a, b]`), `reviewed: ~` → `reviewed:`, a multi-line flow sequence collapsed to one line, and a single-quoted value with a tab re-emitted double-quoted — which directly contradicts MANIFEST:303's promise that quoting style survives. Hand enumeration of this bound has now failed **four** times: the original L5b list, the sweep, the brief I am reviewing, and my own first pass (I initially mis-scored the flow-sequence cases by confounding *unused* anchors with the comment effect, and had to isolate the variable). A decision to "add the missing rows" written from any of those four passes ships an incomplete table again.

(2) D-37 A's Mint-count discriminator fails on the ordinary case. I measured it: delete one document and add an unrelated one in the same sync → "1 indexed" → the false move line still prints. That is the normal git-hook batch pattern, not an edge case. I also found a branch the brief missed entirely: the ambiguity path **falls through** to the same line, so one document gets two contradictory reports. A strictly better discriminator exists that nobody considered — gate on the **orphaned sidecar**, which is literally what the field name `moved_without_sidecar` means. It scored correct on 5/5 of my measured scenarios against 3/5 for the Mint count.

Also corrected: the brief fixed the sweep's citations and introduced its own off-by-one — the false "An anchor on a real value survives" parenthetical is MANIFEST.md:**316**, not 317. And the brief missed VERIFICATION.md:**232**, where `comments survive a rewrite through pnk link` is pinned by a test that asserts **substring presence, not position** — so the misattribution passes it today. The promise is pinned in two places and both pins are blind to this.

One fact the brief admitted it could not check, I closed: **ruamel.yaml 0.19.1 is the latest release on PyPI.** There is no upstream fix to wait for, and the brief's number-one mind-changer cannot be exercised today.

### What was established

- **ruamel.yaml 0.19.1 is the LATEST release on PyPI — the project is already on the newest version. The brief's #1 admitted-unverified fact and #1 mind-changer is now closed: there is no upstream fix to wait for or to bump the floor to.**
  - *Source:* curl https://pypi.org/pypi/ruamel.yaml/json → info.version = 0.19.1; releases ≥0.19 = ['0.19.0','0.19.1']. Installed: .venv/bin/python -c 'import ruamel.yaml; print(version_info)' → (0,19,1). pyproject.toml:39 'ruamel.yaml>=0.19'; uv.lock:1712-1713.
- **12 of 30 constructed sidecar shapes containing NO anchors and NO aliases fail byte-identity through the real sidecar.read()/write() path. Only one of the twelve (4-space nested indent) is covered by an existing bounds row.**
  - *Source:* Wrote scratchpad/adv/fuzz.py, ran under .venv/bin/python: 'IDENTICAL 18  CHANGED 12  ERROR 0'.
- **Column alignment is destroyed on every write. 'title:   Alpha\nshelf:   A12\nstatus:  draft' comes back single-spaced. This is undocumented, untested, and hits any user who aligns their sidecar by hand — a far larger population than YAML anchors.**
  - *Source:* scratchpad/adv/sharp.py case aligned_columns → CHANGED; grep 'align' docs/MANIFEST.md finds no bounds row.
- **Flow-style collections are respaced: 'tags: [a,b]' → 'tags: [a, b]', '{p: 1,q: 2}' → '{p: 1, q: 2}', '{ p: 1 }' → '{p: 1}', '[x, y,]' → '[x, y]', and a multi-line flow sequence is collapsed onto one line. None of it is on the bounds table.**
  - *Source:* scratchpad/adv/fuzz.py and tags.py; grep -n 'flow' docs/MANIFEST.md returns nothing; grep 'flow' tests/test_sidecar.py returns only line 136 (an unclosed-flow parse-error comment).
- **'reviewed: ~' round-trips to 'reviewed:' — the user's explicit null marker is erased from the text. Undocumented.**
  - *Source:* scratchpad/adv/sharp.py case tilde_null → CHANGED, OUT 'reviewed:\n'.
- **A single-quoted value containing a tab is re-emitted double-quoted with an escape: "mine: 'a\tb'" → 'mine: "a\\tb"'. This contradicts MANIFEST.md:303's promise that quoting style survives a rewrite.**
  - *Source:* scratchpad/adv/fuzz.py case tab_in_value → CHANGED; docs/MANIFEST.md:303-304 'Comments, quoting style, block scalars, blank lines and your own key order all survive a rewrite'.
- **test_quoting_style_survives_a_rewrite asserts ONE substring ('title: "Loans outward, 1974"'), not byte-identity — so it samples a single point of the quoting domain and cannot see the tab case.**
  - *Source:* tests/test_sidecar.py:482-486, read directly.
- **A one-line ruamel config fix exists for one deviation and is NOT set. brace_single_entry_mapping_in_flow_sequence=True makes 'mine: [{a: 1}, {b: 2}]' round-trip byte-identically; with the default it becomes 'mine: [a: 1, b: 2]'. sidecar.py's _yaml() sets only preserve_quotes and width.**
  - *Source:* Ran both settings under .venv/bin/python: False→CHANGED, True→IDENTICAL. src/pinakes/sidecar.py:77-80 sets preserve_quotes and width only; grep 'brace_single' src/pinakes/sidecar.py → no hits.
- **D-36 UPHELD: the alias comment detachment reproduces through the real path, not just the CLI. 'sameTags: *c  # alias reusing the anchor above' becomes two lines with the comment padded onto its own line.**
  - *Source:* scratchpad/adv/rt.py — sidecar.read()/write() round trip, case alias_ref → CHANGED. Also reproduced from stock ruamel YAML() in scratchpad/adv/bare.py.
- **D-36's population claim UPHELD once the confound is removed. With USED anchors: alias reference line CHANGED, anchor over block seq/map CHANGED; anchor over scalar/flow seq/flow map and an ordinary key all IDENTICAL. My first pass appeared to contradict this because I used unused anchors, where a separate defect (anchor-dropping) fires.**
  - *Source:* scratchpad/adv/pop2.py, 8 cases, all USED anchors.
- **Root cause CONFIRMED as a ruamel LOADER misfiling, not an emitter bug. For 'b: 2  # c' the token lands in slot 2 of ca.items['b']; for 'b: *v  # c' it lands in slot 1 of ca.items['d'] — the NEXT key's pre-comment list.**
  - *Source:* scratchpad/adv/ca.py printed ca.items for both inputs: plain → (2, '# c\n') on key 'b'; alias → (1, ['# c\n']) on key 'd'.
- **A fifth fix avenue, untried by the brief, is also closed: ruamel's experimental comment_handling API crashes on this input with IndexError: list index out of range.**
  - *Source:* Ran YAML() with comment_handling=2000 on 'x: &v 1\nb: *v  # c\nd: 3\n' → IndexError.
- **The pnk link misattribution is real and worse than 'orphaned with padding'. The comment migrates past the appended links: block to the bottom of the file, indented 14 spaces directly under 'rel: cites'.**
  - *Source:* Live free scratch KB (pnk init --backend light, sync, hand-written sidecar, pnk link). After: 'links:\n- to: pnk://…\n  rel: cites\n              # alias reusing the anchor above\n'.
- **The brief MISSED a second pin. VERIFICATION.md:232 pins 'comments survive a rewrite through pnk link' to test_comments_survive_a_rewrite_through_pnk_link — which asserts only that comment substrings are PRESENT in the text, never their position. The misattribution passes it today.**
  - *Source:* docs/VERIFICATION.md:232; tests/test_cli_link.py:676-690, four bare 'assert "# …" in text' checks.
- **The brief's own citation correction contains an off-by-one. The false parenthetical 'An anchor on a real value survives' is at docs/MANIFEST.md:316, not 317. Line 317 is the pnk://self row. The brief is right that the ten bound rows are 311-320 and that the sweep's '307-319' is wrong.**
  - *Source:* grep -n 'An anchor on a real value survives' docs/MANIFEST.md → 316; numbered read of lines 300-322.
- **test_an_anchor_on_a_real_value_survives samples exactly one shape — 'a: &x 1\nb: *x', a scalar with a used alias — and its docstring generalises from it: 'this is not a general "anchors are lost" caveat.' An unused anchor on a collection IS dropped ('a: &x [1, 2]' → 'a: [1, 2]').**
  - *Source:* tests/test_sidecar.py:1393-1397 read directly; scratchpad/adv/bare.py cases unused_anchor_flowseq and unused_anchor_blockseq → CHANGED, unused_anchor_scalar → IDENTICAL.
- **MANIFEST's bounds table is already known-incomplete: the pinned trailing-comment limitation is in VERIFICATION.md:326 but grep 'trailing' docs/MANIFEST.md returns nothing.**
  - *Source:* grep -n 'trailing' docs/MANIFEST.md → no output; docs/VERIFICATION.md:326; tests/test_sidecar.py:1092-1101 docstring 'a choice between two misplacements, not a bug with a fix'.
- **INVARIANTS names the TEST LIST, not MANIFEST, as authoritative for this bound: 'The invariant is bounded, and each exclusion is pinned by a test — the authoritative list, because a bound stated only in prose cannot notice the library moving under it', pointing at VERIFICATION.md § The sidecar round-trip.**
  - *Source:* docs/INVARIANTS.md:19, read in full.
- **Exposure surface UPHELD as narrow: a free pnk sync does not rewrite an existing sidecar (md5 identical across a sync that re-indexed a changed document and reported metadata-only). write() has four call sites: link.py:95, sync.py:2267, sync.py:2289 (paid extraction), sidecar.py:663 (create/mint).**
  - *Source:* Live md5 check on scratch KB s43 (before=after=b319779bb9c07456c29da60056c366b2); grep -rn 'sidecar.write|write_sidecar' src/.
- **No committed sidecar uses an anchor or alias: 51 .pnk.yaml files under tests/, zero hits for ' &name' or ': *name'. So the L5b exit-criterion round-trip test never exercises any of this.**
  - *Source:* find tests -name '*.pnk.yaml' | wc -l → 51; grep -rln ' &[a-zA-Z_]|: \*[a-zA-Z_]' --include='*.pnk.yaml' tests/ → no hits.
- **D-37 UPHELD: on an ordinary deletion the message is false in every clause. Deleting docs/alpha.md AND its sidecar prints 'moved without its sidecar, so a new id was minted: docs/alpha.md' on a run reporting '0 indexed … 1 removed' — nothing moved, nothing was minted, and no sidecar exists.**
  - *Source:* Live scratch KB s37: pnk init --backend light, sync, rm both files, sync.
- **D-37 UPHELD: the trigger is 'path gone', NOT 'sidecar present'. The guard is 'if document.path not in after_by_path' and mentions no sidecar. SoftDelete is appended unconditionally at pairing.py:400, one line before.**
  - *Source:* src/pinakes/pairing.py:395-403 read directly.
- **D-37 UPHELD: the field is report-only. Declared pairing.py:175, appended 403, returned 415, copied sync.py:1126, read at exactly one place — sync.py:404 — to print one line. Seven hits total; tests/test_pairing.py:245 asserts the FIELD, not the string. The string appears nowhere in tests/ or docs/.**
  - *Source:* grep -rn moved_without_sidecar src/ tests/; grep -rn 'moved without its sidecar' src/ tests/ docs/ → one hit, sync.py:405.
- **NEW — the Mint-count discriminator that D-37 A depends on FAILS on the ordinary batch case. Deleting one document and adding one unrelated document in the same sync reports '1 indexed … 1 removed' and still prints 'moved without its sidecar, so a new id was minted: docs/one.md'. Nothing moved. This is the normal git-hook batch pattern.**
  - *Source:* Live scratch KB s41, measured end to end.
- **NEW — the ambiguity branch falls through, so one document gets two contradictory lines. With a vanished path and two content-identical candidates, sync prints BOTH 'moved without its sidecar, so a new id was minted: docs/a.md' AND 'ambiguous duplicate of docs/a.md: docs/c.md, docs/d.md — fresh ids minted, nothing guessed'. The brief never mentions this branch.**
  - *Source:* Live scratch KB s40; confirmed in code — pairing.py:392-398 appends the Ambiguity then falls through with no 'continue' to SoftDelete and the field append.
- **NEW — a strictly better discriminator exists and is already on the same object: the orphaned sidecar. 'moved WITHOUT ITS SIDECAR' is definitionally 'the sidecar stayed at the old path', which is exactly PairingResult.orphaned_sidecars. Scored across my five measured scenarios: orphan-gate correct 5/5, Mint-count correct 3/5. Both fields are read in the same method (sync.py:404 and 411).**
  - *Source:* Scenarios s37 (rm both → no orphan → suppress, correct), s42 (rm doc only → orphan → hedge, correct), s38 (move unchanged → renamed, no line), s39 (move+edit, sidecar left → orphan → show, TRUE: id 01M0X0D9XBRT… → 01M0X0DABQNPWP…), s40 ambiguity and s41 delete+add (no orphan → suppress, correct where Mint-count is wrong). pairing.py:414 orphaned_sidecars=_orphans(after); pairing.py:421-426.
- **D-37 scenarios C and E UPHELD exactly as the brief states. Move with content unchanged → '1 renamed', no message, plus the orphaned-sidecar line. Move AND edit with the sidecar left behind → the message fires and is TRUE, with a measured id change.**
  - *Source:* Live scratch KBs s38 and s39, ids grepped from the sidecars before and after.
- **DESIGN 6.4 settles the MEANING but NOT the wording choice the brief claims it settles. The table row is 'Path gone, no hash match | Mark state = deleted (soft). Leave the sidecar on disk and report it as orphaned' — deletion, reported via the ORPHAN line. The 'likely moved-without-sidecar case' bullet is scoped to the 'Rename + edit in the same sync' consequence, i.e. scenario (e) only. DESIGN therefore prescribes no per-path move line on an ordinary deletion at all.**
  - *Source:* docs/DESIGN.md:1012-1054 read in full; the bullet is the third of four consequences, opening 'Rename + edit in the same sync:'.
- **ULID permanence UPHELD as not violated. In the deletion case no id is minted. In the move+edit case the new id comes from the separate 'everything still unclaimed is new → Mint' loop, and no existing id is renumbered.**
  - *Source:* src/pinakes/pairing.py:406-409; docs/INVARIANTS.md:18 ULID row; measured id change in scenario s39.
- **The six anchor/alias tests are exactly where the brief says, and none covers a comment on an alias reference line.**
  - *Source:* grep of tests/test_sidecar.py: defs at lines 172, 1121, 1316, 1338, 1380, 1393.

### The options

| Option | What it means | Pros | Cons | Work |
|---|---|---|---|---|
| **D-36 E — Derive the bound from a generative round-trip corpus, and set the free ruamel options (NEW)** ⭐ | Coder adds a generative test to tests/test_sidecar.py: a table of constructed YAML shapes (flow collections, alignment, nulls, quoting, block scalars, anchors, aliases, comments in each position) each round-tripped through sidecar.read()/write() and asserted either byte-identical or present on an explicit, named exclusion list in the test file. The test fails on any shape that is neither. Coder also sets brace_single_entry_mapping_in_flow_sequence=True in sidecar.py's _yaml(), which removes one deviation for one line and zero risk. Planner then rewrites MANIFEST.md's bounds prose to describe the CLASSES (cosmetic reflow vs comment misattribution) and point at the derived list, corrects the false parenthetical at MANIFEST.md:316, and adds the VERIFICATION rows. | It is the only option that survives the measurement. Hand enumeration of this bound has failed four times — the original L5b list (missing the trailing-comment case), the sweep (alias only), the brief under review (alias + anchor-block), and my own first pass. A fifth hand-written list is the option most likely to be wrong again. · It is exactly what INVARIANTS.md:19 already demands: 'each exclusion is pinned by a test — the authoritative list, because a bound stated only in prose cannot notice the library moving under it.' Today that authoritative list is a hand-curated set of one-point samples. · It finds the cheap fixes that the anchor-only framing structurally cannot see. brace_single_entry_mapping_in_flow_sequence is one line and already exists; nobody found it because nobody enumerated the population. · The whole increment is additive — tests plus one parser flag. It touches no byte-deciding logic in the write path. · It subsumes D-36 A entirely: A's two rows fall out of the corpus rather than being remembered into it. · Every future ruamel upgrade is then gated. A version that fixes the loader turns exclusion rows red, which is how the repo learns the bound lifted. | It does NOT fix the pnk link misattribution. A user still gets their comment moved under `rel: cites`, silently. This option makes the bound honest and complete; it does not make the user's file safe. · The corpus is still authored, so it is still a sample. A shape nobody thought to write still escapes — this reduces the failure rate, it does not prove completeness. Calling it 'derived' overstates it. · Bigger than the decision as posed. The sweep asked a yes/no about one alias case; this answers a question about the whole invariant, and a reviewer could reasonably call that scope creep. · It will surface deviations the project then has to triage — the ~10 undocumented ones I measured — and some, like destroyed column alignment, may read as worse news than the alias case that prompted the question. · Needs both owners: the test and the flag are the coder's, the MANIFEST and VERIFICATION rewrite is the planner's, so it cannot land in one turn. | Medium. Coder: one parametrised generative test (~40 shapes), the one-line parser flag, a mutation-battery section appended to the existing sidecar battery. Planner: rewrite the bounds prose by class, fix MANIFEST.md:316, add VERIFICATION rows. One to two days across two turns. |
| **D-36 A — Widen the bound, document it, pin it** | Planner adds rows to MANIFEST.md's bounds table for an EOL comment on an alias reference line and on an anchor definition over a block collection, corrects the false parenthetical at line 316, and adds the matching VERIFICATION rows. Coder adds four tests to test_sidecar.py plus one to test_cli_link.py pinning the pnk link misattribution. | Matches the precedent set in this exact increment: test_a_document_trailing_comment_is_captured_by_an_appended_key is pinned, not fixed, as 'a choice between two misplacements, not a bug with a fix'. · Cheap, fully reversible, and risks nothing in the module that rewrites every user's sidecars. · The pinning tests go red if a future ruamel fixes the loader. · Fixes the false parenthetical at MANIFEST.md:316, which no other option except E addresses. | Measured, the table it produces is still wrong. I found 12 of 30 anchor-free shapes breaking byte-identity; this option documents two anchor cases and leaves roughly ten undocumented, including destroyed column alignment and the tab-quoting case that contradicts MANIFEST:303 outright. · It repeats the exact process that has already failed four times — enumerate by inspection, write the rows, declare the bound described. · The user still silently loses comment placement, and through pnk link gets misattribution rather than displacement. · Documented is not noticed: nobody reads MANIFEST at the moment their comment moves. · A row written from D-36's own decision statement would be wrong, since that statement covers only the alias case and only pnk link. | Small. Planner: 2 rows, 1 correction, 3-4 VERIFICATION rows. Coder: 5 tests plus a battery section. Half a day. |
| **D-36 B — A, plus have pnk link say when a comment moved** | Everything in D-36 A, plus roughly ten lines in link.py: capture the sidecar's comment lines before the write, compare after, and print a warning naming the path when they differ. | Turns a silent change into a visible one at the moment it happens — the shape doctor already uses for the orphaned sidecar. · pnk link is the only free command that rewrites an existing sidecar, so one warning covers the whole free surface. I verified this: a free sync leaves the sidecar's md5 untouched. · Additive, and touches no code path that decides bytes. · The line-diff variant also catches the already-pinned trailing-comment capture, which is silent today. | It warns about comments only, while my measurement shows the drift is mostly NOT about comments — alignment, flow spacing, `~`, quoting. So the warning covers a minority of what actually changes in the user's file and creates a false impression that the rest is safe. · It fires on the deliberately-pinned trailing-comment case too, so the warning is not always about a defect and its wording must say so. · New user-facing output requires a docs/CLI.md sync in the same change, which is planner-owned, so the increment cannot land coder-only. · Inherits every con of D-36 A, including that the bounds table it writes is still incomplete. · A warning about the tool's own behaviour is an admission rather than a repair; some readers will prefer either fixing or staying quiet. | Small-to-medium. D-36 A's work plus ~10 lines, 2 tests, one CLI.md paragraph (planner). One day. |
| **D-36 C — Fix it: repair the emitted bytes in sidecar.write()** | sidecar.write() reads the original bytes before emitting, then post-processes the rendered text to re-attach detached comments — and for the pnk link case, relocate a comment back across an inserted links: block. | Actually keeps the promise MANIFEST.md:303 makes, rather than qualifying it. · Would repair the anchor-over-block-collection case in the same pass. · Removes the need for a warning and for most new bound rows. | No seam exists, and I closed the last avenue the brief left open. Inside ruamel's model: the emitter patch is inert (the AliasEvent carries no comment), slot relocations emit on the wrong line or raise TypeError, and ruamel's experimental comment_handling API raises IndexError on this very input. So the repair must be textual surgery on rendered bytes. · That surgery would live in the one module whose stated purpose is not to move comments, and whose failure mode is moving a comment to the wrong place — the exact defect class. It runs on EVERY sidecar write, so a bug in it reaches every user, not only those using aliases. · The pnk link case needs relocation across an inserted block — reconstruction, not a line join. That is the hard half and the decision statement does not mention it. · It forks behaviour from ruamel, and since 0.19.1 is already the latest release, that fork is indefinite rather than temporary. · It fixes the comment class and leaves the ~10 formatting deviations I measured untouched, so MANIFEST is still wrong afterwards. | Medium-to-large and risk-heavy. Coder: post-processor, an adversarial pass over its own failure modes, mutation coverage. Several days; the review burden is the real cost. |
| **D-36 D — Refuse the write when a comment sits on an alias or anchor line** | sidecar.read() or write() raises a PinakesError with a remedy when the document holds a comment the rewrite would move, the way it already refuses a reused anchor name, a duplicate key and a JSON-unencodable value. | Never damages a user's file. · Uses machinery and a precedent that already exist in this module. · Small code change. | Disproportionate. The repo refuses a reused anchor because the intended value is not recoverable; here nothing is unrecoverable — a comment moved, and the user can see and fix it. · It blocks a legitimate operation: the user cannot author a link until they edit a comment in their own file, which cuts against INVARIANTS' 'docs/ belongs to the user'. · On the paid sync path a refusal is worse — it can block a run that has already spent money. · Detecting the exact population needs the same analysis a fix would, without delivering the fix — and my measurement shows that population is much larger than anyone has stated, so the refusal would either be too narrow to help or broad enough to block ordinary files. | Small in code, large in user cost. Half a day, and the option most likely to be reverted after first contact. |
| **D-37 E — Gate the move hint on the orphaned sidecar (NEW)** ⭐ | Confirm DESIGN 6.4 as written: the state is a soft delete. Coder rewrites sync.py:405 so the unconditional clause states the fact — the document is no longer on disk and is soft-deleted — and appends the move hint plus its remedy only when that document's own sidecar is in this run's orphaned_sidecars. Both fields are already on the same report object and both are already read in the same method. No change to pairing.py, no change to any action. | It is what the field NAME already means. 'moved without its sidecar' is true precisely when the sidecar stayed at the old path — that is the orphaned-sidecar condition, and today the code never checks it. · Measured correct on 5/5 of my scenarios, against 3/5 for the Mint-count discriminator D-37 A relies on. It suppresses the false line on delete-plus-add and on the ambiguity fall-through, where Mint-count still prints it. · It leaves the hint exactly where the state is genuinely ambiguous — document gone, sidecar still there — which is the state the sweep's question actually names, and the case DESIGN §9 calls the most-likely-corruption risk. · It kills the double-report on the ambiguity branch, so a document no longer gets two contradictory lines. · It needs no cross-referencing of counts, so it cannot drift as the sync summary changes. · Blast radius is one line: no test asserts the string, no doc quotes it, sync has no --json flag and no MCP tool. | A user who deletes a document and forgets its sidecar still gets the move hint. That is a real false positive — but it is the one state that is genuinely undecidable from disk, so a hedge there is honest rather than wrong. · It still leaves pairing.py's single field carrying the raw 'path gone' meaning, so the naming stays misleading in the data structure; only the message improves. · On a tidy deletion of both files it prints only the corrected deletion clause, so the wording of that clause has to earn its place against the summary's 'N removed' — that sub-choice remains open and is a matter of taste. · I verified the discriminator on seven scenarios in small KBs. I did not test it on a large sync interleaving many deletions, moves and additions. | Small, coder-only. One message and one membership test in src/pinakes/sync.py, three tests (deletion, orphan-present, ambiguity) plus a VERIFICATION row the coder is permitted to add. DESIGN needs no change — it already says this. Half a day. |
| **D-37 A — Deletion, with the move hint gated on a Mint having happened** | As D-37 E, but the hint is shown when the same run also emitted a Mint rather than when the sidecar was orphaned. | Correctly suppresses the line on a pure deletion where nothing was minted, which is the commonest event and the one that is false in every clause today. · Requires no new data — the mint count is already computed. · Same one-line blast radius as D-37 E. · Matches DESIGN 6.4's classification of the state as a soft delete. | The discriminator fails on the ordinary case. I measured it: delete one document and add one unrelated document in the same sync still prints the false move line. For a KB synced by a git hook over a batch of edits, any batch with at least one deletion and one addition trips it — that is the normal pattern, not an edge case, and the brief rates it as marginal without having measured it. · It does not suppress the ambiguity fall-through, where a Mint always happens, so that document keeps getting two contradictory lines. · It suppresses the hint in scenario (b) — document gone, sidecar left behind — which is precisely the ambiguous state D-37 asks about and where the hint is most warranted. · The Mint count is a run-level fact used to describe a document-level state, so it can never attribute a mint to the vanished document. | Small, coder-only. One message, one count check, two tests. Half a day. |
| **D-37 C — Print nothing when nothing was minted** | Suppress the line entirely on a run with no Mint, relying on the summary's 'N removed' and the existing orphaned-sidecar line. Keep a corrected warning when the run did mint. | Zero noise on the common path, and it is arguably the most faithful reading of DESIGN 6.4's table row, which prescribes reporting the ORPHAN and says nothing about a per-path deletion line. · The summary already reports the removal and the orphan line already covers the leftover half. · Same small blast radius. | Inherits the Mint-count discriminator, so it keeps every false positive I measured — delete-plus-add and the ambiguity fall-through both still print — without D-37 E's compensating precision. · A tidy deletion of both files then names no path at all, so a user who deleted the wrong file loses the one line that would have named it. · Silence is indistinguishable from 'this state cannot happen', which is how the repo has lost track of things before. | Small, coder-only. Same size as D-37 A. Half a day. |
| **D-37 B — Correct only the tense and the minting claim** | Keep the line unconditional and keep the move as its primary reading; fix only 'was minted' and the past tense, e.g. 'no longer at docs/alpha.md — if you moved it, its sidecar did not travel with it'. | Smallest possible diff, one string and one test. · Preserves the warning in the move+edit case with no new condition to get wrong. · Keeps DESIGN §9's most-likely-corruption framing in view. | Still speaks about a move on every ordinary deletion, which is the commoner event by a wide margin. The user deleting a note is told to consider a move they did not make. · Contradicts DESIGN 6.4's table row, which classifies this state as a soft delete first and prescribes the orphan report. · Trains the user to ignore the line, which costs exactly when the real corruption case happens. · Leaves the ambiguity double-report untouched. | Trivial, coder-only. One hour. |
| **D-37 D — Split the state in pairing.py into deletion and identity-loss** | Replace the single moved_without_sidecar tuple with two fields — path vanished with no candidate anywhere, versus id lost while some new path went unclaimed — each with its own report line. Touches pairing.py, sync.py, the PairingResult dataclass, tests/test_pairing.py, and a DESIGN 6.4 row. | Names the distinction in the data structure rather than in a message, so a future reader cannot miss it. · Lets each line carry its own remedy without hedging. · Makes the two cases separately testable in the pure function, which is where this repo prefers to test pairing. | It still cannot attribute a specific Mint to a specific vanished document — the attribution pairing determined it could not do — so the second field is a suspicion with a better name. · It changes a pure function DESIGN 6.4 itself calls 'the part that silently corrupts a KB if left vague', for a reporting improvement. · D-37 E gets the same message-level correctness for one line and no structural change, which makes this the expensive way to reach the same output. · Requires a planner turn for the DESIGN row, so it cannot land coder-only. | Medium, planner-then-coder. Dataclass change, pairing change, 4+ tests, DESIGN and VERIFICATION rows. Two days. |

> ### ⭐ Recommended: **D-36 E — Derive the bound from a generative round-trip corpus, and set the free ruamel options (NEW). D-37 E — Gate the move hint on the orphaned sidecar (NEW).**
>
> D-36: the deciding fact is that the population has never once been stated correctly, and I have the count. Thirty anchor-free sidecar shapes through the real write path produced twelve byte-identity failures, of which one is documented. Four successive attempts to enumerate this bound by inspection — the original L5b list, the sweep, the brief under review, and my own first pass — each produced an incomplete or wrong population, and mine was wrong in a way that flattered the brief until I isolated the variable. Any option that answers "add the rows we now know about" is the fifth attempt at a method with a 0/4 record. The brief's own recommendation is exactly that, plus a warning scoped to comments — which my measurement shows is the minority of what actually changes in a user's file. So the answer is to make the exclusion list something the test suite derives rather than something a reviewer remembers, which is what INVARIANTS.md:19 already asks for in so many words. The fix option stays closed, and now more firmly: ruamel 0.19.1 is the latest release on PyPI, and the last untried avenue — ruamel's experimental comment_handling — raises IndexError on this input. Note the honest cost, which is the same one I charged the brief: this option makes the bound honest without making the user's file safe. The pnk link misattribution survives it, and a decision to add the warning is worth taking separately, once the population is known well enough to word it.

D-37: the deciding fact is that a better discriminator was sitting on the same object the whole time. The brief is right that DESIGN 6.4 already decided the meaning and that this is unimplemented rather than undecided — I read 6.4 in full and it says "likely" while the code asserts a minted id in the past tense on a run that indexed nothing. But the brief then picks the wrong gate. Its Mint-count test fails on delete-one-add-one, which I measured and which is the ordinary batch shape for a git-hook-driven KB, and it fails on the ambiguity branch, which the brief never found and which today prints two contradictory lines about one document. Gating on the orphaned sidecar is exact rather than heuristic — "moved without its sidecar" is true precisely when the sidecar stayed behind — and it scored 5/5 against the Mint count's 3/5 across the scenarios I ran. It is the same one-line change, coder-only, with the same blast radius.

**The first pass recommended _D-36 B — Widen the bound, pin it, and have pnk link say when a comment moved. D-37 A — The state means deletion; the move is a hedge shown only when something was minted._ and the adversary moved it.** The reasoning above is the revised one.

**What would change this.** D-36: (1) If the generative corpus, once written, turns up no deviations beyond the ~12 I measured, then the population IS small enough to enumerate by hand and D-36 A becomes adequate — the corpus would have proved the method it replaced. Falsifiable by running it. (2) If a working placement inside ruamel's comment model is demonstrated that dumps byte-identically for the alias case, D-36 C becomes cheap and is the right answer; five attempts have failed (four in the brief, one of mine via comment_handling), but that is evidence of difficulty, not proof of impossibility. (3) If the project's position is that byte-identity was only ever meant to cover unknown KEYS and their values — INVARIANTS.md:19 does say "an unknown key in a sidecar round-trips byte-identically" — then alignment, flow spacing and `~` are outside the invariant and only the comment cases are in scope. That reading would shrink my population argument sharply and move me to D-36 A or B. It hinges on whether MANIFEST:303's "Comments, quoting style, block scalars, blank lines and your own key order all survive" is a promise or a gloss, and that is the planner's call, not mine. (4) If someone shows a real KB where sidecar anchors are common, the case for fixing rather than documenting the comment class strengthens.

D-37: (1) If the orphan-gate is shown to misfire on a large interleaved sync — many deletions, moves and additions at once — I would want that measured before landing; I tested seven scenarios in small KBs. (2) If the project decides a document-gone line should never print when the summary already says "N removed", that is D-37 C and I would not argue against it; my objection to C is its discriminator, not its silence, and C combined with the orphan gate is a perfectly defensible variant. (3) If a later decision reframed this state as a move — I read DESIGN 6.4 in full and found none, but I did not read every plan.

**Cost of getting it wrong:** medium. **Owner:** planner-then-coder.

**Not established — do not treat these as settled:**

- Whether MANIFEST:303's promise ('Comments, quoting style, block scalars, blank lines and your own key order all survive a rewrite') is intended to bind the whole file or only unknown keys and their values. INVARIANTS.md:19 states the invariant as 'an unknown key in a sidecar round-trips byte-identically', which is narrower. This is the single interpretive fact my D-36 recommendation most depends on, and it is the planner's to settle — I read both lines but they do not settle each other.
- Whether the ~10 undocumented deviations I measured are already known and deliberately accepted somewhere I did not look. I grepped docs/MANIFEST.md, docs/RETROSPECTIVES.md, plans/20260731_0602-decision-ruamel-yaml.md and tests/test_sidecar.py for 'flow', 'comma' and 'align' and found nothing, but I did not read every plan or every retrospective entry.
- Whether setting brace_single_entry_mapping_in_flow_sequence=True has side effects on shapes I did not test. I verified it fixes 'mine: [{a: 1}, {b: 2}]' and nothing else; it must be run against the full corpus and the existing suite before landing.
- Whether the paid sync write path (sync.py:2267, 2289) exhibits the same drift. I traced the call sites but exercised only the free path — no key was set and nothing was spent, per the constraints.
- Whether the orphan-gate discriminator holds on a large sync interleaving many deletions, moves, additions and ambiguities. I measured seven scenarios in two- and three-document KBs.
- Whether pnk upgrade, the git hooks, or any command I did not run rewrites an existing sidecar. I found four write() call sites by grep and confirmed a free sync does not rewrite (md5 unchanged), but I did not execute every CLI command.
- Whether any of the proposed changes pass the gates. I ran no ./check.sh, pytest, ruff, pyright or make docs — the task was read-only, I wrote nothing into the repo, and git status was clean at e6d62e3 throughout. All my scripts and scratch KBs live under the session scratchpad.
- Whether upstream ruamel considers the alias EOL-comment misfiling a bug. I established that 0.19.1 is the latest release on PyPI but did not search the issue tracker or read release notes.
- Real-world frequency of YAML anchors, aliases, column alignment or flow-style tags in sidecars outside this repo. The 51 committed sidecars here contain no anchors or aliases; I did not attempt to estimate frequency in the wild, and my claim that alignment is 'more common than anchors' is a judgement about human YAML habits, not a measurement.
- Whether pnk doctor should also report a soft-deleted document whose source is gone. I read doctor.py's orphaned-sidecar branch but did not audit doctor for this state; sweep item S7 flags doctor's failure ledger separately and the two may interact.

---

## 20260805_1313-requires-pinakes-floor-clause

**The question.** The 20260805 decision requires "a KB whose manifest carries the new value declares a floor", but nothing in Pinakes writes `[kb] requires_pinakes` and D-11 forbids the one surface that could — is that clause withdrawn, discharged, or folded back into the older open question it is an instance of?

**Status of the argument.** **I am OVERTURNING the original recommendation (D) and replacing it with E+F.** Four of the original brief's load-bearing claims did not survive checking, and two options were missing.

**What I verified myself** (ran every command, opened every file): the pre-pass and its ordering; all three cited tests plus the whole of `tests/test_manifest_compat.py` and `tests/test_manifest.py` (74 passed); the key-not-value premise shift; D-11's mutation record; the absence of a key→release map; every tag attribution re-derived from `git log -S` and re-sorted with `--sort=v:refname`; and I **reproduced all four misdiagnoses**, not the two the original ran — `headings` and `metadata` on pinakes 0.12.0, `[deep]` on 0.12.0 (worse: reported at root with **no table name** — `pinakes.toml: unknown key(s): \`deep\``), and `graph_channel` on a fresh 0.10.0 install. I also re-verified end to end that a hand-written floor fixes it.

**What I overturned.** (1) **D-6 is not a taken decision** — `plans/20260804_1016-template-release.md:318` lists it "⭐ A — open", and :671 classes D-3–D-9 as "Recommendations only". Only D-11 is ✅ TAKEN. (2) **The clause's *Test* is already discharged** — `test_the_pre_pass_runs_before_strict_validation` carries a floor plus a future key and asserts the floor message wins and the unknown key is not reported. That property is key-agnostic and pinned. What is undischarged is the clause's ***Required***, a writer-side obligation. That halves the decision. (3) **`docs/GUIDE.md:797` does not miss this case — it is exactly this case**, correctly diagnosed and correctly remedied ("Upgrade Pinakes"), in `## Troubleshooting`, where the harmed reader looks. This removes most of option D's justification. (4) **"No document cites another" is only half true** — `docs/KB-UPDATES.md § 8` has carried the general question since 20260728: *"Does an increment's manifest addition oblige a `requires_pinakes` bump? … A rule is needed: probably 'bump when a key is added'."* The question predates all three decisions and has a published home.

**Why it is still genuinely open.** The clause's *Required* names no agent, its stated mechanism (`table.choice` rejecting an unknown **value**) was superseded the same day by the key-not-value decision at `manifest.py:88-94`, and nothing has ever reconciled it against D-11. And the plan is measurably un-swept: of 23 files in `plans/`, it is one of only two with a **single commit** — never edited since 20260805 — and X2's disposition pass (`8f05e73`) stamped 17 plan files and skipped this one.

### What was established

- **The pre-pass is real, correctly ordered, and its own comment calls the ordering 'the whole feature (G4)'. `_check_required_version(data, path)` is called at manifest.py:355, immediately after `tomllib.loads` and before `Table(...)`; defined at :381.**
  - *Source:* `sed -n '340,410p' src/pinakes/manifest.py`
- **All three cited tests pass, and so does the wider manifest suite: `pytest tests/test_manifest_compat.py tests/test_manifest.py` → **74 passed in 0.16s**; the three targeted tests → 3 passed.**
  - *Source:* `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q ...`
- **CORRECTION — the clause's *Test* is ALREADY DISCHARGED. `tests/test_manifest_compat.py:80 test_the_pre_pass_runs_before_strict_validation` builds a manifest with an unmeetable floor AND a future key (`weekly_eur`), asserts the floor string is in the message and `assert "weekly_eur" not in message`. That is verbatim the clause's Test — 'produces the floor message and not the unknown-value message' — and it is key-agnostic, so it holds for `headings` too. The original brief's claim that the Test is undischarged is wrong. What is undischarged is the clause's *Required*.**
  - *Source:* `grep -n 'def test_the_pre_pass_runs_before_strict_validation' -A 25 tests/test_manifest_compat.py`
- **CORRECTION — D-6 is NOT a taken decision. `plans/20260804_1016-template-release.md:318` records it as `⭐ A — open (it is today's behaviour)`, and :671 puts D-6 in the O-5 group described as **'Recommendations only'**. Only D-11 carries `✅ TAKEN`. The original brief called both 'pinned decisions'. D-6's *outcome* is pinned by a test, and D-11's rationale incorporates D-6's reasoning ('whatever writes `requires_pinakes` inherits B's cost'), so the practical bar stands — but the decision status does not.**
  - *Source:* `sed -n '485,505p' plans/20260804_1016-template-release.md`; `grep -n 'D-6' plans/20260804_1016-template-release.md`
- **CORRECTION — `docs/GUIDE.md:797` does NOT miss this case; it IS this case. The row reads: `| \`unknown key(s)\` in a KB you did not edit | The same cause, on a KB that declares **no** floor, so the refusal can only report the symptom | Upgrade Pinakes. Unknown keys stay a hard error by design (a link to MANIFEST; its target is elided in this quotation) |`. It sits in `## Troubleshooting` (heading at :785), diagnoses the exact failure, and gives the correct remedy. The harmed party — a collaborator opening someone else's KB on an old build — is precisely 'a KB you did not edit'. This removes most of option D's stated justification.**
  - *Source:* `sed -n '790,800p' docs/GUIDE.md`; `grep -n '^#' docs/GUIDE.md`
- **CORRECTION — the question is already recorded in a published planner-owned document, older than all three decisions. `docs/KB-UPDATES.md § 8 Open questions`: **'Does an increment's manifest addition oblige a `requires_pinakes` bump? Additive keys with defaults do not break a *newer* reader, but strictness means an older reader fails on them the moment a user sets one. A rule is needed: probably "bump when a key is added"…'** — and the next bullet: **'What updates `requires_pinakes` on a KB whose owner never runs `upgrade`? Nothing does, so the floor reflects the last write.'** KB-UPDATES was decided 20260728, before D-6/D-11 (20260804) and before the clause (20260805). The 20260805 clause is best read as a one-key answer to it.**
  - *Source:* `sed -n '218,232p' docs/KB-UPDATES.md`
- **MISSING OPTION E, half 1 — the unknown-key remedy's pointer is stale, and the repo already knows it. `_toml.py:218` says 'Check the spelling against docs/DESIGN.md §2.1.' But DESIGN §2.1 now opens: '> **Every field, its default and its validation rule: `[MANIFEST.md](MANIFEST.md)`.** This section carries only the reasoning behind the shape.' `docs/KB-UPDATES.md § 8` records this as owed: 'Small follow-up: the unknown-key remedy still points at `docs/DESIGN.md §2.1`, whose field tables moved to MANIFEST.md in 0.2.1.' Someone must touch that string anyway.**
  - *Source:* `sed -n '210,220p' src/pinakes/_toml.py`; `awk '/^### 2\.1/,/^### 2\.2/' docs/DESIGN.md | head -5`; `sed -n '228,231p' docs/KB-UPDATES.md`
- **MISSING OPTION E, half 2 — the remedy string is pinned by nothing. `grep -rn 'Check the spelling' src/ tests/ docs/ tools/` returns exactly one hit, `src/pinakes/_toml.py:218`. No test asserts it, no doc quotes it. Amending it is a one-string change plus a new test.**
  - *Source:* `grep -rn 'Check the spelling' src/ tests/ docs/ tools/`
- **MISSING OPTION B′ — option B does NOT need a key→release map, and the repo's own shipped precedent proves it. `upgrade.py:1258-1268` already prints exactly the map-free recommendation: it names the introduced keys and says 'you may want `[kb] requires_pinakes` set by hand. **No number is suggested and none is written**'. A `doctor` check firing on hand-set unstamped keys can use the identical shape. The original brief's central objection to B — 'creates the key→release map D-11 option C explicitly rejected' — applies only to the version-carrying variant it happened to design.**
  - *Source:* `sed -n '1255,1270p' src/pinakes/upgrade.py`
- **MISSING COST in options B/B′ — `doctor` cannot see the floor at all. `diagnose(manifest: Manifest)` at doctor.py:152 takes a parsed `Manifest`; `KbSection` (manifest.py:177-181) has fields `name, id, template, created` and **no `requires_pinakes`** — manifest.py:591 does `table.optional_string(REQUIRES_PINAKES)` purely to stop `done()` rejecting it, and discards the value. So a check asking 'does this KB declare a floor?' needs a raw re-read inside doctor or a new field on a core dataclass. Separately, a parsed manifest cannot distinguish an explicitly written `headings = "none"` (documented as a legitimate use) from absence, so any value-based check under-reports by construction.**
  - *Source:* `grep -n 'class KbSection' -A 6 src/pinakes/manifest.py`; `sed -n '588,594p' src/pinakes/manifest.py`; `grep -n 'def diagnose' -A 18 src/pinakes/doctor.py`
- **MISSING COST in option D — `docs/MANIFEST.md:59-61` currently justifies the tool's silence with 'no honest number is available anyway, because **nothing here maps a manifest key to the release that introduced it**.' Putting four release numbers into rows :140/:141/:159/:234 of the same document puts that sentence under strain, so the neighbourhood-audit rule obliges rewriting the floor section too. D is not four row edits.**
  - *Source:* `sed -n '41,66p' docs/MANIFEST.md`
- **MISSING BOUNDARY in option D — advising a hand-written floor is safe only for keys introduced at or after 0.6.0. D-6 option B's recorded con: stamping a floor means 'On any Pinakes < 0.6.0 the KB fails with "unknown key `requires_pinakes`" — a *worse* error'. All four keys D targets are ≥0.11, so a floor ≥0.11 never reaches a pre-0.6.0 reader that could have coped. `adjacent_k` (0.5.0) is the one where the same advice would be actively harmful. The original brief excluded it correctly but never stated why the boundary exists.**
  - *Source:* `sed -n '493,497p' plans/20260804_1016-template-release.md`
- **I reproduced ALL FOUR misdiagnoses, closing the original brief's admitted gap. On pinakes 0.12.0 (fresh venv, `pnk init`-generated manifest, scratch dirs outside the repo): `headings = "numbered"` → ``[chunking]: unknown key(s): `headings` `` + 'Check the spelling'; `metadata = "prefix"` → ``[chunking]: unknown key(s): `metadata` ``; `[deep]` → ``pinakes.toml: unknown key(s): `deep` `` — **at root, with no table name at all**, which is diagnostically worse than the two that were tested. On a fresh 0.10.0 install, `graph_channel = "expand"` → ``[retrieval]: unknown key(s): `graph_channel` ``.**
  - *Source:* `python3 -m venv oldenv{,10}`, `pip install pinakes==0.12.0` / `==0.10.0`, `pnk init`, edited manifest, `pnk doctor` in four scratch KBs outside the repo
- **A hand-written floor fixes it end to end, verified: same 0.12.0 build, same KB plus `requires_pinakes = ">=0.13"` → `error: …pinakes.toml [kb]: this KB requires pinakes >=0.13 (this build is 0.12.0)` with the upgrade remedy and exit 1. The field works; only the writing of it is absent.**
  - *Source:* Scratch KB `kbB`, `../oldenv/bin/pnk doctor`, `echo $?` → 1
- **The premise shift is real and same-day. `manifest.py:87-94`: `CHUNK_STRATEGIES = ("structural",)` unchanged, `HEADING_GRAMMARS = ("none", "numbered")` with the docstring '**A key of its own rather than a second `CHUNK_STRATEGIES` value, decided 20260805.** `strategy` is *inert*: validated here and never read at runtime.' The clause reasons about `table.choice(...)` rejecting an unknown **value**; that error never became reachable.**
  - *Source:* `sed -n '80,100p' src/pinakes/manifest.py`; `grep -rn 'CHUNK_STRATEGIES' src/`
- **D-11 is TAKEN and mutation-pinned. `plans/20260804_1016-template-release.md:588`: '### D-11 … ✅ TAKEN … option A: `--apply` prints a `requires_pinakes` recommendation and writes nothing.' `docs/RETROSPECTIVES.md:5183` records the mutant `write requires_pinakes` killed by `…is_never_written (+2)`. The test asserts absence from the **file**, not the diff (`tests/test_cli_upgrade.py:1682`).**
  - *Source:* `sed -n '585,600p' plans/20260804_1016-template-release.md`; `sed -n '5178,5186p' docs/RETROSPECTIVES.md`; `grep -n 'def test_requires_pinakes_is_never_written' -A 18 tests/test_cli_upgrade.py`
- **D-11's own accepted-cost text already concedes the exact harm the 20260805 clause was reaching for, and the user took that decision one day earlier: '**Accepted cost, stated:** the floor stays a lower bound nothing ever raises, so a KB that *has* adopted a newer key still fails on an older build with "unknown key" — the error `requires_pinakes` exists to improve.' The original brief did not surface this.**
  - *Source:* `sed -n '588,606p' plans/20260804_1016-template-release.md`
- **Every tag attribution independently re-derived, and the lexical-sort trap is real. Introducing commits found with `git log -S`: `adjacent_k`→31d0a8e (L3, 2026-07-30), `graph_channel`→498b94c (G5, 2026-08-04), `headings`→288ee0f ('Numbered plain-text headings, behind [chunking] headings = "numbered"', 2026-08-05), `metadata`→0ea1bda (2026-08-07), `[deep]`→11067e6 (E4, 2026-08-11). First tags with `--sort=v:refname`: 0.5.0, 0.11.0, 0.13.0, 0.16.0, 0.24.0. Unsorted, `adjacent_k` reports v0.10.0 — the trap the original brief flagged, reproduced.**
  - *Source:* `git log --format='%h %ad %s' -S'"<key>"' --reverse -- src/pinakes/manifest.py`; `git tag --contains <sha> --sort=v:refname | head -1` vs `git tag --contains <sha> | head -1`
- **`upgrade` structurally cannot fire for these keys: `upgrade.py:1257` gates on `if result.introduced:`, derived from template hunks, and no template version carries them. `src/pinakes/templates/notes/pinakes.toml.j2` and both archived versions (`_versions/1.1`, `_versions/1.2`) contain neither `headings` nor `metadata` (grep exit 1). Neither `tests/demo-kb/pinakes.toml` nor `tests/partner-kb/pinakes.toml` sets any of the four keys or a floor (grep exit 1). Measured population inside the repo: **zero**.**
  - *Source:* `grep -n 'headings\|metadata' src/pinakes/templates/notes/pinakes.toml.j2 src/pinakes/templates/notes/_versions/*/pinakes.toml.j2`; `grep -rn 'headings\|metadata\|graph_channel\|requires_pinakes\|\[deep\]' tests/*-kb/pinakes.toml`
- **No key→release map exists in code: `grep -rn 'introduced_in|key_to_version|KEY_VERSIONS|since_version' src/ tools/` returns nothing, and `upgrade.py:1264` ships that reasoning to users as prose.**
  - *Source:* `grep -rn 'introduced_in\|key_to_version\|KEY_VERSIONS\|since_version' src/ tools/`
- **A name-list gate for B′ is feasible, which was the original brief's mind-changer #3. `docs/MANIFEST.md` carries the exact phrase 'Not stamped into the template' on precisely 4 rows — 140, 141, 158, 159 — so a test in the shape of `tests/test_verification.py` could assert code and doc agree. Caveat I checked: `[deep]` at :234 uses different wording ('**Why it is unstamped**'), so the anchor is not uniform and the gate needs care.**
  - *Source:* `grep -c 'Not stamped into the template' docs/MANIFEST.md` → 4; `grep -n` → 140,141,158,159; `sed -n '228,240p' docs/MANIFEST.md`
- **The plan is measurably un-swept. Of 23 files in `plans/`, only two have a single commit in their history: this one (`6126546`, its creation, 20260805) and today's `20260825_1240-run-pinakes-sweep.md`. X2 (`8f05e73`, 'a heading in plans/ now carries its own disposition') touched **17 plan files and skipped this one**. `grep -n '^#\|CLOSED\|OPEN'` returns bare headings only.**
  - *Source:* `for f in plans/*.md; do git log --oneline -- "$f" | wc -l; done | sort -n`; `git show --stat --oneline 8f05e73`
- **The shipped docs already answer the agency question against the clause. `docs/MANIFEST.md:57-61`: '**Nothing in Pinakes ever writes it — including `pnk upgrade --apply`.** That is a decision, not a gap … So it is a lower bound **you maintain**, rather than a promise the tool keeps for you.' `docs/DESIGN.md:164` and `docs/KB-UPDATES.md:128` say the same.**
  - *Source:* `sed -n '41,66p' docs/MANIFEST.md`; `sed -n '125,133p' docs/KB-UPDATES.md`
- **`docs/GUIDE.md § Moving, sharing and publishing a KB` (lines 768-783) — where a KB author decides to hand the KB to someone — mentions `.pinakes/`, sidecar privacy and `pnk://` ULIDs, and says **nothing** about the compatibility floor or unstamped keys. This is the real author-facing gap, and it is one sentence wide, not four MANIFEST rows.**
  - *Source:* `sed -n '768,784p' docs/GUIDE.md`
- **Nothing is queued and nothing was written. `CHANGELOG.md`'s `[Unreleased]` is empty; `changelog.d/` and `retro.d/` contain only `README.md`. `git status --short` is empty and HEAD is still e6d62e3 after all my commands. The sweep's note that 'main is now c23359f' is stale — c23359f is an ancestor of HEAD.**
  - *Source:* `sed -n '11,13p' CHANGELOG.md`; `ls changelog.d/ retro.d/`; `git status --short`; `git merge-base --is-ancestor c23359f HEAD` → YES

### The options

| Option | What it means | Pros | Cons | Work |
|---|---|---|---|---|
| **A. Withdraw the clause** | Mark § *Compatibility: it sets a `requires_pinakes` floor, explicitly* in `plans/20260805_1313-decisions-init-titles-and-grammar.md` as CLOSED-withdrawn, recording that its premise (an unknown-**value** error from `table.choice`) was superseded the same day by the key-not-value decision at `manifest.py:88-94`, and cross-citing it beside D-6 and D-11 in `plans/20260804_1016-template-release.md`. No code, no tests, no docs. | One file changed. No test touched, no pin at risk. · Consistent with what is already shipped and published — `docs/MANIFEST.md:61` has told users for months that the floor is 'a lower bound you maintain'. · Removes a live contradiction that has already cost time: the 20260825 sweep's reader closed this item on a false basis, an adversary reinstated it, and it sits as row 9 of the worklist. · Also fixes the fact that X2 skipped this file — it is the only pre-existing plan never revisited. | Records as *withdrawn* a decision the user actually took, on the strength of a premise shift they were never shown. If they meant the clause substantively, this erases it rather than answering it. · **Understates what remains open.** `docs/KB-UPDATES.md § 8` still carries the general question ('does an additive key oblige a floor bump? A rule is needed') and predates all three decisions. Withdrawing the one-key instance leaves the general question open and now less visible, because the only place it was concretely argued has been struck. · Leaves the misdiagnosis in place: `Check the spelling` stays reachable for four keys, and its pointer to `docs/DESIGN.md §2.1` stays stale — a defect KB-UPDATES §8 already records as owed. | Small — one plan-file edit plus two cross-citations. Planner, ~30 minutes. |
| **B′. A `pnk doctor` WARN that names the keys and no version** | Add a `Check` to `src/pinakes/doctor.py`: if the manifest sets any of the known settable-but-unstamped keys (`adjacent_k`, `graph_channel`, `headings`, `metadata`, `[deep]`) and declares no `[kb] requires_pinakes`, WARN naming the keys and pointing at `docs/MANIFEST.md` — **suggesting no version number**, exactly as `upgrade.py:1258-1268` already does for template-introduced keys. Plus tests, one `docs/VERIFICATION.md` row, and a `docs/CLI.md`/`docs/MANIFEST.md` note. | Honours the clause's intent while breaking neither D-11 nor D-6's recommendation: nothing is written to any manifest; the user is told and decides. · **Needs no key→release map** — the original brief's central objection to B was an artifact of the version-carrying variant it designed, not of the option. D-11's own shipped output is the precedent: name the keys, suggest no number, write nothing. · Reaches the population `upgrade` structurally cannot: hand-set keys, which is every case at issue here. · The name-list is gateable — `docs/MANIFEST.md` carries 'Not stamped into the template' on exactly 4 rows, so a `tests/test_verification.py`-shaped check could assert code and doc agree. · Covers all five unstamped keys, not the one the clause names. | **Costs more than one Check function, in a way I verified.** `diagnose(manifest: Manifest)` receives a parsed `Manifest`, and `KbSection` has no `requires_pinakes` field — `manifest.py:591` reads and discards it. The check needs a raw TOML re-read inside doctor or a new field on a core dataclass. · **Under-reports by construction.** A parsed manifest cannot distinguish an explicitly written `headings = "none"` — documented at `docs/MANIFEST.md:140` as a legitimate use — from absence. That KB is unreadable below 0.13 and the check would stay silent. Fixing that needs the raw read too. · Helps only the user who runs `pnk doctor` *before* sharing, and `docs/GUIDE.md § Moving, sharing and publishing a KB` does not prescribe it. The reader on the old build never sees the WARN. · Noise for the common case: a single-machine user with a healthy KB gets warned about a portability risk they do not have. · The name-list gate is feasible but not free — `[deep]` at `docs/MANIFEST.md:234` uses different wording, so the anchor is not uniform. · Real increment cost on a `doctor.py` already 1554 lines. | Medium — one Check plus its wiring, a raw-manifest read path, a gated name-list, tests (empty / already-floored / explicit-`none` cases), a `docs/VERIFICATION.md` row, doc updates, fragments. One coder increment, ~a day, on top of a planner ruling that authorises it. |
| **C. Make a surface write the floor** | Have Pinakes write `requires_pinakes` itself. `init` is ruled out by D-6's recommendation and is wrong anyway (a fresh KB carries no such key). `upgrade --apply` is forbidden by D-11 **and is structurally inapplicable** — `upgrade.py:1257` gates on `result.introduced`, derived from template hunks, and no archived template version carries these keys. So in practice this means a new `pnk floor`-style command, or reopening D-11. | The only option making the guarantee automatic rather than advisory — the reader on the old build gets the right message without anyone having remembered anything. · Fully discharges the clause as literally written. | Reverses a decision the user took 20260804 that is pinned by a **mutation-killed** test (`docs/RETROSPECTIVES.md:5183`). · No honest number is available: there is no key→release map, and `upgrade.py:1264` already ships that reasoning to users. · Writing a floor inherits exactly the cost D-6 option B was rejected for — it locks the KB out of pre-0.6.0 builds that would otherwise read it perfectly well. · For the keys the clause is actually about, the natural surface cannot fire at all, so this is a new command and a new decision, not a discharge of an existing one. · D-11's *own accepted-cost paragraph* already concedes the harm this would prevent, in the user's own decision, one day before the clause. Reversing it needs a reason stronger than 'the clause said so'. · Largest blast radius, for a repo-measured population of zero. | Large — reopen D-11, new surface or changed `upgrade` semantics, a maintained key→release map, rewrite the pinned test and its battery anchor, update MANIFEST/CLI/VERIFICATION. Planner-then-coder, multiple increments. |
| **D. Withdraw plus four floor numbers in docs/MANIFEST.md** | Everything in A, plus giving rows :140 (`headings`, `>=0.13`), :141 (`metadata`, `>=0.16`), :159 (`graph_channel`, `>=0.11`) and :234 (`[deep]`, `>=0.24`) the floor value each currently withholds, cross-linked to the floor section at :41. | Same cost class and risk profile as A — one planner-owned docs change, no pin touched. · Puts the number where a human maintains it, sidestepping D-11's objection: four sentences of prose, not a data structure code depends on. · Covers four keys, including `graph_channel` and `[deep]`, which nobody has raised and which have the identical defect. · A wrong floor fails conservatively — too high refuses a build that would have worked, which is visible and reported. · Leaves B′ available on top; nothing here has to be undone. | **Its central premise did not survive checking.** The original brief justified D by 'the docs name the hazard and withhold the remedy'. `docs/GUIDE.md:797` already gives the harmed reader the correct diagnosis *and* the correct remedy, in `## Troubleshooting`. What MANIFEST withholds is a floor number for the KB **author** — a smaller and differently-located gap. · **Not four row edits.** `docs/MANIFEST.md:59-61` currently justifies the tool's silence with 'no honest number is available anyway, because nothing here maps a manifest key to the release that introduced it.' Adding four numbers to the same document strains that sentence, so the neighbourhood-audit rule pulls the floor section in too. · **Wrong document for the author's moment of decision.** The author decides portability in `docs/GUIDE.md § Moving, sharing and publishing a KB` (768-783), which mentions the floor nowhere. A reader of the `headings` row is configuring chunking, not deciding to share. · Purely advisory. A user who does not read the table still ships a floorless KB. · Four version numbers with nothing gating their correctness — a fifth unstamped key gets no row unless someone remembers. Same maintenance hazard as B′'s list, without B′'s available gate. · Does not satisfy the clause's *Required*, so it is honestly a withdrawal-plus-mitigation. · Depends on a human remembering, twice: to read the row, and to add a row for the next key. | Small — one plan-file edit, four `docs/MANIFEST.md` rows, a reworded floor section, then `make docs`. Planner, ~1–2 hours. |
| **E+F. Fold into KB-UPDATES §8, fix the unknown-key remedy, one line in the sharing section** ⭐ | Three parts. **F (planner):** mark the plan section CLOSED-superseded rather than withdrawn, recording the premise shift, and fold it into `docs/KB-UPDATES.md § 8`'s existing open question — which predates it and already says 'A rule is needed' — cross-citing D-6 and D-11 there so all four sit in one place. **E (coder):** amend `src/pinakes/_toml.py:214-219` so the unknown-key remedy offers the second hypothesis — 'if you did not mistype it, this manifest may have been written by a newer Pinakes: upgrade, or ask its author to declare `[kb] requires_pinakes`' — and fix its stale pointer from `docs/DESIGN.md §2.1` to `docs/MANIFEST.md`, which `docs/KB-UPDATES.md § 8` already records as owed. Pin the new sentence with a test. **Plus (planner):** one sentence in `docs/GUIDE.md § Moving, sharing and publishing a KB` telling an author that hand-set keys make a KB unreadable by older builds and that `[kb] requires_pinakes` is how to say so. | **Fixes the user-visible harm at its only source.** The `Check the spelling` misdiagnosis is one string, and amending it helps *every* future key in every table — including `[deep]`, whose message today names no table at all. · **Nothing here can be wrong.** No version number is asserted anywhere, so there is no map to keep honest and no decimal point to be a guess. · **E is owed anyway.** `docs/KB-UPDATES.md § 8` already records the stale `DESIGN.md §2.1` pointer as a follow-up, and I verified DESIGN §2.1 now delegates every field to MANIFEST.md. The marginal cost of the second sentence is near zero. · **F is more honest than withdrawal.** It does not claim to have answered a question that a published design document has held open since 20260728; it puts the one-key instance where the general question already lives. · Contradicts no decision, no invariant and no pinned test. `grep` confirms no test asserts the remedy string. · Reversible and additive: B′ remains fully available on top, and nothing here has to be undone if a real multi-reader KB appears. · The GUIDE sentence lands where the author actually decides to share, which four MANIFEST rows do not. | **E is strictly forward-only, and this is its honest cost.** It changes the *reading* build, so it cannot help anyone on 0.6.0–0.30.x today. Every window already open — `graph_channel` 0.6.0–0.10.x, `headings` 0.6.0–0.12.x, `metadata` 0.6.0–0.15.x, `[deep]` 0.6.0–0.23.x — stays exactly as it is. The floor is the only lever that reaches an already-shipped reader, and this option declines to pull it. · **F answers nothing.** The user asked-and-answered something on 20260805; this records that the answer's premise moved and hands the question back to a bullet that has been open for a month. Anyone wanting the clause honoured, either way, is not satisfied. · Three files across two owners, so it needs the ownership split respected: the coder proposes the KB-UPDATES §8 strike as a `git diff`, and the planner incorporates it. · Touches a user-facing error string, so the docs-sync rule applies and the new sentence needs its own test — and the repo's discipline says a test is only a pin once it has been seen to go red. · Still leaves the author-facing floor numbers undocumented; a user told 'declare a floor' must work out `>=0.13` themselves from the CHANGELOG. · Deliberately does not reduce the count of documents answering the floor question — it adds a cross-reference rather than consolidating. | Small-to-medium, split. F plus the GUIDE sentence: planner, ~1 hour (plan disposition, KB-UPDATES §8 fold, one GUIDE line, `make docs`). E: one coder increment, ~2 hours (one string in `_toml.py`, one new test, changelog.d + retro.d fragments, and a proposed diff for the KB-UPDATES §8 follow-up bullet). No new data structure, no VERIFICATION row beyond the one the new test needs. |

> ### ⭐ Recommended: **E+F. Fold into KB-UPDATES §8, fix the unknown-key remedy, one line in the sharing section**
>
> **The single deciding fact: `docs/GUIDE.md:797` already exists, and it dissolves the gap D was built to close.** The row reads `| unknown key(s) in a KB you did not edit | The same cause, on a KB that declares no floor, so the refusal can only report the symptom | Upgrade Pinakes. |`. The person the original brief identified as harmed — a collaborator opening someone else's KB on an old build — is exactly 'a KB you did not edit', and the remedy given is the correct one, in the Troubleshooting table where they would look. The original brief asserted this row 'misses this case exactly'. It does not; I opened it.

That removes D's justification, and what is left of the harm is one string: the error message itself only ever offers the typo hypothesis, and points at a `docs/DESIGN.md §2.1` that has delegated its field tables to MANIFEST.md since 0.2.1 — a defect `docs/KB-UPDATES.md § 8` already records as owed. So the cheapest correct action is to fix the message, which costs one string and one test, asserts no version number, and helps every future key rather than the four we happen to have named.

**Why F rather than A's withdrawal.** `docs/KB-UPDATES.md § 8` has carried the general question since 20260728 — *'Does an increment's manifest addition oblige a `requires_pinakes` bump? … A rule is needed'* — which is older than D-6, D-11 and the clause. The 20260805 clause is the one-key instance of it. Writing 'withdrawn' on the instance while the general question stays open in a published document would leave the repo in a worse state than it is now: the question would remain, and the only place it was ever argued concretely would be struck.

**Why I did not recommend B′, having strengthened it.** I removed the original brief's central objection — B needs no key→release map, because `upgrade.py:1258-1268` already ships a map-free recommendation and a `doctor` check can copy it, and the name-list is gateable off the four `docs/MANIFEST.md` rows carrying 'Not stamped into the template'. But I then found two costs the brief missed: `diagnose()` takes a parsed `Manifest`, and `KbSection` has no `requires_pinakes` field (`manifest.py:591` reads and discards it), so the check needs a raw re-read or a core dataclass change; and the parsed manifest cannot see an explicitly written `headings = \"none\"`, which is a documented, portability-breaking case the check would silently miss. Against a repo-measured population of zero KBs, that is a real maintenance surface bought for a hazard nobody has demonstrated.

**C I recommend against outright** — and more strongly than the original did. D-11's own accepted-cost paragraph, taken by the user on 20260804, already states that 'a KB that has adopted a newer key still fails on an older build with \"unknown key\"'. The user was shown this harm and accepted it, a day before the clause. Reversing that needs a reason better than a clause whose stated mechanism never shipped.

**The honest cost of my recommendation, stated plainly:** E is forward-only. It changes the reading build, so every window already open stays open, and a collaborator on 0.12.0 today still gets a bad message. The floor is the only lever that reaches an already-shipped reader, and I am declining to pull it. If reaching *today's* old builds matters, the answer is B′ (or D's numbers as a stopgap) and its cost should be paid deliberately.

**The first pass recommended _D. Withdraw the clause and close the documentation gap in the same change_ and the adversary moved it.** The reasoning above is the revised one.

**What would change this.** **1. One real `pinakes.toml` outside `tests/` that sets `headings`, `metadata`, `graph_channel` or `[deep]` and is opened by a second install.** Falsifiable: produce the file and the second install's version. Zero exist in the repo and nothing writes them, so B′'s maintenance surface currently buys a hazard nobody has demonstrated. One instance flips me to B′.

**2. The user ruling that the clause meant *Pinakes* must write the floor.** The clause is passive and names no agent; I treated that as ambiguity. If they intended an automatic guarantee, this is a genuine contradiction with D-11 rather than a stale premise, and the answer is C with D-11 explicitly reopened — not E+F.

**3. `docs/GUIDE.md:797` turning out to be unreachable in practice.** My whole case against D rests on that row serving the harmed reader. If someone shows the Troubleshooting table is not where a user with this error lands — a support instance, a dogfooding note, anything — D's premise is restored and its four numbers are worth their cost.

**4. `pnk doctor` becoming a prescribed pre-share step.** I checked and it is not: `docs/GUIDE.md § Moving, sharing and publishing a KB` (768-783) never mentions it. If the planner adds that prescription — which the GUIDE sentence in my own recommendation makes natural — then B′'s biggest weakness (the warning reaches the wrong person) shrinks and B′ rises above E+F.

**5. A cheap way to make `doctor` see the raw manifest.** If adding `requires_pinakes` to `KbSection` (or a raw-dict field on `Manifest`) turns out to be a small, well-precedented change rather than a core-dataclass amendment, B′'s cost drops to roughly D's and it becomes the better answer, because it also catches the explicitly-written `headings = \"none\"` case that nothing else reaches.

**6. Evidence that the unstamped-key pattern is ending.** My case for E rests on the class recurring — five keys across 25 releases. If the planner states that future keys will be stamped with a deliberate break instead (`manifest.py:798` leaves that door open: 'until a release deliberately accepts the break'), E's forward-looking value largely evaporates and A or F alone is enough.

**Cost of getting it wrong:** low. **Owner:** planner-then-coder.

**Not established — do not treat these as settled:**

- **Whether any Pinakes KB outside this repository sets any of the four keys.** I checked `tests/demo-kb`, `tests/partner-kb`, the live `notes` template and both archived versions — none do. I did not check PyPI download statistics, GitHub dependents, or any external KB. The population argument that keeps B′ off the recommendation rests entirely on this, and it is the weakest link in this brief exactly as it was in the original.
- **Whether builds in the misdiagnosis windows (0.6.0–0.23.x depending on key) are in use anywhere.** Unknown and probably unknowable from here.
- **I did not run `./check.sh` or the full test suite.** I ran `tests/test_manifest_compat.py` + `tests/test_manifest.py` (74 passed) and three targeted tests (3 passed). I am not asserting the gate is green at e6d62e3.
- **I did not revert anything to watch a test go red.** I am relying on `docs/RETROSPECTIVES.md:5183` for D-11's mutation kill and did not re-run the battery. Per this repo's own rule, 'pinned' is a claim about a failing test, and I did not make it fail.
- **I did not write or test option E's replacement string.** I verified the current text, that nothing pins it, and that its `DESIGN.md §2.1` pointer is stale. The exact wording, its `ruff`/`pyright` cleanliness and its fit with the project's error-message conventions are unverified.
- **I did not verify that a name-list gate for B′ would actually be accepted or would hold.** I established the anchor phrase appears on exactly 4 `docs/MANIFEST.md` rows and that `[deep]` uses different wording; I did not write the gate or check it against the planner's rule that headings must not be renamed to fix anchors.
- **The user's 20260805 intent.** The plan records the decision, not the conversation. My reading — that the clause's agency was left open rather than assigned to Pinakes — is an inference from the passive voice and the absence of any cited surface, not a fact I can source.
- **Whether any live peer or in-flight branch is touching `plans/20260805_1313-…md`, `docs/KB-UPDATES.md`, `docs/GUIDE.md` or `src/pinakes/_toml.py`.** I did not run `ListAgents` or `tools/shared_file_overlap.py` — this was read-only. Whoever lands the change must, and `docs/GUIDE.md` and `docs/KB-UPDATES.md` are both high-traffic planner-owned files.
- **Whether `docs/KB-UPDATES.md § 8`'s open question is the planner's preferred home for this.** I established it is the older and more general statement of the same question; I did not check whether the planner considers KB-UPDATES ('Status: mostly proposal') a live decision surface or an archived design note.

---

## g5-gate-rerun-and-parent-child-arity

**The question.** Two questions the original brief bundled into one: (a) does arity requirement 3 close now, on the finding that its "if the ceiling is alarming" conditional does not fire on any real corpus measured; and (b) separately, do we spend ~2.4 h of free CPU to re-run G5's three-leg gate on the RFC corpus at `[chunking] headings = "numbered"`, so a shipped default's published verdict stops resting on an index where three of seven edge kinds derived zero?

**Status of the argument.** **I am partially overturning.** I uphold running the gate re-run, but I overturn the brief's deciding fact, its bundling, and three of its factual assertions. I re-ran its harness from scratch rather than accepting its numbers.

**What I verified myself (independent re-run, nothing accepted on the brief's word):** I cloned `pinakes-corpus-rfc` and re-chunked all 300 documents through the real `chunk_document` with the real fastembed `BAAI/bge-small-en-v1.5` tokenizer, then re-derived `_hierarchy_edges`' exact relation. **Every substantive measurement in the brief reproduces** — 80 054 heading-carrying chunks over 283/300 documents, 366 953 transitive `parent-child` rows, 257 002 immediate, 3.80 vs 2.66 per chunk, −30.0%, per-document median 0.75 / p90 5.74 / max 20.44, **0 of 300 reaching 53.42**, and **8 of 20 gated questions** blind with all three recorded regressions inside that set. Those hold. I also opened `graph_gate.py`, `graph_matrix.py`, `edges.py`, `store.py`, `chunk.py`, the arity plan, the two G5 retrospectives and STATUS.

**What does not survive.**

1. **The deciding fact is a misreading.** The brief rests on the arity plan's *"It would be deciding a retrieval question on a cost argument"* to argue that closing requirement 3 without measuring the arm is forbidden. Read in place (`20260804_1844:66`), that sentence's subject is **adopting** immediate-parent-only as the default, not declining to measure it. The plan forbids *switching* on cost grounds; requirement 3 is a plain conditional — *"**If** the ceiling is alarming…"* — and the brief's own best evidence shows the antecedent is false on every real corpus (4.95 / 3.80 / 0). Nothing about retrieval is decided by letting a false antecedent close a conditional; the transitive form stays exactly as built. **The coupling breaks, and with it the brief's "this collapses to a binary".**

2. **`store.py:175` has a CHECK constraint on `edges.kind`.** The brief asserted *"There is no CHECK constraint on `edges.kind`, so no schema bump is implied"* — the one blast-radius item it claimed to have checked. It is wrong. A new kind name is rejected by the constraint, so it would need a schema change and `SCHEMA_VERSION` 3→4, forcing **every existing KB to rebuild**. Reusing `parent-child` avoids that but then the arm cannot coexist with the transitive set in one index — and `run_leg` opens the index with `store.connect_ro` and filters kinds at query time, so **an immediate-parent arm is not a `LEGS` append**; `graph_matrix.py` has no place to re-derive.

3. **The arm, as designed, licenses nothing.** `graph_gate.py:165`: *"The arms are reported beside the headline, never gated."* The gate takes exactly three legs and there is no arm-vs-arm statistic anywhere in this repo. The precedent is explicit: `expand-in-degree` outperformed and was declared *"a direction for a future measurement on a corpus that can carry one, not a result."* So the brief's pro *"discharges requirement 3 by measurement"* is overstated.

4. **A cheap grammar fix does exist, and the brief dismissed it on an inverted citation.** It claimed §5.4 rejects amendments that *"change exactly one verdict across the corpus"*. The docstring says the opposite — start-at-1 *"changes exactly one verdict across the corpus, and it is the wrong one"* is the **reason it was adopted**; the rejected discriminator was rejected for taking *"three genuine documents with it"*. I then measured the amendment: allowing a trailing `.` in clause 4 **gains rfc3261 and rfc4097 and loses rfc2865** (283→284 of 300). Since rfc3261 alone carries **5 of the 8** blind questions, that one character takes the blind set from 8/20 to **3/20**. It is not free and it does not reach rfc2616 — I confirmed the source genuinely has no `13.1`, only `13.1.1`–`13.1.6` — but "no cheap fix" is wrong.

5. **Two named bounds, and the re-run fixes one.** `STATUS.md:585` names both: zero heading paths **and** `[retrieval.confidence]` commented out, so *"two of the gate's four clauses could not fire on it."* The committed manifest still has it commented out. The brief quoted the heading half of that paragraph and stopped. **A re-run at `headings = "numbered"` leaves clauses 3 and 4 exactly as vacuous as they were** — a cost absent from its cons.

6. **A correction in the brief's favour, and one against.** Its chunk counts were 38 off and it blamed *"the unpinned backend revision"*; my run reproduces the committed **106 806 exactly** on today's fastembed 0.8.0, so the gap was its harness and the drift explanation was a rationalisation. Conversely its `~2 h` is **firmer than it admitted**: it listed sync cores as unverified, but `RETROSPECTIVES.md:4203` already records 55 RFCs / 16 557 chunks in 1 497.7 s at mean 4.8 of 10 cores under fastembed. Scaling by chunks gives **~2.4 h**. Its own falsifier — *"if it is 25 minutes"* — is dead: 25 minutes is the measured time for 55 documents.

**Why it stayed open** is unchanged and correct: an ownership seam. `RETROSPECTIVES.md:3571` handed the arity input to the planner in its own words, no planner document picked it up in 21 days, and the gate re-run was explicitly excluded from the grammar decision (`20260805_1313:126`) and filed nowhere else.

### What was established

- **My independent re-chunk of all 300 committed documents at the manifest's own settings (max_tokens=510, overlap=64, kind="text", headings="none") returns exactly 106 806 chunks and 0 heading paths — an exact match to the corpus's committed measurements.json. The brief reported 106 768 and attributed the 0.036% gap to backend drift; the gap was its harness, not the backend.**
  - *Source:* Cloned github.com/lucagattoni/pinakes-corpus-rfc; ran pinakes.chunk.chunk_document with the real fastembed BAAI/bge-small-en-v1.5 backend (fastembed 0.8.0); compared against measurements.json {"chunks_total": 106806, "chunks_with_heading_path": 0}
- **At headings = "numbered" the same 300 documents give 96 676 chunks (−9.5%), 80 054 of them carrying a heading_path = 82.81% coverage, across 283 of 300 documents. The staged-gates floor is "at least 50% of chunks carrying a non-empty heading_path", recorded as 0% and failing. Heading-depth distribution: 19 733 at depth 1, 38 010 at 2, 19 809 at 3, 2 391 at 4, 111 at 5 — median depth 2.**
  - *Source:* Same harness at headings="numbered"; floor quoted from plans/20260804_1016-staged-channel-gates.md:315
- **Re-deriving _hierarchy_edges' exact relation over that chunking: transitive parent-child = 366 953 rows (3.796/chunk); immediate-parent-only = 257 002 (2.658/chunk), a 30.0% reduction. Per-document rows/chunk: median 0.752, p90 5.744, max 20.440 (rfc2865.txt). ZERO of 300 documents reach the synthetic corpus's 53.42. These are 300 real technical specifications — the shape RETROSPECTIVES named as "an ordinary specification or manual". Every one of these figures matches the brief exactly.**
  - *Source:* Harness replicating edges._hierarchy_edges (segment prefix on " > ", all ancestors, per document, immediate = d == len(segments)-1) over the cloned corpus
- **store.py:175 DOES carry a CHECK constraint: `kind TEXT NOT NULL CHECK (kind IN ('membership','sibling','parent-child','in-section','co-located','shared-tag'))`. The brief asserted "There is no CHECK constraint on edges.kind, so no schema bump is implied" — the single blast-radius item it claimed to verify. A new kind name needs a schema change and SCHEMA_VERSION 3→4, which forces every existing KB to rebuild.**
  - *Source:* grep -n 'CREATE TABLE edges' -A 10 src/pinakes/store.py; SCHEMA_VERSION: Final = 3 at store.py:28
- **An immediate-parent arm is not a LEGS append. run_leg opens the index with store.connect_ro and passes select_kinds(drop=spec.drop) — all seven legs share ONE derived edge set, filtered at query time. A different relation needs a different edge set in the database, so graph_matrix.py would need a re-derive step it has no place for. ALL_KINDS, SYMMETRIC_KINDS, the per-kind WEIGHT dict and Census ("every kind is a key") all take a new name too.**
  - *Source:* sed -n of run_leg in tools/graph_matrix.py; ALL_KINDS/HUB_KINDS/SYMMETRIC_KINDS/WEIGHT at src/pinakes/graph/edges.py:101-109; select_kinds at :172
- **graph_gate.py:165 states "The arms are reported beside the headline, never gated." The gate takes exactly three legs (--before, --after-without, --after-with) and refuses legs that drop different structural kinds; there is no arm-vs-arm statistic in the repo. The precedent for a reported arm is explicit: expand-in-degree outperformed every other leg and RETROSPECTIVES calls it "reported, never gated ... a direction for a future measurement on a corpus that can carry one, not a result."**
  - *Source:* tools/graph_gate.py:165 and the --before/--after-without/--after-with argparse block at :455-457; docs/RETROSPECTIVES.md § G5 — the gate, run
- **The arity plan's "It would be deciding a retrieval question on a cost argument" has ADOPTING immediate-parent as its subject, not declining to measure it. The full row reads: "Cheap and tempting, but it changes the relation the probe measured ... It would be deciding a retrieval question on a cost argument. It is the right thing to *measure* if the ceiling proves alarming — as an arm, not as a default." Requirement 3 is a conditional: "If the ceiling is alarming, the immediate-parent variant is the arm to measure."**
  - *Source:* plans/20260804_1844-decision-parent-child-arity.md, § Why not the alternatives table and § The decision requirement 3, read in full
- **8 of the 20 gated multi-hop questions cite a grammar-rejecting document, and all three recorded regressions are among them — but the concentration is uneven and decisive: rfc3261 blinds FIVE questions (sip-invite-2xx-retransmission-defect, sip-forking-amplification-defence, sip-ipv6-abnf-correction, sip-event-notification-succession, tls-deprecation-reach-across-protocols) and rfc2616 blinds THREE (http-rate-limit-status-code-provenance, content-disposition-http-takeover, well-known-uri-prefix-succession). The brief treated the two as a single indivisible blocker.**
  - *Source:* Cross-referenced the 17 rejecting documents (computed by my harness) against every `expect` list in corpus eval/questions.yaml; regression ids from docs/RETROSPECTIVES.md § G5 — the gate, run
- **A one-character clause-4 relaxation (allow a trailing ".", keep ",;:") measured across all 300 documents GAINS rfc3261 and rfc4097 and LOSES rfc2865 — acceptance 283 → 284. Since rfc3261 alone blinds 5 of the 8 questions, that single change takes the blind set from 8/20 to 3/20. The brief dismissed this on an inverted citation: chunk.py's docstring gives "changes exactly one verdict across the corpus, and it is the wrong one" as the REASON start-at-1 was adopted, and rejected a rival discriminator because it "took three genuine documents with it".**
  - *Source:* Re-implemented _numbered_candidates/_outline_ok with the punctuation set as a parameter and ran both variants over all 300 documents; docstring at src/pinakes/chunk.py:521-560
- **rfc2616 is genuinely unreachable by any punctuation or spacing fix: grep finds only 13.1.1–13.1.6 (ToC lines 208-213 and body line 4185) and no `13.1` anywhere, so the walk really does jump 13 → 13.1.1. Admitting that gap means weakening the rule the grammar plan calls "the only thing standing between this grammar and an ordered list". Diagnosing all 17 rejections shows the causes are heterogeneous — a missing space (rfc3428 "15.Normative References"), ordered-list contamination (rfc4916 "200 (2)"), an ASN.1 dump (rfc9919), a genuine source typo (rfc4540 5.3.15 → 3.5.16), and two files yielding zero candidates.**
  - *Source:* grep -rn '13\.1' on the corpus copy of rfc2616.txt; per-document break diagnosis running _numbered_candidates/_is_valid_step over all 17; quote from plans/20260805_1721-metadata-as-retrieval-context.md:900
- **STATUS.md names TWO bounds on the G5 verdict, and the re-run removes only one: "The corpus has [retrieval.confidence] commented out, so two of the gate's four clauses could not fire on it ... And no chunk in it carries a heading_path." The committed manifest still has the whole [retrieval.confidence] block commented out, so a re-run at headings="numbered" reproduces the vacuous-clause defect exactly. The brief quoted only the heading half of this paragraph.**
  - *Source:* sed -n '583,596p' docs/STATUS.md; the committed pinakes.toml fetched via gh api, [retrieval.confidence] block commented out
- **The ~2 h estimate is the project's own written figure and is firmer than the brief admitted. plans/20260805_1313:128 and sweep row 16 both say "~2 h of CPU embedding plus a schema_version 3 rebuild". The sync-core question the brief listed as unverified is already measured: 55 RFCs / 16 557 chunks in 1 497.7 s at peak 5.0 / mean 4.8 of 10 cores under fastembed. Scaling by chunks, 96 676/16 557 × 1 497.7 s ≈ 8 743 s ≈ 2.4 h. The brief's falsifier "if it is 25 minutes" is dead — 25 minutes is the measured time for 55 documents.**
  - *Source:* docs/RETROSPECTIVES.md:4203-4213 § The sync-CPU number; docs/STATUS.md:354 (0.14.0); plans/20260805_1313-decisions-init-titles-and-grammar.md:126-129
- **Leg count is NOT a meaningful cost: at the recorded 2012 ms/query and 20 questions, one leg is ~40 s, so all seven are ~5 minutes against a ~2.4 h sync. The brief listed "every leg must be re-produced" as a con of the recommended option; it is cost-trivial. What IS real is that no G5 gate artifact was ever committed to either repository, so there is no per-question diff against 20260804 — and the gate refuses mismatched `chunking` anyway.**
  - *Source:* Per-query ms from docs/RETROSPECTIVES.md § G5 matrix table; `ls eval/` returns no such directory in the Pinakes repo; corpus commits only eval/probe/ (reachability probe, schema_version 2)
- **The re-run adds 445 280 structural edges (366 953 parent-child + 78 327 in-section) while sibling shrinks by ~10 130, and it mints 7 859 new heading hubs with max degree 499 (median 6, p99 65). The recorded harm mechanism at 20260804 was hub displacement pulling whole documents into fusion (co-located 262, shared-tag 643). in-section is a HUB_KIND and damped by degree, so the direction is genuinely open — but this adds hubs of the shape that caused the recorded harm, which sharpens the null prior rather than softening it.**
  - *Source:* Same harness, counting per-(doc, heading_path) buckets with ≥2 members; HUB_KINDS at src/pinakes/graph/edges.py:103; harm mechanism from docs/RETROSPECTIVES.md § The --drop sibling arm answers its question
- **The corpus's committed manifest disagrees with its own builder, today, independently of this decision: tools/build_rfc_corpus.py:252 stamps `headings = "numbered"` into the manifest it writes, while the committed pinakes.toml has no headings key. write_manifest never overwrites an existing manifest, so anyone rebuilding an existing clone silently reproduces zero heading paths. The builder's docstring already carries a ⚠️ saying so.**
  - *Source:* grep -n headings tools/build_rfc_corpus.py (line 252 in the MANIFEST template); gh api repos/lucagattoni/pinakes-corpus-rfc/contents/pinakes.toml; write_manifest docstring at :295-307
- **Statistical bar and prior, confirmed in code: sign_test is an exact one-sided McNemar form and ALPHA = 0.05, so a pass needs ≥5 improvements with 0 regressions (p = 0.031); 5 improved / 1 regressed gives p = 0.109 and fails. The 20260804 run was 0 improved / 3 regressed, p = 1.0000, on a 7/20 baseline. Separately, comparability with 20260804 is broken by 21 releases as well as by the rechunk, so a re-run yields a fresh verdict at today's HEAD — not "the 20260804 verdict corrected".**
  - *Source:* tools/graph_gate.py sign_test at :90-100 and ALPHA at :81; docs/RETROSPECTIVES.md § G5 — the gate, run; git log shows no src/pinakes/graph/ change since 2026-08-05, but __version__ is 0.30.3 against the 0.11.0-era run
- **The corpus is fully reproducible and permanent, so closing is reversible at the cost of re-running: 300 documents, sidecars, manifest and a frozen 20-question golden set are all committed and public (I cloned the whole repo). questions.yaml's own header records the conversion — "20 frozen questions, 20 here", ids carried over one-to-one, all kind: multi-hop — which matches the reported 7/20 baseline and the three regression ids. The unavailable pieces are the index and the unpinned backend revision.**
  - *Source:* git clone --depth 1 of pinakes-corpus-rfc (300 .txt); eval/questions.yaml header block and its 20 entries; eval/multihop-questions.yaml also 20
- **This repository has a settled vocabulary for this decision class, used once and carried in two documents: "T8 is closed as a no-go and T6 is deferred behind a written trigger" — T8 because "waiting cannot move that", T6 behind a condition that can actually fire ("a KB that is actually queried crossing ~50 000 chunks with felt latency").**
  - *Source:* docs/ROADMAP.md:1058-1068 and the template-release row at :197; docs/STATUS.md:48-49

### The options

| Option | What it means | Pros | Cons | Work |
|---|---|---|---|---|
| **Split — close arity requirement 3 now, and run the gate re-run as its own three-leg gate** ⭐ | Two separate acts. (1) Planner, now: write into `plans/20260804_1844-decision-parent-child-arity.md` that requirement 3's conditional does not fire — 0 of 300 real technical specifications reach 53.42, the corpus figure is 3.80 rows/chunk against 4.95 on this repo's own docs, and the 53.42 came from a six-document synthetic corpus the retrospective itself said is "not evidence that real corpora do this". Transitive stays as built; no immediate-parent arm is built. (2) Separately and on its own merits: clone the corpus to a scratch directory, add `headings = "numbered"`, `pnk sync --rebuild` (~2.4 h), run `tools/graph_matrix.py`'s existing seven legs and `tools/graph_gate.py`'s three-leg gate, and write the verdict into `docs/STATUS.md`. Optional rider, decided explicitly rather than assumed: relax clause 4's trailing period first (measured: +rfc3261, +rfc4097, −rfc2865), which takes the blind set from 8/20 to 3/20. Files: no `src/` change at all without the rider; planner-owned `plans/20260804_1844-*.md`, `docs/STATUS.md`, `docs/ROADMAP.md`, `plans/20260825_1252-plans-sweep-findings.md`, `CHANGELOG.md`. | Each half is decided on its own evidence. Requirement 3 closes on a measurement (3.80 rows/chunk over 300 real specifications, 0/300 at the trigger) rather than on a run that could not settle it anyway — no arm-vs-arm statistic exists in this repo. · Removes the whole immediate-parent build, which is where the brief's cost estimate was most wrong: `store.py:175`'s CHECK constraint, `ALL_KINDS`, the per-kind `WEIGHT` dict, `Census`, and a re-derive step `graph_matrix.py` has no place for, since `run_leg` opens the index read-only. · The gate re-run then does the one thing only a run can do: retire a standing caveat on a *shipped default*. `expand` ships `off` on an instrument where three of seven kinds derived zero and `--drop sibling` — 99.2% of the graph's mass — measured size-slice adjacency rather than sections. · Cheap in the dimension that is actually scarce. The seven legs cost ~5 minutes on top of the ~2.4 h sync; no money, no scarce asset, corpus and golden set permanent and public. · Also unblocks the staged PPR gate's heading floor (82.8% measured against a 50% floor), and the rider is now costed rather than hand-waved. | **The gate half is a close call and the facts do not settle it.** Expected outcome is a second null on a 7/20 baseline needing ≥5 improvements and 0 regressions, and the re-run adds 7 859 new heading hubs (max degree 499) — hubs of the shape that caused the recorded 20260804 harm. · **It fixes one of the two bounds STATUS names, not both.** `[retrieval.confidence]` is still commented out in the committed manifest, so clauses 3 and 4 stay as vacuous as they were. The output is a less-caveated null, not a clean gate. · Blind on 8 of 20 questions (3 of 20 with the rider, which costs rfc2865 and needs §5.4's doubling-round re-measurement to license). rfc2616 is unreachable either way — its source genuinely has no `13.1`. · **It depends on a planner session actually happening afterwards.** That is precisely the step that did not happen for 21 days and is the reason this decision exists. A run whose write-up never lands leaves the repo worse than today: an unrecorded measurement. · Not comparable to 20260804 — 21 releases plus a rechunk — so it cannot say "headings changed the verdict", only "here is today's verdict". · If the user rejects my reading of requirement 3, this option throws away the only cheap chance to measure the arm. | Half (1): one short planner pass, no CPU, ~30 minutes — the measurement is already in this brief. Half (2): ~2.4 h unattended CPU, one coder session to drive the rebuild and the legs (no `src/` change without the rider), one planner session for the write-up. Planner owns `docs/`, `plans/`, `CHANGELOG.md`; coder owns the run and, with the rider, `src/pinakes/chunk.py` plus tests and a battery section. |
| **Split — close arity requirement 3 now, and close the gate re-run too** ⭐ | Planner-only, one session. Close requirement 3 exactly as above. Then also record that G5's verdict stands as final: write into `docs/STATUS.md` and `docs/ROADMAP.md` that the gate will not be re-run, naming the heading-coverage bound as accepted rather than outstanding, and citing this brief's numbers (82.8% coverage available, 8/20 questions still blind, both named bounds not fully removable). Update rows 10 and 16 of the sweep. No code, no CPU. | Cheapest, and each half now has a written reason rather than an ownership seam. Two items leave the live list — the state they have been in for 21 days. · Honest about the arity half, which is genuinely settled: the conditional's antecedent is false on every real corpus measured, and this brief supplies the fourth data point. · Defensible on the gate half too: the corpus cannot cleanly answer the question (8/20 blind, no cheap fix reaching rfc2616), and the re-run would leave two of four clauses vacuous regardless. · Matches the T8 precedent — a gate was run, its result stands, waiting cannot move it — and is fully reversible: corpus, golden set and this measurement are permanent and public. · Removes the failure mode that Option 1 carries: no measurement can be left unwritten if none is taken. | **Makes final a verdict whose own record says the instrument could not exercise three of its seven mechanisms.** That is this repository's named failure class — an assertion satisfied by something other than the property it names — accepted deliberately rather than removed for ~2.4 h of free CPU. · The `--drop sibling` finding stays permanently caveated as measuring size-slice adjacency, on 99.2% of the graph's mass, in a shipped default. · Leaves the staged PPR gate blocked on a corpus reason that this brief has just shown is removable by one manifest line and 82.8% coverage — an awkward thing to have written down. · Closes without ever measuring the immediate-parent arm. I argue the conditional makes that legitimate; a reader who disagrees will read it as the cost argument the arity decision warned about. | One planner session — four or five documents, no code, no CPU. Planner only. |
| **Run it bundled — the original recommendation, with the arm as an eighth leg** | As the original brief proposed: rebuild at `headings = "numbered"`, run the seven legs and the gate, and additionally add an immediate-parent branch to `_hierarchy_edges` behind a flag and run it as an eighth leg, to discharge requirement 3 by measurement rather than by argument. | Answers the arity question by measurement, which is what a reader who rejects my conditional reading would demand — and the derive itself really is seconds, since `derive` never re-embeds (214 608 edges in 1.3 s on this corpus). · If the arm is going to be measured at all, this is the only moment it is cheap: `parent-child` derives zero rows on every index that exists today. · Delivers everything Option 1's gate half delivers, plus the arm. | **The arm cannot license what it is being asked to license.** `graph_gate.py:165` — "The arms are reported beside the headline, never gated" — and there is no arm-vs-arm statistic anywhere. The precedent is `expand-in-degree`: it outperformed and was declared "a direction for a future measurement, not a result". · **Materially more work than the brief costed.** `store.py:175`'s CHECK constraint means a new kind name needs a schema change and `SCHEMA_VERSION` 3→4, rebuilding every existing KB; reusing `parent-child` avoids that but then `graph_matrix.py` must re-derive between legs, and `run_leg` opens the index `connect_ro`. Plus `ALL_KINDS`, `WEIGHT`, `SYMMETRIC_KINDS`, `Census`, `pnk doctor`'s hub report, VERIFICATION rows and a battery section. · Touches `src/pinakes/graph/edges.py`, which has not changed since 0.21.1, and risks the two tests that pin the transitive relation as a product. · Carries every con of Option 1's gate half — 8/20 blind, two clauses still vacuous, expected null — and adds the arm's build cost on top. · Spends a coder increment to produce a number that, by this project's own written standard, does not settle the question it was built to ask. | ~2.4 h CPU plus one to two coder increments (the derive-time branch, the schema-or-re-derive choice, tests, battery, census surface) and one planner session. Coder for `src/`, `tools/`, `tests/`; planner for `docs/`, `plans/`, `CHANGELOG.md`. |
| **Close both on the numbers, without splitting the reasoning** | The original brief's Option 2, unchanged: one planner pass that retires requirement 3 and finalises the G5 verdict together, on the combined argument that the ceiling is not alarming and the corpus cannot cleanly answer the retrieval question. | Same cost and same output as the split-and-close option, in one document pass rather than two arguments. · Everything true of split-and-close is true here. | Bundles two questions whose evidence differs sharply in strength. The arity half is settled by measurement (0/300 at the trigger); the gate half is a judgement about whether a removable caveat is worth 2.4 h. Writing them as one closure hides that asymmetry from the next reader, which is how the current seam formed. · A future reader cannot tell which half to reopen when the reason for one stops holding. · Otherwise inherits every con of split-and-close. | One planner session. Planner only. Strictly dominated by the split version — same work, less legible. |
| **Fix the corpus manifest upstream and take neither decision now** | Commit one line — `headings = "numbered"` — to `pinakes-corpus-rfc/pinakes.toml`, with a note reconciling `measurements.json`'s 106 806 / 0 against the new setting. No gate run, no arity write-up here. This is a repair of a defect that exists today independently of the decision: `tools/build_rfc_corpus.py:252` stamps that key into every manifest it writes, the committed manifest lacks it, and `write_manifest` never overwrites — so anyone rebuilding an existing clone silently reproduces zero heading paths. | **Fixes a real, present reproducibility defect** — the committed corpus disagrees with its own builder, and the builder's docstring already carries a ⚠️ about it. Worth doing on its own merits whichever option is chosen. · Turns the trigger the original brief called impossible into one that actually fires: the next person who builds or syncs that corpus gets heading paths by default, so the gate re-run becomes a by-product of any future measurement rather than a scheduled task. · Minutes of work, no CPU, no session cost. | **It is not an answer to either question.** Both items stay on the live list, unowned, in exactly the state that produced this seam — so as a standalone choice it is the worst outcome available. · Changing the committed manifest makes `measurements.json` and `MEASUREMENTS.md` describe a configuration the manifest no longer specifies, which needs its own note or it becomes a fresh inconsistency. · The corpus is a different repository, outside this repo's planner/coder ownership table — the owner and the review path are unstated. · The trigger it creates is silent: nobody is notified when it fires. | Minutes, in `pinakes-corpus-rfc`, plus a short note reconciling the recorded measurements. Owner unstated — needs the user to say. Best treated as a rider on whichever option is chosen, not as the choice. |
| **Grammar first, then the gate — the sequenced option, corrected** | Take the 8/20 blind spot as the thing to fix first, but at the size my measurement actually supports rather than the size the original brief assumed. The minimal version is one character in clause 4 (`title[-1] in ".,;:"` → `",;:"`), re-licensed against §5.4's 980-RFC doubling rounds, then the rebuild and gate of Option 1. Files: `src/pinakes/chunk.py`, tests, a battery section, then everything in Option 1's gate half. | **Much cheaper than the original brief claimed, and it dismissed it on an inverted citation.** Measured across all 300 documents the change gains rfc3261 and rfc4097 and loses rfc2865 — and rfc3261 alone blinds 5 of the 8 questions, so the blind set goes 8/20 → 3/20. · §5.4's actual methodology favours precisely this shape: it adopted start-at-1 *because* it "changes exactly one verdict across the corpus, and it is the wrong one", and rejected a rival for taking "three genuine documents with it". · Produces by far the best version of the experiment available — 17 of 20 questions with structure on all their evidence, against 12 for Option 1 and 0 for 20260804. · Improves the shipped chunker for every `text` KB, not only this corpus — a durable gain the gate re-run does not produce. | **It is not free, and losing rfc2865 is a real regression** — 283 → 284 of 300 is net +1, not a clean win, and rfc2865 is this corpus's worst document for hierarchy arity (20.44 rows/chunk). · **It does not reach rfc2616**, which still blinds 3 questions including 2 of the 3 recorded regressions. Its source genuinely has no `13.1`, and admitting the gap means weakening what the plan calls "the only thing standing between this grammar and an ordered list". · A chunker change is a retrieval change under `CLAUDE.md` § *Changing retrieval*, so it needs the doubling-round re-measurement before any of the gate work starts — and my 300-document result licenses nothing on its own. · Risks the 314/314 modern-era acceptance rate the current clauses earned, and delays the gate behind an increment with its own review loop. · Largest total scope of the live options, for an experiment whose expected result is still a null. | One focused coder increment for the clause plus a re-run of §5.4's doubling-round measurement over 980 RFCs, then all of Option 1's gate half. Coder-heavy, with a planner pass at each boundary. Smaller than the original brief's "two to three increments", but larger than either closing option. |

> ### ⭐ Recommended: **Split — close arity requirement 3 now, and run the gate re-run as its own three-leg gate**
>
> **The single deciding fact is that the two questions are not coupled, and the original brief's claim that they are rests on a misread sentence.** Its whole case for running rather than closing was: closing "would retire arity requirement 3 without ever measuring the arm, which is precisely what the arity decision refused to permit ('It would be deciding a retrieval question on a cost argument')." I opened the plan. That sentence's subject is **adopting** immediate-parent-only as the default — the row is headed *"Immediate parent only"* under *"Why not the alternatives"* — not declining to measure it. Requirement 3 is a conditional, *"**If** the ceiling is alarming…"*, and its antecedent is now measured false on every real corpus: 4.95 rows/chunk on this repo's own documents, **3.80 on 300 real technical specifications, 0 of 300 reaching the synthetic 53.42** — figures I reproduced myself rather than took on trust. A conditional whose antecedent is false closes without deciding anything about retrieval; transitive stays exactly as built. So requirement 3 closes, cheaply, with no CPU and no `src/` change.

That collapses the brief's binary and forces the gate re-run to stand alone — where I still, narrowly, come out in favour, but on much weaker grounds than the brief claimed and for a different reason. The reason is not that a number would discharge anything: `graph_gate.py:165` says arms are "reported beside the headline, never gated", the gate takes exactly three legs, and the `expand-in-degree` precedent already declared a reported arm "not a result". The reason is that **`expand` ships `off` — a shipped default — on an instrument where three of seven edge kinds derived zero and where `--drop sibling`, covering 99.2% of the graph's mass, measured size-slice adjacency rather than sections.** That is this repository's named failure class sitting under a shipped default, and one manifest line plus ~2.4 h of free CPU removes it. Nothing scarce is spent: the corpus and its frozen 20-question golden set are committed and public, and the seven legs cost ~5 minutes on top of the sync.

**I want the weakness of that second half on the record, because it is real and the brief understated it.** The re-run removes one of the two bounds `STATUS.md` names, not both — `[retrieval.confidence]` is still commented out in the committed manifest, so clauses 3 and 4 stay vacuous. It is blind on 8 of 20 questions (3 of 20 only if the clause-4 rider is taken, which costs rfc2865 and needs its own corpus-licensed measurement; rfc2616 is unreachable either way). It adds 7 859 heading hubs of the shape that caused the recorded harm. The expected result is another null, and `expand` stays `off`. And it depends on a planner session landing the write-up afterwards — which is exactly the step that did not happen for 21 days and is the reason this decision exists at all.

So: take the arity closure with confidence, and take the gate re-run as a considered judgement that a removable caveat on a shipped default is worth 2.4 h of idle CPU and two sessions. If session time rather than CPU is the binding constraint, **split-and-close is the correct answer and I would not argue.** Regardless of which is chosen, fix the corpus manifest upstream — the committed manifest contradicts its own builder today, and that is a defect independent of this decision.

**The first pass recommended _Run it — re-run G5's gate at `headings = "numbered"`, with the immediate-parent variant as an eighth leg on the same index_ and the adversary moved it.** The reasoning above is the revised one.

**What would change this.** **Four things, in descending force — the first two are the ones I would actually bet on.**

1. **If the user reads requirement 3's conditional differently from me.** My whole split turns on one reading: that *"If the ceiling is alarming, the immediate-parent variant is the arm to measure"* is a conditional whose antecedent I have now shown false on real corpora, and that *"It would be deciding a retrieval question on a cost argument"* governs **adopting** the variant, not **declining to measure** it. I have quoted both in place and I believe the reading is right — but it is a reading of the user's own prose, and the author outranks me on what it meant. If they say the intent was "measure it once the shape is reachable at all", then the antecedent fired on 20260804 as the retrospective's heading says, and **Option 3 (run it bundled) becomes correct** — the arm rides along, because it is free only if this run happens. I would switch without argument.

2. **If the constraint is planner-session scarcity rather than CPU.** The measurement is the cheap half; the write-up is not, and an unwritten-up run leaves the repo worse than today. Twenty-one days of an unowned measurement is direct evidence about which resource is actually scarce here. If the user says a planner pass is not going to happen soon, **split-and-close becomes correct** and running first would recreate the exact seam this decision exists to close.

3. **If a cheap pairing dry-run fails.** Before committing the 2.4 h: chunk at `headings = "numbered"` and confirm every id in `eval/questions.yaml` still resolves to live chunks, since the gate pairs on question id and refuses unpaired legs. I verified the ids and count match the 20260804 run's reported 7/20 and its three regression ids, but I did not build an index. If pairing breaks, no verdict is producible and closing is forced.

4. **If someone measures the sync and it is far off ~2.4 h.** I derived that from `RETROSPECTIVES.md:4203` (55 RFCs / 16 557 chunks in 1 497.7 s at mean 4.8 of 10 cores) scaled by chunk count. At 6+ h the trade changes. Note this **replaces** the original brief's falsifier — its "if it is 25 minutes, the case strengthens sharply" is dead, because 25 minutes is the measured time for 55 documents, not 300.

**What would NOT change my mind:** an argument from the 3.80 rows/chunk figure that the gate re-run is unnecessary. That figure settles the *cost* question and is why the arity half closes — it says nothing about whether the expansion channel's retrieval changes when hierarchy exists, which is the only thing the run can answer. Nor would "STATUS is already honest about the bound": it is, and I verified it — but an honest permanent caveat on a shipped default is a cost, not a defence.

**Cost of getting it wrong:** low. **Owner:** planner-then-coder.

**Not established — do not treat these as settled:**

- **Whether the gate would pass.** Unknowable without running it. The prior is a null: a 7/20 baseline needing ≥5 improvements with 0 regressions against a recorded 0 improved / 3 regressed. I did not model it and would not.
- **Whether the 445 280 new edges increase or decrease displacement.** I measured that the re-run mints 7 859 new heading hubs with max degree 499, median 6, p99 65 — hubs of the shape that caused the recorded 20260804 harm — but `in-section` is a HUB_KIND and damped by degree, so the direction is genuinely open. This is the mechanism question the run exists to answer.
- **Index size in bytes.** I measured edge and chunk counts, never built an index. DESIGN.md's 214 608 edges = 31 MB on a 265 MB index implies ~0.14 KB/edge, so ~445 000 new edges is roughly 60 MB — but chunks also drop 9.5%, shrinking the embedding half. Treat any growth percentage as derived, not measured.
- **The ~2.4 h sync figure is an extrapolation, not a measurement of this run.** I scaled a real measurement (55 RFCs / 16 557 chunks / 1 497.7 s under fastembed) by chunk count. It agrees closely with the independent 0.10.0 observation (~2.4 documents/minute) and with the project's own written ~2 h, but nobody has synced 300 documents at `headings = "numbered"`.
- **Whether the clause-4 rider survives §5.4's licensing bar.** My +rfc3261/+rfc4097/−rfc2865 result is over the 300 gating documents only. §5.4's methodology licenses a grammar clause against 980 RFCs in doubling rounds; I did not run that, and my 300-document result licenses nothing on its own. It is enough to show the original brief's "no cheap fix exists" is wrong, not enough to authorise the change.
- **Why exactly rfc2865 is lost by the clause-4 relaxation.** I established the verdict flip empirically but did not diagnose which newly-admitted candidate breaks its walk. Anyone taking the rider must.
- **The full blast radius of an immediate-parent arm.** I established more than the brief did — `store.py:175`'s CHECK constraint on `edges.kind`, `run_leg`'s read-only connection, `ALL_KINDS`, `WEIGHT`, `SYMMETRIC_KINDS`, `Census`'s every-kind-is-a-key rule — but I did not enumerate what `pnk doctor`'s hub report or `docs/VERIFICATION.md` would need, and I did not check whether the two tests pinning the transitive relation as a product would tolerate a flagged branch.
- **Whether today's fastembed revision matches 20260804's.** The manifest's `[embedding] revision` is commented out. Internal comparability across legs holds because they run together; comparability with 20260804 is already refused by the gate on `chunking` grounds, and is separately broken by 21 releases of change — so the re-run is a fresh verdict at today's HEAD, not a correction of the old one.
- **Who owns `pinakes-corpus-rfc`'s manifest.** It is a different repository and falls outside this repo's planner/coder ownership table. I verified the defect; I did not establish the review path for fixing it.
- **Whether the eight blind questions overlap the 7/20 that already hit at baseline.** Which specific questions hit at `off` was never recorded in any document I could find, and no G5 gate artifact was committed to either repository — so I could not compute how many of the 8 blind questions were even eligible to move.

---

## deferrals-with-no-owner-reextraction-loop-and-mutate-fields

**The question.** For each of two ownerless deferrals — the paid re-extraction loop (decision 12 / I7c) and `tools/mutate.py`'s `expect_green` + per-mutant zero-kill allowance — do we schedule the work, record a decline, or record a deferral with a trigger; and where does that record go?

**Status of the argument.** **I am UPHOLDING the original's direction and OVERTURNING its reasoning, its work estimate, and its placement.** Decline the mutation fields; do not schedule the loop. But the deciding fact is not the one it named, part of its proposed work is already on `main`, and the trigger belongs in `src/`, not `docs/ROADMAP.md`.

**What I personally verified (not read — ran):**

1. **Reproduced the central measurement.** n=12 page-measurements, distinct coverage values `[1.0]`, native yields 338–363, denominators 40–43. Exactly as claimed. Note the original's cited command does not run as written — `Pypdfium2Extractor().extract()` takes a required `ctx`.

2. **But the fitting set is thinner than "seven distinct pages".** `baseline-12p.pdf` has **6 distinct native page texts across 12 pages** (SHA1: p2≡p7≡p12, p3≡p8, p4≡p9, p5≡p10, p6≡p11; p1 differs from p11 only by a leading newline). Every recorded fixture comes from `baseline-12p.pdf` — the corpus's cleanest fixture — except `refusal-twice`, which the model refused. 1.0 everywhere is close to a tautology of what was recorded, not a discovered distribution. The original presented it as a measured distribution. It is a degenerate sample.

3. **THE FINDING THE ORIGINAL MISSED — the loop is unreachable, floor or no floor.** `pageyield.py:39 SCANNED_PAGE_FRACTION = 0.10`: a document reaches the paid path without `--force` only if **≥10% of its pages fall below the text-yield floor** — i.e. are *audit-exempt*. I ran `survey_free_yield` over all 18 readable corpus fixtures. **Every document the paid path accepts without `--force` has ≤1 auditable page** (the three scanned fixtures have **zero**; `hyphenation-soft` and `ligatures-a` have exactly one). `below_median` is *strictly* below, so with ≤1 auditable page it is structurally empty. **Every document with ≥2 auditable pages is refused without `--force`** (0% below floor). The two gates select near-disjoint page sets: the pages that justify the spend are exempt from the audit; the pages the audit can see are the ones that did not need paying for. A perfect floor changes nothing here.

4. **Verified an item the original listed as unverified.** Ran `reserve_document` directly: a template-1.1 KB (`per_operation_eur = 0.30`) really does refuse a single 5-page slice at call 0 — `allowed=False`, €0.3273. Also confirmed 30 pages = €1.9639 (allowed), 35 pages = €2.2912 (refused at the €2.00 default). The euro arithmetic is exact.

5. **Part of the proposed work is already done.** `8f05e73` ("X2: a heading in `plans/` now carries its own disposition") is an ancestor of HEAD. Both target headings **already carry dispositions** naming the deferral. The original's Option 2 budgets ~1 hour for writing them.

6. **A live contradiction neither the sweep nor the original names:** `docs/README.md:63` calls `plans/20260727_1543-v0.2.md` "**Shipped.**" flat, while that plan's own line 39 now says "except decision 12, whose re-extraction loop was deferred … and has never shipped."

7. **The "unrecorded blocker" claim is half wrong, and self-contradictory.** The plan's PDFScout section (lines 139–141) *does* specify the loop: PDFScout "re-extracts pages that dropped or duplicated content." `audit.py`'s docstring *does* record the page-vs-slice contradiction, explicitly. Only "what varies on the second request" is genuinely unrecorded. The original calls that docstring "the best statement of the argument in the repo" and then claims the argument is unrecorded.

8. **All mutation-side evidence confirmed exactly.** 136 anchors (14+20+15+28+18+41), all resolve, `--check-anchors` wrote nothing. Only 5 mutant keys and 2 top-level keys in use across six batteries — so refusing unknown keys is safe today. `expect_green` in exactly 2 commits, zero source/test/doc hits.

**A bonus defect, adjacent and coder-owned:** `pageyield.py:36-38` justifies the fraction statistic with "a 200-page report with eight scanned inserts … still needs the paid path." 8/200 = 4% < 10% — **the shipped constant refuses that document.** The example contradicts the constant it exists to justify.

### What was established

- **The central measurement reproduces exactly: 12 page-measurements over the recorded fixtures, one distinct word_coverage value — 1.0000. Native yields 338–363 non-whitespace chars/page against a floor of 65.75; denominators 40–43 significant words.**
  - *Source:* My own script: Pypdfium2Extractor().extract(baseline-12p.pdf, ExtractionContext()) per page, vs the recorded page text in happy-five-page-slice / short-final-slice / truncated-then-success, scored with pinakes.extract.quality.word_coverage and gated by load_floors().text_yield_floor exactly as audit.py:124-137 does. Output: `n audited page-measurements = 12`, `distinct coverage values = [1.0]`.
- **CORRECTION to the original: the fitting set is not 'seven distinct pages'. baseline-12p.pdf has only SIX distinct native page texts across its 12 pages — p2≡p7≡p12, p3≡p8, p4≡p9, p5≡p10, p6≡p11, and p1 differs from p11 only by a leading newline. All four recorded fixtures come from baseline-12p.pdf except refusal-twice (headers-repeating.pdf, which the model refused, producing no text).**
  - *Source:* SHA1 of each native page text via my script; `tests/fixtures/claude/*.json` provenance blocks: three name `baseline-12p.pdf`, one names `headers-repeating.pdf, pages 1-5, sent twice` with two refusal responses.
- **DECISIVE AND NEW: the paid-path admission gate and the audit's firing condition select near-disjoint page sets. `SCANNED_PAGE_FRACTION = 0.10` admits a document without --force only when ≥10% of pages fall BELOW the text-yield floor — exactly the pages audit.py exempts. Measured over all 18 readable corpus fixtures: every document accepted without --force has ≤1 auditable page (scanned-clean 0, scanned-low-contrast 0, scanned-skewed 0, hyphenation-soft 1, ligatures-a 1). Since below_median is STRICTLY below, ≤1 auditable page makes it structurally empty. Every document with ≥2 auditable pages (baseline-12p, headers-repeating, two-column-*, tables-*, footers-*, hyphenation-page-break) is REFUSED without --force at 0% below floor.**
  - *Source:* My script over `survey_free_yield` for every tests/pdf-corpus/*.pdf, printing pages_total, pages_below_floor, scanned_fraction, needs_the_paid_path; plus src/pinakes/extract/pageyield.py:39,75-82,119-139 and src/pinakes/extract/audit.py:68-79.
- **The audit is 100% blind on the scanned stratum — measured, not inferred. All 10 pages of the three scanned fixtures score native text_yield 0, below the 65.75 floor, so audit.py exempts every one. Across the whole corpus 12 of 58 pages (20.7%) are exempt. This is the stratum DESIGN §9 records the paid path scoring 1.000 on while the free path scores 0.000 — i.e. the reason the paid path exists.**
  - *Source:* My script computing text_yield per page for every corpus fixture against load_floors().text_yield_floor; src/pinakes/extract/audit.py:124-130; docs/DESIGN.md:1357.
- **VERIFIED what the original left unverified: a template-1.1 KB really does refuse a single 5-page paid slice at call 0. reserve_document returns allowed=False with 'refused: extracting 5 page(s) of 5 with claude-opus-5 is estimated at €0.33 (1 request(s), worst case), which exceeds 1 of the three budget windows'. The €2.00 default admits 30 pages (€1.9639) and refuses 35 (€2.2912) — so pricing a loop's worst case in halves the ceiling to 15 pages (€0.9819 × 2 = €1.9639).**
  - *Source:* My script calling estimate_document + reserve_document from src/pinakes/budget/ with Caps(0.30,1.00,30.00) and Caps(2.00,6.00,30.00), WindowTotals all zero. Constants: estimate.py K=5, PAGE_TOKEN_CEILING=6000, PROMPT_TOKENS=700, MAX_TOKENS=8000; prices.toml $5/$25 per Mtok, usd_per_eur 1.08.
- **CORRECTION: the dispositions the original proposes writing are already on main. Both plans/20260727_1543-v0.2.md:39 ('CLOSED 20260729 — shipped across 0.2.0–0.4.0, except decision 12, whose re-extraction loop was deferred to v0.3 and has never shipped') and :1439 (I7c, '…the re-extraction loop it defers to v0.3 has never shipped') carry them.**
  - *Source:* `git log -S '...' -- plans/20260727_1543-v0.2.md` → 8f05e73 'X2: a heading in plans/ now carries its own disposition', 2026-08-25 14:34:21; `git merge-base --is-ancestor 8f05e73 HEAD` → YES.
- **NEW live contradiction: docs/README.md:63 describes plans/20260727_1543-v0.2.md as '**Shipped.** Historical build orders, still cited by check.sh and tools/paid_path_gate.py' — with no mention of the deferral its own heading now names. The routing table and the plan disagree on main today.**
  - *Source:* `grep -n '20260727_1543-v0.2' docs/README.md` line 63, against plans/20260727_1543-v0.2.md:39.
- **CORRECTION: the loop's design is NOT wholly unspecified. The plan's PDFScout prior-art section states the borrowed mechanism: coverage_node 'compares significant words … and re-extracts pages that dropped or duplicated content'. And audit.py's docstring explicitly records the page-vs-slice contradiction: 'the audit would have re-extracted a page, while the paid extractor forbids re-asking a single page from a slice and the estimator has only a per-slice formula.' Only 'what varies on the second request' is genuinely unrecorded.**
  - *Source:* plans/20260727_1543-v0.2.md:139-141; src/pinakes/extract/audit.py:7-14; src/pinakes/extract/claude.py:497.
- **CORRECTION: the deferral was renewed in the SAME commit that recorded the fixtures, not as a separate later act of re-examination. 820728d ('Record four Claude-vision fixtures from the live API', 2026-07-29 03:46:19 +0200) both landed the recordings and rewrote DESIGN §7.1 from 'the first real output of that pair now exists' to 'One synthetic corpus at one point in time is not yet enough to fit a floor a real document would be judged against.' The conclusion stands; the framing 'somebody looked at the precondition 29 minutes later' overstates it.**
  - *Source:* `git show -s --format='%h %ad %s' --date=iso 820728d`; `git show 820728d -- docs/DESIGN.md` diff lines 48-52; docs/DESIGN.md:1236-1240 today.
- **CORRECTION: doctor.py's report-only promise is already unconditional and would survive a loop built into sync. doctor.py:1419 reads 'never a re-extraction — the audit is report-only, and a health check that could spend money would be the last place anyone would look for one.' That is a property of the health check, not of the release. Only audit.py:1 is release-relative ('report-only in this release').**
  - *Source:* src/pinakes/doctor.py:1414-1425 and :1456; src/pinakes/extract/audit.py:1; src/pinakes/extract/claude.py:719.
- **CORRECTION: the missing test the original flags sits inside an explicitly superseded table. `test_the_audit_spends_nothing` appears only at plans/20260727_1543-v0.2.md:1678, under the heading '## Verification of the whole · **SUPERSEDED as a lookup 20260729 — docs/VERIFICATION.md is the resolved mapping**'. It is not a live dangling reference. docs/VERIFICATION.md § 'The audit, staging, and all-or-nothing commit' carries seven test_extract_audit.py rows, none about spend — and the property is structural: audit_completeness is a pure function over two ExtractedText values, called once at claude.py:731.**
  - *Source:* `grep -rn test_the_audit_spends_nothing tests/` → absent; plans/20260727_1543-v0.2.md:1642,1678; docs/VERIFICATION.md:516-535; `grep -rn audit_completeness src/`; `uv run pytest tests/test_extract_audit.py -q` → 11 passed.
- **All mutation-side evidence confirmed. 136 mutants across six batteries (14+20+15+28+18+41), every anchor resolves, and --check-anchors left the tree clean. The batteries use exactly five mutant keys (file, kills, name, new, old) and two top-level keys (mutant, mutants) — so refusing unknown keys breaks nothing today. load_battery validates presence only and rejects no unknown key, so `expect_green = …` would be parsed, ignored and reported to nobody. The kill rule `if run.failed: return Outcome.KILLED` precedes the setup_errors branch, so a control added to `kills` that went red would read as a KILL, not an error.**
  - *Source:* `python3 tools/mutate.py --check-anchors tools/batteries/*.toml` then `git status --short` (clean); a tomllib script enumerating every key in tools/batteries/*.toml; tools/mutate.py:789-794, :831-914, :1199; tests/test_batteries.py:78-88.
- **`expect_green` exists in exactly two commits in the whole history and in no source, test, doc or CHANGELOG entry. The recorded practice for a misbehaving mutant is to re-aim, replace, or not write it — three times over. The batch-level control need is already answered by a committed convention.**
  - *Source:* `git log -S expect_green --all` → 5ba4aac, 60d4ae4 only; `grep -rn expect_green tools/ tests/ docs/ CHANGELOG.md` → no hits (only plans/); tools-markdown_link_gate.toml:75-80 (re-aimed after SURVIVED, 'The survivor was the battery working'); src-pinakes-init.toml:36-42 ('One of these rows replaced a survivor, and the survivor was the useful part of the run'); tools-mutate.toml:13-16 ('A mutant here would report SURVIVED truthfully and say nothing new'); 'Mutant 1 is the control' in three batteries, and tools-fragments.toml:17 '**No control here.**' with its reason.
- **The demand `expect_green` would serve is thinner than the original credits. The T4 table has FIVE non-dash entries, not four — and at most three are things expect_green could express. Row 2's entry is about WHICH ASSERTION fired ('it must not fail on byte-identity instead'), which no green-control field can assert. Row 11's is the opposite of a green control — it notes three tests that pass under the wrong operands, a weakness rather than a control.**
  - *Source:* docs/RETROSPECTIVES.md:5172-5186, read in full.
- **The one hole tools/batteries/README.md says nothing catches is not the hole expect_green would plug, and the tool says so itself. Its own --check-anchors output prints: 'an anchor that still matches while the code around it moved is caught by nothing.'**
  - *Source:* tools/batteries/README.md:131-137 ('nothing catches the other half, where the edit still compiles and simply changes two things under a name promising one. Read the diff the mutant produces, not the anchor'); live --check-anchors output.
- **ADJACENT DEFECT, coder-owned: pageyield.py's constant is justified by an example the constant refuses. The comment reads 'a 200-page report with eight scanned inserts has a healthy median and still needs the paid path'. 8/200 = 4%, and needs_the_paid_path requires scanned_fraction >= 0.10. That document is refused without --force.**
  - *Source:* src/pinakes/extract/pageyield.py:35-39 and :75-82; arithmetic.

### The options

| Option | What it means | Pros | Cons | Work |
|---|---|---|---|---|
| **Close both as declined** | Planner appends a decline to plans/20260727_1543-v0.2.md (decision 12 row, I7c body — the headings already carry dispositions) and to plans/20260821_0745-mutation-harness.md's preamble. Coder rewrites audit.py:1 from 'report-only in this release' to an unconditional statement. docs/README.md:63 corrected from flat 'Shipped.' | Removes both orphans permanently. No trigger anyone has to remember — which is this repository's demonstrated failure mode, named twice in the sweep itself. · Matches what users are already told: doctor.py:1456 prints 'Report-only — nothing was re-extracted and nothing spent. Open the pages and decide.' · Cheapest. Two document edits and one docstring. · My reachability finding supports it more strongly than the original's floor argument did: on this corpus the loop's trigger cannot fire on any document the paid path accepts without --force. | Overclaims on evidence that is about today's corpus, not about documents. DESIGN §7.1 says 'not **yet** enough'. A real 100-page document with 15 scanned pages would have ~85 auditable pages and clears both gates — the population is thin, not empty, and I did not measure a real one. · Converts a measurement into a design position nobody argued. If a real corpus shows a bimodal distribution AND clears the reachability gate, the decline is simply wrong and must be reopened. · The original's version of this con — 'leaves the real blocker unrecorded' — is overstated and I am correcting it: the PDFScout section specifies the mechanism and audit.py's docstring records the page-vs-slice contradiction. Only 'what varies on the re-ask' is genuinely unrecorded. | Small. Planner ~30 min (two plan bodies, one docs/README.md:63 line). Coder ~10 min (audit.py:1). No tests change, no behaviour changes. |
| **Close the mutation fields; ROADMAP trigger for the loop (the original recommendation)** | Planner records the mutation decline in the harness plan, and moves the loop to a named trigger in docs/ROADMAP.md Part 5 quoting DESIGN §7.1. Coder rewrites audit.py:1 and adds an unknown-key refusal to load_battery with a test and a tools-mutate.toml row. | Treats the two items separately, which is right — they are in genuinely different states. · The loop's deferral becomes findable from a document a planner reads, not only from DESIGN §7.1. · The unknown-key refusal is a real defect and is safe: I confirmed only five mutant keys and two top-level keys are in use, so nothing breaks. | **Its work estimate is wrong.** X2 (`8f05e73`, ancestor of HEAD) already appended dispositions to both target headings. Roughly half the planner work it budgets is done. · **It puts the trigger where nobody will read it.** The brief admits this is the repository's demonstrated failure mode and then does it anyway. Worse, docs/ROADMAP.md Part 5 is policed by tools/release_order_gate.py (Part ranges, the Part floor, 'Part 5 holds no releases'), so the edit needs that gate plus `make docs` — more than the ~1 hour stated. · **Its one coder deliverable is unspecified.** It says rewrite audit.py's release-relative opening line but never says to what under a deferral. Under a decline the target text is obvious; under a deferral it is not. · Bundles an unrelated coder fix (unknown-key refusal) into a decision brief, inflating the option. That defect stands on its own and should be filed on its own. · Rests the loop half on a deciding fact that is weaker than presented — a degenerate sample from the corpus's cleanest fixture, six distinct page texts, not an observed distribution. | Planner ~30 min (halved by X2) plus a release_order_gate + make docs run. Coder, separate increment ~2-3 h. |
| **Close the mutation fields; keep the loop deferred, with the trigger in the code that owns the argument** ⭐ | **Mutation fields:** planner records the decline in plans/20260821_0745-mutation-harness.md's preamble, naming the three mechanisms that answered the need (committed battery comments, the 'Mutant 1 is the control' convention, the report's per-node killer list). **Loop:** no ROADMAP row. The trigger goes into src/pinakes/extract/audit.py's docstring — already the best statement of the argument and the file anyone building the loop must open — restated in two falsifiable terms: (a) a word_coverage distribution over the applied pair on a non-synthetic corpus showing a separable trough, and (b) a *reachability* argument, since ≥10% of pages must be exempt for the document to reach the paid path at all while ≥2 differing auditable pages are needed to fire below_median. floors.toml's comment gains a one-line pointer. Planner fixes the one live contradiction, docs/README.md:63. Separately and independently filed: load_battery unknown-key refusal, and pageyield.py's self-refuting 200-page example. | **Puts the trigger in front of the only person who can act on it.** A ROADMAP row is read by a planner doing a sweep; audit.py's docstring is read by whoever would build the loop. The original's biggest admitted con disappears rather than being accepted. · **Names the free next step.** My reachability finding means the loop's precondition can be re-tested at ZERO cost — run survey_free_yield over a real corpus and count documents with ≥10% exempt pages AND ≥2 auditable pages. Nobody has run it. That reframes 'schedule or decline' into 'run a free check', which neither the sweep nor the original offered. · Honest about state: the deferral is a measurement about today's data, and a measurement can change. Declining converts it into a position nobody argued. · Fixes a real live contradiction (docs/README.md:63 vs the plan heading) that neither the sweep nor the original names. · Smallest planner footprint of any option that keeps the deferral alive — no Part 5 edit, so no release_order_gate exposure. · Unbundles the two incidental coder defects instead of hiding them inside a decision. | **A docstring is still a trigger nobody is obliged to check.** It is better-placed than a ROADMAP row, not self-enforcing. Making it self-enforcing needs a test — and a test asserting 'the corpus cannot exercise a loop' goes red when someone adds a good fixture for unrelated reasons, which is a real annoyance cost. I am not recommending that test; I am naming it so the choice is visible. · Splits work across the ownership boundary: planner writes the plan preamble and docs/README.md:63, coder writes audit.py and floors.toml. Two commits, not one. · **My reachability finding is about this corpus, not about documents in general.** A real document with 15 scanned pages in 100 clears both gates. I did not measure any real corpus — the private pinakes-kb and the RFC corpus were not fetched. · Declining expect_green still rests partly on absence of demand, and no battery *can* express a green control today, so 'zero of 136' may measure the missing field rather than the missing need. I weakened this further than the original did (at most three of the five T4 entries are expressible as expect_green) but I cannot eliminate it. | Planner ~30 min: the harness-plan preamble disposition, plus the one-line docs/README.md:63 correction. Coder ~45 min: audit.py's docstring rewritten to state the deferral, its two trigger terms and the reachability argument; a one-line floors.toml pointer. Both small. The two unbundled defects (load_battery unknown keys ~2 h; pageyield.py's example ~15 min) are separate coder items on their own merits. |
| **Schedule the re-extraction loop; close the mutation fields** | Fit a word_coverage floor and wire the audit to re-extract: a paid corpus run to build a fitting set, a new floor key and fitted_on in floors.toml, a page-vs-slice unit decision, a change to estimate.py so the worst case includes the loop, sync.py/claude.py drive changes, budget and on_exceed behaviour for a mid-document breach, and new VERIFICATION rows. | Closes the gap between what I7c promised and what shipped, in the direction the plan intended. · The audit's below-median line becomes actionable rather than advisory. · Forces the genuinely unspecified question into the open: what the re-ask does differently. | **It would build something that cannot fire on this corpus — and that con is missing from the original entirely.** Every document the paid path accepts without --force has ≤1 auditable page, so below_median (strictly below) is structurally empty on all five. Every document with ≥2 auditable pages is refused without --force. A perfect floor does not change this. · **The two-horn problem.** On a voluntarily-paid document the loop is unreachable (the justifying pages are exempt). On a --force'd document it is reachable but near-pointless — the free text is right there, and doctor.py:1456 already says a low score 'can equally mean the native layer was junk the paid pass correctly dropped'. · The fitting set is degenerate. Every recorded page comes from the corpus's cleanest fixture and scores 1.0; there are six distinct page texts. floors.toml's documented midpoint method needs a highest true negative and there is none. · Getting a real fitting set costs money and is in no plan's scope: tools/record_claude_fixtures.py spends, and the corpus that would carry a verdict does not exist. · **The money arithmetic is not marginal, and I verified it.** Pricing the loop's worst case in halves the ceiling from 30 pages to 15 (€1.9639 either way against the €2.00 default). NOT pricing it in breaks reserve_document's whole-run pre-check, a DESIGN §5 invariant. On a template-1.1 KB the cap is 0.30 and a single slice at €0.3273 is already refused — I ran it and watched allowed=False. · Touches the paid path, guarded by docs/INVARIANTS.md's enumerated allowlist and four gates — the highest-cost area to be wrong in, and the one where being wrong arrives as an invoice. | Large and front-loaded on decisions nobody has taken. Planner first: the re-ask unit and what varies on the second request — a plan, not a task, and CLAUDE.md's 'never assume what the plans have not decided' makes it a stop. Then a paid measurement run against a corpus that does not exist (user must authorise spend). Then a full coder increment. Several days across planner, user and coder. |
| **Schedule both** | Option 4, plus building expect_green as an optional per-mutant green-control selector list (a failure making the row ERRORED rather than KILLED), a per-mutant expected-survivor flag, selector resolution in tests/test_batteries.py, and tools-mutate.toml rows for the tool mutating itself. | expect_green addresses a need the record does show: docs/RETROSPECTIVES.md:5172 is a table with a 'control that had to stay green' column and five filled entries, and the same manual check recurs at 5396. Turning a repeated manual read into an assertion is the land.py / mutate.py precedent. · A per-mutant allowance would let a documented-unpinned backstop live in a committed battery without every run printing a SURVIVED row a reader must chase a comment for. · One decision, both items closed, nothing left to remember. | Inherits every cost of option 4, including the unreachability finding and the paid-path exposure. · **At most three of the five T4 entries are things expect_green could express.** Row 2's is about which assertion fired; row 11's notes three tests that pass under the wrong operands — the opposite of a green control. The demand is thinner than it looks. · expect_green is not the guard for the one hole tools/batteries/README.md says nothing catches — a mutant that compiles and changes two things under a name promising one, whose stated remedy is 'Read the diff the mutant produces, not the anchor'. Building it and believing that hole closed is worse than leaving it open. · The per-mutant allowance contradicts recorded practice three times: every survivor was re-aimed, replaced, or not written — and in src-pinakes-init.toml the survivor 'was the useful part of the run'. A field that silences a survivor makes the cheapest path to green the one that removes information. · Adds a second rot surface: a green-control selector goes stale exactly like a kills selector, and the record says the selector is the half that historically breaks. · Largest change to the tool whose own failure mode is reporting confidently on a question it never asked. | Largest. Everything in option 4, plus a coder increment on tools/mutate.py: two new optional keys, schema validation, a third outcome path, selector resolution in tests/test_batteries.py, tools/batteries/README.md updates, and the tool's own battery extended. Roughly a day on top, and it gates every other increment's mutation step while it lands. |

> ### ⭐ Recommended: **Close the mutation fields; keep the loop deferred, with the trigger in the code that owns the argument**
>
> **The single deciding fact is one I established and the original did not: the loop cannot fire, and the floor is not why.**

`SCANNED_PAGE_FRACTION = 0.10` admits a document to the paid path only when ≥10% of its pages are *below the text-yield floor* — which is precisely the condition `audit.py` uses to *exempt* a page. So the pages that justify the spend are invisible to the audit by construction, and the pages the audit can see are the ones the free path already read. Measured over all 18 readable corpus fixtures: **every document the paid path accepts without `--force` has ≤1 auditable page** — three have zero — and `below_median` is *strictly* below, so it is structurally empty on all five. **Every document with ≥2 auditable pages is refused without `--force`.** A perfectly fitted floor changes none of this.

That is a stronger and cheaper-to-check reason than the original's. Its deciding fact — one distinct coverage value — is real (I reproduced it) but weaker than presented: every recorded page comes from `baseline-12p.pdf`, the corpus's cleanest fixture, which has only six distinct page texts. 1.0 everywhere is close to a property of what was recorded.

**Why deferred and not declined.** The reachability finding is about *this corpus*, not about documents. A real 100-page report with 15 scanned pages clears both gates. I did not measure a real corpus. So the honest disposition is a deferral with a trigger — and the trigger's first term is now **free**: run `survey_free_yield` over a real corpus and count documents clearing both gates. No paid run, no spend, no plan. Nobody has run it.

**Why the trigger goes in `audit.py` and not `docs/ROADMAP.md`.** The original names "a trigger nobody is obliged to check" as this repository's demonstrated failure mode, then writes one into Part 5. `audit.py`'s docstring is read by exactly the person who would build the loop; a ROADMAP row is read by a planner doing a sweep. The original itself calls that docstring "the best statement of the argument in the repo" — so strengthen it rather than demote it. It also avoids a Part 5 edit, which `tools/release_order_gate.py` polices.

**On the mutation fields the original is right and I am making it more so.** `expect_green` exists in two commits and no code. Zero of 136 mutants asks for a green control. Every recorded survivor was re-aimed, replaced, or not written. And the demand is thinner than claimed: of the five filled entries in the T4 table, one is about *which assertion fired* and one records tests that *fail* to catch — neither expressible as a green control. Decisively, `expect_green` is not the guard for the one hole the batteries README says nothing catches, whose stated remedy is a human reading the diff.

**One correction that changes the work, not the direction:** X2 (`8f05e73`) already landed the headings the original budgets an hour to write. What remains is a trigger, an owner, and one live contradiction — `docs/README.md:63` calls the v0.2 plan "**Shipped.**" flat while the plan's own heading says the loop never shipped.

**The first pass recommended _Close the mutation fields; keep the loop deferred behind a written trigger_ and the adversary moved it.** The reasoning above is the revised one.

**What would change this.** **On the loop — three things, in increasing cost, and the first is free:**

1. **Run the reachability check on a real corpus.** `survey_free_yield` over a non-synthetic document set; count documents where `scanned_fraction >= 0.10` AND `pages_total - pages_below_floor >= 2`. If that count is materially non-zero, the loop has an addressable population and my central objection collapses. Costs nothing, needs no key, ~20 lines. **This is the check nobody has run, and it should be run before any other work on this item.**

2. **A distribution with a trough.** Re-run my measurement on a corpus with *degraded-but-non-empty* native text layers — a real scan with a bad OCR layer, a PDF with a broken embedded font map, not the synthetic strata — and show word_coverage over the applied pair with some pages materially below 1.0 and separable from the rest. If (1) and (2) both hold, I switch to option 4.

3. **A written answer to what the re-ask does differently.** A raised `max_tokens`, a different model, a shifted slice window, a re-render at higher DPI. Without one the second request is the first request — `claude.py:497` forbids re-asking a page and the fingerprint hashes model, prompt, request shape and K.

**(1) alone would not flip me to scheduling, but it would make the deferral's cost visible. (1)+(2) would. (2) without (3) buys a loop that re-pays for identical output.**

**On `expect_green`:** one concrete battery row someone wants to write and cannot — a mutant whose correctness claim needs a named test to stay green, where the mutant is right and the report cannot express the claim. Or a recorded post-0.27.0 incident where an over-broad mutant read KILLED, the killer list was there, and the wrong conclusion was drawn anyway. The first would show my "zero of 136" measures a missing field rather than a missing need.

**On the per-mutant zero-kill allowance:** one committed battery deliberately carrying a mutant expected to survive. There is none, and the three recorded instances all went the other way.

**What would NOT change my mind:** another plan, sweep or status document asserting the precondition fired. Three documents say variants of that and the data underneath says otherwise. Nor would a bigger n from the same recorded fixtures — they are six distinct page texts from one clean synthetic document, and adding measurements of the same pages adds no information.

**Cost of getting it wrong:** medium. **Owner:** planner-then-coder.

**Not established — do not treat these as settled:**

- **I ran no battery.** tools/mutate.py writes to the working tree and this task is read-only. I confirmed all 136 anchors resolve and that --check-anchors leaves the tree clean, and that tests/test_batteries.py resolves selectors — I cannot say the mutants still die. The README is explicit that only running says that.
- **I ran no paid path and spent nothing.** I did upgrade one of the original's unverified items to verified — reserve_document really does refuse a 5-page slice at a €0.30 cap — but that is the pure accountant, not an observed API refusal end to end.
- **My reachability finding is a property of tests/pdf-corpus, not of documents.** 18 fixtures, all synthetic, generated by tools in this repo. A real document with ≥10% scanned pages and ≥2 good text-layer pages clears both gates and I did not measure one. This is the single largest gap in my own argument and the reason I recommend deferral rather than decline.
- **No real corpus was fetched.** The private pinakes-kb and the external RFC corpus were not available to me, so I could not run the free reachability check I am recommending. I am recommending it precisely because I could not run it.
- **The 20260729 run's own audit output is unrecoverable.** Its .pinakes/ was a scratch KB under docs/ and .pinakes/ is gitignored. MEASUREMENT-RUN.md step (d) shows the run DID sync tables-bordered, two-column-a, ligatures-a and headers-repeating under --force, so it produced audit measurements on four more documents than the fixtures preserve. Those distributions are gone. My n=12 is a floor on what that run measured, not the run's distribution.
- **I could not resolve what '—' means in DESIGN §7.2's word-coverage column.** The original reads it as 'never recorded'. Given the prose 'Three of the four are identical to the free path on every metric' and that the other cells are deltas, it may equally mean 'no difference'. Either way it is the eval pair (extraction vs hand-authored ground truth), not the audit pair, so it does not supply the distribution — but the original's evidence item is ambiguous and I am flagging it rather than adopting it.
- **I ran only `pytest tests/test_extract_audit.py` (11 passed) plus my own scripts.** No ./check.sh, no full suite, no make docs. I reverted no fix to watch a test go red, so where I say a property is or is not pinned, that is a grep over test names and docs/VERIFICATION.md, not a demonstrated red.
- **Whether `pnk doctor`'s WARN completeness has ever fired on a real KB.** If it fires often, the report-only state is costing a real user something and the loop's value is higher than I credit. My reachability argument predicts it almost never fires — that prediction is testable and untested.
- **Whether the user has expressed a view on either item outside this repository.** git log -S and greps over docs/, CHANGELOG.md and plans/ find nothing. A decision taken in conversation and never written down would be invisible to me — which is precisely the failure mode both items are instances of.
- **Whether declining expect_green is what the user wants rather than what the evidence permits.** The plan calls both fields 'decisions, not omissions', which reads as reserving them for a person. I recommend a decline on the merits; I am not treating the absence of a recorded decision as consent to one.
- **The pageyield.py 200-page-example defect is arithmetically certain but its intent is inferred.** 8/200 = 4% < 10% is not in doubt. Whether the author meant the example as an accepted case rests on reading 'still needs the paid path' as an outcome claim. I did not find a test or a plan row settling it.

---

## residues-tripwire-t6-widening

**The question.** Of the three residues, only two are still open: (1) which reading of the staged-channel-gates clause 1 binds and how that clause is repaired, and (3) whether the fragments.py body-rule widening is deferred, rejected, or built — and if built, which widening. Question (2), D-13's T6 trigger, is already answered verbatim in docs/ROADMAP.md and needs no disposition at all.

**Status of the argument.** I UPHOLD the original brief's recommended shape (its option A) and OVERTURN its content on two of the three questions. Its central evidence claim for question 2 is false, and its proposed repair for question 1 would reintroduce a defect that was repaired in that same file four hours earlier.

What I personally ran, not read: copied both KB indexes on this machine to scratch and queried them with sqlite3; ran eight timed `pnk search` invocations against scratch copies (free paths only, no key set); re-implemented and re-ran the widening-1 measurement on CHANGELOG.md and a setext-residue scan on both target documents; ran `pnk search` against a `vector_tier = "sqlite-vec"` manifest and captured exit 1 bare; read clause 1, D-13, fragments.py, check.sh, manifest.py, store.py, edges.py, graph_matrix.py, pyproject.toml, and the diff of 93cef3e.

Three failures in the brief:

1. "Exactly one KB with built state exists" is false, and the missed KB is the one that matters. /Users/luca/Code/repos/github_lucagattoni/pinakes-corpus-rfc/.pinakes/index.db holds 106,806 chunks over 300 documents — 2x D-13's ~50,000 threshold. It sits at depth 5; the brief scanned to depth 4. Recording the brief's A(ii) as written would have put a false fact ("the only KB ... holds 43,353 chunks") into a planner-owned document — this repository's recorded failure mode, reproduced by the brief that cites it.

2. "No record in this repository holds that fact" is false six times over, and ROADMAP already carries the disposition. docs/ROADMAP.md line 2528 states the trigger and then answers it: "A corpus above the threshold is not enough; one exists and nobody searches it interactively. So the release name stays in the unbuilt-work table." Question 2 dissolves — it was never open, and every reader who stopped at "not checkable from here" stopped one grep short.

3. "Each is a residue nobody was assigned" is false for two of three. plans/20260825_1252-plans-sweep-findings.md row 25 assigns the tripwire to planner with the settling condition written out; row 23 assigns the widening to user-decision; lines 570-572 list all three as open questions. What is missing is the ruling, not the assignment.

What survives: the brief's clause-1 analysis (verified — DESIGN.md carried the third entry from 20260729, five days before the clause), its widening-1 CHANGELOG measurement (reproduced exactly: 67 = 58 link-refs + 9 prose lines, all in ## [0.1.0]), its E5 mis-attribution finding, and every code claim I re-ran.

### What was established

- **A second real KB exists on this machine and holds 106,806 chunks over 300 documents (schema_version 3, built_at 20260804 20:22, vector_tier numpy) — 2x D-13's ~50,000 threshold. The brief's 'exactly one KB with built state exists' is false; it scanned to depth 4 and this KB is at depth 5.**
  - *Source:* find /Users/luca -maxdepth 8 -name index.db -path '*.pinakes*' returned three: /Users/luca/pinakes-rfc-corpus, /Users/luca/Code/repos/github_lucagattoni/pinakes-corpus-rfc, and tests/demo-kb. Copied the second's index.db to scratch: sqlite3 'select count(*) from chunks' -> 106806, documents -> 300, 'select * from meta'.
- **docs/ROADMAP.md already answers question 2 verbatim: 'Its trigger, written before the fact so it is a trigger and not a mood: a KB that is actually queried crosses ~50 000 chunks and its search latency is a felt problem. A corpus above the threshold is not enough; one exists and nobody searches it interactively. So the release name stays in the unbuilt-work table.'**
  - *Source:* sed -n '2520,2532p' docs/ROADMAP.md (lines 2526-2530)
- **The chunk-count fact D-13 needs is recorded in at least six places, contradicting 'no record in this repository holds that fact': docs/STATUS.md:544 ('106 806 chunks is 2x past the NumPy vector tier's 50 000 threshold, and pnk doctor says so'), plans/20260804_1016-staged-channel-gates.md:296 and :343 ('This is no longer hypothetical: the RFC corpus is already 2x past the 50 000 threshold'), docs/GUIDE.md:802, docs/ROADMAP.md:1069, tools/build_rfc_corpus.py:11, and D-13's own text.**
  - *Source:* grep -rn '106 806' --include='*.md' --include='*.py' . and grep -rn '50 000' --include='*.md' .
- **Cold-process search latency, eight runs on scratch copies, same backends throughout (fastembed bge-small + bge-reranker-base, NumPy tier): 106,806-chunk KB = 4.02, 4.15, 4.74, 3.10 s; 43,353-chunk KB = 1.27, 1.41 s; 60-chunk demo-kb = 1.69, 1.72 s. The 60-chunk case being slower than the 43k case shows ~1.3-1.7 s is fixed model-load overhead and run-to-run noise is about +/-0.5 s. These are CLI wall clock, not an isolated vector-scan benchmark.**
  - *Source:* /usr/bin/time -p .venv/bin/pnk search <query> --kb <scratch copy>, with ANTHROPIC_API_KEY and PINAKES_ANTHROPIC_API_KEY unset; manifests compared with grep for provider/model/rerank/vector_tier
- **The three questions are already assigned in a landed planner document. plans/20260825_1252-plans-sweep-findings.md row 25 gives the tripwire owner 'planner' with 'What would settle it: the planner ruling which reading binds, in one line'; row 23 gives the widening owner 'user-decision' with 'What would settle it: the planner stating which of the two widenings (if either) is wanted'; lines 570-572 list all three under '## Open questions — every one a stop'.**
  - *Source:* grep -n 'staged-channel|D-13|fragments.py|widening' plans/20260825_1252-plans-sweep-findings.md; sed -n '540,585p' of the same file
- **Clause 1's enumeration was already false when written — the brief's central claim, verified. git show 21a401f:docs/DESIGN.md returns at line 1078 'the graph release (staged) — graph channels | Each is eval-gated rather than scheduled', and that row entered in 8b7865f dated 2026-07-29, five days before clause 1. git log -L 465,480 on the plan returns exactly one commit, 21a401f (2026-08-04 10:19), and 21a401f's stat shows it touched five files, none of them docs/DESIGN.md.**
  - *Source:* git show 21a401f:docs/DESIGN.md | grep -n 'graph release (staged)'; git log -S 'the graph release (staged)' -- docs/DESIGN.md; git log -L 465,480:plans/20260804_1016-staged-channel-gates.md; git show --stat 21a401f
- **Commit 93cef3e (2026-08-25 14:12, today) repaired this exact file's citation practice: it replaced the header's 'all four STATUS line numbers here still resolve' with a warning and the instruction 'Locate every citation in this file by heading text, never by line number.' The brief's option A(i) — which proposes naming 'docs/DESIGN.md:1331' and the two ROADMAP line numbers in the repaired clause — contradicts a same-day repair to the same file. The plan's line 27 still cites 'docs/STATUS.md:260' (now :392), so a rotted citation is live right where A(i) would edit.**
  - *Source:* git show 93cef3e -- plans/20260804_1016-staged-channel-gates.md; sed -n '1,30p' of the plan; grep -rn 'the graph release (staged)' docs/ shows STATUS at :392
- **The substantive half of clause 1 holds, verified by running rather than reading. manifest.py:77 GRAPH_CHANNELS = ('off', 'expand'); manifest.py:80 documents why 'ppr' is deliberately absent; VECTOR_TIERS = ('auto','numpy') at :52 with UNBUILT_VECTOR_TIERS at :65. A demo-kb copy with vector_tier = 'sqlite-vec' exits 1 with the refusal plus remedy. No [ner] extra in pyproject (only st, light, pdf, claude); no 'mentions' in store.py's edge-kind CHECK, in edges.py ALL_KINDS, or among graph_matrix.py's seven LegSpecs.**
  - *Source:* .venv/bin/pnk search on a scratch demo-kb copy with the tier edited, exit captured bare = 1; grep -n on manifest.py, store.py, edges.py, graph_matrix.py, pyproject.toml
- **The brief's widening-1 measurement reproduces exactly: 67 column-0 violations in CHANGELOG.md, of which 58 are the trailing link-reference block, and all 9 remaining fall in one section, ## [0.1.0] — 20260725 15:27. But the column-0 restriction is load-bearing and unstated: without it the same widened rule reports 3001 violations on today's CHANGELOG, because every wrapped continuation line of a list item is indented prose. So the implementation needs three carve-outs, not the two the brief names, and the third one creates a new blind spot — an indented bare paragraph — in a rule whose sibling widening exists partly because of a wrongly indented entry.**
  - *Source:* PYTHONDONTWRITEBYTECODE=1 python3 -B with the widened rule applied two ways (any non-blank line vs column-0 non-blank line), grouped by ## section
- **Zero live instances of either widening's target class exist today. A setext-heading scan (non-blank line followed by a line of only '=' or '-', outside fenced blocks) returns 0 for CHANGELOG.md and 0 for docs/RETROSPECTIVES.md. The body rule is changelog-only by construction — fragments.py:272 'if stream.categories is None: return problems' — so RETROSPECTIVES is out of widening 1's scope, and the brief's scoping was right.**
  - *Source:* python3 -B setext scan over both files; grep -n 'categories' tools/fragments.py
- **The stdlib constraint is real but does not block what widening 2 actually targets. python3 is /opt/homebrew/bin/python3 (3.14.7) and 'import markdown' raises ModuleNotFoundError; fragments.py:58 commits to 'Stdlib only, importing nothing from this project'; check.sh:189 invokes it with plain python3. But both classes widening 2 names — setext residue, and an entry indented into a sub-item — are detectable with pure string operations, as my own 12-line scan demonstrates. Rejecting widening 2 'because no parser is available' overstates the obstacle.**
  - *Source:* which python3; python3 -c 'import markdown'; sed -n '178,195p' check.sh; tools/fragments.py:58; my setext scan ran on stdlib alone
- **Question 1's ruling has a concrete downstream consequence the brief did not identify. CLAUDE.md's unbuilt-work table holds one row (the template release) and docs/README.md's Conventions block says 'Refer to unbuilt work by name: the template release ... The live names are kept in CLAUDE.md; this is the rule, not the list.' Meanwhile five entries across STATUS, DESIGN, ROADMAP (x2) and graph/PINAKES_APPROACH name 'the graph release, staged' as unbuilt work. Either the naming list is missing a row, or four documents carry entries the clause forbids — the ruling decides which.**
  - *Source:* sed -n '130,160p' docs/README.md; the unbuilt-work table in CLAUDE.md; grep -rn 'the graph release (staged)|graph release, staged' docs/ README.md CLAUDE.md
- **The brief's E5 mis-attribution finding holds. plans/20260825_0749-exposure-and-silent-status.md:291 attributes 'Left open deliberately: widening the body rule' / 'Neither is taken' to 'The E5 item', but that text is in the tools/fragments.py item (open-corrections lines 154-161, under the ### heading at :102), while E5 is the gitignore item at :73. The same numbered list then uses 'E5' correctly for the gitignore item four lines later.**
  - *Source:* sed -n '283,300p' plans/20260825_0749-exposure-and-silent-status.md; grep -n '^## |^### ' and sed -n '148,163p' plans/20260731_1202-open-corrections.md; grep -n E5 in both files
- **Neither real corpus has been touched in 18 days, which is independent support for ROADMAP's 'nobody searches it interactively'. The 43k KB's .pinakes/ was last modified 7 Aug 12:03; the 106k KB's .pinakes/ 4 Aug 23:45, its working tree 5 Aug 08:44.**
  - *Source:* ls -lat on both KB directories and their .pinakes/ subdirectories

### The options

| Option | What it means | Pros | Cons | Work |
|---|---|---|---|---|
| **Rule question 1 without re-enumerating; cite ROADMAP for question 2; defer question 3 behind a trigger that names the cheap implementation** ⭐ | Planner-only, two files. (i) plans/20260804_1016-staged-channel-gates.md clause 1: replace the counting sentence with the class it meant — no build plan, no increment, no numbered item, no version number — and state that index, routing and naming entries are permitted and uncounted. Cite by heading text, never by line number, conforming to the instruction 93cef3e added to this same file today. Rule in the same line whether 'the graph release, staged' belongs in CLAUDE.md's unbuilt-work list. (ii) Question 2: record nothing new; if anything, add one dated bullet to docs/STATUS.md beside the existing 106,806-chunk bullet giving the measured cold-search figures. (iii) plans/20260731_1202-open-corrections.md: give the widening a trigger — the next residue of either class found in a released document — and state that the implementation is the setext-plus-indentation pair, not widening 1. Leave the item where X2 put it. | Repairs a tripwire that cannot fire correctly without inventing a new enumeration that will rot the same way — the file's own line-number citations have rotted twice, once fixed today. · Costs nothing on question 2 because ROADMAP already carries the disposition; the brief's version of this step would have recorded a false fact instead. · Records the one thing that is genuinely new — that the cheap correct implementation for the widening is the setext-plus-indentation pair, not the widening the item lists first — so the next implementer does not build the expensive wrong thing. · Forces the CLAUDE.md unbuilt-work-list question into the open, which is the ruling's only concrete consequence and is currently unowned. | The trigger for question 3 only fires after the harm it exists to prevent — the previous five residues sat on the published site for weeks with every gate green. This is a real weakness and the option does not fix it. · Two planner-owned files, one of them among the repo's most contended, for a change that alters no behaviour and blocks nothing. · Leaves ## Live carrying an item that is otherwise closed, which the file's own header calls a defect in it. · The STATUS latency bullet is weak evidence: my numbers are CLI wall clock including ~1.3-1.7 s of model load, not an isolated vector-scan measurement, and 'felt' remains the user's judgement regardless. | Planner, one small increment, documents only. Two edits, no code, no tests, no release. Smaller than the brief's option A because question 2 drops out. |
| **Rule question 1; cite ROADMAP for question 2; reject the widening and close the item** | As above for (i) and (ii), plus moving the tools/fragments.py item from ## Live to ## Closed with the measurement recorded: zero setext residues live in either document today, and zero widening-1 violations outside ## [0.1.0] — 20260725 15:27. The setext blind spot stays documented in fragments.py's own docstring, where it already is. | Ends the residue and empties ## Live, which the file's header demands. · Honest about today's measurement: both target classes are at zero, verified by scans I ran rather than by reading. · Nothing is lost — the blind spot is recorded in the code, closer to the fix than a plan entry is. · One document edit; the smallest option that resolves anything. | Rejects on a measurement of the current file when the class it guards is about files not yet written. Both target classes were also at zero the day before the five residues landed. · This item has already been closed once on a mechanism dated before the defect it claimed to fix, and the defect then shipped twice more. Confident closure here has a bad track record specific to this item. · Makes the two shapes that actually reached the published site nobody's job, when the cheap fix for them is about a dozen stdlib lines. · Forecloses: reopening costs a new decision, whereas a deferral costs a grep. | Planner only, one document edit. The only option that forecloses. |
| **Rule question 1; cite ROADMAP for question 2; build the setext-plus-indentation checks now (not widening 1)** | As above for (i) and (ii), plus an implementer increment in tools/fragments.py adding two source-line checks to document_problems: a setext-heading detector (a non-blank line followed by a line of only '=' or '-', outside fenced blocks) and an indentation check for a top-level entry indented into a sub-item. Tests in tests/test_fragments.py, a section appended to the existing fragments battery, and the one docs/VERIFICATION.md row the new test requires. Widening 1 is explicitly not built. | Closes the class that actually reached the published site — five spurious <h2> headings, permalinks and search index included, invisible to mkdocs --strict and to the current rule. · Stdlib-only and cheap: my own scan of both documents is twelve lines and needs no parser, so the check.sh 'plain python3' constraint is untouched. · Arrives green — both scans return 0 today — so it blocks nothing on landing. · Avoids widening 1's three carve-outs entirely, including the [0.1.0] allowlist entry that the item's own retrospective argues against. | It is not what the item asked for. The item names 'reads rendered structure rather than source lines'; this approximates two named symptoms of that and leaves the general class open, so it can only be recorded as a partial answer, not a closure. · Spends an implementer increment on a rule that fires on nothing live, while the same sweep ranks four LIVE user-facing defects above it — a sync that silently drops a document while doctor reports healthy, serve reusing one SQLite connection across threads, sync aborting the whole index on one unreadable file, and a KB name that bricks a KB at creation. · A detector for setext headings must handle fenced blocks correctly or it fires inside code samples; this repository has already shipped one fence detector anchored wrongly at column zero (RETROSPECTIVES, 'The gate that read everything except its own'). · Still requires the planner ruling first, so it is option 1 plus code, never instead of it. | Planner writes the ruling; then coder, one increment: code, tests, a battery section, a VERIFICATION row, fragments. Small but a full build/review/land loop. |
| **Record only the two measured latency figures; rule nothing** | One append to plans/20260825_1252-plans-sweep-findings.md or docs/STATUS.md: the two corpora on this machine (106,806 chunks and 43,353), their cold-search wall-clock figures, and the note that this refutes the sweep's own line 573 ('the 300-document corpus died with the machine; that could be neither confirmed nor refuted'). No ruling on question 1, no disposition for question 3. | Captures the only genuinely new facts this session produced; they die with the session otherwise. · Corrects a claim two live documents still carry — the 300-RFC corpus and a built index for it are on this machine now. · One edit, one file, no invented trigger, no foreclosure. · Leaves both real decisions with the owners the sweep already gave them. | Leaves clause 1 stating a tripwire that was violated at birth, so the next reader repeats this analysis or files drift that is not drift. · Leaves the widening residue with no trigger and no owner — the exact state that produced it. · The latency figures are the weakest of the new facts: they include model load, they are noisy at +/-0.5 s, and 'felt' is not a number. · Records facts against a question (2) that is already closed in ROADMAP, so most of the value is corrective rather than decisive. | Planner, one append. Fifteen minutes. |
| **Do nothing — the questions are already recorded and owned** | No file changes. The sweep's rows 23 and 25 and its lines 570-572 already carry all three questions with owners and settling conditions, landed on main today. This analysis stands as the session record only. | Zero cost, zero merge surface, and no invented trigger. · Contrary to the original brief's framing, the questions are not unrecorded: they are in a landed planner document with owners and 'what would settle it' written out. · Nothing here blocks a release, a user-facing surface, or the held 0.30.3 tag. · X2's heading dispositions already stop the worst failure mode — a cleared coder reading the fragments item as live and rebuilding it. | The corrected facts die with this session: the 106,806-chunk KB's existence, the latency figures, and the refutation of the brief's own 'only one KB' claim. · Clause 1 keeps a tripwire that produces 'either obedience or drift' verdicts on a count nobody can satisfy — I am the second reader in one day to burn a pass on it. · The sweep's line 571 will keep telling readers D-13 is 'not checkable from this repository' when ROADMAP:2528 answers it — a false signpost, cheap to fix, left standing. · Question 3 keeps no trigger and no owner, which is how it got here. | None. |

> ### ⭐ Recommended: **Rule question 1 without re-enumerating; cite ROADMAP for question 2; defer question 3 behind a trigger that names the cheap implementation**
>
> The single deciding fact is that question 2 is already answered on main, in the published roadmap, in exactly the terms the trigger requires: docs/ROADMAP.md:2528-2530 states D-13's trigger and then rules on it — "A corpus above the threshold is not enough; one exists and nobody searches it interactively. So the release name stays in the unbuilt-work table." That removes a third of the decision and, more importantly, disqualifies the original brief's version of it: A(ii) proposed recording "the only KB with built state on this machine holds 43,353 chunks" when a 106,806-chunk KB sits at depth 5 on the same disk, above the threshold, and named by the trigger itself. Writing that line would have manufactured the failure the brief was written to avoid.

What remains splits cleanly. Question 1 is a one-line planner ruling, and the purposive reading is the only one that was ever true: clause 1's count was false at birth (DESIGN.md carried a third entry of the same shape from 20260729, five days earlier), while the substantive class holds today — "ppr" is refused at load time and manifest.py:80 documents that refusal as deliberate, there is no [ner] extra, and "mentions" appears in no edge-kind CHECK, no ALL_KINDS, and no LegSpec. ROADMAP is obedience: both its entries sit under a Part titled "What is not built" and say "There is deliberately no implementation plan". The repair must not re-enumerate with line numbers, because commit 93cef3e added the opposite instruction to this very file four hours ago: "Locate every citation in this file by heading text, never by line number." The ruling also settles something the brief missed — whether "the graph release, staged" belongs in CLAUDE.md's unbuilt-work list, which today holds only the template release while five entries elsewhere use the staged name.

Question 3 is genuinely the user's and stays deferred, because both target classes measure at zero live instances today and four LIVE user-facing defects outrank it on the same sweep. But the deferral should carry the finding this session produced: the cheap correct implementation is a setext-plus-indentation pair, not widening 1. Widening 1 reaches one section written 20260725 and cannot reach the class that shipped, and it needs three carve-outs, not two — without a column-0 restriction the same rule reports 3001 violations on today's CHANGELOG. Deferring without recording that leaves the next implementer to build the expensive wrong thing.

I am recommending, not deciding. Question 3 is the user's call and I say so; option 3 becomes correct the moment one residue of either class appears in a live section.

**The first pass recommended _A. Record the two measured answers; give the widening a written trigger and an owner_ and the adversary moved it.** The reasoning above is the revised one.

**What would change this.** On question 1: any build plan, increment, numbered item or version number for either channel — a plan file under plans/ for PPR or the [ner] extra, "ppr" restored to GRAPH_CHANNELS, a ner entry in pyproject's optional-dependencies, "mentions" in store.py's edge-kind CHECK or edges.py ALL_KINDS, or a mentions LegSpec in graph_matrix.py. I found none of these and checked all six; any one flips the verdict from "repair the clause" to "the tripwire fired", and I would then be wrong to call ROADMAP obedience.

On question 2: one sentence from the user saying they query either corpus interactively and that 3-5 s hurts. The first conjunct is already satisfied — 106,806 chunks is 2x the threshold, which STATUS.md:544 has said since 20260804 — so the trigger now hangs entirely on the second, and only the user can fire it. If they say yes, T6 becomes schedulable today and ROADMAP:2530 needs rewriting, not citing.

On question 3: one violation of either class in a live section. Concretely — a non-blank line followed by a line of only "=" or "-" outside a fence in CHANGELOG.md or docs/RETROSPECTIVES.md (my scan: 0 and 0), or a column-0 non-list line under a ### heading in any CHANGELOG section other than ## [0.1.0] (my scan: 0). A single instance turns the deferral into a build and makes option 3 correct rather than option 1. Equally, if mkdocs or another gate gains a spurious-heading check, the item dissolves rather than deferring.

On the whole brief: a KB I could not see. My scan covered /Users/luca to depth 8 with permission-denied paths suppressed and one volume mounted. Another machine, a network volume, or the private pinakes-kb repo could hold a KB above the threshold that someone does query — which would fire the trigger and, under the broad reading, change nothing about question 1 but everything about question 2.

**Cost of getting it wrong:** low. **Owner:** planner.

**Not established — do not treat these as settled:**

- Whether /Users/luca/Code/repos/github_lucagattoni/pinakes-corpus-rfc is the same physical corpus CHANGELOG 0.13.0 called dead, or a clone or rebuild. Its index was built 20260804 20:22 and that CHANGELOG entry is dated 20260805 21:01, so the entry post-dates the index it says is gone. I could not reconcile that from here — I established only that a 300-document, 106,806-chunk index exists on this machine today.
- Whether any KB exists outside /Users/luca at depth <= 8, on another machine, on a network volume, or in the private pinakes-kb repo. I fetched no external repository. Everything I say about the KB population rests on one find over one home directory with permission-denied paths suppressed.
- Whether 3-5 s is a felt problem. My figures are CLI wall clock on a cold process and include Python startup plus fastembed model load; a 60-chunk demo KB measured 1.69-1.72 s, so roughly 1.3-1.7 s is fixed overhead and the run-to-run spread is about +/-0.5 s. I did not isolate the vector-scan component, and a long-running pnk serve would not pay the load cost per query. My numbers bound CLI pain, not the tier's value.
- I ran no gate. Not ./check.sh, not pytest, not make docs. Every code claim rests on reading source plus six pnk invocations against scratch copies outside the repo. I reverted no fix to watch a test go red, so I make no 'pinned by test X' claim.
- Whether the widened body rule would false-positive on shapes the current rule permits — a table row, fenced block, ordered list or block quote at column 0 inside a changelog entry. Today's CHANGELOG contains none (my scan found only 58 link-reference lines and 9 prose lines), so I established the rule arrives green but not that it stays green.
- Whether the item's author would accept a setext-plus-indentation pair as satisfying 'reads rendered structure rather than source lines'. I established that no Markdown parser is available to the gate's interpreter and that both named symptoms are stdlib-detectable; I did not establish what would count as satisfying the phrase.
- Whether plans/20260825_0749-exposure-and-silent-status.md:291's 'The E5 item' is a mis-attribution or a shorthand for the whole file. I established the quoted text lives in the tools/fragments.py item and not in the gitignore item, and that the same numbered list uses E5 correctly four lines later.
- Whether the ~40 undispositioned heading/body mismatches recorded in 960653a's message hold a fourth residue of this kind. I read the three questions asked and the sweep's own worklist, not the full backlog.

---

## Facts that must not be lost

Established during this pass, and load-bearing beyond the decision that produced them.

- 0.30.3 is complete and unblocked: `[Unreleased]` in CHANGELOG.md is empty, changelog.d/ and retro.d/ contain only their READMEs, `__version__` = 0.30.3, and `git diff --stat v0.30.2 HEAD -- src tests tools pyproject.toml` shows exactly 4 files (__init__.py, cli.py, init.py, test_init.py). Every other module is byte-identical to the published 0.30.2. All fifteen sweep findings are therefore live in the wheel PyPI serves today, and none shares a line with 0.30.3's six hunks.
- THE HONEST LIMIT ON THE X7 RIDER, which no brief states. D-35 layer 2 compares line 3 to R = the tail of STATUS's *Published versions* row: line3 > R → marker required; line3 == R → marker forbidden. But docs/RELEASING.md:118 explicitly permits that row to LAG a release, because an entry is held back until verified from the index. So after tagging, line3 (0.30.3) > R (0.30.2) still holds and layer 2 stays GREEN with a now-false marker on the page. Layer 2 enforces removal at the moment the row is updated, not at the moment of publication; layer 3 (the PyPI index query) covers that window and is SOFT. X7-before-tag is still worth doing — it is just a narrower guard than "converts the only real cost of shipping into a red build".
- THE RE-EXTRACTION LOOP IS UNREACHABLE, AND THE FLOOR IS NOT WHY. src/pinakes/extract/pageyield.py:39 `SCANNED_PAGE_FRACTION: Final = 0.10` admits a document to the paid path only when ≥10% of pages fall below the text-yield floor — precisely the condition audit.py uses to EXEMPT a page. Measured over all 18 readable corpus fixtures: every document the paid path accepts without --force has ≤1 auditable page (three have zero), and `below_median` is strictly-below, so it is structurally empty. A perfectly fitted floor changes nothing. The free falsification test nobody has run: `survey_free_yield` over a real corpus, counting documents that clear both gates.
- A SEPARATE, CODER-OWNED DEFECT FOUND EN ROUTE, VERIFIED BY ME: pageyield.py:36-38 justifies the constant with "a 200-page report with eight scanned inserts ... still needs the paid path". 8/200 = 4%, and the predicate is `scanned_fraction >= 0.10` — the shipped constant REFUSES the document its own docstring says it exists to accept.
- SIDECAR DRIFT IS FAR WIDER THAN ANY DOCUMENT SAYS. 12 of 30 anchor-free sidecar shapes break byte-identity through the real sidecar.read()/write() path; only 1 is on docs/MANIFEST.md's bounds table. Undocumented casualties: column alignment (`title:   Alpha` → `title: Alpha`), flow-comma spacing (`[a,b]` → `[a, b]`), `reviewed: ~` → `reviewed:`, a multi-line flow sequence collapsed to one line, and a single-quoted value with a tab re-emitted double-quoted — which directly contradicts MANIFEST.md:303's promise that quoting style survives. Root cause is a ruamel LOADER misfiling (comment slot 2 for a plain key vs slot 1 of the NEXT key for an alias). ruamel.yaml 0.19.1 IS the latest on PyPI — there is no upstream fix to wait for, and comment_handling raises IndexError on this input.
- TWO BLIND PINS ON THE SAME PROMISE. docs/VERIFICATION.md:232 pins "comments survive a rewrite through pnk link" by asserting SUBSTRING PRESENCE, not position — so the misattribution passes it today. The correct citation for "an anchor on a real value survives" is docs/MANIFEST.md:316, not 317.
- D-37's DISCRIMINATOR: gate the move hint on the ORPHANED SIDECAR, not the Mint count. `moved_without_sidecar` is true precisely when the sidecar stayed behind. Scored 5/5 vs the Mint count's 3/5; the Mint count fails on delete-one-add-one (the ordinary git-hook batch shape) and on the ambiguity branch, which falls through to the same line and prints two contradictory reports about one document.
- THE INVARIANT THAT DECIDES D-31. docs/INVARIANTS.md:22 promises a deep-run transcript "never leaves `.pinakes/`" and justifies it with "it lives with the state that is already gitignored". M1 falsifies that premise — a KB can read ignored=True and tracked=True at the same instant, with deep/op1.json in git's index — and nothing in the repo checks it. `pnk doctor` prints 33 rows on such a KB and not one mentions it. Also: docs/DESIGN.md:1367 still calls the exposure "Mitigated by shipped .gitignore", which M1 falsifies — planner-owned doc debt owed whichever option is chosen.
- THE PERFORMANCE OBJECTION TO THE DOCTOR CHECK IS DEAD. A 100,001-entry git index costs 10.7–11.1 ms (`ls-files`) and 13.1–13.9 ms (`check-ignore`) against a 2,320–2,400 ms `pnk doctor` run. `check-ignore -v` does NOT break `_all_probes_ignored`'s parse (identical verdicts with and without), so D-33's option D is cheaper than it was costed; the objection that survives is coverage, not cost. A linked worktree returns tracked=True, a submodule tracked=False. Also: doctor._hooks decides git-ness with `(root/'.git').exists()`, so a repo-aware exposure row prints beside a false `WARN git hooks: not a git repository` on a nested KB — fix the wording in the same change.
- SCHEMA BLAST RADIUS FOR ANY NEW EDGE KIND, verified at src/pinakes/store.py:172-177: `kind TEXT NOT NULL CHECK (kind IN ('membership','sibling','parent-child','in-section','co-located','shared-tag'))`. A new kind name is rejected by the constraint, forcing SCHEMA_VERSION 3→4 and a rebuild of every existing KB. Reusing `parent-child` avoids that but then an immediate-parent arm cannot coexist with the transitive set in one index — and graph_matrix.py has no place to re-derive, so it is not a LEGS append. Separately, graph_gate.py:165: "The arms are reported beside the headline, never gated" — an arm licenses nothing.
- THE CHEAP GRAMMAR FIX EXISTS. Allowing a trailing `.` in clause 4 takes the RFC corpus from 283 to 284 documents of 300 — gains rfc3261 and rfc4097, loses rfc2865 — and since rfc3261 alone carries 5 of the 8 blind gated questions, it takes the blind set from 8/20 to 3/20. rfc2616 is unreachable either way (the source genuinely has no `13.1`, only `13.1.1`–`13.1.6`). Costs a corpus-licensed measurement of its own. And the committed corpus manifest contradicts its own builder today — fix that regardless of the decision.
- docs/ROADMAP.md:2528-2530 already rules on D-13's tripwire: "A corpus above the threshold is not enough; one exists and nobody searches it interactively. So the release name stays in the unbuilt-work table." The corpus it means is /Users/luca/Code/repos/github_lucagattoni/pinakes-corpus-rfc/.pinakes/index.db — 106,806 chunks over 300 documents, twice the ~50,000 threshold, at depth 5. Never record "the only KB with built state holds 43,353 chunks".
- D-34's residue: tests/test_serve.py::test_get_refuses_anything_that_is_not_a_known_id asserts the MCP surface refuses `../../etc/passwd` and its docstring calls it "the whole server boundary" — nothing in the 1,127-line VERIFICATION table covers traversal at that boundary. Existence is established; the RATE across the 929 is not (row-coverage was checked for 6 of a 50-sample, chosen upward-biased). Two of those six turned out to be redundant per-surface re-assertions of already-rowed promises — which is the real argument for promises-only, and for a bounded sweep rather than a backfill.
- LIVE CONTRADICTIONS TO RESOLVE WHATEVER IS DECIDED: docs/README.md:63 calls plans/20260727_1543-v0.2.md "Shipped." flat while that plan's own line 39 says the re-extraction loop never shipped. tools/status_header_gate.py's docstring lines 1 and 10-13 are literally false today ("The invariant holds with no exception window" — there is one, used deliberately three times). docs/graph/PINAKES_APPROACH.md:420 assigns `pnk adopt` to the template release while docs/README.md:28 files it under "What is still a proposal", and that release closed at 0.22.0 with both standing.
- PROCESS FACTS THAT SAVE A SESSION: **[⚠️ no longer true — fixed 20260826 in `674eda6`, four legs, absence red]** `make release-check` (Makefile:102-106) runs NO gate — it prints the version and echoes the tag command; the assurance for 0.30.3 is CI run 32854256771 against e6d62e33 = HEAD. Precedent for folding S2 into a PATCH is WORSE than assumed — 0.17.0 and 0.18.0 both open with a new `pnk doctor` report and both are MINOR. `expect_green` appears in exactly 2 commits and zero source/test/doc files; all 136 battery anchors resolve; only 5 mutant keys and 2 top-level keys are in use. 8f05e73 already landed the plan-heading dispositions that one brief budgeted an hour to write.

## What this pass got wrong about itself

**One adversarial correction was itself wrong, and it is recorded because the shape recurs.** An adversary
reported the stamp *"Measured 20260825 12:49 UTC on `c23359f`"* as **impossible**, on the grounds that
`c23359f` was committed at 13:42. It reads `13:42` as UTC; `git log --format=%%aI` gives
`2026-08-25T13:42:55+01:00`, so the commit is **12:42:55 UTC** and a 12:49 UTC measurement is six minutes
after it. **The stamp stands.** A timezone read as UTC when it is local is exactly what this repository's
*read the clock, never compose it* rule exists to prevent, and it appeared inside a correction about
false precision.

**One was right, and the error was the planner's.** `docs/VERIFICATION.md`'s scope paragraph said the table
holds **923 rows**. That count included **33 table headers** which the repo's own gate skips; the content
figure is **890**. Corrected in the same change as this file.
